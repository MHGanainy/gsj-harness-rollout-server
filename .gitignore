.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
dist/
build/
.DS_Store
# estate/forgejo/ live-instance data + API tokens (never committed — ADR-0002)
estate/forgejo/forgejo-data/
estate/forgejo/.token*
# estate bring-up runtime state written by estate/serving/serve.sh
# (endpoint.env, tunnel.pid) — regenerated every bring-up, never evidence
# (wishlist 12, opened at CP-17 and closed at CP-18)
estate/serving/run/
# CP-48: the development record is untracked (the operator's decision on
# CP-47's classification). The files stay on disk — the operator's copy is
# the only one. The `!` rows ARE the keep list: every negated path is read
# by CI, the release gate, a test suite, or the pins walk (CLASSIFICATION
# §4 rows 1-8, 16-17, re-verified at CP-48). CP-48.md, the last
# tracked prompt, came out at CP-49a (the operator's direction) —
# no prompt is tracked now.
docs/prompts/
docs/reports/
docs/decisions/
docs/AUDIT-2026-08-24.md
docs/CLASSIFICATION.md
# CP-49: the three Mac-golden fixtures left this list — re-homed to
# tests/fixtures/golden-mac/ (tracked, plain; §4 rows 8, 16-17) — so
# docs/golden/ ignores whole and the negated rows below are the seven
# polar bodies (§4 rows 1-7).
docs/golden/
docs/polar/*
!docs/polar/pi-corpus/
docs/polar/pi-corpus/*
!docs/polar/pi-corpus/callback_session_result.json
!docs/polar/pi-corpus/trace.json
!docs/polar/fidelity/
docs/polar/fidelity/*
!docs/polar/fidelity/callback_session_result.json
!docs/polar/fidelity/trace.json
!docs/polar/h200-fidelity/
docs/polar/h200-fidelity/*
!docs/polar/h200-fidelity/callback_session_result.json
!docs/polar/h200-stitch/
docs/polar/h200-stitch/*
!docs/polar/h200-stitch/attempt5.accepted.json
!docs/polar/thinking/
docs/polar/thinking/*
!docs/polar/thinking/episode-on.quarantined.json
stranger/*
stranger-*.md

# the operator's round-four orchestration evidence (untracked like stranger/)
round4-orchestration-*/
