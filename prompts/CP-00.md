# CP-00 — M0a: repo scaffold

You are executing CP-00, the opening checkpoint of `gsj-harness-rollout-server`.
This checkpoint creates governance and structure ONLY. **No logic, no moves,
no vendoring, no dependencies installed beyond what `pyproject.toml`
declares. Every module file created is empty except for a docstring stating
its purpose.**

Context you need (read before writing): the predecessor repo
`gsj-envloader` @ v0.8.0 — specifically its `README.md`, `docs/HOLES.md`,
`ASSUMPTIONS.md`, and `docs/phases.md`. Its path is <PREDECESSOR_PATH>. If
it is not reachable, write `docs/reports/CP-00.md` with `status: blocked`
naming what is missing, commit that alone, and stop.

## Step 0 — Protocol
This prompt lives at `prompts/CP-00.md`; commit it with the CP. If any span
of this prompt arrives corrupted, reconstruct it, apply the reconstruction,
and flag it under `questions:` in the report.

## Step 1 — `CLAUDE.md`
Six sections, headers verbatim, in this order:
`## Project` · `## Scope lawsssumptions` · `## Workflow` ·
`## Engineering rules` · `## Layout & commands`

**Project** — one paragraph: this is a **rollout server for our corpus**.
Given a task `(case, timestep, prompt)` it runs our agent in an isolated
sandbox with temporally-scoped retrieval and emits a training-ready
trajectory. Trainer-agnostic, algorithm-agnostic, parameterization-agnostic.
Built on NVIDIA's Polar for episode execution and trajectory
reconstruction. Predecessor: `gsj-envloader` @ v0.8.0 — **alive, frozen, not
retired**; it is the fallback and the golden reference. Point at
`docs/CHARTER.md` as the normative document.

**Scope laws** — number these seven:
1. **The scope law**, verbatim: "The rollout server owns: task → sandbox →
   agent → trace. Nothing else. If it stores, schedules, scores, weights,
   versions, or trains — it's out."
2. **Size budget**: our own code under 1,500 lines, excluding vendored
   Polar, tests, and the moved components (`corpus/`, `mcp-service/`,
   `forgejo/`). A chec pushes past it must stop and
   justify.
3. **The predecessor is frozen.** No checkpoint here modifies
   `gsj-envloader`.
4. **Vendor, don't depend.** Polar has no releases. Pin a SHA, record it,
   document the re-vendor recipe, expect to carry patches.
5. **Nothing in `gsj_rollout/` assumes Docker semantics.** The runtime is a
   config value; Polar's interface is start/stop/exec/upload/download. This
   keeps Apptainer free when we want it (A-11).
6. **`checks.py` runs on both sides** — the receiver drops bad traces at the
   source, the trainer verifies what arrived. Same code, no trust required
   across the wire.
7. **Findings over features.** This is an evaluation. A checkpoint that
   discovers Polar cannot do something is as valuable as one that builds.

**Assumptions** — pointer to `docs/CHARTER.md` §4; every new assumption gets
a row there immediately, and unverified is not false.

**Workflow** — work happens only inside numbered CP prompts, one at a time,
saved verbatim to `prompts/CP-Xnd committed with the CP. Each CP ends
with a hard STOP wall. Mid-CP questions: choose a best-guess default,
proceed, list it under `questions:`. Every CP writes `docs/reports/CP-XX.md`
in the template below, prints it, makes one commit `CP-XX: <summary>`, and
leaves the tree clean. **Every CP updates the gap register in
`docs/CHARTER.md` §7.** ADRs are append-only in `docs/decisions/`, one file
per decision (`ADR-0001-title.md`), Context → Decision → Consequence.
Embed this template verbatim:

```
### CP-XX REPORT
status: done | partial | blocked
scope_drift: none | <what and why>
files: <git show --stat summary>
tests: <command> → <counts> | n/a
adrs: <ids or none>        assumptions: <ids or none>
gap_register: <rows touched>
questions: <each with the applied default> | none
next: <advisory>
```

**Engineering rules** — three, operationalized: (R1) do not reinvent — name
the off-the-shelf candidate before writing >50 lines of infrastructure;
going custom needs an ADR. (R2) if confused, search rt; found →
adopt and cite; not found → propose with a default and never stall. (R3)
simplicity — the simplest thing that satisfies the charter and the CP's
Definition of Done; no abstraction the charter doesn't mandate.

**Layout & commands** — the tree from Step 3; commands: `pip install -e
".[dev]"`, `pytest -q`, and the two console scripts (which do nothing yet).

## Step 2 — `docs/CHARTER.md`
The normative document. Nine sections:
1. **What we are building** — the objective paragraph, the scope law, the
   size budget.
2. **Why this repo exists** — the predecessor works and is externally
   verified, but ~1,800 of its lines are episode execution and capture, and
   that layer was expensive to maintain. This tests whether Polar can own
   it. We are not migrating; the predecessor stays alive.
3. **What we start with** — three tables: *carried over* (corpus ~1,076,
   mcp-service ~1,038, forgejo ~120, pins/G2 tooling ~170, the corpus
   contract), *adopted* (Polar, vendored by SHA), *writ_harness.py` 50–150, `receiver.py` 50–100, `checks.py` 150–250,
   `config.py` ~100, `client.py` ~80, `cli.py` ~60). Plus **deliberately
   dropped**: the store, the loader (`ready`/`mix`/leases/serve
   accounting/SPI), staleness tracking, collation, `GsjCaseTask`,
   `uniagent_driver`, `collector` — each with "trainer's problem" or
   "Polar's problem" as the reason.
4. **What we assume** — a table with columns `# | assumption | basis | if
   false`. Rows A-1…A-11 covering: prefix_merging correctness (paper +
   README, not line-verified); the proxy handling pi 0.83.0; pi package
   identity (RESOLVED — same tool, `@mariozechner` deprecated →
   `@earendil-works`, preset pins 0.67.68, we run 0.83.0); per-episode
   token injectable via `run_steps()`; callback payload sufficiency; slime
   OPD against Polar traces; **the four reported fork defects are
   UNVERIFIED, not false** (whitelist leak, `-9999.0` sentinel,
   abort→ERROR, reasoning mask); vendoring a release-less branch is
   sust components transfer unmodified; reward sparsity
   at demo scale is method/scale not infrastructure; **A-11 Apptainer
   deferred** — Polar supports it natively, we stay on Docker so the
   CP-09 comparison has one variable, revisit on a cluster without a
   daemon.
5. **What we are testing** — the four questions (does Polar run our pi;
   does our cutoff survive; are the traces trustworthy; can a trainer
   consume them) and the success criterion verbatim: *one real episode
   against our corpus, through our MCP service, with the cutoff enforced,
   producing a trace whose token ids, `loss_mask`, and logprobs verifiably
   match the golden reference the predecessor produced for the same task.*
6. **The plan** — the CP table: CP-00 scaffold · CP-01 moves · CP-02 fork
   audit · CP-03 vendor Polar · CP-04 golden reference · CP-05 source audit
   · CP-06 harness spike + stub · CP-07 the harness · CP-08 receiver/config/
   CLI · CP-09 fidelity · CP-10 sentinels + cutoff · CP-11 surviving gateP-12 the verdict. Mark CP-05, CP-09, CP-12 as decision points
   where the plan may legitimately stop, and note GPU is needed only at
   CP-04 and CP-09.
7. **Gap register** — the table this repo is judged by. Columns: `# |
   capability | gsj-envloader | here | status | notes`, status ∈ `PARITY |
   DROPPED | GAP | BETTER | TBD`. Seed ~31 rows from the predecessor's
   capabilities (read its README/HOLES/ASSUMPTIONS): corpus contract,
   retrieval cutoff, git host, taskbank, episode isolation,
   harvest-before-reset, token/logprob capture, multi-turn stitching, the
   seven gates individually, quarantine, store, ready/mix, staleness,
   serve accounting, collation, provenance, pins, deterministic env
   pinning, one-YAML config, bounded collection, logprob sentinel guard,
   async staging, multi-provider harness support, HPC runtime. Include
   explicitly as a **GAP**: *tool roster visible in config* — the
   predecessor had `task.tools_allowlist` as a hashed config field; if our
   harness bakes ther into argv, G3 has no pinned input. State in the
   section header that **every CP updates this table**, and that a
   capability silently disappearing is the failure this table exists to
   prevent.
8. **Standing rules** — the seven scope laws restated as operating rules.
9. **What would make us abandon this** — stated in advance so the decision
   isn't made under sunk cost: prefix_merging output not matching the
   golden reference with no fix in our code; the proxy needing a forked
   translation layer for pi 0.83.0; the cutoff not injectable per-episode;
   G5 unreconstructable from the trace; our own code exceeding ~1,500 lines.

## Step 3 — The tree
Create exactly this, modules empty but for a one-paragraph docstring stating
purpose and which side (server / trainer / both) they run on:

```
.
├── CLAUDE.md
├── README.md                    # short: what it is, status, pointer to CHARTER
├── pyproject.toml
├── .gitignore
├── docs/
│   ├── CHARTER.md
│   � reports/CP-00.md
├── prompts/CP-00.md
├── gsj_rollout/
│   ├── __init__.py              # consumer surface (empty for now)
│   ├── pi_harness.py            # SERVER — our pi via Polar import_path
│   ├── receiver.py              # SERVER — callback endpoint + validation
│   ├── checks.py                # BOTH  — trace validators
│   ├── config.py                # SERVER — one YAML
│   ├── client.py                # TRAINER — submit + collect
│   └── cli.py                   # SERVER — gsj-rollout serve | submit
└── tests/
    └── test_scaffold.py         # imports the package, asserts version
```

`ADR-0001` records the naming decisions taken before CP-00: repo/dist
`gsj-harness-rollout-server`, import `gsj_rollout` (flat — no PEP 420
namespace machinery, no sibling packages to share with), module
`pi_harness.py` rather than `harness.py` (the word "harness" already means
Polar's `BaseHarness` and pi itself; three  rather than
two packages — Polar's own `slime_bridge` precedent, and the reason the
trainer half installs light.

## Step 4 — `pyproject.toml`
Distribution `gsj-harness-rollout-server`, version `0.1.0`, Python ≥ 3.11.
Core deps: `pydantic`, `httpx`, `pyyaml` — the trainer-side weight, nothing
more. Extras: `[server]` (Polar's runtime needs — leave as a documented
placeholder until CP-03 vendors it and the real deps are known) and `[dev]`
(pytest). Console scripts `gsj-rollout = gsj_rollout.cli:main`. Note in a
comment that `[server]` is deliberately empty until CP-03.

## Definition of Done — run and show output
```
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]" -q
pytest -q                                  # the scaffold test passes
python -c "import gsj_rollout; print(gsj_rollout.__version__)"
gsj-rollout --help                         # exits 0 (may print a stub message)
grep -n "^## " CLAUDE.md                   # exactly the six headers, in order
grep -c '^| A-' .md            # ≥ 11 assumption rows
grep -c '^| [0-9]' docs/CHARTER.md         # gap-register rows, count shown
test -f docs/decisions/ADR-0001-repo-identity.md && echo OK
git status --porcelain                     # → empty after commit
```
One commit: `CP-00: repo scaffold`.

## STOP — hard wall
No moves from the predecessor. No Polar vendoring. No logic in any module.
No stub server. Do not draft or begin CP-01.

## Report
`docs/reports/CP-00.md` per the template, printed. Under notes: the gap
register as seeded (row count and the rows already marked GAP or DROPPED);
the assumption list; anything about the predecessor that surprised you while
reading it for §7.
