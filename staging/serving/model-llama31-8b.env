# CP-38 second-family pin — Llama-3.1-8B-Instruct via the unsloth mirror,
# the SAME snapshot family CP-37 measured offline ("official template"),
# so the live-vs-offline derivation comparison is apples to apples. The
# meta-llama origin repo is gated; this mirror carries Meta's weights and
# Meta's full chat template (embedded; sha256 e10ca381… — recorded by
# serve-llama31.sh at serve time from the snapshot actually served).
# DATA file, sourced by serve-llama31.sh. Revision = the mirror's main at
# CP-38 (2026-08-24), resolved via the HF API and pinned here.
GSJ_MODEL_ID=unsloth/Meta-Llama-3.1-8B-Instruct
GSJ_MODEL_REVISION=a2856192dd7c25b842431f39c179a6c2c2f627d1
