# CP-04' student pin — the SAME model as the Mac pair, deliberately a pair
# in numerics and not in model (CP-04' Step 1). DATA file, sourced by
# serve.sh; the revision is the codec snapshot (the predecessor's A-01-family
# pin, the CP-09 generation-config source).
GSJ_MODEL_ID=Qwen/Qwen3-0.6B
GSJ_MODEL_REVISION=c1899de289a04d12100db370d81485cdf75e47ca
