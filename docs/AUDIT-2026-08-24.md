<!-- Committed at CP-39 (see docs/reports/CP-39.md, questions). Produced
2026-08-24 by the post-CP-38 three-repo audit — 24 agents, ~1,150 tool
calls, adversarial verification of every finding — against
gsj-harness-rollout-server @ 3467524 (CP-38), -examples @ 8df5a9f
(CP-33), gsj-rollout-demo @ a505a9a (CP-38). Verbatim below. This is the
specification CP-39, CP-40 and CP-41 cite by finding id (W/S/O/C/M). -->

# AUDIT — what was missed

**Scope:** `gsj-harness-rollout-server` @ 3467524 (CP-38), `gsj-harness-rollout-server-examples` @ 8df5a9f (CP-33), `gsj-rollout-demo` @ a505a9a (CP-38). Method: 14 parallel domain sweeps + adversarial verification of every finding + a completeness critic + 4 follow-up executions. 24 agents, ~1,150 tool calls. Everything below was verified against files, git history, executed suites, live PyPI, and live GHCR — not against the registers being audited. 96 raw findings deduplicated to the items below. No commits, no edits, no fixes were made; all three trees are clean.

---

## WRONG — false now, with the proof

**W1. `staging/README.md:34` tells the operator to clone the Megatron pin its own CP measured broken.** The trainer-recipe table names `--branch 26.04-alpha.rc1` (Megatron-LM) as the mounted checkout — the exact pin F-06 and CP-17's own report measured dying at `ModuleNotFoundError: No module named 'megatron.training.tokenizer'`. The recipe that actually ran (`slime_bridge/cp17_loop/train_one_step.sh:8`) uses the image's `/root/Megatron-LM @ 1dcf0dafa` and says "NOT from a separate checkout". An estate restore from this table fails at import. The contradiction was written by the same commit as the report that contradicts it (a647359) and was never recorded. *Mislead: high. Fix: one line.*

**W2. `forgejo/` carries five un-namespaced predecessor citations that all resolve wrong in this repo — and CP-01's report claims they were qualified.** `docker-compose.yml:1` "(CP-29, M7a)", `:9` "(CP-29, measured)", `up.sh:2` "the CP-02 up.sh recipe", `create_owner.sh:3` "(CP-33, ADR-0046(e)/ADR-0047)", `:8` "created empty at CP-33". In this repo CP-29 is the PyPI release, CP-02 is the Polar-pin audit, CP-33 is the wishlist cluster, M7a is a *different live milestone* (the re-vendor door), and ADR-0046/0047 don't exist (ADRs end at 0025). Diff against `gsj-envloader` @ v0.8.0 proves CP-01 qualified the same class in adjacent lines ("The predecessor's CP-02 recipe") and left these five byte-identical — so `docs/reports/CP-01.md:27` ("predecessor citations qualified") is contradicted by the files. Note: `forgejo/` is a DoD-protected path (CP-37/38 require an empty diff), so the fix needs a conscious freeze-lift. *Mislead: high. Fix: small + one report erratum.*

**W3. Charter A-28 claims the `gsj-mcp-service` image is multi-arch; it publishes linux/amd64 only.** `docs/CHARTER.md:377` says both GHCR images are "rebuilt/republished, **multi-arch**". CP-36's ARM stranger run died at exactly this pull (F-54), the durable fix is wishlist row 40 — still open — and a live manifest probe in this audit confirms: `gsj-polar` is a multi-arch index; `gsj-mcp-service:0.3.0` (and `gsj-pi-harness:pi0.83.0-3`) are amd64-only. The row was never annotated. *Mislead: medium. Fix: trivial.*

**W4. Examples `FINDINGS.md:35` — F-25 says OPEN; the cure has been live since CP-27 and cites F-25 by name.** `gsj_rollout/config.py:246-248` `_null_sections_to_empty` — docstring literally "F-25 (wishlist 23)…"; VERDICT row 23 is DONE (CP-27). The row survived two later register passes (CP-27, CP-33) already false. *Mislead: medium. Fix: trivial.*

**W5. Both bridge README install commands fail as written.** `slime_bridge/README.md:23` and `verl_bridge/README.md:26` pip-install `dist/gsj_harness_rollout_server-0.1.0-py3-none-any.whl` right after instructing a fresh `python -m build --wheel` — which produces 0.1.2; `dist/` holds only 0.1.1. ENOENT either way. (Follow-up execution: with the path corrected, both suites pass against 0.1.2 today — slime 14 passed, verl 26 passed with verl @ 1ae9455 still fetchable — so only the filename is broken, not the suites.) `install.sh` already solved this with a glob. *Mislead: medium. Fix: small (4 spots incl. both requirements.txt comments).*

**W6. `MODEL-SURFACE.md:30` — "the bootstrap writes G4 hashes empty for a non-reference model" is false on the derivation-failure path.** `bootstrap.py` empties `tokenizer_hash`/`chat_template_hash` only inside the success branch (line 699 `if derived["why"] is None:`); when a non-reference model's endpoint lacks `/tokenize`, the written pins keep the *reference estate's* G4 values — the exact "carry the reference's values as a lie" the doc says is refused, and the failure-path messages never mention G4. *Mislead: medium. Fix: trivial doc-side (or small code-side).*

**W7. Demo `README.md:259` describes the corpus *source* tree with the *generated repo-side* page name.** It says `make_corpus.py` writes "per-case `md/page_NNNN.md`"; the source tree (and the contract, and the script — `tdir = case / f"timestep-{t}" / "pages"`) uses `timestep-<T>/pages/page_NNNN.md`; `md/` is what ingest creates inside the case repos. A stranger hand-building a tree to this sentence fails validation. *Mislead: medium. Fix: trivial.*

**W8. VERDICT row 41's [CP-38] note says "slash-free path exercised" — the exercised name was `unsloth/Meta-Llama-3.1-8B-Instruct`.** Contains a slash; `config.py:408` prepends `gsj/` regardless; the CP-38 report itself never claims slash-free. *Mislead: low. Fix: trivial.*

**W9. Two small false numbers/cites:** demo `README.md:218` asserts "25–50 s" per episode while citing its own 22.5 s CP-36 measurement in the same sentence; `checks.py:97` cites "ADR-0007(e)" unqualified — that's the *predecessor's* ADR (this repo's ADR-0007 is the unrelated glue stitch with no lettered clauses), against the repo's own explicit-qualification convention. *Mislead: low. Fix: trivial each.*

---

## STALE — true when written, not now

**S1. `CLAUDE.md` still describes the CP-08 stub — and it's injected into every session.** "`gsj-rollout serve` … does nothing yet (CP-08)", "`__init__.py` # consumer surface (empty for now)", a layout with no `builder.py` and one test file. Reality: a working 241-line CLI that has driven real H200 collections since CP-09′, an `__init__` exporting the consumer surface at 0.1.2, `builder.py` load-bearing, 10 test files. Last committed CP-12 — 26 checkpoints ago. Every future CP starts by reading this. *Mislead: high. Fix: small.*

**S2. The examples repo presents v0.1.0 as the current release and pins its behaviors to it.** `README.md:54`, `RUNBOOK.md:17,97`, `install.sh:10`, `requirements.txt:5` all say v0.1.0; `slime_bridge/requirements.txt` still says "No release channel exists yet". PyPI has served 0.1.2 since CP-34 (verified live; the wheel was downloaded and unzipped — both pin sets + `ingest_corpus.py` present). Installs still *work* (`>=0.1.0` resolves to 0.1.2) — the words are what's wrong. *Mislead: medium. Fix: small.*

**S3. The RUNBOOK's closing "what can still surprise you" list teaches three bugs CP-27 fixed.** (a) "`python -m gsj_rollout.cli` exits 0 doing nothing" — the `__main__` guard is at `cli.py:238` with subprocess tests; (b) "/v1-suffixed `serving_base_url` or a gateway port/`public_url` mismatch all pass `load_config`" — both validators live (`config.py:63`, `:179`); every released wheel ships all of it. The bullets were written at CP-26, one CP before the fixes, and survived every later pass. *Mislead: medium. Fix: trivial.*

**S4. The second family's ripples only partially propagated.** CP-38 fixed two Qwen-branded demo texts and MODEL-SURFACE, but missed:
- `RUNBOOK.md:62-66` — the "four legs" serve flags read model-general; CP-38 measured the tool parser and template legs family-bound (hermes "would leave every call unparsed text" on Llama);
- examples `config.yaml:113-117` — the `end_of_turn_token_id` re-derive recipe is `convert_tokens_to_ids("<|im_end|>")`, a token that doesn't exist outside ChatML-family vocabs (Llama's answer is 128009, derived per A-30);
- charter A-2 (`CHARTER.md:354`) — still names hermes as *the* engine requirement, unannotated;
- `bootstrap.py:566-570` — docstring still asserts A-30's stop half "true for every family measured", the precise claim CP-38 measured false;
- `read.py:767-770` — the G6 quarantine cure still calls non-Qwen "the demo's known seam" needing a manual "tokenizer re-pin", obsoleted by CP-37's automatic derivation at `up`;
- `preflight.py:272-278` — a third Qwen-branded text ("(Qwen3)" labeling a possibly-Llama-derived pinned tail);
- MODEL-SURFACE's opener "Every measured number in the README came from one [Qwen] estate" — falsified by CP-38's own README edit.
*Mislead: medium. Fix: trivial each, ~7 spots across three repos.*

**S5. The external register's OPEN/UNVERIFIED side systematically lags the checkpoints (the DONE side doesn't).** Six rows describe as pending what a checkpoint already did: F-02 ("OPEN, CP-17 input" — the citation grader landed at CP-17 and the loop trained on it), F-04 ("UNVERIFIED until CP-17" — CP-17 verified all four flagged fields, A-26 HOLDS), F-43 ("NO thinking-on batch has crossed the GPU leg" — CP-32 measured it: mean|Δ| 0.016546, guidance holds), F-20 (the serve-print flush landed CP-27), F-21 (absolute paths CP-27 + missing-venv hint CP-33), F-23 (`estate.model_revision` exists since CP-27). *Mislead: medium. Fix: trivial, 6 status cells.*

**S6. `slime_bridge/README.md:5-6` — the CP-31 mode-note rider still teaches "the wheel's pins are the *off-mode* reference only".** False since CP-33/0.1.1 (both mode files verified inside the published wheels); F-40's own retirement row one file away says so. The rider that F-42's "MITIGATED" status vouches for now re-teaches the confusion it was built to prevent. *Mislead: medium. Fix: trivial.*

**S7. Library README trio.** Badge paragraph claims a 136-test root suite (executed today: **161 passed**, matching ci.yml and the charter); the gate roster omits live G6 (landed CP-23 — the README discusses G6 elsewhere on the same page); "the published package contains `gsj_rollout/` and the two pins files, nothing else" predates CP-34's `ingest_corpus.py` force-include (verified inside the published 0.1.2 wheel). *Mislead: medium. Fix: trivial.*

**S8. `pins/pins.gsj.json`'s two stale notes ship in every wheel.** Line 86: "resolution stays taskbank-deferred (ADR-0003)" — resolved at CP-24/ADR-0022, recorded as wishlist 19(a), yet the note has survived every pins-adjacent lift since (CP-27, CP-30, CP-33, CP-34's release) and a pip consumer has no VERDICT to correct it. Line 102: G6 presented as a future rule ("CP-11c or later") — it landed at CP-23 exactly as described. *Mislead: medium/low. Fix: trivial, but needs a pins freeze-lift.*

**S9. `staging/README.md` claims to be the complete estate delta and omits CP-38's second-family recipe.** "The estate is reproducible from the committed tree" — the component table predates `serve-llama31.sh` + `model-llama31-8b.env`; unlike CP-24's careful pins-note precedent, CP-38's report doesn't record the omission. (Same file as W1; it has not been re-read by any CP since CP-17.) *Mislead: medium. Fix: small.*

**S10. Wishlist row 14 says the ULP oracle "passes on x86_64 … nothing is owed today"; row 26 in the same table evidences it failing on hosted x86_64** (run 32126927920, `test_stored_vectors_are_the_pinned_encoders`, CP-33's own push). Row 14's arm64-only framing was never revisited. *Mislead: medium. Fix: trivial.*

**S11. VERDICT's "What comes next" still lists the template flip (M5) and thinking (M6) as future agenda.** Both landed (CP-04′; CP-30/31), "M6" now names the verl milestone in the same document's dated sections, and the "still owed by the loop CP" clause closed at CP-17. *Mislead: medium. Fix: small.*

**S12. Two ADRs superseded without cross-reference.** ADR-0011's ground moved at CP-37: "deriving the ids needs the tokenizer at PIN time" — CP-37 derived the same ids from the live endpoint alone, four HTTP calls, no tokenizer, and deliberately moved the venue off the pins walk; no amendment. ADR-0020's Consequence still declares the package unpublished, the name unclaimed, and row 17 OPEN — all closed at CP-29, no appended marker (the ADR-0003 in-file-resolution precedent exists). *Mislead: medium/low. Fix: trivial.*

**S13. The pre-CP-33 archive filename `<session_id>.json` survives in three frozen texts** — gap rows 16 and 22 (`CHARTER.md:1281,:1287`) and ADR-0008:83,85 — while the receiver has stamped `<session_id>.<pins-mode>.json` since CP-33 (F-48). *Mislead: low. Fix: trivial.*

**S14. Small stale numbers, one line each:** `ci.yml:23` still says `requires-python ">=3.11"` (narrowed to `>=3.12` at CP-19; survived three later edits of the same file); RUNBOOK "root 150 (since library CP-30)" (161 since CP-34); examples `config.yaml` zero-logprob band "4–25%" (CP-38's Llama measured 1.8% — threshold fine, range stale); checks-spec's pins inventory omits `pins/thinking-on/`; `docs/polar/README.md` indexes 5 of its 9 evidence directories (missing the CP-17/21 loop dirs and both thinking dirs); charter A-13's "two data points" omits CP-32's third serialized sync; A-9 is still a bare row though CP-01 exercised it ("held, four caveats" — the caveats are findable in no charter text); A-29's evidence list omits CP-38's Llama body (its strongest data point). *Mislead: low. Fix: trivial each.*

---

## ORPHANED — real, recorded, owned by nobody

**O1. The library's public mirror is 3 commits behind — the exact failure F-52 recorded has recurred, and it's holding the register hostage.** `git status -sb`: `ahead 3` — CP-36, CP-37, CP-38 unpushed. The public repo therefore lacks the entire stranger-test record *including register rows F-54..F-68*, the model-surface derivation record, and `serve-llama31.sh`. F-52's cure ("the protocol sentence is the operator's to add") was never added; F-14's "mirror pushed current" claim is stale with it. Both consumer repos are fully pushed (verified) — this is library-only. *Mislead: high. Fix: `git push` + one protocol sentence.*

**O2. `release.yml`'s required-entries tuple is now TWO must-ship files short, and its named window passed.** CP-33 recorded it one short (thinking-on pins) as "a one-line follow-up for the next `.github` lift". The next lift was CP-34 — which edited ci.yml, *executed release.yml to publish 0.1.2*, and didn't add the entry, while adding a second unasserted must-ship (`ingest_corpus.py`). The tuple still asserts only the original three (`release.yml:90-91`, untouched since CP-19). In-suite tests guard both files, but only on the CI path — `release.yml:12` deliberately doesn't re-run CI on tags. *Mislead: medium. Fix: one line.*

**O3. C-3 (thinking-on collection at scale) was committed by CP-28's GO and then vanished.** "Phase C stays open: C-2 and C-3 run" → C-2 landed at CP-30; C-3 appears in no document after CP-30's `next:` line. CP-32's 72-episode pooled-ON run covers adjacent ground, but nothing says it satisfies C-3; nothing closed or re-parked it. *Mislead: medium. Fix: small (a disposition note).*

**O4. Gap row 2's estate residual (anonymous Forgejo read → credentialed-clone/egress decision) lost its owner at CP-09′.** Assigned to CP-04′/CP-09′ by VERDICT; both punted ("remains OPEN, unchanged"); zero mentions since, across ~8 estate bring-ups — including the new *public* demo estate, whose compose sets no sign-in requirement. *Mislead: medium. Fix: medium (a decision, not a line).*

**O5. Gap row 22's per-episode provenance binding — cure named "at CP-04′ bring-up"; CP-04′ ran without it.** Still one of the final two GAPs at CP-38. Its sampling half was independently reborn as wishlist row 38 (CP-35, demo-side) with no cross-reference to row 22 — the same residual now recorded twice, owned zero times. *Mislead: medium. Fix: medium.*

**O6. Two trigger-parked wishlist rows whose trigger fired quietly.** Row 19 (pins seam for card edits): "cheap at the next `pins/` lift" — CP-30 lifted `pins/derive_pins.py` itself (+27 lines) and cured none of the row's three facts (the stale ADR-0003 note, the hardcoded `("summarize", "analyze")` tuple, no bank-build integration). Row 10 (skills interior strictness): "cheap at the next `corpus/` freeze-lift" — CP-24 put +289 lines into the very validator file; the row landed at neither that nor CP-30's lift. *Mislead: medium/low. Fix: small each, or an honest re-park.*

**O7. F-44's park expired six CPs ago.** "Unfixed deliberately (would confound CP-32's A/B)" — the A/B completed at CP-32; `reward_cited_pages.py:34-37` still grades `sorted(glob("*.md"))[0]` only, silently; the named durable fix (declared per-record artifact path) has no owner. Mode-amplified under the shipped thinking-on default (8/15 vs 1/15 deliverable rate). *Mislead: medium. Fix: trivial re-own; small to fix.*

**O8. Wishlist rows 24 and 26 defer to "mcp-service's own cadence" — a cadence that hasn't existed since CP-15 and that nothing schedules.** Row 26's own unblock condition (an evidenced CI flake) *already fired* at CP-33. These rows are the only reason such a lift would open; nothing external ever will. *Mislead: medium. Fix: re-own explicitly.*

**O9. Minor:** `cp17_loop/smoke.py` is real CP-17 evidence (the library report lists it among the five committed loop files) but is absent from its own run book's file table — indistinguishable from residue to an examples-repo reader. Honorable mentions with live-but-thin ownership: wishlist row 9 (the oldest open row, CP-13-era), row 22's F-39 warn-once half ("needs its own ADR" that no CP is scoped to write), row 16 (the four mandatory carried-patch tests run in no CI — re-executed manually in this audit: 25 passed). *Mislead: low.*

---

## CONTRADICTORY — two documents disagree

**C1. `mcp-service/README.md:6-8` blanket disclaimer "CP/ADR numbers in this file are the predecessor's" vs its own body.** Since CP-15 the body carries *this* repo's numbers unmarked (CP-15, ADR-0016, A-25, CP-10, CP-14 — eight-plus sites). The body is right; the disclaimer misroutes an auditor to `gsj-envloader`. *Fix: trivial.*

**C2. `docs/corpus-contract.md:1` — "v1: CP-33 / ADR-0046" resolves to nothing in this repo and collides with this repo's own CP-33.** It's the predecessor's numbering, inherited verbatim at CP-01, un-namespaced — beside CP-14/CP-24/ADR-0015/0022 citations that *are* this repo's. The library's ADRs end at 0025, and its CP-33 (2026-08-14) post-dates the v2 contract — chronologically impossible as v1's origin. Same class as W2; `corpus/ingest_corpus.py:65` repeats it. *Fix: trivial (add "predecessor's").*

**C3. The CP-33 paragraph inside charter §7 cites wishlist rows as bare "Row 24/26/28/32" — inside the gap register's own row-number namespace.** Each sentence is true of its wishlist row and false-reading of the same-numbered gap row (gap 24 = env pinning, 26 = bounded collection, 28 = async staging, 32 = split enforcement/DROPPED); the same paragraph writes "Wishlist 20 landed…" when it means the wishlist, proving the convention exists. *Fix: trivial.*

**C4. Two wishlist rows are both numbered 19, and cross-references split between them.** Pins-seam (CP-24, OPEN) at `VERDICT.md:521` vs cli-guard (CP-21, DONE CP-27) at `:526`. CP-27's "wishlist 19 → DONE" means the cli row; CP-24/CP-33 mean the pins row. The table's three *other* numbering slips each carry an in-row note; this one is acknowledged only in the examples RUNBOOK. A reader tracing "wishlist 19" can conclude the open pins-seam row is done. *Fix: trivial.*

**C5. F-42's mitigation rider vs F-40's retirement, one file apart** (the rider says off-mode-only pins; F-40/pyproject/the published wheels say both since 0.1.1 — F-40 is right; see S6). *Fix: trivial.*

**C6. CP-22's report says A-14 gained a live re-verification note; the A-14 row carries zero annotations** — it still reads as a pre-CP-06 prediction ("can host `gsj_rollout` *when* CP-06 wires the harness"). The seam defect CP-22 found lives only inside A-8's residual (c) and REVENDOR.md. *Fix: trivial.*

**C7. ADR-0019's earmark arithmetic (9+2 closing a 23-line earmark) vs ADR-0021's measured 12+9+2.** ADR-0021 is right (from `git show`), and VERDICT row 18 flags the original text — but ADR-0019 itself carries no marker. *Fix: trivial.*

**C8. ADR-0005 "requires-python stays `>=3.11`" vs ADR-0020/pyproject `>=3.12`.** ADR-0020 records the narrowing but never names ADR-0005 as what it supersedes; ADR-0005 has no amendment. *Fix: trivial.*

**C9. `config.yaml.example:40-41` says on `/tokenize`-less engines the eot default "silently stands" — bootstrap NOTEs it loudly, README says "names, out loud, what it cannot derive", MODEL-SURFACE says "never silently".** The three newer texts are right (`bootstrap.py:754-763` prints the two-part cure). *Fix: trivial.*

**C10. MODEL-SURFACE's chat-template row says "the four rows below are derived from it" — CP-38 inserted the tool-parser row directly below it**, and that row is serve-argv-bound, not template-derived; the derived four are now rows 2–5. Both the who-derives and if-wrong cells carry the stale pointer. *Fix: trivial.*

**C11. The demo's generated `pins.gsj.json` contradicts itself.** `derive_pins()` rewrites the G1/G2 *values* (verified: the demo skill card's real sha) but never rewrites `host` ("H200 estate…"), `walk_status` ("done, CP-11 Mac…"), or the G1/G2 provenance blocks — so a stranger's generated artifact claims H200 provenance for values derived on their laptop. *Fix: small (`bootstrap.py:657-770`).*

---

## MISSING — the record implies it exists; it doesn't

**M1. F-54..F-68 have no register home.** The examples `FINDINGS.md` titles itself "the external register" and ends at F-53 with no continuation pointer; the demo repo — whose findings they are — has no `FINDINGS.md` at all; the fifteen rows exist only inside `docs/reports/CP-36.md` (currently unpushed, see O1). CP-36 itself had to reconstruct the numbering basis from a corrupted prompt. A future examples-side row minted from the register file alone would collide at F-54. *Mislead: medium-high. Fix: small (a pointer, or a demo-side register file).*

**M2. `MODEL-SURFACE.md:25` cites `healthcheck.sh` — no such file exists anywhere in the demo repo.** It lives in the *library's* `staging/serving/`, a repo the demo's stranger is never told to clone. This is precisely the F-68 class CP-36 fixed — reintroduced by CP-38's own new table row. *Fix: trivial.*

**M3. A-17..A-20 don't exist, and only a report says so.** §4 numbers through A-30 with a silent A-14→A-16 jump (A-15 filed out of order); the only statement that A-17–A-20 were never assigned is CP-12's report. Compounding: CP-06's report contains a dangling "(A-20 nuance)" — a predecessor-register id — so a reader hunting the ghosts gets a false hit. *Fix: trivial (one register note).*

**M4. `git.date` parseability is a contract requirement nothing validates.** The contract says "any fixed git-parseable date" and promises every failure names the file and rule at validate; `load_corpus` checks only non-empty-string, and a garbage date dies later at scaffold with git's own `fatal: invalid date format` (tested live) — loud, but after validate has said PASS, naming no corpus file. *Fix: small.*

**M5. Both golden MANIFESTs cite `staging/collector/` and `staging/pins/` — paths that exist only in the predecessor checkout**, un-namespaced, in the same files that elsewhere say "this repo's `staging/…`". Correct provenance for predecessor-collected goldens; wrong address book. *Fix: trivial.*

---

## RESIDUE — the catalogue (nothing removed; dispositions marked)

**Remove (1 item):**
- **`dist/gsj_harness_rollout_server-0.1.1-py3-none-any.whl`** — untracked, gitignored, one release behind, missing CP-34's `ingest_corpus.py`. Not part of the record — but it has teeth: examples `install.sh` *prefers* any sibling `dist/` wheel over PyPI, so on this machine the documented "run ahead of a release" developer path silently runs one release *behind*; and a habitual `twine upload dist/*` would push the stale artifact. Nobody would miss it; `install.sh` itself prints the delete-dist cure.

**Evidence — keep (all suspicions disproven):**
- `spike/` — deliberate frozen CP-06 evidence (single commit; protected by every later scope-drift freeze diff; the tracked `auth.json` is literally `{}`).
- `cp17_loop/` and `cp21_loop/` — confirmed deliberate evidence run books (each README opens with its CP provenance; shapes recorded in consumer ADR-0002/0004). `smoke.py` belongs to this set — see O9.
- `staging/serving/serve.sh` / `serve-updated.sh` / `serve-llama31.sh` — **not** a superseded chain: pristine bring-up (exercised as late as CP-37), weight-sync half (CP-17/21/32, test-guarded), and the CP-38 second family. All three alive.
- `example_project/pins/` — a deliberate tombstone README documenting the CP-33 retirement.
- `taskbank.parquet` (examples + library staging) — deliberate committed freeze records; byte-identical to each other and to the lock's sha (verified).
- Bridge `fixtures/`, `tests/fixtures/`, `tests/golden/`, `docs/golden/mac/tokens.npz`, `mcp-service/tests/measure_determinism.py`, `docs/golden/mac/artifact/NOTE` — all loaded/referenced; the NOTE is a deliberate explicit-empty marker.
- **Nothing generated is committed by accident in any repo** — every `__pycache__`, venv, cache, `dist/`, and the demo's `work/` runtime state is gitignored-untracked (checked with `git ls-files` + `--ignored` in all three).
- `rebuild_taskbank.sh`, `read.py`/`bootstrap.py`/`preflight.py`/`make_corpus.py` — all referenced, zero uncalled functions (AST-checked). `gsj_rollout/` has no dead defs; the one apparent orphan (`ValidatingPrefixMergingBuilder`) is loaded by import-path string.

---

## The honest summary

**This record is unusually good where it counts, and its failures are of one specific kind.** What held under execution and byte-level verification: every DONE wishlist row actually landed (all ~28 verified in code, none was planned-only); the gap register's 32 rows and every tally transition re-compute exactly; every suite claim is true *by execution today* — root 161, corpus 58, mcp-service 89 (twice, including a fresh dependency resolve), vendored Polar exactly 175/3 with the identical three recorded pre-existing failures, the four carried patches' 25 tests, slime 14, verl 26 against 0.1.2; the 1,999/2,000 line census reproduces to the digit and `checks.py` sits at its machine-checked 528; all eight golden sha256s are byte-exact; the published wheels contain exactly what the record claims (verified by unzipping PyPI's own artifacts); GHCR names and platforms match the registers (including row 40's honestly-OPEN amd64-only status); POLAR_SHA and every cross-repo version pin agree everywhere except the bridge docs; and of ~120 forward-pointing ownership items, nearly all were picked up by their named CP. The verify pass refuted **zero** of 91 findings — but also note what the findings are: overwhelmingly annotation debt, not broken mechanisms. Not one live gate, pin value, or code path was found wrong.

**What didn't hold — the pattern, not the list:** the record's *center* is maintained and its *periphery* is not. Four specific decay modes account for nearly everything above: (1) **consumer-facing copies lag the authoritative registers** — the examples repo's FINDINGS/RUNBOOK/bridge READMEs teach fixed bugs as live, a dead install path, and the wrong current version, while the library-side rows they mirror are correct; (2) **the OPEN side of registers decays while the DONE side stays true** — closures get annotated, but rows whose *reason-to-wait* expired (F-44, rows 19/10, release.yml's tuple, C-3) just go quiet; (3) **un-namespaced predecessor citations** (corpus-contract header, five in `forgejo/`, `checks.py:97`, the golden MANIFEST paths) that were harmless when this repo's own numbering hadn't caught up, and now actively collide; (4) **CP-38's second family propagated exactly as far as CP-38's file list and no further** — seven Qwen-scoped texts survive in files the CP didn't open.

**The three things a production-prep phase should fix first:**

1. **Publish the record: `git push` the library (3 commits), then give F-54..F-68 a register home and add F-52's protocol sentence.** Until this happens the public repo is missing the stranger test, the model-surface derivation, the second serve recipe, and fifteen register rows — the highest-leverage trivial fix in the entire audit (O1 + M1).
2. **One refresh pass over the examples repo's consumer surface** — the six lagging FINDINGS statuses, the F-42 rider, the v0.1.0 texts, the two ENOENT wheel commands, the three CP-27-fixed "still live" bullets, and the two Qwen-bound recipes (S2–S6, W5). This is the repo a stranger consumes; today it is the least accurate of the three.
3. **The two one-line traps on the publish and restore paths:** `release.yml`'s assertion tuple (two must-ship files unguarded on the tag path, O2) and `staging/README.md:34`'s Megatron pin that fails at import (W1) — plus, in the same sitting, `CLAUDE.md` (S1), because it is the one stale document that every future checkpoint is guaranteed to read first.

---

**STOP.** No commits, no edits, no fixes were made. All three working trees verified clean.