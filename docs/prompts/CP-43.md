# CP-43 — the demo, in brief

Scope: the demo repo's front door, for someone with documents and an
inference endpoint who wants their first trajectory.

**Audience**: not an engineer evaluating architecture — someone deciding
whether this is worth an afternoon. They have data. They want to know
what they'd get.

**Freeze-lift: the demo repo. Plus `docs/**` in the library for the
report.**

Read first: the demo's `README.md` and `RUNBOOK.md`, `MODEL-SURFACE.md`,
`FINDINGS.md`, and `docs/reports/CP-36.md` (what a real stranger hit).

## Step 1 — What you get, in one paragraph and one picture
The picture is the sequence: your corpus + your endpoint + one command →
a running estate → an episode → a trajectory you can read. Show the three
things they bring and the one command that does the rest.

## Step 2 — The measured expectations, up front
CP-36 measured a stranger's whole path. Put the numbers where they'll be
read: install time, disk, cold-`up`, per-episode wall, and the two
platform fa the amd64 pull; a non-Qwen endpoint derives
automatically but the serve argv is theirs to write).

**F-18's lesson binds here**: wrong expectations made a working system
read as broken. Say what's normal.

## Step 3 — What a trajectory looks like
An actual excerpt — the transcript reader's output on a real episode.
Someone deciding whether to spend an afternoon wants to see the thing,
not a description of it.

## Step 4 — The corpus, in a picture
The contract's tree in a diagram, with the synthetic corpus as the worked
example. Someone bringing their own documents needs the shape, and a tree
diagram is worth two paragraphs of prose.

## Step 5 — Close what you can of the audit's open demo items
W6, W7, W9's demo half, C9, C10, C11, M2, and S4's four demo texts —
all frozen to CP-41 by its no-consumer-edits wall, all owned by "the
demo's next sitting." **This is that sitting.** Take what belongs in the
files you're already editing; record what doesn't.

## Step 6 — Register and push
FINDINGS updtated.

## DoD
The README read as a stranger; the audit ids closed; the push.
One commit: `CP-43: the demo, in brief`.

## Report
Brief, in the library's `docs/reports/CP-43.md`: what landed, which
audit ids closed, and whether the front door now answers "is this worth
an afternoon" in under two minutes.
