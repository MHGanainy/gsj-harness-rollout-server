# CP-41 — M13c: namespaces, amendments, residue

You are executing CP-41 of `gsj-harness-rollout-server`. The audit's
last third, and the smallest of the three.

**Everything here is a correction to a document or a citation.** No
behaviour changes. If any item turns out to need one, stop and record it.

**Freeze-lift: `forgejo/` (W2 — the conscious lift CP-39 deferred),
`corpus/ingest_corpus.py` (C2's header comment only),
`gsj_rollout/checks.py` (W9's citation comment only — see the warning
below), `pins/**` (the recorded ask), `docs/**`, and `dist/` (the
residue). NOT lifted: anything else under `gsj_rollout/`, `vendor/`,
`mcp-service/`, `spike/`, `staging/`, `tests/`, `.github/`, and the two
consumer repos — CP-40 refreshed them and they stay put.**

**No GPU, no estate.**

Read first: `docs/AUDIT-2026-08-24.md` — the specification; every item
cites its id. `docs/reports/CP-40.md` §next (the inherited list,
including the recorded ask).

## Step 0 — Protocol
Prompt at `docs/prompts/CP-4 with the CP. **Push and state
the outcome** — §8 rules 8 and 9. Corrupted spans: reconstruct, apply,
list under `questions:`.

## Step 1 — The `checks.py` warning, read before touching it
`checks.py` is at **528/528** with ADR-0021's machine-checked *equality*
tripwire, and the tree is at 1,999/2,000 with headroom 1.

W9 is a comment fix — `checks.py:97` cites "ADR-0007(e)" unqualified,
which is the *predecessor's* ADR; this repo's ADR-0007 is the glue stitch
and has no lettered clauses. **A citation correction must not change the
line count.** If the corrected text will not fit on the existing lines,
stop: that is an ADR-0021 allowance question and a §1 budget question,
and this CP is neither.

State plainly which happened.

## Step 2 — The namespace collisions
Un-namespaced predecessor citations that were harmless when this repo's
numbering hadn't caught up, and now resolve to the wrong thing:

- **W2 — `forgejo/`'s five.** `docker-compose.yml:1` and `:9`, `up.sh:2`,
  `create_owner.sh:3` andis repo CP-29 is the PyPI release,
  CP-02 is the Polar-pin audit, CP-33 is the wishlist cluster, M7a is the
  re-vendor door, and ADR-0046/0047 do not exist. CP-01 qualified the same
  class in adjacent lines ("The predecessor's CP-02 recipe") and left
  these five — so **`docs/reports/CP-01.md:27`'s claim that predecessor
  citations were qualified is contradicted by the files.** Fix the five;
  record the erratum.
- **C2 — the corpus contract's header** cites "v1: CP-33 / ADR-0046",
  which resolves to nothing here and collides with this repo's own CP-33.
  `corpus/ingest_corpus.py:65` repeats it.
- **W9 — `checks.py:97`** (Step 1's warning applies).
- **M5 — both golden MANIFESTs** cite `staging/collector/` and
  `staging/pins/`, paths that exist only in the predecessor checkout, in
  files that elsewhere say "this repo's `staging/`". The provenance is
  correct (predecessor-collected goldens); the address book is wrong.

The rule these all violate is one CP-01 already applied: **a predecessor
cis qualified as the predecessor's.** Apply it consistently.

## Step 3 — The ADR amendments
Append-only, dated, in the ADR-0003 precedent's shape — record what
moved, do not rewrite the decision:

- **C8 / ADR-0005** says `requires-python` stays `>=3.11`; CP-19 narrowed
  it to `>=3.12`, and ADR-0020 never names what it supersedes.
- **S12 / ADR-0011** — its ground moved at CP-37: the decision assumed a
  tokenizer at pin time, and CP-37 derived the same ids from a live
  endpoint in four HTTP calls with no tokenizer, deliberately moving the
  venue off the pins walk.
- **S12 / ADR-0020** — its Consequence still declares the package
  unpublished, the name unclaimed, and row 17 open. All closed at CP-29.
- **C7 / ADR-0019** — its earmark arithmetic (9+2 against a 23-line
  earmark) was corrected by ADR-0021's measured 12+9+2, and VERDICT row
  18 flags it, but the ADR carries no marker.

Also **C6** — CP-22's report says A-14 gained a live re-verification note;
the A-14 row carries none and stil pre-CP-06 prediction.

## Step 4 — The residue, and the recorded ask
- **The stale wheel.** `dist/gsj_harness_rollout_server-0.1.1-py3-none-any.whl`
  — untracked, gitignored, and with teeth: `install.sh` prefers a sibling
  `dist/` wheel over PyPI and takes the lexical first, so the documented
  developer path runs one release behind. CP-40's proof run left a 0.1.2
  beside it. Remove the 0.1.1; say what remains.
- **The recorded ask — wishlist 19(a) / audit S8.** `pins/pins.gsj.json`
  carries two stale notes that **ship in every wheel**: line 86 says skill
  resolution "stays taskbank-deferred" (resolved at CP-24/ADR-0022) and
  line 102 presents G6 as a future rule (landed CP-23). A pip consumer has
  no VERDICT to correct them. CP-40 asked this sitting to take it.

  **Verify before editing**: `derive_pins.py` must still reproduce every
  approved value, and the values must not move. A note edit that changes
  a hash is not a note edit.

  If you decline, say why — CP-40 made declining a visibion.

## Step 5 — The remaining stale items
The audit's leftovers, each one line: **S13** (the pre-CP-33 archive
filename in gap rows 16/22 and ADR-0008), **S14**'s library half
(`ci.yml`'s `>=3.11` — but `.github/` is frozen, so record it),
`checks-spec`'s pins inventory omitting `pins/thinking-on/`,
`docs/polar/README.md` indexing 5 of 9 evidence directories, A-13's "two
data points", A-9's bare row, A-29's evidence list omitting CP-38's Llama
body. **S11** — VERDICT's "What comes next" still lists M5/M6 as pending.
**C1** — `mcp-service/README.md`'s blanket disclaimer is false since
CP-15, but that file is frozen: record it.

**C3, C4** — the charter's row-number namespace collisions and the two
rows both numbered 19. Fix or record.

## Step 6 — Register and push
- Every audit id closed, by id, and every one deliberately left with why.
- The `checks.py` outcome from Step 1.
- **The push outcome** for every repo touched.

## Definition of Done — run and show output
```
git diff HEAD -- vendo/ spike/ staging/ tests/ .github/ \
  gsj_rollout/pi_harness.py gsj_rollout/builder.py \
  gsj_rollout/receiver.py gsj_rollout/client.py gsj_rollout/cli.py \
  gsj_rollout/config.py                            # → empty
pytest -q                                          # 161, unchanged
wc -l gsj_rollout/checks.py                        # 528, unchanged
python pins/derive_pins.py | tail -1               # all approved values reproduced
ls dist/                                           # the 0.1.1 gone
grep -rn 'CP-29\|CP-33\|ADR-004' forgejo/          # qualified, or empty
git log --oneline origin/main -1                   # pushed
test -f docs/reports/CP-41.md && echo OK
git status --porcelain                             # → empty
```
One commit: `CP-41: namespaces, amendments, residue`.

## STOP — hard wall
No behaviour changes anywhere. No consumer-repo edits. Do not draft
beyond CP-41.

## Report
`docs/reports/CP-41.md` per the template, printed. Under notes: the
`checks.py` line-count outcome; th `forgejo/` citations with the
CP-01 erratum; the four ADR amendments; the pins-notes decision with its
derive proof; what was recorded rather than fixed and why; and — since
this closes the audit — **which audit ids remain open after CP-39, 40,
and 41, and who owns each.**
