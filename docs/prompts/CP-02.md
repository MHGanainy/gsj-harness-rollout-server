# CP-02 — M0c: the fork audit

You are executing CP-02 of `gsj-harness-rollout-server`. Scope: resolve A-7
with evidence. The prior research reached Polar's paper and READMEs but was
robots-blocked on every GitHub fork, branch, tag, commit, and compare page —
so the four reported defects, the maintainer forks, and the freeze date are
all UNVERIFIED. **The authenticated REST API is not robots-blocked.** Use it.

**Output is a decision document and a pin recommendation. No code, no
vendoring, no `gsj_rollout/` edits, no moved-component edits.**

Preflight: `gh auth status` must show a token with public-repo read. If not,
write `docs/reports/CP-02.md` with `status: blocked` naming what is missing,
commit prompts/ + report only, stop.

Read first: this repo's `docs/CHARTER.md` (A-7, and §9's abandon
conditions), `docs/checks-spec.md` (what our validation will need from
Polar's capture).

## Step 0 — Protocol
Prompt at `prompts/CP-02.md`; commit with the CP. Corrupted spans:
reconstruct, apply, flag`questions:`.

## Step 1 — The upstream baseline
Via `gh api`, establish for `NVIDIA-NeMo/ProRL-Agent-Server`:
- default branch, its HEAD SHA and **committer date**
- whether `f0e8343a` exists and is HEAD (the reported freeze)
- all branches with their HEAD SHAs and dates; is anything more current than
  the default branch
- tags and releases — confirm none, or list them
- commits on the default branch since 2026-06-01, with dates and messages
- open PRs, their authors, and how long they have been open

**The question this answers:** is the project frozen, slow, or just
squash-merging from elsewhere? That determines whether "vendor a pinned SHA"
is a reasonable posture or a euphemism for forking abandoned code.

## Step 2 — The fork network
`gh api /repos/NVIDIA-NeMo/ProRL-Agent-Server/forks --paginate`. For each
fork, get `pushed_at` vs `created_at` to separate bookmarks from work. For
every fork with real pushes, list branches and identify which branch names
are **absent from upstream** — those arrk's own work; the rest are
inherited history.

Then, for the forks the prior research named — `skzhang1`, `billxbf`,
`BryanChen408`, `shamanez`, `leeyykk`, `rucnyz`, `Fraser-Greenlee`,
`barry-jin`, `alexps9`, `jbarnes850` — confirm existence and summarize what
each actually changed, using `gh api /repos/{owner}/{repo}/compare/{base}...
{head}` for the diffstat. **Report which of the prior research's fork claims
are confirmed, which are refuted, and which could not be resolved.**

## Step 3 — The four defects (the core of this CP)
For each, fetch the commit by SHA
(`gh api /repos/{owner}/{repo}/commits/{sha}`) and **read the patch**, not
the message. If a SHA does not resolve, say so and search the fork's commits
for the described change before concluding it doesn't exist.

**D1 — content-whitelist leak** (`cb613282`, `BryanChen408`). Claim:
`_is_non_agent_side_completion` gates on a hardcoded content whitelist in
addition to a shape check, so non-agent-side completions with other bodies
leak througain_length=1` trainable traces receiving session-level
reward; density up to 27% of a batch. Determine: does the whitelist exist in
upstream at the SHA we'd pin? What exactly does the fix change? Is this the
same phenomenon the paper reports as `per_request` reward hacking, or a
second, distinct bug?

**D2 — logprob sentinels** (`56b993de`). Claim: `_logprob_integrity` exists
in `record_utils.py`; `prefix_merging` writes `0.0` at interstitials; vLLM
emits `-9999.0` for absent logprobs and upstream doesn't treat it as
missing. Determine: does `_logprob_integrity` exist upstream, what does it
check, when does it run, and is `-9999.0` handled. **Our `checks.py` rejects
both sentinels regardless — this tells us whether we're duplicating a
guard or supplying a missing one.**

**D3 — abort → ERROR** (`3c930e6d`). Claim: `finish_reason == abort` should
set `trajectory.status = ERROR` so downstream drops it; upstream doesn't.
Determine what statuses exist upstream, who sets them, and what the slime
bridge d each. Also enumerate any *other* terminal state that should
be excluded but isn't — timeout with partial capture, empty generation,
tool-error loops.

**D4 — reasoning masked by default** (`cdf9ece4`). Claim: `<think>…</think>`
spans are masked out of `_response_loss_mask`, undocumented, and a fork
added `POLAR_MASK_REASONING` to toggle it. Determine: is the masking present
upstream, is it conditional, and what happens for a model whose reasoning is
not `<think>`-delimited. **Note whether it is a bug or a defensible default
— there is a real argument each way.**

**D5 — the integrity template** (`08e59e75`, the "155 lines" claim). Read
the diff. Is it accurate? Which four files, and are
`gateway/storage.py` + `prefix_merging.py` genuinely the insertion points
for our own checks, or does extending require patching elsewhere?

## Step 4 — The pin recommendation
Given Steps 1–3, recommend what CP-03 vendors, with reasoning:
- upstream default-branch HEAD at a named SHA, or
- a maintainer-fork HEustify against the risk of pinning a
  personal fork), or
- upstream HEAD **plus a named cherry-pick set** — list the SHAs, in order,
  and note conflict risk given squash-merge history.

State the re-vendor cost you expect, and whether any of D1–D4 must be
patched before CP-06's harness spike or can wait until CP-10's checks.

## Step 5 — Charter updates
- **A-7 resolves** to one of: confirmed / refuted / partially confirmed —
  with per-defect status. Rewrite the row to state what is now known.
- Add assumption rows for anything the audit surfaces (e.g. if upstream
  turns out to have no `_logprob_integrity` at all, that changes what
  `checks.py` must carry).
- Gap register: rows touching trajectory correctness, logprob handling, and
  quarantine gain notes from the audit. Any row whose status is now
  *knowable* moves off TBD.
- If the audit surfaces something that meets a §9 abandon condition, say so
  plainly in the report and do not soften it.

## Definition of Done — run and show output
`status                                   # token present
pytest -q                                        # unchanged, 1 passed
git diff HEAD -- gsj_rollout/ corpus/ mcp-service/ forgejo/ pins/   # → empty
test -f docs/reports/CP-02.md && test -f docs/decisions/ADR-0004-polar-pin.md && echo OK
grep -n 'A-7' docs/CHARTER.md                    # the resolved row
git status --porcelain                           # → empty after commit
```
One commit: `CP-02: fork audit`.

## STOP — hard wall
No vendoring, no code, no edits to `gsj_rollout/` or the moved components.
Do not draft or begin CP-03.

## Report
`docs/reports/CP-02.md` per the template, printed. Under notes:
- the upstream baseline (HEAD, date, branch/tag/PR reality)
- a **defect table**: claim → SHA resolves? → present upstream? → what the
  patch actually does → severity for us → must-patch or can-wait
- the fork-network summary: which prior claims confirmed / refuted /
  unresolved
- the pin recommendation with its reasoning and the - what this changes about `checks.py`'s job
- anything that hit a §9 abandon condition
