# CP-30 — M9b: green the badge, and land thinking

You are executing CP-30 of `gsj-harness-rollout-server`. Two pieces, in
order: fix the red CI that now fronts both public landing pages, then
implement what CP-28's GO committed to.

**Order matters.** The badge is the first thing every stranger sees on a
freshly published package, and a red one reads as abandoned. Five lines
first, then the substance.

**Freeze-lift: `corpus/tests/test_taskbank.py` (the fixture line only),
`.github/workflows/ci.yml`, `pins/**` (the per-mode tail — this is its
purpose), `gsj_rollout/config.py` (the thinking validator),
`gsj_rollout/pi_harness.py` **only if the mode flip genuinely requires it**
— decide and keep it minimal, `tests/`, `docs/**`. NOT lifted:
`checks.py` (528/528 with an equality tripwire — CP-28 measured that the
per-mode pin keeps G6 mechanically unchanged, so a change there means
something is wrong with the approach), `builder.py`, `receiver.py`,
`client.py`, `cli.py`, `vendor/`, `mcp-service/`jo/`, `spike/`,
`staging/`.**

**Needs GPU and estate for Step 4 only.** Steps 1–3 are desk work.

Read first: `docs/reports/CP-29.md` §the CI finding (wishlist 26's two
causes, both in frozen paths); `docs/reports/CP-28.md` — the GO verdict
and what it commits C-2 to, especially the re-pin geometry (**the 3-id
tail is not a suffix of the 7-id tail**, so a per-mode single-tail pin
keeps G6 unchanged) and the `thinking: "medium"` silent-clamp finding;
`docs/checks-spec.md` §G6 (the three options CP-23 recorded and their
costs); `docs/reports/CP-27.md` (the validator pattern — the message is
the deliverable).

## Step 0 — Protocol
Prompt at `docs/prompts/CP-30.md`; commit with the CP. Corrupted spans:
reconstruct, apply, list under `questions:`.

## Step 1 — Green the badge
Wishlist 26, both causes:

- **The fixture** — `corpus/tests/test_taskbank.py` carries a
  `serving_base_url` with the `/v1` suffix that CP-27's validator now
  correctly rejects. Drop the suffix. **The validator is right and is stale** — do not weaken the validator.
- **CI's corpus job** — its dependency set has been stale since CP-24
  (pyarrow, and whatever else the 17 additional CI-only failures name).
  Refresh it from `corpus/requirements.txt` rather than adding packages
  by hand, and update the job's label and comment to the real counts.

Then verify: the corpus suite green locally **and** in CI, the badge
green on both landing pages. Report the before and after counts —
CP-29 measured 56/58 local and 39/58 CI.

If either cause turns out to be more than the ~5 lines estimated, stop
and say what it actually is.

## Step 2 — ADR: the thinking mode
CP-28's GO, made concrete. Decide and record:

- **G6's per-mode pin.** CP-28 measured the geometry: thinking-off ends
  `[151644, 77091, 198, 151667, 271, 151668, 271]`, thinking-on ends
  `[151644, 77091, 198]`, and the second is *not* a suffix of the first —
  so a single-tail pin selected per mode keeps the rule mechanically
  identical. State how the mode reaches  second pins key, a
  per-mode approved set, or a config value the check reads. **Whichever
  it is, `checks.py` must not change** — if it must, that is a finding
  and the approach needs revisiting.
- **What the weaker gate now asserts.** Thinking-on's 3-id tail is
  template integrity, not thinking-suppression — G6 stops being able to
  prove thinking is off, because it isn't. Say plainly what the gate
  covers in each mode, in one sentence per mode.
- **The `thinking` validator.** CP-28 found pi **silently clamps unknown
  values to off** — so a typo produces a control run wearing the
  measurement's label, with no error. Reject anything pi wouldn't honour,
  at load, with a message naming the accepted values and the silent-clamp
  hazard. CP-27's messages are the standard.
- **Whether `pi_harness.py` needs anything.** The flag already exists in
  the harness settings; CP-28 flipped it in a scratch config. If the
  harness needs no change, say so — that is the result.

## Step 3 — Land it
The plidator, and whatever the ADR names. Tests:

- each accepted `thinking` value loads; each pi-clamped value is rejected
  with the message
- G6 passes a thinking-off trace against the off-tail and a thinking-on
  trace against the on-tail
- **G6 fails a thinking-on trace checked against the off-tail** — the
  CP-28 behaviour, still available, proving the mode selection is doing
  the work rather than the gate being loosened
- the pins snapshot and `derive_pins.py` handle the new shape
- the ADR-0021 equality tripwire still passes with `checks.py` untouched

## Step 4 — Prove it on the estate
Bring up per CP-09′'s fast path. One thinking-on episode through
`gsj-rollout submit`, and one thinking-off, both through the receiver:

- **thinking-on passes the full seam clean** — no G6 findings, no
  quarantine. That is the whole point of C-2 and the thing CP-28 could
  not have.
- thinking-off still passes, unchanged.
- `chains_total == 1` on both (CP-28 measured this for thinking-on
  across 15 episodes; ct survives the real receiver path).

Report both, with the attempt counts. Deposit under
`docs/polar/thinking-on/`.

## Step 5 — Charter, register, spec
- Row 14 (G6) — its mode-dependence recorded; what the gate proves in
  each mode.
- A-22 was EXERCISED at CP-28; note whether anything here changes it.
- `docs/checks-spec.md` §G6 — the Phase-C decision, resolved, with which
  option was taken and why.
- Wishlist 26 → done; 27 noted as healing at the next release.
- §3's budget: `config.py` grows by the validator; state the arithmetic.

## Definition of Done — run and show output
```
git diff HEAD -- gsj_rollout/checks.py gsj_rollout/builder.py \
  gsj_rollout/receiver.py gsj_rollout/client.py gsj_rollout/cli.py \
  vendor/ mcp-service/ forgejo/ spike/ staging/    # → empty
pytest -q                                          # 143+, say what changed
cd corpus && python -m pytest -q ; cd ..           # 58, green
# the CI run, green — the badge
wc -l gsj_rollout/*.py                           0; checks.py 528
# the validator's message, verbatim
# G6 both ways: on-trace/on-tail clean, on-trace/off-tail fails
# the two live episodes, both clean through the receiver
ls docs/polar/thinking-on/
test -f docs/reports/CP-30.md && echo OK
git status --porcelain                             # → empty after commit
```
One commit: `CP-30: green the badge, and land thinking`.

## STOP — hard wall
No `checks.py` changes. No examples-repo work (CP-31), no stranger test
(CP-32). Do not draft beyond CP-30.

## Report
`docs/reports/CP-30.md` per the template, printed. Under notes: the CI
fix with before/after counts and whether it was the estimated size; the
ADR's decisions; **what G6 asserts in each mode, one sentence each**; the
validator's message verbatim; the pin's shape and how the mode selects
it; the two live episodes; and whether `pi_harness.py` needed anything.
