# CP-29 — M8d: publish

You are executing CP-29 of `gsj-harness-rollout-server`. Scope: push the
public remote current, rehearse the release path on TestPyPI, and publish
`v0.1.0` to PyPI.

**This retires F-14 — CP-26's only BLOCKER**, and the last thing standing
between a stranger and `pip install gsj-harness-rollout-server`.

**The operator has already done the four out-of-band steps**: pending
publishers registered on both indexes (project
`gsj-harness-rollout-server`, owner `MHGanainy`, repo
`gsj-harness-rollout-server`, workflow `release.yml`, environments `pypi`
and `testpypi` respectively), and both GitHub environments created. Do
not re-do them; verify what you can and report anything that looks wrong.

**Freeze-lift: `docs/**`, `README.md`, and `pyproject.toml` **only if a
metadata defect surfaces** — a version bump or a URL fix is in scope; a
feature is not. NOT lifted: everything under `gsj_rollout/`, `vendor/`,
`corpus/`, `mcp-service/`, `forgejo/`, `pins/`, `spike/`, `staging/`,
`tests/`. If the release needs a library change, stop and record it.**

**No GPU, no estate.**

Read first: `docs/reports/CP-19.md` (the release workflow, what it
deliberately does not do, and the publish decision this CP reverses);
`.github/workflows/release.yml` — **read it before running it**, since
its wiring has never executed; `docs/reports/CP-26.md` §F-14 (what a
stranger cannot obtain, and that the library repo is needed *twice* —
wheel build and `vendor/polar/`); `docs/decisions/ADR-0023` (the install
story, which names the local wheel deliberately before the index).

## Step 0 — Protocol
Prompt at `docs/prompts/CP-29.md`; commit with the CP. Corrupted spans:
reconstruct, apply, list under `questions:`.

## Step 1 — Preflight, before anything is pushed
Verify rather than assume, and report each:

- **The remote's actual state.** F-14 measured the public mirror six
  commits stale, predating CP-20. What is on it now, what would pushing
  add, and does anything in the local history need to *notCheck for secrets, tokens, estate hostnames, and internal paths in the
  commits about to be pushed — `staging/` carries real IPs and the MCP
  secret's *filename*, and CP-04′ recorded an 0600 scratch file. **If
  anything sensitive is in the history, stop and report** rather than
  pushing and rotating afterwards.
- **The wheel's contents**, one more time: the nine exclusions and three
  inclusions CP-19 asserted. A local `python -m build --wheel` plus the
  assertions, before CI does it.
- **The metadata as PyPI will show it.** `twine check`, and read the
  rendered README as a landing page — CP-25 rewrote it for a stranger,
  and this is where that gets tested by a real renderer. Absolute links
  especially (relative markdown 404s on PyPI — CP-19's finding).
- **The version.** `0.1.0`, and PyPI never allows re-uploading a version.
  Confirm it is what you mean to burn.

## Step 2 — Push the remote current
Push `main` to the public repository. Then verify from the outside: does
the tree a stranglone contain the consumer surface (the example
project lives in the *examples* repo — say what a stranger gets from each
and whether the README makes that clear).

**The examples repo has no remote** (F-14's other half). Decide: push it
too, or state why not and what a stranger does instead. A published wheel
whose run book lives nowhere fetchable is half a cure.

## Step 3 — Rehearse on TestPyPI
`workflow_dispatch` on the release workflow. It builds, asserts the
exclusions, runs `twine check`, and uploads to TestPyPI — and the PyPI
job is gated on a `v*` ref, so a dispatch structurally cannot reach the
real index.

**Three things have never executed and this is what tests them**: the
OIDC exchange (a mismatch in environment, workflow filename, or repo
fails `invalid-publisher`), the exclusion assertions in CI rather than
locally, and whether an index accepts PEP 639's SPDX expression plus
`license-files` — `twine check` is not the same validator.

Report the run job by job. **If it fails, that is tfinding** —
diagnose before fixing, and say whether the failure was in the workflow,
the metadata, or the publisher configuration.

Then verify the artifact from outside: install from TestPyPI into a
scratch venv **outside every repo**, import `checks`, and validate the
real CP-09′ callback body — CP-16's proof, now against a published
artifact rather than a local wheel. Note TestPyPI has no dependency
mirror, so the install needs `--extra-index-url`; say what you used.

## Step 4 — Publish
Only if Step 3 is green. Tag `v0.1.0` at a CI-green commit and push it.
The workflow runs the same path with the PyPI job appended.

Report the run, then **the real proof**: a scratch venv outside every
repo, `pip install gsj-harness-rollout-server` with no index flags and no
local wheel, then `checks.validate_session_result` on the real body →
`[]`. That is F-14 retired, demonstrated rather than asserted.

Also check what a stranger sees: the PyPI project page, the rendered
README, the links.

## Step 5 — Upblication makes stale
- `README.md` — "not yet published" becomes the install line. ADR-0023's
  local-wheel-first ordering in `install.sh` was justified by the name
  being unclaimed; it no longer is. **That is an examples-repo change and
  a real decision** — state it, and either make it or say why not.
- The run book's install section, same reason.
- `docs/VERDICT.md` wishlist 17 → done, F-14 → retired.

## Step 6 — Charter and register
- F-14's retirement, with the demonstrated install as evidence.
- What a stranger hits now that F-14 is gone — CP-26's list, re-ordered.
- Any finding from Steps 1–4 as a wishlist row.

## Definition of Done — run and show output
```
git diff HEAD -- gsj_rollout/ vendor/ corpus/ mcp-service/ forgejo/ \
  pins/ spike/ staging/ tests/                     # → empty
pytest -q                                          # 143, unchanged
# the preflight: remote state, wheel contents, twine check, the secret sweep
git log --oneline origin/main -1                   # the remote, current
# the TestPyPI dispatch, job by job
# the TestPyPI install proof, from outside every repo
# the v0.1.0 run, job by job
# the PyPI install proof — no flags, no local wheel, findings: []
test -f docs/reports/CP-29.md && echo OK
git status --porcelain                             # → empty after commit
```
One commit: `CP-29: publish`.

## STOP — hard wall
No library changes. No GPU, no estate, no thinking-on. If the release
path needs a `gsj_rollout/` fix, stop and record it rather than shipping
a patched version. Do not draft beyond CP-29.

## Report
`docs/reports/CP-29.md` per the template, printed. Under notes: the
preflight including **the secret sweep's result**; what the remote gained;
the TestPyPI run job by job with anything that failed and why; the
install proofs verbatim, both indexes; the examples-repo decision; what
publication made stale and what you changed; and CP-26's stranger list
re-ordered now that F-14 is gone.
