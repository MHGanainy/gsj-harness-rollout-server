# CP-01 — M0b: the moves

You are executing CP-01 of `gsj-harness-rollout-server`. Scope: bring the
Polar-independent components across from the predecessor, reconcile paths and
imports, and prove the repo stands alone. **No Polar, no vendoring, no logic
in `gsj_rollout/` — every module stays as CP-00 left it. The predecessor is
frozen: read from it, never write to it (law 3). No estate bring-up, no GPU,
no Docker.**

Predecessor: `/Users/elganayni/mg/workspace/gsj-envloader` @ v0.8.0 — read
its `README.md`, `docs/HOLES.md`, `docs/corpus-contract.md`,
`corpus/ingest_corpus.py`, `mcp-service/README.md`, and
`staging/pins/derive_g2.py` before moving anything. Read this repo's
`CLAUDE.md` and `docs/CHARTER.md` first.

## Step 0 — Protocol
Prompt at `prompts/CP-01.md`; commit with the CP. Corrupted spans:
reconstruct, apply, flag under `questions:`.

## Step 1 — ADR-0002: the ownership boundary
CP-00's report flagged that the predecessor's `mcp-service/` measures ~852k
lines raw against ~1,038 of own code — a venv, a vector index, or vendored
artifacts inside the tree. **Define the boundary before moving anything.**

For each component, enumerate what comes and what does not, and state the
rule that decides:
- `corpus/` — the pipeline, its tests, `docs/corpus-contract.md`, and the
  staging source tree (`corpus/staging/`: cases, prompts, skills,
  `corpus.yaml`). **Not**: generated artifacts (`corpus.lock.json`,
  `taskbank.parquet`) if they are reproducible — decide and state whether
  they come as committed data or get regenerated here.
- `mcp-service/` — the service package, config, Dockerfile, compose, tests,
  README. **Not**: `.venv/`, `data/` (clone cache + vector index), any
  `__pycache__`, any built image layers. State the raw-vs-own numbers you
  measure and confirm the gap is artifacts.
- `forgejo/` — compose, up/down, `create_owner.sh`. **Not**: `forgejo-data/`
  (the bind mount — it holds the live estate's repositories), tokens.
- pins/G2 tooling — `derive_g2.py`, the pcaptured
  G2 texts. **Decide**: do the predecessor's *pinned values* come across, or
  only the tooling? They were derived against Docker mounts that Polar will
  change (CP-00's finding), so they are almost certainly stale here.
- `docs/corpus-contract.md` — comes as-is; it is the data-prep team's
  specification and Polar-independent.

The rule to state and apply: **committed source and specifications move;
generated artifacts, environments, and live-estate data do not** — with the
one exception you decide for the corpus lock/taskbank.

Also record: whether these components are now *maintained here* (yes — the
charter says so) and what that means for the predecessor's copies (nothing;
it is frozen, its copies stay).

## Step 2 — The moves
Copy (not `git mv` — different repos) per the boundary. Then reconcile:
- **Paths**: any absolute or predecessor-relative path in a script, config,
  or doc. Expect `staging/`-rooted paths in the pins tooling and
  `devharness/`-era leftovers in comments.
- ** `corpus/ingest_corpus.py` imports the predecessor's public
  library API (`CaseSpec`, `PromptSpec`, `build_taskbank`,
  `write_taskbank`) for the taskbank phase. **That library is not here.**
  Decide and record in ADR-0003: (a) depend on `gsj-envloader` as a pinned
  dependency for the taskbank phase only, (b) inline the ~200 lines of
  taskbank building into `corpus/`, or (c) defer the taskbank phase until
  CP-07 tells us what shape Polar wants tasks in. **Recommendation: (c)** —
  Polar takes `TaskRequest`s, not §3.1 parquet rows, so building a parquet
  we may never use is speculative work; make the phase raise a clear
  "deferred to CP-07, see ADR-0003" and keep validate/scaffold/ingest/verify
  working. If you take (a) or (b), justify against the size budget.
- **Cross-references**: READMEs pointing at predecessor paths, the corpus
  contract's mentions of the library, `mcp-service/README.md`'s references.

## Step 3 — Prove it stands alone
The bar is that a fresh clone of *this* repo, with no s to the
predecessor, can run the moved components' own tests:
- `corpus/` tests pass (whatever subset survives the taskbank decision —
  state the count moved, the count passing, and any skipped with reason).
- `mcp-service/` tests pass. These build their bares through the pipeline's
  `file://` rail, so they should be self-contained — verify that, and if any
  test reaches for a predecessor path or a gitignored fixture, fix it here.
- `pytest -q` at the root still passes (the CP-00 scaffold test).
- A grep proving no file in this repo references the predecessor by path,
  outside `docs/` and `prompts/` where the citation is deliberate.

If a moved test cannot pass without something we deliberately left behind,
that is a finding: record it, skip the test with a stated reason, and put a
row in the gap register.

## Step 4 — Gap register + charter
Update `docs/CHARTER.md` §7. Expected: rows for corpus contract, retrieval
cutoff, and git host move from TBD toward **PARITY** (or PARITY-with-caveat
if thank phase is deferred). Add a row if the pins decision leaves a
gap. Update §3's carried-over table with the **measured** own-code line
counts rather than the estimates from CP-00.

Also land the `checks.py` specification while the predecessor is fresh —
**as a doc, not code**: `docs/checks-spec.md` capturing what CP-00's report
surfaced, so CP-10/CP-11 implement rather than re-derive:
- the four hashing conventions (UTF-8 text sha256 for G1/G2; canonical-JSON
  sha256 for G3/G7; git-blob OID sha1 + template-string sha256 for G4;
  no hash for G6 — verbatim tail compared with `endswith`)
- the byte-stable `G{n}:{slug}` failure vocabulary, and that downstream
  forensics greps it
- the logprob discipline: finite, ≤ 0 everywhere, literal `0.0` only at
  mask==0 — and that the fork-reported `-9999.0` sentinel is exactly what
  this rejects
- G7's chain-snapshot requirement (1 active chain, 0 rollbacks, 0 dropped
  trainable tokens, 1 finalized trajectory) and that Polar must surface an
  equivalent or closed
- the H-41 lesson: validation must fail loudly when the tool-parser stack is
  incomplete, because its absence produced gates-green tool-free episodes
- G3's actual mechanism: the tools array **as sent on the wire**, canonical
  JSON — stricter than a config field

## Definition of Done — run and show output
```
source .venv/bin/activate && pip install -e ".[dev]" -q
pytest -q                                       # scaffold test still green
cd corpus && python -m pytest -q ; cd ..        # counts shown
cd mcp-service && python -m pytest -q ; cd ..   # counts shown
python corpus/ingest_corpus.py validate --corpus corpus/staging   # PASS table
du -sh corpus mcp-service forgejo               # sizes — artifacts left behind
grep -rn 'gsj-envloader' --include='*.py' --include='*.yaml' \
  --include='*.sh' --include='*.toml' . | grep -v '^docs/\|^prompts/\|^\./docs/\|^\./prompts/'
                                                # → empty
test -f docs/checks-spec.md && test -f docs/decisions/ADR-0rship-boundary.md && echo OK
git status --porcelain                          # → empty after commit
```
One commit: `CP-01: the moves`.

## STOP — hard wall
No Polar, no vendoring, no logic in `gsj_rollout/`, no estate bring-up, no
Docker, no writes to the predecessor. Do not draft or begin CP-02.

## Report
`docs/reports/CP-01.md` per the template, printed. Under notes: the
ownership boundary as applied (what came, what didn't, measured sizes before
and after); the taskbank decision and its reasoning; every path and import
reconciled; test counts moved vs. passing vs. skipped; the gap-register
rows touched; and anything in the moved components that turned out to be
coupled to the predecessor in a way the charter didn't anticipate.
