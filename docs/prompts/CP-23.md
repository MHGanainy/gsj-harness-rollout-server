# CP-23 — M7b: the budget, and G6

You are executing CP-23 of `gsj-harness-rollout-server`. Scope: resolve
`checks.py`'s size ceiling, then land the last designed-but-unlanded gate.

**Order matters and is not negotiable.** `checks.py` sits at exactly
520/520 (ADR-0014's ceiling). G6 cannot be written into zero lines.
Resolve the budget in Step 1, measure what it recovered, and only then
write the rule — a checkpoint that discovers the ceiling with a
half-written gate in the tree is the failure this ordering prevents.

**Freeze-lift: `gsj_rollout/checks.py`, `docs/checks-spec.md`,
`docs/CHARTER.md`, `docs/decisions/`, `tests/test_checks.py`, and
`gsj_rollout/{cli,config,builder}.py` **for prose recovery only** — no
logic changes in those three, and say so. NOT lifted: `pi_harness.py`,
`receiver.py`, `client.py`, `vendor/`, `corpus/`, `mcp-service/`,
`forgejo/`, `pins/`, `spike/`.**

**No GPU, no estate.** G6 is fixture-driven: the pin exists
(`g6_expected_tail_ids`, derived at CP-04′) and the  callback
bodies are the evidence.

Read first: `docs/decisions/ADR-0011-codec-evidence.md` (G6's landing
design — the tokenizer-free ids rule, and the **first-turn clause** the
CP-11b verification added: the first mask-1 span starts at
`response_ids[0]` on both real traces, so a `response_ids`-only rule
checks zero turns on a single-turn episode); `docs/reports/CP-04prime.md`
§the pins walk (what the tail ids are and what a rule keyed on them
checks); `docs/reports/CP-11.md` §Step 3 (the prose→spec migration
mechanism — it recovered 82 lines with every rule body byte-identical);
`docs/decisions/ADR-0014` and `ADR-0009` (the allowance history);
`docs/checks-spec.md` (the gate conventions — implement, do not
re-derive).

## Step 0 — Protocol
Prompt at `docs/prompts/CP-23.md`; commit with the CP. Corrupted spans:
reconstruct, apply, list under `questions:`.

## Step 1 — The budget, first
Two mechanisms, and they are not exclusive:

**Bank prose into the spec.** CP-11's migration recovered 82 linrule body byte-identical and all 59 tests passing unmodified. Is
there prose left to move? CP-11 claimed it moved everything; CP-19's
resolver and CP-11b's gates have added since. Measure rather than assume.

**Raise the allowance by ADR.** ADR-0009 (150→250–420) and ADR-0014
(→520) are the precedent, and both were declared rather than absorbed.
A third raise is legitimate if the module genuinely is the largest thing
here — but say what it is *for*, since ADR-0014's earmark for G6 was
spent by CP-16 and CP-19 without either noticing.

**Report the recovery before writing anything**, with the arithmetic:
what G6 is estimated to cost, what you recovered, and whether the two
close. If they don't, say so and either recover more or raise further —
but do it here, not at the last rule.

The 2,000-line law is untouched either way. State the tree total.

## Step 2 — G6, per ADR-0011's landing design
The rule, as designed:

- **ids-`endswith`**, not text — no tokenizer at check time (the whole
  reason what it is: neither this package nor Polar's has
  one, A-14).
- Compare against the pinned `g6_expected_tail_ids` =
  `[151644, 77091, 198, 151667, 271, 151668, 271]`.
- **Over each pre-turn interstitial span**: the mask-0 run preceding
  every mask-1 span in `response_ids`.
- **And over the `prompt_ids` suffix for turn 1** — the first-turn clause.
  Without it, a single-turn episode has zero pre-turn interstitials and
  the rule checks nothing while reporting clean.
- **Zero turns checked ⇒ fail closed** (`G6:missing_evidence:...`), the
  posture every other gate holds.

Two things to get right and state:

**What G6 now means.** CP-04′ changed its subject. The symmetric template
puts the tail in *history* renders too, so the tail appears at more
positions than it used to — the rule checks turn openings, and after the
flip those openings exist in both places. Say what the rule asserts now,
in one sentence a reader can hold.

**Thinking-on.** The pinned tail *is* the empty think block. With thinkinnever appears, so this rule would fail every episode. That is
correct behavior for a gate named "thinking off" — but Phase C is coming,
so record what C-2 must do to it: re-pin, re-conceive, or retire.

## Step 3 — Tests
The CP-10/CP-11b pattern, no exceptions:

- both real callback bodies pass clean (`docs/polar/pi-corpus/`,
  `docs/polar/fidelity/` — and the CP-04′-era bodies if the tail moved)
- one doctored trace per clause, each failing for its own reason:
  a mutated interstitial tail, a mutated `prompt_ids` suffix, a trace
  with no mask-1 span at all (fail-closed)
- **the single-turn case explicitly** — the first-turn clause's whole
  reason for existing; prove a single-turn trace is actually checked
- the vocabulary snapshot gains G6's entries and stays byte-stable

## Step 4 — Charter, register, spec
- Row 14 (G6) → PARITY if landed, or GAP with the new blocker.
- §3's budget with the post-recovery arithmetic.
- `docs/checks-spec.md`: G6 as landed, and the thinking-on note for
  Phaat this repo checks and does not" list gains G6 — it is
  CP-12's inheritance document and should stay accurate.

## Definition of Done — run and show output
```
git diff HEAD -- gsj_rollout/pi_harness.py gsj_rollout/receiver.py \
  gsj_rollout/client.py vendor/ corpus/ mcp-service/ forgejo/ pins/ spike/   # → empty
pytest -q                                          # green, counts
wc -l gsj_rollout/*.py                             # checks.py vs its allowance, tree vs 2,000
# the budget recovery, measured, before/after
# G6: clean pass on both real bodies, one doctored failure per clause
# the single-turn test, verbatim
grep -n 'row 14' docs/CHARTER.md
test -f docs/reports/CP-23.md && echo OK
git status --porcelain                             # → empty after commit
```
One commit: `CP-23: the budget, and G6`.

## STOP — hard wall
No taskbank (CP-24), no consumer-story work, no thinking-on, no GPU, no
estate. No logic changes in `cli.py`, `config.py`, or `builder.py` —
prose only. Do not draft o4.

## Report
`docs/reports/CP-23.md` per the template, printed. Under notes: the
budget mechanism chosen and what it recovered, with the arithmetic that
justified proceeding; G6 as landed with its clean passes and per-clause
failures; **the single-turn evidence**; what G6 asserts now that the
template is symmetric; the thinking-on note for Phase C; and the
"what this repo checks" list updated.
