# CP-27 — M8c: the strangerward lift

You are executing CP-27 of `gsj-harness-rollout-server`. CP-26 measured
what a stranger hits. This fixes the library-side half — the findings that
words alone could not close.

**Small and coherent by design.** Four wishlist rows, all
strangerward, all in two files. If it grows past that, something is
wrong with the scope rather than with the estimate.

**No GPU, no estate.** Every fix is testable from fixtures.

**Freeze-lift: `gsj_rollout/cli.py`, `gsj_rollout/config.py`,
`tests/`, `docs/**`, and the external examples repo (for the F-27/29/34
polish, wishlist 25). NOT lifted: `checks.py` (528/528, equality
tripwire — F-39's code half needs its own ADR and this CP is not it),
`pi_harness.py`, `builder.py`, `receiver.py`, `client.py`, `vendor/`,
`corpus/`, `mcp-service/`, `forgejo/`, `pins/`, `spike/`, `staging/`.**

Read first: `docs/reports/CP-26.md` — the findings are the specification,
and each fix below cites its finding number; `docs/VERDICT.md` wishs 19–25; `gsj_rollout/config.py` (the CP-25 shape — defaults,
comments, `extra="forbid"`); `gsj_rollout/cli.py` (the CP-08 contract —
bounded exit, stated codes, no signal handler at import).

## Step 0 — Protocol
Prompt at `docs/prompts/CP-27.md`; commit with the CP. The external repo
takes its own commits. Corrupted spans: reconstruct, apply, list under
`questions:`.

## Step 1 — The two config validators (F-25, F-23's half; rows 21, 23)
Both traps CP-26 confirmed armed, with their misleading runtime shapes
now measured:

- **`serving_base_url` with a `/v1` suffix** — accepted by the schema,
  produces a bare 404 on `/v1/v1/…` at run time, which reads as a wrong
  host. Reject at load with a message naming the fix (Polar's proxy
  appends the path — CP-04′ measured this).
- **`gateway.port` ≠ the port in `gateway.public_url`** — accepted,
  produces connection-refused on the advertised URL. One fact, two keys.
  Reject, or derive one from the other and say which.

And **F-25's error line deleted whole leaves a
comment-only section, which YAML reads as null, which pydantic reports
as "Input should be a valid dictionary" naming no field. A stranger
cannot act on that. Make the message name the section and what it needs.

Keep it to the ~6 lines the wishlist estimated. If a validator wants more
than that, say why.

## Step 2 — The CLI's two strangerward defects (F-20, hypothesis (f);
rows 19, 22)

- **`python -m gsj_rollout.cli serve` exits 0, prints nothing, starts no
  server.** CP-26 measured it; CP-25 predicted it. A module invocation
  that silently no-ops is worse than one that fails. Add the guard.
- **`serve`'s output is block-buffered**, so backgrounding it with
  `nohup` loses the only instructions the session produces — the rendered
  topology and the two Polar commands. The stranger's log held the pins
  warning and nothing else. Flush, or write them somewhere durable, or
  both — decide and say which.

While you are in `serve`: CP-26 found the printed `vendor/polar/.venh is relative to an unstated cwd (F-21). If making it absolute or
naming the cwd is a one-line change, take it. If it needs more, wishlist
it.

## Step 3 — The examples-repo polish (F-27, F-29, F-34; row 25)
External repo, no library freeze involved:

- **F-27** — the promised "reward distribution" is 71 unaggregated
  lines, and `71/72` was hand-counted. Aggregate both.
- **F-34** — GPU selection is undocumented and hardcoded to the first
  visible device; this run succeeded on tenancy luck. Make it a
  parameter with the `CUDA_VISIBLE_DEVICES` instruction beside it.
- **F-29** — `train.py`'s closing printout repeats the run book's wrong
  side for the sync. Fix the printout; the topology constraint itself
  stays what it is.

## Step 4 — Tests
Each fix gets one that fails when the fix is removed — the standing
standard:

- both validators reject their trap and accept the correct form
- the null-section message names the section
- `python -m gsj_rollout.cli --help` exits 0 and prints; `serve` wconfig exits with the stated code rather than 0-and-silence
- the buffering fix is observable (a subprocess with a pipe, not a tty)
- no signal handler at import still holds (CP-08's embeddability
  property — do not regress it while touching `serve`)

Root suite stays green; say what changed and why.

## Step 5 — Charter and register
- Wishlist rows 19, 21, 22, 23, 25 → done or partially done with what
  remains.
- Row 24 (the MCP secret probe, F-22) stays open — `mcp-service/` is
  frozen; say so.
- §3's budget: `cli.py` and `config.py` may grow. State the arithmetic
  against 2,000, and note `checks.py` is untouched at 528/528.
- A note on what the next stranger hits after this, updating CP-26's
  list.

## Definition of Done — run and show output
```
git diff HEAD -- gsj_rollout/checks.py gsj_rollout/pi_harness.py \
  gsj_rollout/builder.py gsj_rollout/receiver.py gsj_rollout/client.py \
  vendor/ corpus/ mcp-service/ forgejo/ pins/ spike/ staging/   # → empty
pytest -q                             # 136+, say what changed
wc -l gsj_rollout/*.py                             # against 2,000; checks.py 528
# each validator: the trap rejected, the correct form accepted, verbatim
# the null-section message, verbatim
python -m gsj_rollout.cli --help                   # exits 0, prints
# the buffering fix, demonstrated through a pipe
git -C <external> log --oneline | head -3
test -f docs/reports/CP-27.md && echo OK
git status --porcelain                             # → empty in BOTH repos
```
One commit here: `CP-27: the strangerward lift`.

## STOP — hard wall
No `checks.py` changes. No GPU, no estate, no thinking-on, no publishing.
Do not draft beyond CP-27.

## Report
`docs/reports/CP-27.md` per the template, printed. Under notes: each fix
with its finding number and its failing-when-removed evidence; the
validators' messages verbatim (they are the deliverable — a message a
stranger can act on is the whole point); the buffering decision; the
budget arithmetic; and what the next stranger hits now.
