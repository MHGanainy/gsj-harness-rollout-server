# CP-45 — archive the predecessor

You are executing CP-45 of `gsj-harness-rollout-server`. Scope: retire
`gsj-envloader`'s live roles and archive it, without breaking anything
that depends on it.

**Archive, not delete.** The golden references in `docs/golden/mac/` and
`docs/golden/h200/` were collected *through* it — they are what CP-09 and
CP-09′ compared against, and A-1's resolution rests on them. Their
provenance must stay checkable.

**Freeze-lift: `docs/**`, and `gsj-envloader` itself (read-only markers
and its own README — this is the one CP that may write there, and law 3
is what it amends).** NOT lifted: anything under `gsj_rollout/`,
`vendor/`, `corpus/`, `mcp-service/`, `forgejo/`, `pins/`, `spike/`,
`staging/`, `tests/`, `.github/`, or the consumer repos.

**No GPU, no estate.**

Read first: `docs/CHARTER.md` §1 (law 3 — the predecessor is read and
run, never written), `docs/VERDICT.md` (the "frozen-alive" condition on
the provisional adopt, and whether it survived the convers/golden/mac/MANIFEST.md` and `docs/golden/h200/MANIFEST.md` (what
they cite), `docs/reports/CP-04prime.md` §the collector stack (what was
stood up on the H200 and where).

## Step 0 — Protocol
Prompt at `docs/prompts/CP-45.md`; commit with the CP. **Push and state
the outcome** for every repo touched (§8 rules 8 and 9). Corrupted spans:
reconstruct, apply, list under `questions:`.

## Step 1 — What still depends on it
Enumerate before changing anything. For each: does archiving break it,
weaken it, or leave it untouched?

- the two golden references and their manifests
- A-1's resolution (structural at CP-05, empirical at CP-09/CP-09′)
- law 3, and every scope_drift line that asserted it
- the VERDICT's "frozen-alive" fallback condition
- the H200's `.venv-cp04prime` and any estate state
- anything citing its paths after CP-41's M5 fix

**If something breaks, say so and stop** — an archive that costs
verifiability is not an archive.

## Step 2 — What "archived" means here
Decide and record, in o:

- **The fallback role is retired.** The VERDICT's frozen-alive condition
  was a term of the *provisional* adopt; that converted at CP-17 on two
  trainers. Say plainly that the condition expired and when.
- **The evidence role continues.** It is the collecting stack for both
  goldens, and those goldens are cited in live assumptions. Archiving
  must preserve that citation's meaning.
- **Law 3 becomes historical.** It bound every checkpoint from CP-00 to
  CP-44 and did its job — the predecessor is byte-untouched, verifiable
  by `git -C gsj-envloader status --porcelain`. Amend it rather than
  delete it: say what it protected and that the protection is now
  archival rather than operational.

## Step 3 — Mark it
In `gsj-envloader` itself, minimally:

- a header on its README stating it is archived, frozen at v0.8.0, what
  it was, what superseded it, and **that it is the collecting stack for
  two golden references in the successor repo** — the one live reason it
  must remain readable
- set the  repo to archived if it is public (operator action if the
  CLI cannot; say which)

Nothing else. Do not tidy, do not update, do not fix. An archived repo
that got edited is no longer the artifact the goldens were collected on.

**Verify afterward** that its content is otherwise byte-unchanged.

## Step 4 — Update the successor's references
- `docs/CHARTER.md` §1 law 3 — amended per Step 2.
- `docs/VERDICT.md` — the frozen-alive condition marked expired, with
  the date and the converting checkpoint.
- both golden `MANIFEST.md`s — one line each: the collecting stack is
  archived at v0.8.0, here, and the trace is what it is regardless.
- A-1's row — if its evidence cites a live stack, make it cite an
  archived one.

## Step 5 — Register and push
- The audit ids or wishlist rows this touches, if any.
- The ADR.
- Push outcomes, both repos.

## Definition of Done — run and show output
```
git diff HEAD -- gsj_rollout/ vendor/ corpus/ mcp-service/ forgejo/ \
  pins/ spike/ staging/ tests/ .gith → empty
pytest -q                                          # 161, unchanged
git -C <predecessor> log --oneline -3              # the archive marker, and nothing else
git -C <predecessor> diff v0.8.0 --stat            # only the README header
grep -n 'law 3' docs/CHARTER.md                    # amended
grep -n 'frozen-alive' docs/VERDICT.md             # expired, dated
git log --oneline origin/main -1                   # pushed
test -f docs/reports/CP-45.md && echo OK
git status --porcelain                             # → empty
```
One commit here, one in the predecessor: `CP-45: archive the predecessor`.

## STOP
No code changes. No consumer-repo edits. Do not draft beyond CP-45.

## Report
`docs/reports/CP-45.md`, brief. Under notes: Step 1's dependency list
with each verdict; what archived means as decided; the predecessor's
diff against v0.8.0 (should be one header); and whether anything was
found that archiving would have broken.
