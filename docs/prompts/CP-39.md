# CP-39 — M13a: publish, and de-trap

You are executing CP-39 of `gsj-harness-rollout-server`. The audit named
three things a production-prep phase should fix first. This is them, plus
the two traps that sit on paths a stranger or an operator actually walks.

**Small by design.** Almost every item is one line. If any grows past
that, say so rather than absorbing it.

**Freeze-lift: `CLAUDE.md`, `.github/workflows/release.yml`,
`staging/README.md`, `docs/**`, and the demo repo (for M1's register
home). NOT lifted: anything under `gsj_rollout/`, `vendor/`, `corpus/`,
`mcp-service/`, `forgejo/`, `pins/`, `spike/`, `tests/`. `forgejo/`'s
five mis-resolving citations (W2) need a conscious freeze-lift and are
CP-41's — record that they stay.**

**No GPU, no estate.**

Read first: the audit document — **it is the specification**; each item
below cites its finding id. `docs/reports/CP-36.md` §F-52 (the push
failure recurring is what O1 is); `docs/reports/CP-33.md` §row 28 (the
release.yml tuple noted one short, now two).

## Step 0 — Protocol
Prompt at `docs/prompts/CP-39.md`; commit with the CP. The demo repo
takes its own commits. Corrupted spans: reconstruct, apply, list under
`questions:`.

## Step 1 — Publish the record (O1)
The library is three commits behind its public mirror — CP-36, CP-37,
CP-38 unpushed. The public repo therefore lacks the entire stranger-test
record, the model-surface derivation, `serve-llama31.sh`, and register
rows F-54–F-68.

Push. Then verify from outside: clone the public repo fresh and confirm
the three checkpoints and the second serve recipe are there.

**And add F-52's cure**, which was named at CP-36 and never written: a
protocol rule that a checkpoint's push is part of the checkpoint, not an
operator afterthought. `docs/CHARTER.md` §8 gained rule 8 at CP-33 for
exactly this class — check whether it already says this and is simply not
being followed, or whether it says something narrower. **If the rule
exists and was ignored, that is the more interesting fx is different.

## Step 2 — A register home for F-54–F-68 (M1)
Fifteen findings exist only inside `docs/reports/CP-36.md`. The examples
repo's `FINDINGS.md` calls itself "the external register" and ends at
F-53 with no continuation pointer; the demo repo — whose findings these
are — has no register at all.

Decide: a `FINDINGS.md` in the demo repo continuing the series, a
continuation pointer in the examples register, or both. **Whichever, a
future checkpoint minting a row from the register file alone must not
collide at F-54.**

## Step 3 — The two publish/restore traps
- **O2 — `release.yml`'s required-entries tuple is two files short.**
  It asserts three entries; the wheel must ship the thinking-on pins
  (since 0.1.1) and `ingest_corpus.py` (since 0.1.2). In-suite tests
  guard both, but only on the CI path — and `release.yml:12` deliberately
  does not re-run CI on tags. One line.
- **W1 — `staging/README.md:34` names a Megatron pin its own checkpoint
  measured broken.** The table sa04-alpha.rc1`; F-06 and
  CP-17 measured that exact pin dying at `ModuleNotFoundError: No module
  named 'megatron.training.tokenizer'`, and the recipe that ran uses the
  image's `/root/Megatron-LM`. An estate restore from this table fails at
  import. Fix the line, and note the erratum — the contradiction was
  written by the same commit as the report contradicting it.

While in `staging/README.md`: **S9** — it claims to be the complete
estate delta and omits CP-38's `serve-llama31.sh` + `model-llama31-8b.env`.

## Step 4 — `CLAUDE.md` (S1)
Twenty-six checkpoints stale, and injected into every session — so every
future checkpoint reads it first. It describes the CP-08 stub: a `serve`
that "does nothing yet," an empty `__init__.py`, a layout with no
`builder.py` and one test file.

Reality: a 241-line CLI that has driven real collections since CP-09′, an
`__init__` exporting the consumer surface at 0.1.2, `builder.py`
load-bearing, ten test files, 161 tests.

Update it to what is true. **Keep it it is context every
session pays for, not documentation.

## Step 5 — What this CP does not do
Say so explicitly, so CP-40 and CP-41 inherit a clean list:

- the consumer-surface refresh (the examples repo's lagging statuses,
  the v0.1.0 texts, the ENOENT wheel commands) — CP-40
- the orphan re-owning (F-44, rows 19/10/24/26, C-3, rows 2 and 22) —
  CP-40
- the namespace collisions (`forgejo/`, the corpus contract header,
  `checks.py:97`, the golden MANIFESTs) — CP-41
- the ADR amendments (0005, 0011, 0019, 0020) — CP-41
- the residue removal (the stale 0.1.1 wheel in `dist/`) — CP-41, and
  note that `install.sh` prefers a sibling `dist/` wheel over PyPI, so
  the documented developer path currently runs one release behind

## Step 6 — Register
- The audit's findings that this CP closed, by id.
- Anything Step 1 discovered about §8's rule.
- The wishlist rows this touches (O2's is row-adjacent; say which).

## Definition of Done — run and show output
```
git diff HEAD -- gsj_rollout/ venservice/ forgejo/ \
  pins/ spike/ tests/                              # → empty
pytest -q                                          # 161, unchanged
git log --oneline origin/main -1                   # the mirror, current
# a fresh clone of the public repo showing CP-36/37/38 and serve-llama31.sh
# the F-54 register home, and why a future row cannot collide
grep -n 'required' .github/workflows/release.yml   # five entries
grep -n 'Megatron' staging/README.md               # the corrected pin
head -30 CLAUDE.md                                 # true of today
git -C <demo repo> log --oneline | head -3
test -f docs/reports/CP-39.md && echo OK
git status --porcelain                             # → empty in ALL repos
```
One commit here: `CP-39: publish, and de-trap`.

## STOP — hard wall
No `gsj_rollout/` changes. No consumer-surface refresh, no orphan
re-owning, no namespace fixes. Do not draft beyond CP-39.

## Report
`docs/reports/CP-39.md` per the template, printed. Under notes: what the
push added anoutside verification; **whether §8's rule already
covered F-52 and was ignored** — and what you did about it; the register
home decision; the two traps with their before/after; `CLAUDE.md`'s
delta; and the explicit list CP-40 and CP-41 inherit.
