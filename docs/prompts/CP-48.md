# CP-48 — untrack the record

You are executing CP-48 of `gsj-harness-rollout-server`. The operator's
decision, taken with CP-47's classification in hand: **the development
record is removed from tracking.** Prompts, reports, ADRs, and the audit
leave `HEAD` and stay in the working tree, untracked.

**The estate stays.** `forgejo/`, `mcp-service/`, `staging/`, and
`corpus/staging/` are unchanged by this CP — the operator's explicit
instruction.

**Seventeen files that look like record are load-bearing and stay
tracked.** CP-47 identified them with proof. Untracking any of them
breaks CI, the pins walk, or the suites. Getting this list exactly right
is this CP's central risk.

**No GPU, no estate.**

Read first: `docs/CLASSIFICATION.md` §4 — the contradiction list, with
its file:line proofs. `docs/reports/CP-47.md` §the contradiction list.
`.github/workflows/{ci,release}.yml`, `pins/derive_pins.py`,
`tests/conftest.py`, `tests/test_checks.py` — the readers.

## Step 0 — Protocol
Prompt at `/CP-48.md`. **This is the last prompt that will
be tracked** — say so in the report, and decide where CP-49's prompt and
report go (Step 6). Commit with the CP; push and state the outcome.
Corrupted spans: reconstruct, apply, list under `questions:`.

## Step 1 — The keep list, verified before anything is removed
CP-47 named seventeen. **Re-verify every one** — a stale entry here is a
red CI on the next push.

For each: the path, and the file:line that reads it. Then extend the
check: grep every workflow, script, test, and source file in **all four
repos** for path literals under `docs/`. CP-47 swept them; this CP is
the one that acts on the sweep, so it re-runs it.

Expected keepers, per CP-47 — confirm or correct each:
- 7 `docs/polar/` bodies (`pi-corpus/` ×2, `fidelity/` ×2,
  `h200-fidelity/` ×1, `h200-stitch/` ×1, `thinking/` ×1)
- `docs/golden/mac/tokens.npz`
- `docs/golden/mac/MANIFEST.md` and `record.json` (DERIVED-FROM — the
  fixture's transcribed values)
- 6 `pins/` captures — * `docs/` and are not moving**;
  confirm and move on
- `CLAUDE.md` — session-injected, not build-read. **The operator's
  decision applies to the record; `CLAUDE.md` is the workflow's own
  instruction file.** Decide explicitly: track or untrack, and say why.

**Anything you find that CP-47 missed is the finding.**

## Step 2 — The go list
Everything else under `docs/` plus the audit:

- `docs/prompts/` (52)
- `docs/reports/` (52)
- `docs/decisions/` (26 ADRs)
- `docs/AUDIT-2026-08-24.md`
- `docs/golden/` and `docs/polar/` — **the files not on the keep list**
- `docs/CLASSIFICATION.md`, `docs/README.md` — decide; the index
  describes a shelf that is about to empty

**Do not untrack**: `docs/CHARTER.md`, `docs/VERDICT.md`,
`docs/checks-spec.md`, `docs/corpus-contract.md`. These are normative,
consumer- and contributor-facing, and cited from published PyPI project
URLs. Confirm that reading, or challenge it with evidence.

State the count.

## Step 3 — What breaks in the documents that stay
The normative documents cite the record constantly. After this CP those
citations dangle for anyone but the operator.

- `README.md`'s evidence table cites CP reports per row
- `CHARTER.md`'s register and assumptions cite reports throughout
- `VERDICT.md` cites checkpoints by number
- `checks-spec.md` cites reports and ADRs
- the consumer repos' READMEs and FINDINGS cite library reports
- **`pins.gsj.json`'s provenance strings ship in the published wheel**
  and cite `docs/polar/` paths — those files are on the keep list, so
  verify they still resolve

**Do not rewrite the citations.** Instead: one honest paragraph, in the
README and in `docs/README.md`, stating that the development record
(prompts, reports, ADRs, audit) is maintained privately and that
`CP-NN` references resolve there. A reader deserves to know the
footnotes are not followable, rather than clicking into a 404.

Write that paragraph carefully. It is the cost of this decision made
visible, and understating it would be the overclaim this project rty-seven checkpoints avoiding.

## Step 4 — Execute
In order, and verify between steps:

1. `.gitignore` gains the go-list paths.
2. `git rm --cached -r` the go list. **Verify the working tree still
   has every file** — `git rm --cached` must not touch the disk.
3. Run everything: root 161, corpus 58, mcp-service 89,
   `derive_pins.py`, `python -m build --wheel` (16 entries).
4. **If anything is red, the keep list was wrong** — restore, fix the
   list, repeat. Do not proceed with a red suite.

## Step 5 — Verify from outside
A fresh clone into a scratch directory. Then:

- the file listing — what a stranger now sees
- `pytest -q` in the clone — **this is the real test**; if the suite
  needs a file the clone lacks, the keep list is wrong
- CI green on the push

## Step 6 — Where the workflow writes now
Every checkpoint has written a prompt to `docs/prompts/` and a report to
`docs/reports/` for forty-eight checkpoints, and those directories are
now untracked.

Decide and record: future proms go to the same untracked
paths (the working tree keeps them, nothing is committed), or somewhere
else, or the practice changes. **Whatever you decide, `CLAUDE.md` must
say it** — that file is what the next session reads.

State plainly what a future checkpoint's `git status --porcelain` will
show, since "empty after commit" has been the DoD's line since CP-00 and
it will no longer mean what it meant.

## Step 7 — Register and push
The §7 append and the register updates go in the charter, which stays
tracked. State the push outcome for every repo touched.

## Definition of Done — run and show output
```
git diff HEAD --stat                               # the removals, counted
ls docs/                                           # what remains tracked
ls docs/reports/ | wc -l                           # still on disk, untracked
pytest -q                                          # 161
cd corpus && python -m pytest -q ; cd ..           # 58
cd mcp-service && .venv/bin/python -m pytest -q ; cd ..   # 89
pins/derive_pins.py | tail -1               # all approved values reproduced
python -m build --wheel && unzip -l dist/*.whl | wc -l    # 16 entries
# the fresh clone: listing, and pytest -q inside it
git log --oneline origin/main -1                   # pushed
git status --porcelain                             # the untracked record, expected
```
One commit: `CP-48: untrack the record`.

## STOP — hard wall
No file deleted from disk. The estate untouched. No citation rewrites.
Do not proceed past Step 4.2 with a red suite.

## Report
Where the report goes is Step 6's answer. Under notes: the verified keep
list with proofs; the go count; the disclosure paragraph verbatim; the
fresh-clone `pytest` result; what a stranger's listing shows now; and
Step 6's decision with what the next checkpoint's DoD looks like.
