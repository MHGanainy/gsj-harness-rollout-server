# CP-42 — the library, in brief

You are executing CP-42 of `gsj-harness-rollout-server`. Scope: one
document that tells someone what this is, what it does, and what has
been proven — **in plain language, with diagrams, before production.**

**The audience**: a competent engineer who has never seen this repo and
is deciding whether to trust it. Not a checkpoint reader. They will not
read forty-one reports, and should not have to.

**Freeze-lift: `README.md`, `docs/**`. NOT lifted: anything under
`gsj_rollout/`, `vendor/`, `corpus/`, `mcp-service/`, `forgejo/`,
`pins/`, `spike/`, `staging/`, `tests/`, `.github/`.**

**No GPU, no estate. No new claims** — every number comes from a report,
and you cite which. If something cannot be sourced, leave it out.

Read first: `docs/VERDICT.md` (the standing verdict), `docs/CHARTER.md`
§1 (the scope law) and §7 (the register), `README.md` as it stands, and
`docs/AUDIT-2026-08-24.md` §the honest summary (what actually held).

## Step 0 — Protocol
Prompt ompts/CP-42.md`; commit with the CP. **Push and state
the outcome** (§8 rules 8 and 9). Corrupted spans: reconstruct, apply,
list under `questions:`.

## Step 1 — What this is, in one paragraph
Written for someone with no context. What problem it solves, what it
produces, and what it deliberately does not do. The scope law — *task →
sandbox → agent → trace, nothing else* — is the sentence that makes the
rest make sense; say it plainly rather than quoting it.

## Step 2 — The diagrams
Three, no more. Plain ASCII or Mermaid, readable in a terminal and on
GitHub:

1. **The shape** — a task goes in, a validated trace comes out, and
   what sits between (the estate, the sandbox, the agent, the capture,
   the checks). Show where *your* code is versus Polar's versus the
   trainer's — the thin-shell picture is the whole architecture.
2. **The cutoff** — how a timestep becomes a boundary the agent cannot
   cross, and where the token is minted and verified. This is the
   property nobody else 
3. **The two roles** — the server leg needs an estate; the trainer leg
   is a `pip install`. Someone confuses these on first contact every
   time.

Diagrams earn their place by replacing paragraphs. If one doesn't, cut it.

## Step 3 — What has been proven, quantified
A short table, each row with its evidence. Suggested, not binding:

- traces match a reference implementation — masks exact, tokens
  byte-identical (CP-09, CP-09′)
- the cutoff holds under a forged claim (CP-07)
- two trainers, two loops, zero server changes (CP-17, CP-21)
- two model families (CP-04′, CP-38)
- a stranger can run it from nothing (CP-36)
- the size: our code vs the layer it drives
- tests, and what they do and don't cover

**Numbers with sources, not adjectives.** And a "measured as of" date —
these go stale, and a reader should know when.

## Step 4 — What it does not do
As short as Step 3 and as honest. The trainer's problems (weight sync,
staleness, reward), the estate's (sampling and codec provenance,
anonnd the two open gaps with their owners. A reader who
finds a limit you didn't name stops trusting the ones you did.

## Step 5 — Where the record lives
One short section: the charter, the verdict, the reports, the audit —
what each is for, so someone who wants the evidence can find it and
someone who doesn't can skip it. **The record is the repo's strongest
asset and its most intimidating surface**; a map turns the second into
the first.

## Step 6 — Fit it into `README.md`
This replaces or absorbs the current README rather than sitting beside
it. **Close S7 while you are here** — the audit's open item: the badge
paragraph's stale test count, the gate roster omitting G6, and the
"nothing else" wheel claim that predates CP-34's `ingest_corpus.py`.

Keep it short. A reader who scrolls past the diagrams has already
decided; everything after that is for the ones who didn't.

## Step 7 — Register and push
- S7 closed; any other audit id this touches.
- The push outcome.

## Definition of Done — run aut
```
git diff HEAD -- gsj_rollout/ vendor/ corpus/ mcp-service/ forgejo/ \
  pins/ spike/ staging/ tests/ .github/           # → empty
pytest -q                                          # 161, unchanged
# the README, rendered — read it as a stranger and say whether it works
wc -l README.md                                    # short enough?
git log --oneline origin/main -1                   # pushed
test -f docs/reports/CP-42.md && echo OK
git status --porcelain                             # → empty
```
One commit: `CP-42: the library, in brief`.

## STOP
No code changes. No consumer-repo edits. Do not draft beyond CP-42.

## Report
`docs/reports/CP-42.md`, brief. Under notes: the diagrams as they
landed; every number with its source; what you left out and why; and
your honest read of whether a stranger would trust this after five
minutes.
