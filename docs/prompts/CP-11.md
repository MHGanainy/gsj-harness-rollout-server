# CP-11 — M3b: the security fix, the budget, and the pins walk

You are executing CP-11 of `gsj-harness-rollout-server`. CP-10's inherited
list is two checkpoints' worth; this is the first. It clears the ground for
gates rather than landing them: close the cutoff leak, resolve the budget,
walk the pins, and give G5 a structural timestep.

**Freeze-lift: `gsj_rollout/pi_harness.py` (the clone flags only),
`gsj_rollout/config.py` (the timestep into `TaskRequest.metadata`, and the
`CheckPolicy` operator surface), `gsj_rollout/checks.py` (prose→spec
migration only — no rule changes), `docs/**`, `pins/` (derivation
artifacts). NOT lifted: `builder.py`, `receiver.py`, `client.py`,
`cli.py`, `vendor/`, `corpus/`, `mcp-service/`, `forgejo/`, `spike/`.
No gates G1–G4/G6/G7 — that is CP-11b, and this CP exists to make it
possible.**

Read first: `docs/reports/CP-10.md` — the inherited list, the git-history
leak with its reproduction, the budget declaration; `docs/checks-spec.md`
§CP-11's inherited list; `docs/CHARTER.md` (rows 2, 12, 13, 23; §3's budget
law; §5's corrected claim); `docs/reports/CP-04.md` (how pins were
regenerated on the Mac and why they came out value-identical).

## Step 0 — Protocol
Prompt at `docs/prompts/CP-11.md`; commit with the CP. Corrupted spans:
reconstruct, apply, list each under `questions:`.

## Step 1 — Close the cutoff leak (do this first)
CP-10 reproduced it: `git show HEAD~1:md/page_0018.md` from inside the
sandbox returns a post-cutoff page, offline, with no MCP call, because the
clone carries full history and `bash` is on the roster.

Fix in `pi_harness.py`'s clone: `--depth 1` (the truncation commit is the
only one needed) **and drop the `origin` remote** after cloning, so the
agent cannot re-fetch. Verify by reproducing the leak, applying the fix,
and re-running the same commands — `git show HEAD~1:…` must fail, `git
log` must show one commit, and `git fetch` must have no remote.

Then assert the cure end to end: the agent can still `read`, `grep`, and
`find` normally (a shallow clone must not break the tools it
depends on — CP-07's rg/fd trap is a reminder that filesystem assumptions
bite). If the estate is unavailable, run the reproduction and cure in a
scratch clone of a corpus case repo and say so.

Update §5's corrected claim: it becomes true again for **this** repo (the
predecessor's wider exposure stays recorded as inherited and unfixed —
law 3 forbids touching it). Row 2 gains the fix.

## Step 2 — The structural timestep
G5 currently reaches T through the `mcp_gsj_case_status` result — the
service asserting its own scope, which is circular, and which rejects any
episode whose agent never calls `case_status`.

Put the timestep (and `case_id`) into `TaskRequest.metadata` in
`config.py:render_task_request`, so it rides to the trace's metadata
independently of what the agent did. Then make `check_page_cutoff` prefer
it, keeping the `case_status` fallback and the fail-closed behavior.

Two things to verify: the metadata actually survives to the callback
(CP-05 established the channel; confirm the *task* metadata leg
specifically), and the golden-file topology/task-request tests from CP-08
are updated rather than broken.

## Step 3 — The budget
`checks.py` is 367 lines against a 150–250 allowance; `gsj_rollout/` is
1,480 of 1,500 with six gates still to come. Resolve it before CP-11b,
which cannot land otherwise.

**Recommendation: move the reasoning wholesale into
`docs/checks-spec.md`, leaving one-line pointers in code.** The spec is
already normative, it is where an auditor looks, and prose in the
enforcement module is the wrong home for it. Measure what that recovers.
If it is not enough for six gates plus four hashing conventions, write an
ADR raising `checks.py`'s per-module allowance with the reasoning — the
1,500 law is what binds, and a module that is genuinely the largest thing
here may deserve a larger share.

**No rule may change while doing this.** The 59 tests are the proof: they
must pass unmodified, and the failure-vocabulary snapshot is a tripwire.

## Step 4 — The pins walk (row 23)
No approved sets exist in this repo — CP-01 deliberately left the
predecessor's values behind as stale-by-construction. Every hash gate
(G1/G2/G3/G4/G7) is blocked on this, so CP-11b needs it done.

Derive what is derivable **from evidence this repo already owns** — the
CP-07 and CP-09 episodes carry real wire artifacts:
- **G3** (tool roster): the wire `tools` array is in the dumped traces,
  and CP-06 proved persisted == wire for `tools`. Canonical-JSON hash it.
- **G4** (codec): the tokenizer OID and template hash — **and apply
  CP-10's finding (a)**: G4's chat-template input must be the template the
  engine actually renders with (the *served* snapshot, `87a2728c…`), not
  the codec snapshot's (`a55ee1b1…`). Record both and say which is pinned
  and why.
- **G1** (skill card): the card's bytes as resolved, if the corpus
  carries them.
- **G2** (system prompt): CP-04 found docker-mode G2 is the `/workspace`
  singleton and reproduced byte-identical on the Mac — derive it and
  say whether the singleton property holds here too.
- **G7** (settings): whatever the harness renders, if it is stable.

Write `pins/pins.gsj.json` (or whatever `pins/`'s existing tooling
expects) with an approved *set* per key, and a provenance note per value:
which episode, which artifact, which host. **Anything not derivable from
what this repo owns is recorded as not-yet-derivable with the reason** —
do not invent a value to fill a slot.

Note plainly which of these are Mac-estate-specific and will re-derive at
CP-04′.

## Step 5 — The `CheckPolicy` operator surface
"A CUDA estate sets `zero_at_mask1_max_rate` to 0.0" currently has no
mechanism — receiver and client call with defaults. Add a `checks:`
section to the config so a policy can be configured, and thread it to
both call sites. Small; the point is that a documented knob nobody can
turn is not a knob.

## Step 6 — Charter, register, spec
- Row 2 (retrieval cutoff): the leak closed here; the predecessor's
  exposure stays inherited.
- Row 23 (pins): moves off GAP to whatever the walk achieved.
- Row 13 (G5): the structural timestep landed; the checkout-census
  clauses **decided** — dropped with a note, or sourced from a harness
  probe. Make the call rather than deferring again.
- §5's claim restored, scoped honestly.
- §3's budget status updated with what Step 3 achieved.
- CP-11b's list: which gates are now landable, which are still blocked,
  and on what.

## Definition of Done — run and show output
```
git diff HEAD -- vendor/ corpus/ mcp-service/ forgejo/ spike/ \
  gsj_rollout/builder.py gsj_rollout/receiver.py gsj_rollout/client.py \
  gsj_rollout/cli.py                                    # → empty
pytest -q                                               # 59+ passed, rules unchanged
wc -l gsj_rollout/*.py                                  # the budget after Step 3
# the leak reproduction and its cure, verbatim
# the pins walk: what derived, what didn't, with provenance
ls pins/
test -f docs/reports/CP-11.md && echo OK
git status --porcelain                                  # → empty after commit
```
One commit: `CP-11: cutoff leak, budget, pins walk`.

## STOP — hard wall
No gates G1–G4/G6/G7 implemented (CP-11b). No rule changes in `checks.py`.
No estate-wide re-collection. No template change. Do not draft or begin
CP-11b.

## Report
`docs/reports/CP-11.md` per the template, printed. Under notes: the leak
reproduction and cure with the verbatim commands; whether the shallow
clone broke anything; the timestep's path from config to trace metadata,
proven; what the budget resolution recovered and whether it suffices; the
pins walk table (key → value → provenance → Mac-specific?); the G5
checkout-census decision; and CP-11b's blocked/unblocked list.
