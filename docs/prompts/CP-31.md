# CP-31 — M9c: thinking in the consumer surface

You are executing CP-31 of `gsj-harness-rollout-server`. C-2 landed the
mechanism (CP-30). This makes it a choice a stranger can make.

**Decisions already taken by the operator — implement, don't relitigate:**
`thinking` is set in `config.yaml`; `train.py` takes a flag that overrides
it; **the shipped default is on.**

**No GPU, no estate — CP-32 is the stranger test and it runs both modes.**

**Freeze-lift: the external examples repo (unrestricted — it is the
deliverable), plus `docs/**` here. NOT lifted: anything under
`gsj_rollout/`, `vendor/`, `corpus/`, `mcp-service/`, `forgejo/`,
`pins/`, `spike/`, `staging/`, `tests/`, `.github/`. If the consumer
surface needs a library change, that is a finding and CP-33's problem.**

Read first: `docs/reports/CP-30.md` — the per-mode pins file, the
`GSJ_PINS_PATH` selection on **both law-6 legs**, the validator's message,
and the YAML 1.1 boolean trap; `docs/reports/CP-28.md` (the measured
costs and  the run book's expectations come from here);
`docs/reports/CP-26.md` (F-18: expectations that primed the wrong regime
made a correct system read as broken — the failure mode to avoid twice);
the examples repo's `example_project/` as it stands.

## Step 0 — Protocol
Prompt at `docs/prompts/CP-31.md`; commit with the CP. The external repo
takes its own commits. Corrupted spans: reconstruct, apply, list under
`questions:`.

## Step 1 — The pins problem, first
**This is the load-bearing piece.** Thinking-on requires
`GSJ_PINS_PATH` pointing at `pins/thinking-on/pins.gsj.json` on *both*
the receiver and the submit leg. Default-on plus an unset variable means
every collected episode is quarantined on G6 — a stranger's first run
producing nothing, with a finding they cannot interpret.

Worse: the pins live in the **library repo's checkout**, and CP-19 ships
only the off-mode set in the wheel. A `pip install`-only consumer has no
thinking-on pins at all.

Decide and record. Candidates, none obviously rightin.py` derives and sets `GSJ_PINS_PATH` from the config's
  `thinking` value before anything imports `checks` (resolution is
  once-per-process — CP-11b's operational fact, so ordering matters)
- the example ships its own copy of the thinking-on pins file
- the run book makes it a step, loudly, with a check that fails early
- a library change so the wheel carries both — **out of scope here**, but
  say if that is the right answer, because then this CP's fix is interim

Whatever you choose: **a stranger must not be able to run thinking-on
against off-mode pins and see only a quarantine.** If the wrong pins are
resolved, something must say so in words, before the estate is spent.

## Step 2 — The config
`thinking` becomes a documented, first-class value in
`example_project/config.yaml`: default `"medium"` (CP-30: every non-off
level is wire-equivalent under `qwen-chat-template`, and `medium` is the
stated convention), with a comment carrying what CP-28 measured — the
cost, the benefit, and that `off` ontrol.

Two traps to write into the comment rather than let a stranger find:
- **the YAML 1.1 boolean** — `thinking: on` and `thinking: off` are
  booleans, not strings; the validator handles both but the comment
  should show the quoted form
- **the pins coupling** from Step 1, whatever shape it took

## Step 3 — `train.py`
A `--thinking` flag overriding the config, and the mode visible in the
run's output so a stranger reading a log knows which mode produced it.

Two things not to hide, per CP-25's standard:
- **the mode's cost**, printed once at start: ~2.7× wall clock and ~2×
  tokens versus off (CP-28), so a slow run reads as expected rather than
  broken
- **the trainability fact for RLVR** — 67% of gradient mass rides think
  tokens (CP-28). A consumer computing rewards over a thinking-on
  collection should meet that number where the reward is attached, not
  in a report they have not read.

## Step 4 — The run book
This is where F-18 was made and where it can be made again. Each mode
getured expectations, dated and attributed:

- **on** (the default): the wall clock, the token cost, the context
  observation (one episode's final completion came back empty at 81% of
  the 32k window), the deliverable and retrieval rates, and that G6
  asserts template integrity rather than thinking-suppression in this
  mode
- **off**: the control, its numbers, and when a consumer would want it

And plainly: **what running both modes takes** — one flag, one pins
consideration, and how to compare the results. CP-32 will follow this
section literally.

## Step 5 — What else the flip touches in the example
Walk the project and say what changed and what didn't: the bank
(unchanged — the mode is a harness setting, not a task property), the
requirements, `install.sh`, the reward grader, the README. Anything that
silently assumes off is a finding.

## Step 6 — Register
- Wishlist rows for whatever Step 1 deferred to the library.
- `docs/VERDICT.md` and the examples repo's `FINDINGS.md` — the
  consumer-ste after this CP.
- A note on what CP-32 should specifically look for, given what this CP
  could not test without an estate.

## Definition of Done — run and show output
```
git diff HEAD -- gsj_rollout/ vendor/ corpus/ mcp-service/ forgejo/ \
  pins/ spike/ staging/ tests/ .github/           # → empty
pytest -q                                          # 150, unchanged
cat <the example config's thinking section>        # the comment is the deliverable
# the pins decision, and what a stranger sees if it goes wrong
python train.py --help                             # the flag, its default, its cost note
# the run book's two-mode section
git -C <external> log --oneline | head -3
test -f docs/reports/CP-31.md && echo OK
git status --porcelain                             # → empty in BOTH repos
```
One commit here: `CP-31: thinking in the consumer surface`.

## STOP — hard wall
No library changes, no GPU, no estate, no stranger test. Do not draft
beyond CP-31.

## Report
`docs/reports/CP-31.md` per the , printed. Under notes: **the
pins decision with its failure mode** — what a stranger sees if the wrong
pins resolve, and whether the cure is complete or interim; the config
comment verbatim; the flag's shape and what it prints; the run book's
two-mode section; what else the flip touched; and what CP-32 should hunt
for.
