# CP-40 — M13b: the consumer surface, and the orphans

You are executing CP-40 of `gsj-harness-rollout-server`. The audit's
second priority, and its two halves are different work.

**The refresh** is edits: the examples repo is the least accurate of the
three and it is the one a stranger consumes.

**The orphans** are decisions. Several rows are parked against triggers
that already fired, or owned by checkpoints that have already run. The
current state — waiting for something that happened — is the only answer
that is wrong. Either is fine; silence is not.

**Freeze-lift: the examples repo and the demo repo (unrestricted),
`docs/**` here. NOT lifted: anything under `gsj_rollout/`, `vendor/`,
`corpus/`, `mcp-service/`, `forgejo/`, `pins/`, `spike/`, `staging/`,
`tests/`, `.github/`. A row whose fix needs a frozen path gets a decision
and an owner, not an edit.**

**No GPU, no estate.**

Read first: `docs/AUDIT-2026-08-24.md` — **it is the specification**;
every item below cites its id. `docs/re-39.md` §what this CP does
not do (the inherited list). The examples repo's `FINDINGS.md` and
`RUNBOOK.md`, read as a stranger would.

## Step 0 — Protocol
Prompt at `docs/prompts/CP-40.md`; commit with the CP. Both consumer
repos take their own commits. **Push all three, and state each push
outcome** — §8 rule 8 as amended at CP-39; silence is non-compliance.
Corrupted spans: reconstruct, apply, list under `questions:`.

## Step 1 — The consumer-surface refresh
The examples repo, in one pass:

- **W5** — both bridge READMEs pip-install a `0.1.0` wheel right after
  telling the reader to build one, which produces `0.1.2`. ENOENT either
  way. `install.sh` already solved this with a glob; use it. Four spots
  including both `requirements.txt` comments.
- **S2** — the repo presents `v0.1.0` as current in five places; PyPI
  has served 0.1.2 since CP-34. Installs work (`>=0.1.0` resolves); the
  words are wrong.
- **S3** — the RUNBOOK's "what can still surprise you" list teaches three
  bugs CP-2odule-form no-op, the `/v1` suffix, the
  port/`public_url` mismatch. All three have live validators and
  subprocess tests.
- **S6** — `slime_bridge/README.md`'s CP-31 mode-note rider says the
  wheel carries off-mode pins only. False since 0.1.1, and F-40's own
  retirement row one file away says so.
- **W4, S5, C5** — the register's status cells lag: F-25 says OPEN with
  the cure live since CP-27 and citing it by name; six more rows describe
  as pending what a checkpoint already did (F-02, F-04, F-43, F-20,
  F-21, F-23); F-42's rider vouches for the S6 text.
- **S4's examples half** — the Qwen-bound recipe texts: the RUNBOOK's
  "four legs" reads model-general when CP-38 measured them family-bound;
  `config.yaml`'s EOT re-derive recipe names `<|im_end|>`, a token that
  does not exist outside ChatML vocabs.

Then read the result as a stranger and say whether it is now accurate,
not just corrected.

## Step 2 — The orphans, decided
Each gets **kept with an owner and a trigger that has not firee-parked with a real condition**, or **closed with the reason**. No
row may end this CP waiting for something that already happened.

- **O7 / F-44** — the grader reads `sorted(glob("*.md"))[0]` and ignores
  the rest, silently; parked to avoid confounding CP-32's A/B, which ran
  eight CPs ago. Mode-amplified under the shipped thinking-on default.
- **O6 / rows 19 and 10** — both parked against "the next lift" of a
  path that has since been lifted (CP-30 lifted `derive_pins.py`; CP-24
  put +289 lines into the validator). The triggers fired and the rows
  did not move.
- **O8 / rows 24 and 26** — both defer to "`mcp-service`'s own cadence,"
  and nothing schedules one. These rows are the only reason such a lift
  would open. Row 26's own unblock condition (an evidenced CI flake)
  fired at CP-33.
- **O3 / C-3** — committed by CP-28's GO, landed nowhere after CP-30's
  `next:` line. CP-32's 72-episode pooled-ON run covers adjacent ground;
  nothing says it satisfies C-3.
- **O4 / gap row 2** — ths-Forgejo-read residual, assigned to
  CP-04′/CP-09′, punted by both, unmentioned across ~8 bring-ups. **The
  demo's public bootstrap sets no sign-in requirement**, which widens it.
- **O5 / gap row 22** — per-episode provenance binding, cure named "at
  CP-04′ bring-up," which ran without it. Its sampling half was reborn
  as wishlist row 38 with no cross-reference.
- **O9** — the thin-ownership set: row 9 (the oldest open row), row 22's
  F-39 half ("needs its own ADR" no CP is scoped to write), row 16 (the
  four mandatory carried-patch tests run in no CI).

For rows 2 and 22 specifically: they are the last two GAPs in the
register and both are estate posture. **Say plainly whether they are
production-prep work or permanently accepted**, because "open" has meant
neither for eight checkpoints.

## Step 3 — The register mechanic
The audit's decay mode 2: *the OPEN side of registers decays while the
DONE side stays true.* Closures get annotated because a checkpoint is
standing there; rows whoseait expires go quiet.

That is a mechanic, not a set of rows. Propose the smallest thing that
would catch it — a trigger field, a review cadence, something a
checkpoint's DoD could check. **Propose, and say what it would cost.**
Do not build it here unless it is genuinely one line.

## Step 4 — Register and push
- Every audit id this CP closed, by id.
- Every orphan's disposition, with its new owner or its closing reason.
- Step 3's proposal.
- **The push outcome for all three repos**, stated.

## Definition of Done — run and show output
```
git diff HEAD -- gsj_rollout/ vendor/ corpus/ mcp-service/ forgejo/ \
  pins/ spike/ staging/ tests/ .github/           # → empty
pytest -q                                          # 161, unchanged
# the bridge install commands, corrected — and one run to prove it
grep -rn '0\.1\.0' <examples repo> | head          # what remains, and why
# the register's status cells, before and after, for the seven lagging rows
# each orphan's disposition, one line each
git lne origin/main -1                   # library, pushed
git -C <examples> log --oneline origin/main -1     # pushed
git -C <demo> log --oneline origin/main -1         # pushed
test -f docs/reports/CP-40.md && echo OK
git status --porcelain                             # → empty in ALL repos
```
One commit here: `CP-40: the consumer surface, and the orphans`.

## STOP — hard wall
No library changes. No namespace fixes, no ADR amendments, no residue
removal — CP-41. Do not draft beyond CP-40.

## Report
`docs/reports/CP-40.md` per the template, printed. Under notes: the
refresh with the stranger re-read; **each orphan's disposition and its
reasoning** — this is the CP; rows 2 and 22 answered plainly; Step 3's
proposal with its cost; and the three push outcomes.
