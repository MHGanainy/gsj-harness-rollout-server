# CP-36 — M11c: the stranger test, from nothing

### CP-36 REPORT
status: done
scope_drift: none — the library repo takes `docs/**` only (prompt, this
report, CHARTER §7 [CP-36], VERDICT row 34 retired + rows 35/38/39
annotated + row 40 opened); the DoD's frozen-path diff is empty and the
root suite is byte-unchanged at 161. Every fix landed in the demo repo,
whose lift was unrestricted.
files: library — docs/prompts/CP-36.md, docs/reports/CP-36.md (new);
docs/CHARTER.md (+1 block: §7 [CP-36] paragraph); docs/VERDICT.md
(row 34 [CP-36] DONE-retired, rows 35/38/39 [CP-36] annotations, row 40
NEW). External — the demo repo **github.com/MHGanainy/gsj-rollout-demo**,
one commit, pushed: `96f2329` (bootstrap.py `ensure_amd64_image` —
the F-54 auto-cure — plus truthful manifest-error messages; read.py
header `deliverable` line, conditional footer, export
`archive.file`/`archive.sha256`/`conventions`/`deliverable`, message
section-names aligned; README.md: ARM paragraph, the three values named,
host-local endpoint sentence, taskbank-location truth, the reference
serve argv, glossary, thinking levels, row-count clause, measured disk
and timing numbers). Evidence artifact: `~/mg/workspace/cp36-stranger/`
on the operator Mac (fresh clone + venv + all pass logs + reader
transcripts), kept like `~/cp26-stranger`/`~/cp32-stranger`.
tests: `./.venv/bin/python -m pytest -q` → **161 passed** (byte-unchanged
— no library code touched). Demo-side verification: every edited surface
re-run against the four real archived bodies of the stranger estate
(show ×4, export with sha256 re-verified against the body bytes, ls,
quarantine), plus an 11-agent adversarial review workflow over the fix
diff (3 dimension reviewers, 8 verify agents) that CONFIRMED 8 defects
in the first draft of the fixes, 0 refuted — all 8 fixed and re-verified
before the demo commit was made (detail under Step 7).
adrs: none        assumptions: none touched (A-29 exercised by the ON
transcript decode, unchanged)
gap_register: §7 [CP-36] paragraph added; no row moves; tally unchanged.
Wishlist row 34 **retired (DONE)** — the GHCR flip verified by anonymous
pulls during a real cold `up`; row 40 **opened** (multi-arch publish for
`gsj-mcp-service`/`gsj-pi-harness` — F-54's durable half); rows 35/38/39
annotated with what this CP measured.
questions: (1) four corrupted prompt spans reconstructed and applied —
"failing the preflight'swith the cure" → "failing the preflight's
tokenizer rows, with the cure still being a manual walk" (CP-35 §what
the next stranger hits, item 2); '"rea mostly because of the collapse"'
→ '"readable, mostly because of the collapse"' (CP-35's honest-read
sentence); "## Step 6 — The findred list continuing the demo repo's
register, each with:" → "## Step 6 — The findings / One numbered list
continuing the demo repo's register, each with:" (the register is the
F-series; F-53 is the highest anywhere, so this CP numbers F-54
onward); "# the fresh-environme: clone URL, pip install, docker pull"
→ "# the fresh-environment proof: clone URL, pip install, docker
pull". (2) The session first received a DIFFERENT CP-36 draft ("M11c:
the model-dependent surface" — a second-model derivation CP); the
operator interrupted orientation and replaced it with this prompt. The
saved prompt is the operative stranger-test text; the derivation CP's
concerns reappear below as what wishlist 35/CP-37 still owes. (3)
Applied default, the venue: the H200 box cannot play the stranger — its
dockerd registry egress is still firewalled (`docker pull hello-world`
→ registry timeout, re-measured this CP) and its estate/images are
standing state, not "from nothing" — so the stranger box is the
operator's Apple Silicon Mac (Docker Desktop 28.5.1, fresh directory
`~/mg/workspace/cp36-stranger`, fresh Homebrew-3.13 venv, nothing
reused), which is exactly what made F-54 findable. (4) Applied default,
cold-store hygiene: the Mac cached all four images from CP-34's builds;
the ghcr-tagged copies and their layer-sharing siblings were deleted
(17 untag/delete ops) after verifying anonymous re-pullability, but the
containerd store retained gsj-polar's and gsj-pi-harness's layers under
other references, so only the forgejo (~13 s incl. start) and
gsj-mcp-service (18.0 s, the 4.25 GB one) pulls are claimably cold —
stated wherever the numbers appear. (5) Applied default, the stranger's
eyes: three fresh-context agents (README desk-read; OFF and ON
transcript reads; export read) stand in for the human stranger — the
closest available approximation, since the operator's own eyes carry 35
CPs of context; their verbatim verdicts are quoted below and the
strongest demands (F-63/64/65) were fixed. (6) Applied default: SIGTERM
stands in for Ctrl-C in the interrupted-`up` injection (background jobs
of a non-interactive shell inherit SIGINT-ignored — measured: two SIGINT
attempts were no-ops and those runs completed normally, which is itself
the ~41 s after-`down` timing now in the README). (7) The engine: the
reference stack restored via `staging/serving/serve.sh` on **GPU 5** —
GPU 3, serve.sh's default, now carries a ~36 GB co-tenant (tenancy
shifts; discovered at run time per the estate rule) — tunneled to the
Mac as `127.0.0.1:8100`, which is the `base_url` the stranger config
carried. Engine STOPPED after the CP (shared-GPU courtesy); the five
stranger-estate containers left UP on the Mac, archive at 4 accepted
episodes under two mode stamps.
next: advisory — CP-37 is the non-Qwen run: wishlist 35's manual pins
walk is the one cure this CP could name but not execute (P2 was NOT
HIT — the endpoint brought was the reference stack), and rows 40
(multi-arch publish) and 38 (sampling attestation) are what the next
stranger hits that this one could not.

---

## Notes

### The posture, kept

The README (at `cc9aeff`, byte-identical to the operator copy) was the
only input to the run; every deviation from it is a numbered finding.
Fixes were logged during the run and applied only after Steps 2–5
completed (`~/mg/workspace/cp36-stranger/findings-log.md` is the
during-run log, kept). Nothing was side-loaded: clone from the public
URL, wheel from PyPI, images from GHCR — anonymously.

### Step 1 — CP-35's four residuals, as predictions (scored at the end)

- **P1** the GHCR pull, unauthenticated: predicted to work (row 34's
  flip); the anonymous token probe answered 200 on all three packages
  before the run, and the real test is the pull itself.
- **P2** a non-Qwen endpoint fails the preflight's tokenizer rows with
  the cure still a manual walk: expected NOT testable live (the
  endpoint brought is the reference stack); the rows' text reviewed.
- **P3** `show` on a thinking-on archive without `tokenizers` degrades
  to per-turn "cannot be shown": directly testable — the fresh venv has
  no tokenizers and the README's Both-modes section produces an ON
  episode.
- **P4** the 0.6B produces degenerate episodes and a stranger judges
  the SYSTEM by them: tested against fresh-context readers.

### Step 2 — the honest path, end to end (all timings measured)

| step | outcome | time |
|---|---|---|
| `git clone` (public URL) | `cc9aeff`, == operator copy | 1.5 s |
| venv + `pip install 'gsj-harness-rollout-server>=0.1.2'` (PyPI) | 0.1.2 + httpx/pydantic/PyYAML | 4.6 s |
| `./synthetic/make_corpus.py` | 2 cases, the deed-on-page-4 proof | 0.5 s |
| `cp config.yaml.example config.yaml` + fill | the "three values" are honest: corpus prefilled, base_url `:8100`, model already matching | — |
| `./bootstrap.py validate` | 3 PASS | 0.7 s |
| `up` attempt 1 (cold) | forgejo pulled + healthy 13.6 s, scaffold converged 15.8 s, then **FAIL: mcp pull, ghcr.io TLS handshake timeout** (transient; the message says the compose error is authoritative and to re-run) | 26.0 s |
| `up` attempt 2 | forgejo/scaffold idempotent in 2.1 s, then **FAIL: "no matching manifest for linux/arm64/v8"** → **F-54** | 3.7 s |
| workaround `docker pull --platform linux/amd64` (mcp) | the cold pull of the 4.25 GB image | **18.0 s** |
| `up` attempt 3 | mcp start + **first embed under amd64 emulation 115 s**, ingest 2.5 s, taskbank 4 rows, verify 13 PASS, pins derived, engine rewrite line printed, then **FAIL: sandbox pull, same arm64 error** (its message misdiagnoses: "if this host cannot reach ghcr.io…") | 123.0 s |
| workaround pull (pi-harness) | warm — layers survived in the containerd store | 1.8 s |
| `up` attempt 4 | **complete**: 5 containers, engine `ok`, the submit one-liner printed with absolute paths | 11.4 s |
| `./preflight.py` | all six probes ok + the sampling warn (the control) | 2.6 s |
| submit row 0 (the printout's one-liner, verbatim) | `collected 1/1`, `length-terminated: 0/1` | **22.5 s** |
| `./read.py` / `show` / `export` / `quarantine` | the OFF transcript below; quarantine empty | ~1 s each |
| `thinking: medium` + re-`up` | pins re-derived thinking-on, 3 Polar services RECREATED with the reason printed | ~15 s |
| submit (ON) | accepted, `.thinking-on.json` | **38.6 s** |
| `show` (ON, no tokenizers) → `pip install tokenizers` → `show` | P3 verbatim, then decoded | — |

**The cold numbers the prompt asked for**: pulls — forgejo ~13 s
(cold), mcp 18.0 s (cold, the big one), polar + pi-harness not
claimably cold (containerd layer survival, stated); on an amd64 box the
pull-inclusive cold `up` composes to **~2.7 min** (attempt 1's 26 s +
attempt 3's 123 s + attempt 4's 11 s, minus the ARM detours), which is
the README's ~2.5 min claim, honest. Disk: **6.3 GB** of images
measured here (650 MB polar-arm64 + 4.25 GB mcp + 1.1 GB harness +
273 MB forgejo; ~7.1 GB with amd64 variants) — **2× the README's old
"~3.5 GB"** (F-55, corrected). `work/` after four episodes: 9.7 MB.
Total operator interventions on the honest path: **two manual pulls
that should not have been needed** (F-54) and one transient-network
re-run (F-56).

### Step 3 — the transcript, read as a stranger

Three fresh-context agents, each given exactly one artifact and no
project knowledge.

**The OFF transcript** (6 turns, `stop`, 30% trainable — notably NOT
the 71-turn degenerate loop CP-34/35 saw; this episode read all three
pages in order, ran one search, declined the irrelevant hits, and wrote
a coherent brief *as chat text*, never calling `write`). The reader
reconstructed the episode turn-by-turn correctly, separated verdicts
exactly as hoped — software: "worked, essentially end-to-end", agent:
"mediocre — competent process, failed contract" — and read the cutoff
mechanism correctly from the header + status-tool triangulation. What
they could NOT do: see the cutoff *working* (this episode never
searched `mcp_gsj_search_case`, so no page-census line rendered — the
row-1 `case_orchard@t2` episode does render it: `1 hits, pages [2] —
all <= timestep 2 (the cutoff holds)`); expand `G2`, `rebuilt`,
`sk-polar`; or explain **why only 30% of the response is trainable**.
Their sharpest catch: `status COMPLETED` reads as task success until
the deliverable footer, 100 lines later (→ F-63).

**The ON transcript** (4 turns: a hallucinated
`/workspace/case_file.md` read, ENOENT, two byte-identical `ls` calls
with near-photocopied thinking, an empty final turn, nothing written).
The P4 answer, verbatim: *"pipeline works, model is tiny —
conditionally"*; the reader blamed the model on specific lines (the
invented path, the repeated `ls` against an unchanged listing, the
shrinking thinking blocks — "+3,240 / +1,786 / +310 chars — the
shrinking numbers narrate the model's collapse") and credited the
transcript's honesty ("a demo that hides failures would scare me far
more"). But they flagged the load-bearing caveat honestly: **their own
ML priors, not the transcript, did the exoneration** — and the withheld
system prompt is the one artifact that could adjudicate turn-1 blame.
Their demand: an explicit outcome field in the header, because
"accepted, 95% trainable" one line above a buried no-deliverable
parenthetical is what a tired engineer filters on (→ F-63, fixed: the
header now carries `deliverable NONE — no write call this session
(acceptance checks provenance, not task success)`, and the export a
first-class `deliverable` object).

**The export** ("a manifest with excellent manners, not yet a
consumable interface"): the reader reconciled `token_span` sums against
`trainable_tokens` exactly — "which bought the document real
credibility" — but had to GUESS the three load-bearing conventions
(span half-openness, mask polarity, logprob alignment), and called the
arrays pointer "an absolute path on one person's Mac … a human can
fetch them; a portable program cannot" (→ F-64/F-65, fixed:
`archive.file` + `archive.sha256` + a `conventions` block, verified
against the real bodies).

**My honest judgement**, with the operator's bias declared: CP-35's
"readable, mostly because of the collapse" survives contact with
strangers — all three readers understood what they were looking at,
none walked away, and the two failure-shaped episodes read as exactly
what they are. But the readers converged on the same soft spot from
three directions: the system grades its own *provenance* loudly and its
*outcome* quietly, and a stranger notices the asymmetry ("slightly too
pleased with itself in the header"). That is why F-63 was fixed this
CP rather than wishlisted.

### Step 4 — the preflight (the control)

All six probes pass against the reference stack; the sampling warn is
the only warn, verbatim the CP-35 shape. What it would tell a differing
stranger is actionable row-by-row (measured this CP where injectable:
the `/v1` config produces `[FAIL] reachable … probes /v1/v1/models —
drop the suffix`); the tokenizer rows' FAIL text names the G6
quarantine consequence and the pins-walk cure — but P2's live
confirmation needs a real non-Qwen endpoint (CP-37).

### Step 5 — the injected failures (each with its self-diagnosis)

| injection | diagnosed itself? |
|---|---|
| page-numbering gap (page_0002→page_0005) | **YES, exactly**: `FAIL page census must be exactly 1..2 (absolute numbering): missing pages [2]; pages beyond the cutoff [5]` + bootstrap stops, names the contract URL |
| missing `prompts.yaml` | **NO — and correctly so (F-67, wrong shape)**: `PASS 4 pages, 0 prompts`, `up` completes; corpus-contract §4 makes an absent prompts.yaml LEGAL (the timestep contributes no tasks); the honest 0-count in the row is the guard |
| `base_url` with `/v1` | **YES, twice**: bootstrap dies at load naming the /v1/v1 death + the exact fixed line; preflight FAILs naming the same trap |
| unknown key (`samplig_temperature`) | **YES**: names the key, prints the allowed six, points at the example |
| `read.py` bad id / ambiguous id / empty archive | **YES**: exit 1 + cures (ls-count, longer-piece, submit-first); one cosmetic miss — the message cited a README section name that doesn't exist (F-68, fixed) |
| `up` killed mid-flight (SIGTERM at 6 s, during mcp start; 2/5 containers) | **idempotence HOLDS**: re-run completes in 11.4 s, 5/5 containers, and the recovered estate accepted two further submits |

### Step 6 — the findings (F-54..F-68, continuing the register; pass-tagged)

**F-54 — blocking (ARM hosts) [pass 1, honest path].**
`gsj-mcp-service:0.3.0` and `gsj-pi-harness:pi0.83.0-3` publish
`linux/amd64` only (`gsj-polar` is multi-arch — the estate is
asymmetric); an ARM docker REFUSES a manifest list with no matching
platform rather than emulating, so `up` died at the mcp pull and,
cured, again at the sandbox pull. The sandbox message misdiagnosed
("cannot reach ghcr.io"). What a stranger does: a docker-literate one
reconstructs `docker pull --platform linux/amd64`; anyone else stops
dead on the demo's headline promise. Fix: demo (bootstrap
`ensure_amd64_image` — on ARM, absent → pull amd64 explicitly and say
so; measured working: the mcp embed ran 115 s under emulation, episodes
unaffected) + README ARM paragraph + truthful messages. Durable:
**wishlist row 40** — multi-arch publish, an operator/publishing op
that is provenance-sensitive (an arm64 rebuild today is not the
measured artifact).
**F-55 — costly [pass 1].** README disk claim ~3.5 GB vs 6.3–7.1 GB
measured. Fixed: measured numbers, the old number named as compressed.
**F-56 — cosmetic [pass 1].** A transient GHCR TLS timeout fails `up`
one-shot; the message is actionable and the re-run recovered. Recorded;
no retry added (it would mask real registry errors).
**F-57 — costly [pass 1, desk].** "fill in the three values" names two;
the third (`corpus:`) lived only in the example file. Fixed.
**F-58 — costly [pass 1, desk].** The host-local-endpoint rewrite
(`host.docker.internal`) was absent from the README while its "no host
ports" framing pointed the reader at the wrong worry. Fixed.
**F-59 — cosmetic [pass 1, desk].** "Everything … lives under `work/`"
vs the taskbank + lock written beside the corpus. Fixed (the exception
stated with its reason).
**F-60 — costly [pass 1, desk].** The reference serve argv was nowhere;
the desk-reader budgeted "30–60 minutes of vLLM wrangling" and
predicted an issue asking for it. Fixed: the argv block, with the two
load-bearing flags (symmetric template, pinned generation config)
called out as such.
**F-61 — cosmetic [pass 1, desk].** estate / Polar / pins / qualified
undefined at first use; "five services" ambiguous against five
containers. Fixed (glossary block).
**F-62 — cosmetic [pass 1, desk].** No row count for "row 0"; thinking
levels unlisted. Fixed.
**F-63 — costly [pass 3, both transcript readers].** `status COMPLETED`
+ `accepted` misread as task success; the outcome lived in a footer
parenthetical ("correct design stance, wrong font size"). Fixed: `show`
header `deliverable` line; export `deliverable` object.
**F-64 — costly [pass 3, export reader].** `arrays.where` was an
absolute path from this host in ad-hoc notation, with no integrity
check. Fixed: `archive.file`, `archive.sha256` (verified matching the
body), `where` keyed to `archive.file`.
**F-65 — costly [pass 3, export reader].** Span half-openness, mask
polarity, and logprob alignment were recoverable only by arithmetic.
Fixed: the `conventions` block (each claim verified against a real
body before being written down).
**F-66 — cosmetic [pass 2/3].** The deliverable footer's "The skill
asks for out/<task_id>.md" rendered on free-form rows that asked no
such thing (`case_orchard@t2`). Fixed: conditional on `prompt_source`.
**F-67 — wrong shape [pass 5].** The injected "missing prompts.yaml"
is not an invalid corpus: contract §4 blesses it. No fix; recorded so
it is not re-filed as a bug.
**F-68 — cosmetic [pass 5].** read.py messages cited a README section
by a nonexistent name. Fixed.
Unnumbered notes: `load_tokenizer` hardcodes `~/.cache/huggingface/hub`
(ignores `HF_HOME`) before its hub-fetch fallback — the snapshot-less
online stranger is covered, the offline one gets the honest
`--tokenizer` cure; the row-0 episode happens never to exercise the
page-census line (row 1 does), so the README's "every transcript"
phrasing is slightly generous; the synthetic decisions corpus repeats a
sentence verbatim inside one decision text (a generator artifact a
sharp reader flagged as such).

### Step 7 — fixed vs listed

Fixed (demo repo, one commit `96f2329`, verified against the four
real bodies and re-run on the live stranger estate): F-54 (demo half),
F-55, F-57..F-66, F-68. Listed, not fixed: F-54's durable half →
**wishlist row 40**; F-56 (recorded, deliberately unfixed); F-67 (not
a defect). The pre-commit adversarial review (CP-35's
discipline, 11 agents) confirmed 8 defects in the fixes themselves, all
repaired before committing: the footer asserted "free-form" as fact when
a body names no prompt_source at all (third branch added); the new
conventions/`where` pointers were WRONG for quarantined bodies (the
file root is the findings wrapper — the pointer is now
disposition-aware, `session_result.trajectory.traces[0]` on wrapped
files, verified against a constructed wrapper); the ARM gate keyed on
the client python's `platform.machine()` — a Rosetta x86_64 python or
Windows-on-ARM's uppercase `ARM64` would dodge it — and now asks the
DAEMON (`docker version --format {{.Server.Arch}}`, interpreter
fallback); the sandbox path printed a false "already present" right
after the gate itself pulled (it now says "pulled (amd64)"); and four
README attribution errors — "re-measured end-to-end on a cold store"
(no single cold run completed uninterrupted; now stated compositionally),
the ~2 min embed attributed to the reference stack (it is the
ARM-emulated figure; now attributed), "~7 GB measured" (the four images
sum to 6.3 GB; now "6–7 GB (measured 6.3)"), and the glossary's "one
published container" re-embedding the exact services-vs-containers
ambiguity F-61 was curing (now "one published image, run here as three
of those containers").

### Step 8 — CP-35's list scored, and the honest answer

1. **GHCR pull — CONFIRMED working** (anonymous 200 pre-probe; real
   unauthenticated pulls during a genuine cold `up`). Row 34 retired.
   **What the list missed is the finding**: the pull works per-platform,
   and two of the four images have only one platform (F-54).
2. **Non-Qwen preflight — NOT HIT**: the endpoint brought was the
   reference stack (the control), so the FAIL rows and the
   manual-walk cure were reviewed, not suffered. Wishlist 35 stands;
   CP-37 territory.
3. **ON-`show` without tokenizers — CONFIRMED verbatim**: per-turn
   `[thinking happened this session but cannot be shown: the
   'tokenizers' package is not installed — pip install tokenizers
   (then re-run)]`, and the cure works (cache→hub chain).
4. **0.6B degeneracy misjudgment — WRONG SHAPE, in the good
   direction**: the fresh episodes were not the 71-turn loop (a 6-turn
   coherent-but-fileless brief; a 4-turn ENOENT loop), and neither
   reader damned the system — both separated pipeline from model,
   crediting the honest labels. The residual is sharper than predicted:
   the ON reader said plainly that their ML priors did the load-bearing
   exoneration and demanded the outcome field (F-63, fixed) — a
   stranger WITHOUT those priors is still exposed.

Not on the list at all (the value): the platform wall (F-54), the 2×
disk number (F-55), the header/outcome asymmetry (F-63), the
non-portable export pointer (F-64/65), and the endpoint-recipe hole
(F-60).

**The unhedged answer**: on an amd64 Linux box with Docker, ~7 GB of
disk, and a reference-shaped endpoint — **yes**: clone → pip → up →
submit → read is real at roughly five minutes of wall time plus the
endpoint you bring, and every failure injected into that path
diagnosed itself. On Apple Silicon, before this CP — **no**: `up` died
at a pull wall whose message undersold the cure; after this CP's demo
commit, yes, slower, said out loud. With a non-Qwen endpoint — **still
unproven**: the preflight names the mismatch before an episode is
spent, but the cure remains the manual pins walk. That is CP-37.
