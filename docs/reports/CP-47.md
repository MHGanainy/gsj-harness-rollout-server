# CP-47 — classify every file

### CP-47 REPORT
status: done
scope_drift: none — read-only against the tree throughout: nothing
moved, hidden, deleted, or `.gitignore`d; no repository created. The
deliverable is the committed classification plus this report; the §7
append is the workflow's standing per-CP obligation (CP-46's applied
default, reused — see questions).
files: 4 files, one commit `CP-47: classify every file`, **pushed**
(outcome verified at the remote below): docs/CLASSIFICATION.md (new,
366 lines — the two-axis table, the contradiction list, the
hypothetical consumer repo, the three options, the recommendation),
docs/CHARTER.md +27 (the §7 [CP-47] paragraph at the [CP-46] seam),
docs/prompts/CP-47.md (verbatim, corrupted spans preserved), and this
report. Sibling repos untouched (read-only greps only).
tests: `.venv/bin/pytest -q` → **161 passed** (unchanged — the CP
wrote only documents)
adrs: none        assumptions: none
gap_register: §7 [CP-47] paragraph appended — no row moves, tally
unchanged (**21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD**).
Rule-9 pass: no parked row's named event fired inside this CP's
read-only scope. Audit ids relied on, none disturbed: §RESIDUE (every
"is this dead" disposition reused as evidence), S1 (CLAUDE.md's
session-injection — the basis of contradiction row 15), M2 (the
demo's healthcheck.sh citation), O6 (derive_pins' hardcoded skill
tuple).
questions:
  - Corrupted span (Read first): "the deciding fact for sevs" →
    reconstructed "the deciding fact for **the coupling axis**" (what
    reads what is what axis 2 measures) — applied.
  - Corrupted span (Axis 2, FREE): "nothing reads it, nothing depen
    it costs only the information in it" → "nothing reads it, nothing
    **depends on it; removing** it costs only the information in it."
  - Corrupted span (Step 3): "classification, soof:" →
    "classification, **supply proof**:".
  - Corrupted span (Step 5 heading): "The options, gainst the
    classification" → "The options, **priced against** the
    classification" (the step's own body says "each priced from Step
    2's counts").
  - Corrupted span (Step 6): "One recommendation, ing, its cost." →
    "One recommendation, **its reasoning**, its cost."
  - DoD's bare `pytest -q` → run as `.venv/bin/pytest -q` (the
    CP-45/46 convention).
  - DoD comment "docs/CLASSIFICATION.md + report only" vs Step 0's
    save-the-prompt mandate and the workflow's every-CP §7 append →
    default applied: the four-file commit, CP-46's precedent that the
    standing obligations ride along with the named deliverables.
next: advisory — three things the classification surfaced that a
later CP could take: (1) the one accidental coupling, test_cli.py:232
leaving cli.py:51's `isdir` unpatched so the root suite requires
`vendor/polar/` to exist — a few-line edit at the next test
freeze-lift; (2) recommendation (c)'s remaining shelf READMEs
(`corpus/` fronts 163 staging files with no index; `vendor/`,
`staging/`, `forgejo/` likewise — CP-46's own named follow-up); (3)
if the operator elects (a) or (b) despite the pricing, the first
step is the §5 re-homing list, not a move.

#### notes

**The two-axis summary (Step 2).** 701 tracked files, every one
classified; cells report the strongest coupling (RUNTIME >
DERIVED-FROM > CITED > FREE):

| | RUNTIME | DERIVED-FROM | CITED | FREE | total |
| --- | ---: | ---: | ---: | ---: | ---: |
| CONSUMER | 13 | 0 | 7 | 0 | **20** |
| OPERATOR | 405 | 0 | 11 | 0 | **416** |
| CONTRIBUTOR | 25 | 4 | 29 | 0 | **58** |
| INTERNAL | 15 | 2 | 190 | 0 | **207** |
| **total** | **458** | **6** | **237** | **0** | **701** |

Directories split because their files disagree on an axis: root (6
rows), `pins/` (sets / scripts / captures), `docs/` (normative vs
record vs runtime-read evidence; `golden/` and `polar/` file-by-file),
`corpus/` (pipeline 8 vs staging 163), `staging/` (2 code-read files
vs 7 recipes), `vendor/` (Polar 210 vs meta 5), `spike/` (the CP-46
header vs 24 evidence files). **FREE = 0** is the deciding
measurement: "nothing reads it" was checked for every candidate and
true of none — even `spike/` is cited by path from the normative
checks-spec (:891, :900) and the charter's assumption register.

**The contradiction list (Step 1's interesting cells) — 17 files
that look like process and behave like dependencies, each with
proof (docs/CLASSIFICATION.md §4 carries the full rows):**
INTERNAL + RUNTIME (15): `docs/polar/pi-corpus/callback_session_result.json`
(conftest.py:19; constants at test_checks.py:114, test_client.py:29),
`docs/polar/pi-corpus/trace.json` (derive_pins.py:76; pins provenance
ships in every wheel), `docs/polar/fidelity/callback_session_result.json`
(conftest.py:20), `docs/polar/fidelity/trace.json` (conftest.py:21 +
derive_pins.py:76 + test_checks.py:107), `docs/polar/h200-fidelity/
callback_session_result.json` (ci.yml:150 + release.yml:133 +
test_checks.py:721,:935 — three independent readers),
`docs/polar/h200-stitch/attempt5.accepted.json` (test_checks.py:720),
`docs/polar/thinking/episode-on.quarantined.json`
(test_checks.py:1007,:1018), `docs/golden/mac/tokens.npz`
(conftest.py:22; tuple at test_checks.py:141), `pins/tools.captured.json`
(derive_pins.py:72 — the convention anchor), `pins/settings.rendered.json`
(conftest.py:23→:51 stamps every fixture trace; test_pi_harness.py:154),
`pins/g6_tail.captured.txt` (derive_pins.py:111; the pin IS its text),
`pins/thinking-on/g6_tail.captured.txt` (derive_pins.py:122 +
byte-prefix identity :126-128), `pins/container/
system_prompt.container.derived.txt` (derive_pins.py:85; demo runtime
tripwire bootstrap.py:677-681 on the byte-identical copy),
`pins/system_prompt.captured.txt` (derive_g2.py:56 default;
checks-spec:1424 "byte-load-bearing, never rewrite"), and `CLAUDE.md`
(the session harness injects it every CP — AUDIT S1; caveat stated:
the runtime is the process, not the build). INTERNAL + DERIVED-FROM
(2): `docs/golden/mac/MANIFEST.md` and `record.json` — the golden
fixture's transcribed `timestep: 12` and `finish_reason: "stop"`
(conftest.py:110-125). Named beside them: `corpus/staging/` (163,
OPERATOR — the near-contradiction: six runtime readers across all
three suites and both derive scripts; five derivation chains ending
in the lock's 16 SHAs, the taskbank sha, the wheel's skill_card_hash,
both golden manifests, the demo's tripwired copies), and the
citations-with-teeth: `docs/reports/CP-04prime.md` + `CP-11b.md` and
the `docs/polar/*` episode paths inside the wheel-shipped
`pins.gsj.json`, plus VERDICT/CHARTER/checks-spec as published PyPI
0.1.2 project URLs — immutable citers, danglable but not fixable.

**The hypothetical consumer repository (Step 4).** Strictly
CONSUMER + FREE: FREE contributes nothing, so it is the 20 CONSUMER
files — README.md, LICENSE, `gsj_rollout/` (8), both pins sets,
`corpus/ingest_corpus.py`, docs/{README, VERDICT, checks-spec,
corpus-contract, AUDIT-2026-08-24}.md, docs/polar/README.md,
spike/README.md. It does not install (no pyproject.toml), test, or
build; add the 58 CONTRIBUTOR files and it builds — and the moment
`tests/` arrives the record is required again: the suite reads seven
`docs/polar/` bodies, `tokens.npz`, all six pins captures, the
163-file staging tree, `serve-updated.sh`, and requires
`vendor/polar/` to exist. The consumer loses: all 26 ADRs, all 51+51
reports and prompts (the primary evidence behind every README
claim), the golden pair, the run artifacts, the CHARTER (a published
PyPI URL — it 404s), and the estate — which also breaks the demo's
image build (polar.Dockerfile:39-43 clones this repo for
`vendor/polar` + `POLAR_SHA`). The README's evidence table cites
`docs/golden/COMPARISON.md` and per-row CP reports that would no
longer resolve; the wheel-shipped provenance strings dangle forever.

**The three options, costed from the counts (Step 5).**
*(a) Split* — moves 207 INTERNAL files at the price of: ~25 reader
edits across 5 test files, both workflows, derive_pins.py and
conftest.py; a pins re-derivation plus a 0.1.3 release (0.1.2's
provenance strings are PyPI-immutable); CP-46's counted **740**
move-breaking citations across the three repos (this CP's per-file
recount in the consumer repos found supersets: ~49 citation lines in
examples, ~15 in the demo), most inside verbatim prompts and
append-only ADRs this project forbids rewriting; history extraction
that breaks the `4037bc2` golden anchor; and two-repo non-atomic CPs
forever. Honest yield against the requirement: ~190 INTERNAL-CITED
files leave the view, but the 17 runtime-coupled ones stay or force
the full bill — and the listing barely changes because its bulk is
the 416-file OPERATOR estate, which no internal/consumer split
touches. *(b) Untrack* — the same re-homing bill and the same 740
breaks, plus the record stops being checkable by anyone but the
operator (the audit's "citable at its current paths" premise, the
README evidence table, and the shipped provenance all point at
public 404s), plus one disk failure from gone, plus the CI badge
dies (the suite cannot run without the evidence files). It does hide
the material — by converting a checkable record into an uncheckable
one. *(c) Frame harder* — publish the classification (done, this
CP), extend the shelf-README mechanism to the unframed interiors
(`corpus/`, `vendor/`, `staging/`, `forgejo/`), optionally label
docs/README's shelf table with pointers here. Cost near zero, zero
citations touched, zero re-releases. It removes nothing from view;
it makes skipping cheap and deliberate — the one defect the table
shows fixable at this price: a consumer currently cannot tell which
20 of 701 files are for them.

**The recommendation (Step 6): (c), extended — and this table
published as the map.** Grounds: FREE = 0 (no free hide list); the
17 load-bearing INTERNAL files pin the rest through 740 citations,
two CI gates, the pins walk, and PyPI-immutable provenance; the
listing's bulk is the operator estate either way. If the requirement
is strictly that a consumer never *sees* the record, only (a)
executed in full achieves it, at the counted price — the operator's
call, taken with the table in hand. Nothing was acted on: no moves,
no removals, no `.gitignore` changes, no repository created.

**Method.** Seven parallel evidence agents swept CI/build, pins/,
tests/ + gsj_rollout/, the moved components, vendor/ + spike/ + root
files, both consumer checkouts, and docs/ — every RUNTIME and
DERIVED-FROM claim carries a file:line or a committed value;
negatives were grep-verified. Census reconciled to the digit:
701 = root 6 + .github 2 + gsj_rollout 8 + tests 13 + pins 10 +
docs 209 (6+51+51+26+11+64) + corpus 171 (8+163) + mcp-service 29 +
forgejo 4 + staging 9 + vendor 215 + spike 25.
