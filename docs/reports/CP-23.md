### CP-23 REPORT
status: done
scope_drift: none in code — the NOT-lifted paths diff empty (DoD, run below), `cli.py`/`config.py`/`builder.py` **untouched entirely** (the prose-only lift went unused: the tree had 216 lines of law headroom and the module ceiling was the binding constraint, so no logic-adjacent file needed opening — said here because the lift's terms demand saying so), `pins/` untouched (no re-pin; the CP-04′ pin consumed as data), the predecessor untouched. Three doc files outside the named lift list, each declared: `docs/reports/CP-11b.md` (sanctioned by Step 4 itself — the "what this repo checks and does not" list lives there per CP-12's recorded applied default; amended with dated brackets, never rewritten), `docs/VERDICT.md` (wishlist row 18 OPEN → DONE + the dated M7b section — the standing convention every CP since CP-12 has followed; prior CPs were covered by wholesale `docs/**` lifts that this CP's narrower list did not repeat), `README.md` (one word: the badge-coverage suite count 129 → 135, the CP-18 declared-stale-clause class). Tests outside the freeze as always; the three pre-existing expectation changes are each forced by G6's landing, not silent behavior changes (§notes)
files: 10 files changed — new: `docs/prompts/CP-23.md` (verbatim, corruption preserved), `docs/reports/CP-23.md` (this), `docs/decisions/ADR-0021-checks-allowance-528-machine-checked.md`; modified: `gsj_rollout/checks.py` (the banking pass −23, then G6 +31: 520 → 497 → 528), `tests/test_checks.py` (+7 tests incl. the equality tripwire; 3 deliberate expectation updates), `docs/checks-spec.md` (convention 4 as-landed, §G6 LANDED with the Phase-C note, vocabulary [CP-23], the two banked-prose homes, the [CP-23] header note), `docs/CHARTER.md` (row 14 → PARITY, the register tally, §3 [CP-23] + module table 520 → 528 + the [CP-19] earmark-account correction), `docs/VERDICT.md` (row 18 DONE, M7b section), `docs/reports/CP-11b.md` (the inheritance list, dated brackets), `README.md` (the count)
tests: `.venv/bin/pytest -q` → **135 passed** (129 pre-existing — three updated deliberately, each forced by the landing: the vocabulary snapshot gained G6's three entries; the empty-mask LP7 row moved out of the one-per-rule parametrize into a dedicated test expecting the honest pair `[LP7, G6:missing_evidence:turns]`; the off-domain-mask LP9 cases gained `G6:missing_evidence:turns` because the same one-field attack destroys G6's evidence too — plus 7 new: the four-real-bodies clean pass, one doctored failure per clause ×2, fail-closed, the single-turn case, the empty-mask pair, the ADR-0021 equality tripwire) | additionally verified by a 6-agent adversarial workflow — see §Verification
adrs: ADR-0021 (allowance 520 → **528, the landed size exactly, zero headroom by design, machine-checked as an equality by the suite**; the corrected three-CP earmark-erosion account)        assumptions: none (A-14 relied on as recorded, not changed)
gap_register: row 14 GAP → **PARITY** (G6 landed; the register's last designed-but-unlanded gate); the running tally **20 PARITY · 7 DROPPED · 3 GAP · 1 BETTER · 1 TBD** (remaining GAPs: 4 taskbank-deliberate, 12 G4-by-decision, 22 estate provenance); §3 gains the [CP-23] budget status, the module table moves 520 → 528 (ADR-0021 joins the chain), and the [CP-19] paragraph gains the measured earmark correction; VERDICT wishlist row 18 closes
questions:
  - Corrupted span, scope block: "the pin exists (`g6_expected_tail_ids`, derived at CP-04′) and the  callback bodies are the evidence" → "…and the real callback bodies are the evidence" (the double space marks the drop; Step 3 names which bodies).
  - Corrupted span, Step 1: "CP-11's migration recovered 82 linrule body byte-identical and all 59 tests passing unmodified" → "…recovered 82 lines with every rule body byte-identical…" (CP-11's own report words).
  - Corrupted span, Step 2: "the whole reason what it is: neither this package nor Polar's has one, A-14" → "the whole reason the rule is what it is: neither this package nor Polar's venv has a tokenizer (A-14)" (ADR-0011's wording).
  - Corrupted span, Step 2: "With thinkinnever appears" → "With thinking on, the tail never appears".
  - Corrupted span, Step 4: "the thinking-on note for Phaat this repo checks and does not\" list gains G6" → two merged bullets: "…the thinking-on note for Phase C." and "The \"what this repo checks and does not\" list gains G6 — it is CP-12's inheritance document…".
  - Applied default, **where the "what this repo checks" list lives**: no heading by that name exists in the spec — CP-12's report records the applied default that the list is `docs/reports/CP-11b.md` §"What this repo now checks, and what it does not". Amended there with dated bracketed annotations; and since the prompt says the list "should stay accurate", the three other entries that had gone stale were annotated in the same pass (G1 and the settings clause landed CP-13, the census CP-13a, TR3 CP-14 — CP attributions verified against git history by the verification pass).
  - Applied default, **Phase C / C-2 are not defined anywhere in-repo** (grep-verified across docs/): read as the operator's forward plan for a thinking-on phase, C-2 a checkpoint within it. The note is recorded in the spec §G6 naming Phase C/C-2 as received, with the decision C-2 owns (re-pin / re-conceive / retire) and its deadline (before the first thinking-on episode reaches a receiver).
  - Applied default, **G6's seam position**: after the other pins-consuming gates (following `check_settings_echo`) rather than beside the G5 family — the receiver's misconfigured-pins contract (`test_missing_pins_key_is_a_500…` pins the first missing key named as `tool_roster_hash`) survives with zero test accommodation.
  - Applied default, **the turn-1 opening generalizes to `prompt_ids` + any leading mask-0 run**: on every real trace the first mask-1 span starts at `response_ids[0]`, so the concatenation reduces exactly to the ADR's `prompt_ids`-suffix clause; the general form covers a first span at index > 0 with no blind spot between the two clauses. The ADR leaves that shape unspecified (its clause is conditioned on the measured start==0 fact); the landed behavior is stated normatively in the spec §G6. The verification pass probed the divergence adversarially and rated it non-exploitable (§Verification).
  - Applied default, **the CP-04′-era bodies are asserted even though the tail did NOT move** (Step 3's conditional): the pins provenance records the tail bytes measured byte-identical under the symmetric template, so the condition is false — but those two bodies (h200-stitch attempt5, h200-fidelity) are precisely the history-render subject G6 now asserts, so the clean-pass test names them explicitly rather than leaning on the Mac pair alone.
  - Applied default, **the allowance tripwire asserts equality, not ≤**: a ≤ ceiling silently recreates unpoliced headroom the moment the file shrinks — the miniature of the earmark failure — so the test asserts `== 528` and ADR-0021 §2 states the both-directions discipline. Tests are outside the size law.
next: advisory — no successor CP is implied. CP-24 (taskbank, ADR-0003) and the consumer story remain the operator's call; C-2's G6 decision is recorded in the spec §G6 and waits for Phase C; wishlist 17 (PyPI upload) stays operator-gated. The register's GAPs are down to the two deliberate deferrals (rows 4, 12) and the estate-provenance residual (row 22)

---

#### Step 1 — the budget, measured (before → after, per movement)

```
                 HEAD   bank(−23)   G6(+31)   final
checks.py         520         497       528     528   (/528, ADR-0021 — equality)
everything else 1,264       1,264     1,264   1,264
total           1,784       1,761     1,792   1,792   (/2,000)
```

The banking ran FIRST, per the CP's non-negotiable order, and its
arithmetic was stated before any rule code existed: the CP-11 migration
re-applied to the gate docstrings that had crept back to 2–4 lines
across CP-13/13a/14, every one compressed to its spec-pointer form —
**−23 (520 → 497)**, verified AST-identical after docstring strip
against `git show HEAD:`, all 129 pre-existing tests passing
unmodified. One deleted line was stale rather than banked (the
`check_chain_snapshot` docstring still said the settings clause "has no
callback evidence — recorded gap, row 15", false since CP-13). Spec
fidelity: TR3's presence-based clause was the one genuinely un-migrated
fragment and moved to §The split label; the G3 caveat's content already
had a spec home and only its framing changed ("stands in the docstring"
→ stated in place).

**The estimate against the recovery, before writing**: G6 per ADR-0011
measures 31 lines (3 vocabulary constants + 1 snapshot line + 1
`run_trace_checks` line + a 24-line rule including its pointer
docstring + 2 blanks). 497 + 31 = **528 > 520 — the two do not close**,
by 8, and there is no honest prose left. So the remainder was raised by
**ADR-0021**, declared before the rule landed: allowance = **528, the
landed size exactly, zero headroom by design**, and the ceiling moved
into the suite as an **equality** tripwire
(`test_checks_allowance_is_machine_checked`) so it is enforced on every
push in both directions — growth is §3's stop-and-justify, and a shrink
must lower the allowance with it. That last clause is the answer to the
finding that opened this CP: ADR-0014's 23-line earmark was eroded by
**three** CPs (the CP-19 account named only two — corrected this CP,
measured from `git show`: CP-14's TR3 tripwire took the first 12,
CP-16's resolver 9, CP-19's pins signal the last 2), and no paragraph
proved able to guard a number the suite doesn't check. The 2,000-line
law is untouched: **1,792 / 2,000**, and the
`cli.py`/`config.py`/`builder.py` prose lift went **unused** — nothing
logic-adjacent was opened at all.

#### Step 2 — G6 as landed (`check_thinking_tail`, both law-6 legs)

Exactly ADR-0011's landing design, fed by the CP-04′ pin: turns are the
maximal mask-1 runs of `loss_mask`; **turn 1's opening is the
`prompt_ids` suffix** (plus any leading mask-0 run — the measured shape
on all four real bodies is first span at `response_ids[0]`, tail at
`prompt_ids[-7:]`), **each later turn's opening is the mask-0
interstitial run preceding its span**; every opening must ids-`endswith`
an entry of `g6_expected_tail_ids` (`[151644, 77091, 198, 151667, 271,
151668, 271]` — read from the pins at check time, no literal in code, no
tokenizer anywhere: A-14 honored, the whole point of the ids form).
Zero mask-1 spans ⇒ `G6:missing_evidence:turns`, fail-closed. Findings:
`G6:prompt_suffix_ne_tail_ids` (the first-turn clause),
`G6:interstitial_ne_tail_ids:first={turn}:count={n}` (flood-guarded —
a 400k-position alternating mask yields 2 findings, measured by the
verification). Never raises on content: pinned entries are used only if
non-empty lists, so a wrong-shaped pin fails closed and an empty pinned
tail cannot fail open against an empty opening; token ids only, never
`response_messages` (the standing rule).

**What G6 asserts now that the template is symmetric, in one sentence:**
every assistant turn of the merged stream — the first at the end of the
generation prompt, every later one at its history-render interstitial —
opens from the pinned empty think block, i.e. **thinking stayed off at
every turn opening the episode carries, not merely at the final
generation prompt** (CP-04′ changed the subject: the tail rides history
re-renders too, and the rule checks it everywhere it appears).

**Thinking-on, recorded for Phase C** (spec §G6): the pinned tail IS the
empty think block, so on a thinking-on estate it never appears and this
gate fails **every** episode — correct behavior for a gate named
"thinking off", loud and fail-closed. C-2 must decide before the first
thinking-on episode reaches a receiver: **re-pin** (the thinking-on
opening ends at `<|im_start|>assistant\n` = ids `[151644, 77091, 198]` —
a shorter tail that re-conceives the gate as template-integrity and
weakens it), **re-conceive** (a paired gate asserting thinking-on
openings do NOT end with the empty-block ids), or **retire** (row 14
reverts to estate-side enforcement). Re-pinning is data; any rule
reshaping pays ADR-0021's zero-headroom discipline.

#### Step 3 — the evidence, verbatim

Clean pass, all four real bodies, full seam (the two H200 bodies carry
the CP-13/13a statements natively and pass **raw**; the Mac-pair bodies
pass in the post-CP-13 stamped shape, their raw fixture-era findings
unchanged):

```
CP-07  sk-polar-c4eef751 (stamped):  validate_session_result -> []
CP-09  sk-polar-180dd057 (stamped):  validate_session_result -> []
CP-04′ h200-stitch attempt5 (raw):   validate_session_result -> []
CP-09′ h200-fidelity (raw):          validate_session_result -> []
golden mac + h200 (structural map):  check_thinking_tail -> []  (both)
```

One doctored failure per clause, each firing for its own reason
(equality-asserted in the suite, so a co-firing rule breaks the test):

```
prompt_ids[-1] mutated                  -> ['G6:prompt_suffix_ne_tail_ids']
interstitial last id (r[3592]) mutated  -> ['G6:interstitial_ne_tail_ids:first=2:count=1']
all-mask-0 (no span at all)             -> ['G6:missing_evidence:turns']
```

**The single-turn evidence** (`test_the_single_turn_case_is_actually_checked`),
the first-turn clause's whole reason for existing — the real CP-09
episode sliced to its first turn (166 tokens, one mask-1 span starting
at `response_ids[0]`, zero pre-turn interstitials — the shape where a
`response_ids`-only rule checks nothing while reporting clean):

```
single-turn slice [0:166]           -> []                                  (clean, whole seam)
single-turn, prompt suffix mutated  -> ['G6:prompt_suffix_ne_tail_ids']    (checked — not vacuous)
```

The vocabulary snapshot gains exactly `G6:interstitial_ne_tail_ids`,
`G6:missing_evidence:turns`, `G6:prompt_suffix_ne_tail_ids`; every
pre-existing entry byte-identical (snapshot-tested).

#### The "what this repo checks" list

`docs/reports/CP-11b.md` §"What this repo now checks, and what it does
not" (the home CP-12's applied default recorded) now carries G6 on the
checked side with the CP-23 mechanics, and — per "should stay accurate"
— dated bracketed annotations for the other landings the list predated
(G1/settings CP-13, census CP-13a, TR3 CP-14). History preserved: the
annotations are marked, nothing rewritten.

#### Verification

Six adversarial agents (five refutation lenses — the G6 rule under
hostile payloads, banking fidelity by AST and raw-token comparison,
budget arithmetic reproduction, cross-document fidelity, scope/freeze
compliance — plus a fresh-eyes critic re-executing every finding),
every claim settled by an executed command or a file:line citation.
Verdicts: 3 CONFIRMED_SOUND, 2 DEFECT_FOUND, critic DEFECT_FOUND.
Dispositions:

**Real, fixed here before the commit.** (1) Charter §3's normative
module table still said `250–520 (… → ADR-0014)` while ADR-0021, the
tripwire, and the [CP-23] paragraph all said 528 — the exact
stale-paragraph failure the ADR claims to close, reproduced in the same
section; table updated, ADR-0021 joins the chain. (2) **ADR-0021's
earmark account was factually wrong** — "CP-16 took 9, CP-19 the last
2" sums to 11, not 23; `git show` across the pins gives CP-14 +12
(497→509), CP-16 +9, CP-19 +2. Inherited verbatim from the [CP-19]
paragraph and wishlist row 18, but a new append-only ADR restating an
error is worse than the error — corrected in the ADR, the [CP-19]
paragraph (dated bracket), and row 18: **three** CPs eroded the earmark.
(3) The tripwire asserted `<=`, under which a future shrink silently
recreates unpoliced headroom — the miniature of the failure it polices;
now `==`, both directions, ADR §2 reworded. (4) The out-of-lift
`docs/VERDICT.md` and `README.md` edits needed declaring — declared
under scope_drift above. (5) The rule docstring's spec pointer named a
non-existent heading — re-aimed at §The gates as landed, line-neutral.

**Probed and rated non-exploitable, recorded.** The turn-1
concatenation divergence from ADR-0011's literal wording (a garbage
`prompt_ids` suffix hidden by a tail-ending leading mask-0 run): the
shape is unspecified by the ADR (its clause is conditioned on the
measured start==0 fact), no real body has it, the inverse attack IS
flagged, and the landed behavior is normative in the spec — the applied
default above. Held under attack, executed: ten hostile `_pins_cache`
shapes all fail closed or raise the sanctioned configuration error,
never open; 21 hostile mask/ids type attacks and a 3,000-case
randomized fuzz of `validate_session_result` produced zero raises; the
flood guard aggregates a 400k alternating mask to 2 findings in 0.08 s;
the banking is insert-only at the raw-token level with zero pre-existing
rule bodies changed; the register tally, GAP rows, CP attributions, and
every stated number reproduce from `git show`/`wc -l`.

**Noise, dispositioned**: the DoD's `grep -n 'row 14'` is
case-sensitive against the landed "Row 14" heading (run `-i` below,
shown honestly); the spec's inherited CP-11 gate table still says
"decoded assistant-turn openings" (the historical record — the landed
mechanism lives in convention 4's [CP-23] note and §G6, both updated);
ADR wording descriptors ("one-line-pointer") tightened.

#### DoD, run and shown

```
$ git diff HEAD -- gsj_rollout/pi_harness.py gsj_rollout/receiver.py \
    gsj_rollout/client.py vendor/ corpus/ mcp-service/ forgejo/ pins/ spike/
(empty)
$ git diff HEAD -- gsj_rollout/cli.py gsj_rollout/config.py gsj_rollout/builder.py
(empty — the prose lift went unused)
$ .venv/bin/pytest -q
135 passed in 13.18s
$ wc -l gsj_rollout/*.py
  18 __init__.py  159 builder.py  528 checks.py  163 cli.py  123 client.py
  295 config.py  322 pi_harness.py  184 receiver.py  → 1792 total (/2000; checks 528/528, ADR-0021)
# budget recovery, measured: 520 → 497 (bank, −23) → 528 (G6, +31)   (§Step 1)
# G6: clean pass on all four real bodies; one doctored failure per clause  (§Step 3, verbatim)
# the single-turn test, verbatim                                            (§Step 3)
$ grep -n -i 'row 14' docs/CHARTER.md | head -1
614:**[CP-23] Row 14 GAP → PARITY — the last designed-but-unlanded gate
$ test -f docs/reports/CP-23.md && echo OK
OK
$ git status --porcelain      # after commit
(empty)
```
