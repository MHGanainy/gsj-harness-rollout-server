# CP-45 — archive the predecessor

### CP-45 REPORT
status: done
scope_drift: declared and bounded — `CLAUDE.md` (2 lines-worth: the
predecessor sentence and scope law 3) and `README.md` (the predecessor
paragraph), both root files outside the `docs/**` lift. They are two of
law 3's four statement sites; amending the charter's two and leaving
these stale is the exact §8-rule-8 failure mode CP-33/CP-39 paid for (a
rule amended in one place, silent in another), and both files are
deliberately absent from the DoD's frozen-surface diff, which is empty.
Everything else within the lift: `docs/**` here, and the predecessor's
README — the one write law 3 ever permitted, this CP, ADR-0026.
files: this repo — 9 files: CLAUDE.md +4/−2, README.md +6/−2,
docs/CHARTER.md +53/−5 (preamble, §2 parenthetical, A-1 row, §7 [CP-45]
paragraph, §8 rule 3), docs/VERDICT.md +29/−2 (head status, §What-was-
tested parenthetical, the while-provisional expiry annotation, the dated
2026-08-25 section), both golden MANIFEST.md +6 each, plus ADR-0026,
docs/prompts/CP-45.md, and this report; one commit `CP-45: archive the
predecessor`, **pushed** (outcome verified at the remote before this CP
claimed done). Predecessor repo — 1 file, README.md +16 (the archive
header, nothing else), commit `69defe9` `CP-45: archive the
predecessor`, **pushed**: `4037bc2..69defe9 main -> main`, then the
GitHub repo set to archived via `gh repo archive` (verified
`isArchived: true`) — CLI, not operator action.
tests: `.venv/bin/pytest -q` → **161 passed** (docs-only in this repo;
run before and after the edits, identical)
adrs: ADR-0026        assumptions: A-1 (row annotated — the resolution
and its evidence unchanged; the citation now names an archived stack by
design)
gap_register: §7 [CP-45] paragraph added — no row moves, tally
unchanged (**21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD**).
Audit M5 adjacent only; wishlist row 9's `task.py:878-885` citation
stays checkable at the tag.
questions:
  - Four corrupted spans in the prompt, reconstructed and applied:
    (1) "whether it survived the convers/golden/mac/MANIFEST.md`" read
    as "…survived the conversion), `docs/golden/mac/MANIFEST.md`…";
    (2) Step 2's "record, in o:" read as "record, in one ADR:" (Step 5
    names "The ADR" — ADR-0026 is where the decisions are recorded);
    (3) Step 3's "set the  repo to archived" read as "set the GitHub
    repo to archived"; (4) the DoD's ".gith → empty" read as
    ".github/ → empty" (the diff below includes `.github/`).
  - Law 3's charter home: the prompt says "§1 law 3", but charter §1
    contains only the scope law and the size budget — law 3 is stated
    in the preamble and §8 rule 3 (and in CLAUDE.md and README.md).
    Default applied: amended all four statement sites per Step 2.
  - The DoD greps `frozen-alive` in docs/VERDICT.md, but that literal
    did not exist there pre-CP-45 (only in docs/reports/CP-12.md:70 —
    VERDICT said "alive and frozen"). Default applied: the expiry
    annotation and the dated section introduce the spelling, so the
    grep documents the amendment rather than the pre-existing text.
next: nothing owed on the predecessor again — any future write there
would need un-archiving plus an ADR superseding 0026. The frozen
`gsj-envloader-examples` repo still frames the predecessor as a going
concern and cannot be annotated (accepted in ADR-0026); if a CP ever
lifts that repo, a one-line archive pointer is the natural rider.

## notes

**Step 1 — the dependency list, each with its verdict** (six-agent
read-only sweep before any change):

- *The two goldens and their manifests* — **untouched, strengthened**.
  Every collecting-stack citation is past-tense and content-addressed:
  wheel sha256 `a6921de6…` (identical in both manifests), checkout
  `4037bc250e…` which IS the v0.8.0 tag commit, file paths committed at
  v0.8.0. No citation presumes liveness; archived repos stay readable
  and cloneable, and the archive commit sits on top of the tag without
  moving it.
- *A-1's resolution* — **untouched**. Structural at CP-05, empirical at
  CP-09/CP-09′, all on this repo's own comparisons against golden bytes
  that live here. The row now says its evidence cites an archived stack
  by design.
- *Law 3 and every scope_drift line that asserted it* — the standing
  rule **breaks as written** (CP-45's own commit is the modification it
  forbids) and is cured in the same commit by the historical amendment
  the prompt ordered; every *past* assertion (CP-00–CP-24 scope_drift
  lines, CP-41's tag-anchored audit) is time-stamped and survives — the
  porcelain-empty recipe still passes (the write is committed), and
  diff-vs-tag now reads "one README-header commit past v0.8.0", the
  predicate the DoD itself encodes.
- *The VERDICT's frozen-alive fallback condition* — **weakens** as
  present-tense text, cured by recording the truth: it was a term of
  the provisional adopt and **expired at the conversion, 2026-08-11
  (CP-17** — condition 2, the slime loop; condition 1 the same day;
  the second trainer, verl, followed at CP-20/CP-21 on 2026-08-12**)**.
  Annotated in place (§5 preserved as written per the document's own
  rule) plus a dated section.
- *The H200's `.venv-cp04prime` and estate state* — **untouched**. The
  checkout, the venvs, and the `gsj-envloader-staging` compose project
  are local disk a GitHub archive flag cannot reach; no documented
  estate procedure pushes to the GitHub repo (pushes target the local
  Forgejo; the checkout was fed by rsync). Judged from the committed
  documents only — no GPU, no estate this CP.
- *Path citations after CP-41's M5 fix* — **untouched, strengthened**.
  The M5-qualified manifest paths need a readable predecessor at
  v0.8.0, exactly what archiving preserves; zero GitHub URLs to the
  predecessor exist in this repo, so nothing 404s. Residual accepted:
  the frozen examples repo's going-concern framing now meets a "Public
  archive" banner and cannot be annotated.

**What archived means, as decided (ADR-0026)**: the fallback role
RETIRED (the frozen-alive term expired 2026-08-11, CP-17 — CP-45 only
records it); the evidence role CONTINUES (the goldens' collecting
stack, readable at v0.8.0, now platform-frozen); law 3 HISTORICAL
(bound CP-00–CP-44, did its job, one permitted write on the way out,
GitHub read-only enforces it from here).

**The predecessor's diff against v0.8.0**: `README.md | 16 ++++++` —
one file, the archive header, nothing else. HEAD `69defe9`, parent
`4037bc2` = v0.8.0. Byte-unchanged otherwise, porcelain empty.

**Anything archiving would have broken**: nothing found. The single
"breaks" verdict in the sweep was law 3 as a standing rule — the thing
this CP exists to amend, cured in the same commit; no evidence chain,
golden citation, estate procedure, or past assertion loses
verifiability.
