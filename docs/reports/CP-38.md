# CP-38 — M12b: the second family

### CP-38 REPORT
status: done
scope_drift: none — `gsj_rollout/` untouched (1,999/2,000; the DoD's
frozen-path diff is empty and the root suite is byte-unchanged at 161);
the lifts taken are exactly the three the prompt opened: the demo repo
(MODEL-SURFACE.md corrected and extended, README honest-status,
preflight.py's two Qwen-branded cure texts made family-bound),
`staging/serving/` (the second serve recipe: `serve-llama31.sh` +
`model-llama31-8b.env`), and `docs/**` here. No third model, no library
fix — the one library-adjacent fragility found (vendored Polar's EOT
auto-detect) is a FINDING, recorded in wishlist row 43, defended today
by A15 + `G7:chains_truncated`, priced only if a consumer without the
gsj builder materializes.
files: library — docs/prompts/CP-38.md, docs/reports/CP-38.md (new);
docs/CHARTER.md (A-30 [CP-38] annotation — the STOP half measured
false-but-harmless, the assumption re-scoped to the RENDER-side closer;
§7 [CP-38] block); docs/VERDICT.md (rows 35/41/42 [CP-38] annotations;
row **43 NEW** — the two-terminator geometry); docs/checks-spec.md (§G7
[CP-38] note — chain identity is canonical-vs-canonical, measured);
staging/serving/serve-llama31.sh + model-llama31-8b.env (NEW — the
estate's second model recipe, distinct state files, refuses to start
over a live Qwen pidfile). External — the demo repo
**github.com/MHGanainy/gsj-rollout-demo**, one commit: MODEL-SURFACE.md
(tool-parser row added; G2 row verified-live; the prefix-extension
"honest limit" claim WITHDRAWN and replaced with the measured
canonical-vs-canonical mechanism; the end-of-turn section rewritten
around the two-terminator measurement + the A15/G7-defended auto-detect
warning; mode-pair measured-live note; NEW "second family, measured"
section), README ("Bring your own model" honest status updated to the
accepted episode), preflight.py (family-bound tool-parser cure texts).
tests: `./.venv/bin/python -m pytest -q` → **161 passed**
(byte-unchanged — no library code touched; the staging-script tripwire
passes over the new recipe). Estate-side: the full preflight all-ok
against the live Llama endpoint; the derivation control (mode pair)
live; one episode collected and accepted through the receiver seam.
adrs: none (the serve recipe is estate configuration; the geometry
finding changes no decision — ADR-0007's stitch stays dormant, ADR-0024's
per-mode pins degrade exactly as row 42 predicted)
assumptions: **A-30 annotated** — its STOP half measured FALSE on the
first live foreign family and HARMLESS, which narrows the assumption to
its sound core: the derivation's value is the render-side closer, and
the vendored merge only ever matches it against renders.
gap_register: §7 [CP-38] paragraph added; no row moves; tally unchanged
(21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD). Wishlist row 35
episode-half **CLOSED** (residual: G4 snapshot walk, `/tokenize`-less
endpoints); row 41 annotated (still none bit — now by measurement); row
42 no-mode half **MEASURED LIVE**; row **43 opened** (the
two-terminator geometry).
questions:
  - Corrupted span, Read-first: "thsification table is the map" → "the
    classification table is the map" (CP-37 §Step 1's own name).
  - Corrupted span, Step 2 first bullet: "Llama renders tool calls in
    its own JSON\n tool-call-parser llama3_json` is the candidate" →
    "…in its own JSON dialect; `--tool-call-parser llama3_json` is the
    candidate".
  - Corrupted span, Step 3: "what was named-not-defaulted, r the
    template probe agrees" → "…named-not-defaulted, and whether the
    template probe agrees".
  - Corrupted span, Step 4: "Zerool calls with a green trace" → "Zero
    tool calls with a green trace" (H-41's shape).
  - Corrupted span, Step 4: "A transcript is Qwen-tuned in its display
    heuristics" → "The transcript reader is Qwen-tuned in its display
    heuristics" (read.py; CP-37's item 15).
  - Corrupted span, Step 6: "If a stranger with ot get to an episode" →
    "If a stranger with Llama could not get to an episode".
  - Applied default, the mirror: **unsloth/Meta-Llama-3.1-8B-Instruct
    @ `a2856192…`** (pinned via the HF API at CP time) — CP-37's
    "official template" mirror, so live-vs-offline is apples to apples;
    the meta-llama origin repo is gated. The NousResearch wild mirror
    was deliberately NOT served (CP-37 already measured it
    underivable).
  - Applied default, the template: the snapshot's EMBEDDED one — no
    `--chat-template` flag; identity recorded from the snapshot
    actually served (sha256 `e10ca381…`, 4,614 chars — byte-equal to
    the published mirror's; tokenizer.json blob-sha1 `475b3115…`).
  - Applied default, GPU: **3** (serve.sh's default; free at run time —
    0/1/2/3/6/7 free, 4/5 co-tenanted; tenancy shifts, discovered
    live), gpu-frac 0.30, port 8000 behind the estate's existing
    `ops-forward-8100` forwarder so the demo estate's container path
    (`host.docker.internal:8100`) needed no change.
  - Applied default, the collecting estate: the box's
    `~/cp34-demo/gsj-rollout-demo` checkout pulled to origin/main
    (`f546abd` — the CP-37 derivation), config.yaml (gitignored) edited
    to the Llama model, `thinking` absent → off; episode = the `up`
    printout's one-liner verbatim, taskbank row 0.
  - Applied default, estate hygiene after the CP: engine STOPPED and
    the Mac tunnel closed (shared-GPU courtesy, the CP-35/36 pattern);
    the five gsj-demo-* containers stay UP with the estate's pins now
    Llama-derived and the box config.yaml still naming the Llama model
    — the estate records what it last ran; the Llama snapshot stays in
    the box HF cache for the fast path.
next: advisory — the geometry row (43) wants a SECOND two-terminator
family (or a trainer actually consuming an eom-at-mask-1 stream) before
"generalizes" is claimed; row 42's different-geometry half still wants
a real non-Qwen hybrid reasoner (the CP-28-shape measurement); wishlist
35's residual is now exactly G4's snapshot walk + `/tokenize`-less
endpoints. If a CP ever serves Llama thinking-ON-style models
(DeepSeek-R1-Distill-Llama etc.), row 41's thinkingFormat constants
become the live subject and will need the §1-lift pricing.

---

## Notes

### Step 1 — the predictions, scored

Written to the session scratchpad before any Llama artifact existed on
the box (engine down, HF cache empty of llama); the addendum (P13–P16)
added after reading the pinned mirror's template bytes, still before
the engine was up. Scored:

| # | prediction (compressed) | score |
|---|---|---|
| P1 | sampled-vs-rerender identity conditions `chains_total`; Llama's tool-call dialect breaks it → G7 split at even odds | **wrong shape** — the mismatch it feared is REAL (see the geometry below) but the premise was false: Polar's merge is canonical-vs-canonical and `chains_total: 1` held |
| P2 | G2 render passes on a foreign model | **confirmed** — hash reproduced on the accepted episode |
| P3 | mode pair derives identical pins live (row 42) | **confirmed** — byte-identical, off and medium |
| P4 | llama3_json parses the canonical single call | **confirmed** (the parser half); the per-episode text-fallback risk it left open materialized at turn 8 |
| P5 | derivation live == CP-37 offline (tail/EOT), else mirror drift | **confirmed** — exact agreement, and the served template's sha equals the published mirror's |
| P6 | BOS 128000 visible, harmless | **confirmed** — `prompt_ids[0] == 128000`, no gate reads it |
| P7 | zero-logprob rate ELEVATED vs reference; LP6 rejection possible | **not hit — inverted**: 1.8% vs the reference's 6–14% (and 24.9% worst) |
| P8 | healthcheck round trip passes; episode makes ≥1 `mcp_gsj_*` call | **wrong shape, twice** — the round trip FAILS 4/4 on its answer half (parser half passes), and the episode made ZERO retrieval calls (7 file-tool calls) |
| P9 | read.py degrades only in the think machinery | **confirmed** — think sections inert, `think_tokens: null` with the labelled basis; everything else renders |
| P10 | preflight all-ok + the one sampling warn | **confirmed** — the control shape on a foreign family |
| P11 | genconfig 0.6/0.9 pinned, stated by the startup line | **confirmed** verbatim |
| P12 | nothing outside the model surface breaks | **confirmed** — warm `up` 19 s, all phases green |
| P13 | template drops prose on tool-call turns → short episodes or splits | **not hit** — every parsed turn sampled pure JSON; the drop is real at render level (probe-confirmed) but its split consequence was wrong for P1's reason |
| P14 | template raise_exception on parallel calls → mid-episode 400 | **not hit** — single calls throughout |
| P15 | tojson serialization drift → G7 split | **wrong shape** — serialization drift cannot split chains (canonical-vs-canonical); none occurred anyway |
| P16 | tool-result JSON-quoting is deterministic noise | **confirmed** — quoted in every render, zero reconstruction cost |

**What was NOT on the list — the value:** (1) the two-terminator
geometry itself (`<|eom_id|>` sampled vs `<|eot_id|>` re-rendered —
none of CP-37's four named it, and my addendum missed it despite
holding the template bytes); (2) the discovery that Polar's prefix
comparison never consults sampled bytes (the merge reads the sampled
LAST token only to decide whether the canonical close is deduplicated
or appended) — which rewrites CP-37's "honest limit" claim; (3) a malformed sampled JSON (one literal newline) quietly
ending the episode with only the stub deliverable written; (4) the
retrieval-free-green episode shape; (5) the round trip's answer half
failing on a model that then uses tool results perfectly well
mid-episode.

### Step 2 — the serve argv, verbatim

`staging/serving/serve-llama31.sh` (committed; run from the Mac; the
say-lines record the snapshot path and artifact identities). The engine
process, as started on the box:

```
CUDA_VISIBLE_DEVICES=3 VLLM_LOGGING_LEVEL=DEBUG \
VLLM_ATTENTION_BACKEND=FLASH_ATTN VLLM_USE_FLASHINFER_SAMPLER=0 \
nohup ./venv/bin/vllm serve unsloth/Meta-Llama-3.1-8B-Instruct \
  --revision a2856192dd7c25b842431f39c179a6c2c2f627d1 \
  --host 127.0.0.1 --port 8000 \
  --max-model-len 32768 \
  --gpu-memory-utilization 0.30 \
  --enable-auto-tool-choice \
  --tool-call-parser llama3_json \
  --generation-config /home/sysadmin/gsj-vllm/genconfig-llama31 \
  --enable-log-requests \
  --enforce-eager
```

The four legs, and which differ from Qwen's: **tool parser**
`llama3_json` (hermes would leave every call unparsed text); **chat
template** the snapshot's EMBEDDED Meta template — no `--chat-template`
flag (sha256 `e10ca381…` recorded from the served snapshot, byte-equal
to the published mirror's; the Qwen recipe's `--reasoning-parser
qwen3` and `--default-chat-template-kwargs` legs are dropped — both
Qwen-bound); **generation config** pinned to the snapshot's own file
(sha256 `280388b9…`), startup line verbatim: `Default vLLM sampling
parameters have been overridden by /home/sysadmin/gsj-vllm/genconfig-llama31:
{'temperature': 0.6, 'top_p': 0.9}` — note **no top_k**, where Qwen
pins `top_k: 20`; **--max-model-len 32768** == the config's window.
The verify-before-collecting round trip (healthcheck.sh with the new
env): `OK /health · OK /v1/models lists unsloth/Meta-Llama-3.1-8B-Instruct
· OK tool call parsed: get_utc_time (finish_reason=tool_calls)` — then
the ANSWER half failed, 4/4 runs, the model producing meta-commentary
about its own call ("This JSON object calls the `get_utc_time`
function…") or, with a system prompt, silently re-calling the tool
(`stop_reason: 128008` — the first `<|eom_id|>` sighting). The parser
half is the half that protects episodes; recorded as model temperament
in MODEL-SURFACE, and the episode below used its tool results fine.

### Step 3 — the derivation, live vs CP-37's offline snapshot

`bootstrap.py up` say-lines, verbatim (the foreign branch, live):

```
pins — 'unsloth/Meta-Llama-3.1-8B-Instruct' is not the reference model; derived from the endpoint's own template render:
pins —   g6_expected_tail_ids = [128006, 78191, 128007, 271]  ('<|start_header_id|>assistant<|end_header_id|>\n\n')
pins —   end_of_turn_token_id = 128009  ('<|eot_id|>') -> rollout.yaml builder
pins — NOT derivable and left empty, on purpose: G4's tokenizer/chat-template hashes (...)
```

| value | live (served endpoint) | CP-37 offline (snapshot) | verdict |
|---|---|---|---|
| tail ids | `[128006, 78191, 128007, 271]` | same | **AGREES** |
| tail text | `<\|start_header_id\|>assistant<\|end_header_id\|>\n\n` | same | **AGREES** |
| EOT id | `128009` (`<\|eot_id\|>`) | `128009` | **AGREES** |
| mode pair (off vs medium) | byte-identical tails + EOT | byte-identical | **AGREES** — row 42's no-mode half now measured live |
| prefix-extension | TRUE, 58 → 96 ids | TRUE, 59 → 99 ids | **AGREES on the property**; the id-counts differ by probe environment (transformers-side render offline vs vLLM `/tokenize` live), NOT by artifact — the served template's sha256 equals the published mirror's byte-for-byte. Named so a future reader does not mistake the count delta for drift |
| G4 hashes | empty, named | empty, named | by design |

No disagreement — the served artifact IS the published one (and this CP
recorded the identities that make that checkable next time: template
sha256 `e10ca381…`, tokenizer blob-sha1 `475b3115…`).

The full preflight, every row, verbatim:

```
probing http://127.0.0.1:8100 for 'unsloth/Meta-Llama-3.1-8B-Instruct' (host-side, as written in config.yaml)

[ ok ] reachable         GET /v1/models answered (HTTP 200)
[ ok ] model             'unsloth/Meta-Llama-3.1-8B-Instruct' is served
[ ok ] context window    max_model_len 32,768 >= the harness's planning window 32,768
[ ok ] tool parser       the endpoint returned a structured tool_call for a tool-begging prompt
[ ok ] tokenizer tail    /tokenize renders the pinned G6 tail exactly ([128006, 78191, 128007, 271])
[ ok ] end-of-turn id    the served template terminates assistant turns with [128009] ('<|eot_id|>') — matches work/estate/rollout.yaml (what episodes run with)
[ ok ] template          turn 2's prompt render strictly extends turn 1's (58 -> 96 ids) — multi-turn episodes reconstruct as one chain
[warn] sampling defaults NOT PROBEABLE over the API. (...)

preflight: no fatal mismatch found.
```

All seven probe rows ok, the sampling warn the only warn — the
CP-36/37 control shape, reproduced on a foreign family. The passing
rows carry the information the prompt promised: the tokenizer-tail and
end-of-turn rows passing against FRESHLY derived Llama pins is the
derivation and the verification agreeing through two independent code
paths.

### Step 4 — chains_total, the quarantine, the tool calls — in that order

Episode `sk-polar-e46b6550…`, submitted through the `up` printout's
one-liner verbatim (taskbank row 0, `case_mill @ timestep 3`,
skill:brief), `collected 1/1 episodes`:

1. **`chains_total: 1`** — the full stats:
   `{"chains_total": 1, "chains_reconstructed_full": 1,
   "chains_reconstructed_truncated": 0, "raw_completions_total": 8,
   "completions_total": 8, "completions_merged": 8}` — eight
   completions, one chain, native merge on a non-Qwen family.
2. **The quarantine directory is EMPTY.** `gsj_validation: findings=[]`,
   `glue_stitched: 0`, gate_findings `[]` — accepted through the full
   receiver seam under the Llama-derived pins.
3. **The tool calls are real and parsed**: 7 structured calls (write,
   grep×4, read×2 — every one `finish_reason: tool_calls` with
   arguments intact), plus a final turn whose `write` arrived as TEXT
   (below). Zero `mcp_gsj_*` calls. Not H-41's tool-free shape — but
   its cousin: a **retrieval-free green** episode, rightly accepted
   (the gates check provenance, not retrieval; named in
   MODEL-SURFACE).

**What `chains_total: 1` means — the single most valuable measurement,
and it is not the one CP-37 expected it to be.** The template property
held under sampling, yes — but the episode also PROVED WHY that is the
whole property. Every tool-call turn's sampled span ends with
`<|eom_id|>` (128008 — Meta's ipython-protocol stop), while the
template re-renders those turns closed with `<|eot_id|>` (128009):
sampled bytes and re-rendered bytes DISAGREE at every tool-call turn's
terminator, and the chain did not split. Mechanism, read from the
vendored merge and confirmed at token level in the archive: Polar
matches each request's prompt render against the PREVIOUS request's
prompt render (canonical-vs-canonical — the sampled bytes never enter
the prefix comparison), and `_slice_interstitial` locates the previous
turn's canonical close by `end_of_turn_token_id` INSIDE THE RENDER —
keeping the sampled `<|eom_id|>` at mask 1 and appending the canonical
`<|eot_id|>` + interstitial at mask 0 ("so the stream still closes the
assistant turn", in the vendored code's own words). Token-level, from
the archive: every tool-call span ends `[..., 32075, 128008]` (mask 1);
every following glue span opens `<|eot_id|><|start_header_id|>ipython…`
and closes with the pinned tail (mask 0); the final answer span ends
`128009`. Consequences, all recorded: the derived EOT (128009, the
RENDER-side closer) is exactly what made the slice work — A-30's stop
half is false and harmless, re-scoped in the charter; the trace teaches
the model its real protocol (eom) while the context stream stays
template-canonical; and CP-37's "sampled-vs-rerendered identity is an
unprobed extra condition" claim is measured FALSE and withdrawn in
MODEL-SURFACE — the preflight's template row measures the entire
chain-GROUPING condition (`chains_total`); the one further condition a
full merge needs, the derived end-of-turn id occurring in every
canonical tail, is the end-of-turn row's territory, and a wrong id
fails closed at `G7:chains_truncated` (row 43's residual).

Trace vitals: prompt_ids 3,329 (`prompt_ids[0] == 128000`,
`<|begin_of_text|>` — the family's BOS, harmless); response_ids 1,106;
mask-1 705 (64% trainable); 8 mask-1 spans; logprobs: 0 positive, 0
sentinel, exact-0.0 at mask-1 **13/705 = 1.8%** (reference: 6.2–14.3%,
worst 24.9%), all-0.0 at mask-0; finish `stop`;
`model_requested == model_used == unsloth/Meta-Llama-3.1-8B-Instruct`.

**The transcript** (`read.py show`): renders fully — header (model, the
`via openai_chat` line, checkout/pages, token counts, the rebuilt
line), the task prompt, all 8 turns with calls, arguments and results,
the deliverable section. What degrades, exactly as CP-37's item 15
predicted: the thinking machinery is inert (correctly — the marker ids
never occur), and `export` says so in its own words: `think_tokens:
null`, basis `"not computed: non-Qwen tokenizer"`. What the reader
showed that a Qwen eye would misread: **turn 8 renders as a raw JSON
blob** — honestly, because that is what the model actually emitted:

```
[turn 8]
   {"name": "write", "parameters": {"path": "out/<task_id>.md", "content": "## Parties\nGreven Mill Cooperative (claimant) vs. Bertha Hartl (respondent)\n\n## The dispute\n...
```

The model's final `write` call contains SEVEN literal newlines inside
its JSON strings (the first at char 537 — where strict `json.loads`
fails — beside 19 properly escaped `\n`; the model mixed the two) —
`llama3_json`'s strict `json.loads` fails, the whole turn falls back to
text content, pi reads a text answer, and the episode ends with the
real brief never written. The deliverable that exists is turn 1's
63-byte skeleton — and the export's outcome line (`deliverable:
written: true, turn: 1`) is technically true and worth reading
carefully: disposition and outcome say provenance, not success.
(Notable: the brief's CONTENT quotes page-1/page-2 facts from the read
results — the model that failed the healthcheck's answer half used its
tool results perfectly well under pi's drive.)

### Step 5 — the divergence table, attributed

Reference = a CP-04′/09′-shape reference-stack episode. Attribution
classes per the prompt: **model** / **estate** / **demo** / **library**.

| # | divergence from the reference | attribution | cost |
|---|---|---|---|
| 1 | serve legs: `llama3_json` (not hermes); embedded Meta template (no `--chat-template`); no reasoning-parser / thinking kwargs; genconfig `0.6/0.9/no-top_k` (not `0.6/0.95/top_k 20`) | **estate** — re-decided per family; now a second committed recipe (`serve-llama31.sh`) | none; the point of Step 2 |
| 2 | two-terminator stream: tool-call spans END sampled `<\|eom_id\|>` (mask 1), each interstitial OPENS canonical `<\|eot_id\|>` (mask 0); reference has one id (`<\|im_end\|>`) in both roles | **model** (family protocol) — absorbed by the vendored merge's canonical-vs-canonical design | none at reconstruction; trainers see the model's real protocol at mask 1 (row 43) |
| 3 | `prompt_ids[0] == 128000` (BOS); reference has none | **model** | none — no gate reads position 0 |
| 4 | the render injects `Environment: ipython`, a fixed `Today Date`, and the 11 tool definitions into the FIRST USER message (`tools_in_user_message`) | **model** (template design) | none for G2 — the gate hashes the wire message content, not the render; prompt grows to 3,329 ids |
| 5 | tool results render JSON-quoted (strings are jinja-`iterable` → `tojson`) | **model** (template) | none — deterministic in every render |
| 6 | healthcheck round trip: parser half OK, answer half FAILS 4/4 (meta-commentary / repeat-call with `stop_reason` eom) | **model** (temperament on a toy probe) | none in-episode; healthcheck-as-written cannot go green on this model — recorded in MODEL-SURFACE so a stranger knows which half a red probe shows |
| 7 | final tool call arrived as TEXT: raw newlines inside the sampled JSON strings (7, first at char 537, beside 19 escaped `\n`) → strict parse fails → episode ends; deliverable = the turn-1 stub | **model** (malformed escapes), surfaced by the estate's parser strictness | the episode's task outcome; provenance unaffected (accepted, honestly) |
| 8 | zero `mcp_gsj_*` calls — retrieval-free green; page census empty, cutoff machinery unexercised | **model** (behavior); the gates' silence about it is the **library's designed scope** (provenance, not usage; H41 gate is policy-off and fires only on zero TOOL calls) | consumers must assert retrieval on the export, not the disposition — added to MODEL-SURFACE |
| 9 | exact-0.0 logprob rate 1.8% vs 6.2–24.9% | **estate** (sampling policy: no top_k) + **model** (peakedness) — not separable from one episode | none; LP6 far under allowance; the rate is an estate property, said so in MODEL-SURFACE |
| 10 | read.py/export degradations: think machinery inert; `think_tokens: null` with labelled basis; turn 8 renders as a raw JSON blob (honest) | **demo** (display heuristics, all labelled and graceful) | cosmetic only; nothing misrendered |
| 11 | preflight's tool-parser cure texts named hermes only | **demo** (document/tooling Qwen-branded) | a Llama stranger got the wrong cure — FIXED this CP (family-bound text) |
| 12 | MODEL-SURFACE's "honest limit" (sampled-vs-rerender an unprobed extra condition) | **demo** (document) | measured FALSE — withdrawn and replaced with the canonical-vs-canonical mechanism |
| 13 | template-probe id counts: live 58→96 vs offline 59→99 | **demo** (probe environment, transformers vs `/tokenize`) | none — property and derived values identical; named to prevent a future drift false-alarm |
| 14 | vendored Polar's EOT auto-detect picks the last SAMPLED token — on this family the stop token, absent from renders → every chain would truncate | **library-adjacent** (vendored Polar; NOT `gsj_rollout/` — the gsj builder never relies on auto-detect, A-15) | none through any gsj path (A15 finding at source, `G7:chains_truncated` at receiver, and the demo always configures the derived id); wishlist 43 carries it |

**The library column, said plainly: no Qwen assumption baked into
`gsj_rollout/` bit this family.** Row 41's frozen constants
(`thinkingFormat: "qwen-chat-template"`, `supportsReasoningEffort`)
were deliberately not exercised (thinking-off, as MODEL-SURFACE tells
modeless families); the served-name mechanics worked end to end; G6
degraded to template integrity exactly as spec §G6 [CP-37] says;
`checks.py` ran both sides with zero findings. The one fragile
mechanism found (row 14 above) lives in vendored Polar, is unreachable
through the demo path, and is double-defended — a finding, not a fix.

### Step 6 — MODEL-SURFACE.md, judged by its first user

**Right, and load-bearing:** the derivation classes ([endpoint] /
[snapshot] / [estate] / [you]) matched reality exactly — everything
promised derivable WAS derived, live, correctly, first try; the
"mirrors ship different templates" warning is the reason this CP could
pin the right artifact; the G6 meaning table ("any other family:
template integrity only") is precisely what the accepted episode
measured; the thinking recommendation (keep `off`) was followed and
correct; the eos-list cross-check (`[128001, 128008, 128009]`)
foreshadowed the geometry finding without knowing it.

**Thin:** no tool-call-parser row existed — the one serve leg a Llama
stranger MUST change had no table entry (the README's argv is
Qwen-branded, and preflight's cure text said hermes). Added, plus the
family-bound cure texts in preflight.py.

**Wrong:** the prefix-extension section's "one honest limit" — the
claim that sampled-vs-rerendered identity is an extra condition only an
episode's `chains_total` can confirm. Measured false (the merge's
prefix comparison never reads sampled bytes); withdrawn in place,
replaced with the measured mechanism and the mask geometry. The end-of-turn section's framing
("the template's terminator is also the id your engine stops on") was
the A-30 assumption — rewritten around the two-terminator measurement:
derive the RENDER-side closer; the stop id need not equal it; direct
library consumers warned about the auto-detect with both fail-closed
defenses named.

**Added:** "The second family, measured" — the serve legs that moved,
the round trip's two halves, malformed-JSON-ends-episodes, the
retrieval-free green shape, and the zero-rate-is-an-estate-property
lesson. A stranger with Llama, following the document as it now stands,
gets to an accepted episode with no missing piece: serve (four legs,
parser named) → `up` (derives) → `preflight` (verifies) → submit → read.

### Step 7 — rows 41/42, and the honest answer

**Row 41 (the harness's Qwen-bound constants): still theoretical —
now by measurement rather than absence.** The wire no-op half is
measured live (mode pair byte-identical against the served endpoint);
the stamp half was deliberately not exercised (thinking-off); the
name-shape mechanics worked. Nothing bit. **Row 42 (per-mode pin
geometry): the no-mode half is MEASURED** — both modes derive the same
tail live, G6 held as pure template integrity on an accepted episode,
the mode assertion vanished correctly and silently. The
different-geometry half (a real non-Qwen hybrid reasoner) remains
theoretical.

**Is this system model-agnostic, and in what sense?** Unhedged: **the
pipeline is model-agnostic in its mechanism and Qwen-fitted in its
defaults, and CP-38 is the first proof of the first half.** What is
genuinely agnostic, now measured rather than argued: reconstruction
(canonical-vs-canonical — it absorbed a family whose stop id differs
from its template's closer, the hardest case yet seen), the gates
(every G-gate ran and meant something true on the foreign family, with
G6's meaning degrading exactly as documented), the derivation (every
[endpoint] value derived correctly, live, on the first try), and the
demo path end to end (config → up → preflight → episode → read, no
step model-branded). What is NOT agnostic, each named and priced: the
serve argv is family-work a human must re-decide (now two committed
recipes); the thinking surface is Qwen's (rows 41/42 — a hybrid
non-Qwen family cannot express its mode without a §1-lift library
change); G4's byte identities need a snapshot; sampling is forever
[estate]; and every MEASURED number (episode wall, zero-rates,
qualification rates) is estate-bound. The honest formula: **bring any
OpenAI-compatible, `/tokenize`-capable, single-closer-rendering chat
model and the system runs it correctly without a code change; bring a
different reasoning geometry and the library's constants are the
wall.** One family is one data point — row 43 says exactly what a
second one must confirm.

### DoD, run and shown

```
$ git diff HEAD -- gsj_rollout/ vendor/ corpus/ mcp-service/ forgejo/ \
    pins/ spike/ tests/ .github/
(empty)
$ ./.venv/bin/python -m pytest -q
161 passed in 16.68s
# the serve argv, verbatim                          (§Step 2 above)
# the derivation live vs CP-37 offline              (§Step 3 table — AGREES on every value)
# the full preflight, every row                     (§Step 3, verbatim — all ok + the sampling warn)
# chains_total, the quarantine, the tool calls      (§Step 4, in that order: 1 / empty / 7 parsed + 1 text-fallback)
# a transcript excerpt, and what degrades           (§Step 4 — turn 8's raw-JSON blob; think machinery inert, labelled)
# the divergence table with attributions            (§Step 5)
# CP-37's predictions scored                        (§Step 1 — 4 named by CP-37 + 12 mine: 9 confirmed, 3 wrong shape, 3 not hit, 1 inverted)
$ git -C ~/mg/workspace/gsj-rollout-demo log --oneline | head -3
a505a9a the second family, measured: the two-terminator geometry, the chain-grouping condition as it actually is, and what an episode taught that no probe could
f546abd the model-dependent surface: derive from the endpoint, probe the template, write down what changes
96f2329 what the first true stranger hit: the arm64 wall, the outcome line, the portable export, the missing sentences
$ test -f docs/reports/CP-38.md && echo OK
OK
$ git status --porcelain      # after commit — and the demo repo is clean too
(empty)
```

Estate after the CP: engine STOPPED (pidfile kill; tunnel closed in a
separate ssh call — the CP-28 lesson), GPU 3 released; the five
gsj-demo-* containers UP; `work/traces/` now holds FOUR accepted
episodes under three stamps (three Qwen + this Llama one — the estate's
archive is deliberately a mixed history); the estate's pins/rollout.yaml
are Llama-derived and the box config.yaml (gitignored) still names the
Llama model — the estate records what it last ran; the Llama snapshot
and `genconfig-llama31/` stay under `~/gsj-vllm` for the fast path.
