# CP-46 — the repository a consumer sees

### CP-46 REPORT
status: done
scope_drift: none — the operator's mid-CP amendments (the description
trimmed to its first sentence; the spike header) arrived through the
Step-4 confirmation the prompt mandates, and the §7 append is the
workflow's own per-CP obligation, in CP-45's format. Nothing moved,
nothing deleted, no record file's content edited.
files: this repo — 5 files: docs/CHARTER.md +25 (the §7 [CP-46]
paragraph, appended at the [CP-45] seam), docs/README.md (new, 70
lines — the record's shelf map), spike/README.md (new, 4 lines — the
frozen-evidence header), docs/prompts/CP-46.md (verbatim), and this
report; one commit `CP-46: the repository a consumer sees`,
**pushed** (outcome verified at the remote below). Sibling repos —
untouched by design (both porcelain-clean, level with origin,
verified). Off-tree, recorded here because no diff carries it: the
GitHub description set (was null) and seven topics added (were
empty), via `gh repo edit`, values under notes.
tests: `.venv/bin/pytest -q` → **161 passed** · `cd corpus &&
../.venv/bin/python -m pytest -q` → **58 passed** · `cd mcp-service
&& .venv/bin/python -m pytest -q` → **89 passed** · `python -m build
--wheel` → 0.1.2 rebuilt, `unzip -l` shows the CP-19 set unchanged
(the eight modules + ingest_corpus.py + both pins sets + dist-info,
16 entries) · `python pins/derive_pins.py | tail -1` → `all approved
values reproduced`
adrs: none        assumptions: none
gap_register: §7 [CP-46] paragraph appended — no row moves, tally
unchanged (**21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD**).
Audit ids touched: S14 (its `docs/polar/README.md` mechanism reused
one level up; its own 5-of-9 indexing gap is content, left for a
content CP), §RESIDUE (every disposition relied on and none
disturbed; `spike/`'s keep now carries its header). Wishlist rows:
none moved.
questions:
  - Corrupted span (Step 1): "the operatate up. Needs `staging/`,…"
    → reconstructed "the **operator** — stands the estate up.
    Needs…" (the needs-list survived intact).
  - Corrupted span (Step 2): "telling thhing false about what this
    is" → "telling something false about what this is" — applied as
    the premise to test; measurement then showed the bar tells
    nothing false (notes).
  - Corrupted span (Step 3b): "They are deons about the code." →
    "They are decisions about the code."
  - Corrupted span (Step 5): "**Ndeleted.**" → "**Nothing is
    deleted.** Moves and indexes only."
  - Corrupted span (Report): "the recommendati the operator's
    answer" → "the recommendation and the operator's answer".
  - The operator's "header in the directory" for `spike/` → default
    applied: `spike/README.md`, a title plus one sentence — the one
    mechanism GitHub renders as a directory header; the freeze
    protects the evidence bytes, and an index beside them touches
    none.
  - The workflow law "every CP updates the gap register" vs. this
    CP's "no content edits" wall → default applied: the §7 paragraph
    is the standing per-CP append (CP-45's format), not a rewrite.
  - DoD's bare `pytest` and `dist/*.whl` glob → run as
    `.venv/bin/pytest -q` (the CP-45 convention) and against the
    0.1.2 wheel by name (dist/ also holds prior builds; nothing
    deleted this CP).
next: advisory — the same mechanism extends to the moved components
(`corpus/`, `mcp-service/`, `forgejo/` have no shelf READMEs;
advocate-(c)'s named follow-up), and S14's small stale numbers
(including `docs/polar/README.md` indexing 5 of its 9 evidence
directories) remain the natural content pass. Neither belongs in a
moves-and-indexes CP.

#### notes

**The audience table (Step 1).** C consumer · O operator · T
contributor · A auditor; bold = needs it, parenthetical = merely
confused by its presence.

| entry | files | needed by | confused |
| --- | --- | --- | --- |
| README.md, LICENSE, pyproject.toml | 3 | **C O T A** | — |
| gsj_rollout/ | 8 | **C O T A** — the product | — |
| docs/ top level (VERDICT, CHARTER, AUDIT, checks-spec, corpus-contract) | 5 | **C** (verdict, checks-spec, contract) **O T A** | — |
| tests/, .github/ | 15 | **T A** (the badge backs C's trust) | — |
| pins/ | 10 | **O T A** (the wheel carries C's copies) | C |
| POLAR_SHA, CLAUDE.md | 2 | **O T A** / **T A** | C |
| corpus/ | 171 | **C** (ingest ships in the wheel) **O T A** | C — the 163-file staging interior |
| mcp-service/, forgejo/, staging/ | 42 | **O A** (mcp also T) | C, partly T |
| vendor/ | 215 | **O** (Polar runs from its venv) **T A** | C |
| docs/{reports,prompts,golden,polar} | 175 | **A** at current paths; golden+polar also **CI, tests, derive_pins.py at runtime** | C |
| docs/decisions/ | 26 | **T A** — decisions about the code | C |
| spike/ | 24 | **A** only | C O T |

The reading that decided everything: only `spike/` serves a single
audience, and every entry that confuses the consumer is needed by
another audience *at its current path*. Shelving would take from one
audience to soothe another; framing serves the consumer at nobody's
expense.

**What GitHub actually showed before (Step 2, measured 2026-08-25).**
The prompt's two premises did not survive measurement. (1) The
language bar: Python 541,579 B (94.4%), Shell 24,583, Jinja 4,360,
Dockerfile 2,989 — no Markdown, no vendored Python. Linguist's
defaults already exclude both: `vendor.yml`'s `(^|/)vendors?/`
(any depth), `documentation.yml`'s `^[Dd]ocs?/` (root only), and
Markdown is type `prose`, never counted. The Python figure is
*exactly* the non-vendor non-docs Python (mcp-service 161,299 +
tests 128,295 + corpus 110,603 + gsj_rollout 93,357 + spike 32,555 +
pins 15,470 = 541,579 — arithmetic, not estimate). The one
distortion no attribute can fix: the library is 17% of counted
Python, because tests and the moved components are genuinely
first-party. (2) The clone: 5,926 KB per the GitHub API; compressed
HEAD content is vendor ~57%, the record ~21% — the clone's weight is
vendor, not the record. What was actually broken: the description
null, the topics empty, no docs/README.md — and the file census, 697
tracked: vendor 215, docs 206 (201 record + 5 normative), corpus 171
(163 staging), mcp-service 29, spike 24, tests 13, pins 10, staging
9, gsj_rollout 8, forgejo 4, root 6, .github 2; docs is 182,864 of
~255,700 lines (72%).

**The options, costed (Step 3; a seven-agent panel — a stranger
simulation, one advocate per option, three verifiers — each fed the
measurements, plus a census red-team).**

*(a) Reorganize in place — rejected, jointly unsatisfiable with the
CP's own constraints.* The red-teamed census: **725** move-breaking
occurrences in this repo (714 substring-countable + 7 pathlib-joined
path constants in tests/conftest.py:19-22, tests/test_checks.py:707
and :935, corpus/tests/test_taskbank.py:257 — invisible to `git
grep -F` + a wrapped-line citation at docs/prompts/CP-09prime.md:22-23
+ 2 corrupted-span golden refs + 1 no-slash `ls docs/decisions`),
**10** in the examples repo, **5** in the demo (incl. 2 no-slash
tree-URL forms) — **740** total, in 117+ files, most inside verbatim
prompts, append-only ADRs, and historical reports this CP is
forbidden to rewrite. Beyond citations: ci.yml:150 and
release.yml:133 read the `docs/polar/h200-fidelity/` fixture;
`pins/derive_pins.py:76-82` reads `docs/polar/{episode}/trace.json`
— the DoD runs that script; and the wheel already on PyPI embeds
`docs/polar/…` and `docs/reports/…` provenance strings in
`pins.gsj.json` — immutable, so a move dangles shipped provenance
forever. GitHub renders symlinks as text, closing the path-stable
escape. The option's only surviving form — move nothing, add an
index — is option (c).

*(b) Split the record into its own repo — rejected.* No weight
argument survives a 5.9 MB clone. A true move deletes the record
from this repo's HEAD (forbidden); a copy forks the truth at the
next CP. History extraction rewrites SHAs, and for an evaluation
record the commit history is the authentication — CP-45's archive
paragraph anchors the goldens to `4037bc2` by hash. ADRs are
decisions about the code and belong beside it; every future CP
becomes a two-repo non-atomic commit; every cross-repo citation fix
becomes an absolute URL — a mass content edit welding the record to
a hosting platform.

*(c) Fix the framing — recommended and confirmed.* `docs/README.md`
renders directly beneath the `docs/` listing — the only place the
300-file view exists (the top level is 16 entries) — by the exact
mechanism CP-41 proved with `docs/polar/README.md` (S14). The root
README needed zero edits: its "Where the record lives" map meets the
new index at the click boundary. Honest residuals, accepted: clone
weight unchanged; the 16-entry listing still carries no per-row
labels; the library's 17%-of-Python share is inexpressible in a
language bar.

*`corpus/staging/` — kept, and the verifier closed the question.*
Generate-at-test-time is impossible in principle: the collecting
pipeline lived in the predecessor, archived and platform-frozen at
CP-45, and byte-fidelity would be unprovable — one changed byte
breaks the lock's 16 commit SHAs (pure functions of these bytes,
`corpus/tests/test_scaffold.py`'s scaffold-twice identity), the
golden manifests' "converged on the frozen SHAs" (h200:73, mac:63),
the mcp bares built from `CORPUS_ROOT` at import
(mcp-service/tests/helpers.py:36), and the wheel's
`skill_card_hash` pins derived from
`corpus/staging/skills/*/SKILL.md` (derive_pins.py:94). A fixture
archive trades <100 KB compressed for edits in four load-bearing
files and a tar that destroys the diffability a committed freeze
record exists for. Keeping costs <2% of the clone and zero shipped
bytes.

**The recommendation and the operator's answer (Step 4).** Presented
at the wall: (c) with `docs/README.md` + GitHub metadata, no
`.gitattributes`, staging kept. The operator confirmed all three
choices and amended two things: the description trimmed to its first
sentence — "Rollout server for RL over a temporally-scoped corpus:
(case, timestep, prompt) → a pinned agent in a sandbox truncated at
that timestep → one validated, training-ready trajectory." — on the
ground that the line count, the vendoring posture, and "Verdict:
ADOPT" are internal framing ("a repo announcing its own verdict
reads as self-assessment, not as a claim"); those live in the README
one click away. And `spike/` gets its honesty as a directory header,
not a linguist attribute — "mislabeling it to move a bar by 0.3%
would be a small lie in a repo that has spent forty-five checkpoints
not telling them." Topics as proposed: reinforcement-learning,
llm-agents, rollouts, rl-infrastructure, trajectory-collection,
sandboxing, training-data (verified applied via the API).

**The citation count and how it was fixed (Step 5).** Zero
citations broke — nothing moved; the fix count is 0 by
construction. The census that priced the alternative — 740
would-break occurrences across the three repos — is recorded above
as the decision's evidence. Spot-checks after: `docs/README.md` and
`spike/README.md` resolve; the load-bearing paths
(`docs/polar/h200-fidelity/callback_session_result.json`,
`docs/golden/mac/tokens.npz`, `corpus/staging/corpus.lock.json`)
resolve untouched; the wheel and `derive_pins.py` reproduce (tests
line above).

**The stranger's first thirty seconds, after (Step 6; a fresh
simulated read against the changed repo).** Seconds 0–5: the
description "does real work now — it tells me the category (RL
infra, trajectory collection) and the one unusual thing (temporal
truncation) before I read a single file. I arrive at the file
listing already knowing what kind of box this is — that was not true
before." Seconds 5–15: the listing "still fights the description"
(sixteen entries; CLAUDE.md, forgejo/, spike/ snag the eye), but
primed by the description, `gsj_rollout/` + pyproject + tests + the
badge parse as "the library is in there somewhere." **Is the library
findable? Yes** — the install name appears within one screen of
scrolling (the two-roles diagram), and now also on the second click:
docs/README.md's opening line re-anchors the split and repeats the
pip name. The docs/ click is "the big improvement" — the stranger
singled out "Who needs what" ("a reading list with a stopping point
is rare and it is the single most useful sentence in the file") and
"Load-bearing paths" (it preempts "why is a JSON fixture living
under docs/?" — the record staying put becomes a build constraint,
not hoarding). The spike/ header "converts a red flag into a labeled
exhibit." What the change deliberately left alone, honestly listed:
the root README still has no quickstart block near the top and opens
its second paragraph in checkpoint dialect; what "pi" is, and whose
corpus, are not answered in two clicks; "maintained software or
finished experiment?" is sharpened, not settled; the sixteen-entry
listing still costs credibility in the first fifteen seconds before
the README earns it back; and vendored-Polar-by-SHA "remains the
largest trust question a stranger carries out of the visit." All
content questions, all outside a moves-and-indexes CP — the shape
question this CP was scoped to is answered: the library is findable,
three times over (description, README screen one, docs index).
