# CP-26 — M8b: the stranger test

### CP-26 REPORT
status: done
scope_drift: `example_project/requirements.txt` and `FINDINGS.md` edited beyond Step 6's enumerated four (RUNBOOK / example config / install.sh / README) — both inside the fully-lifted external repo; requirements.txt is the F-17 fix's only home (the install story's other half) and FINDINGS.md is the register the findings ARE. Nothing frozen touched: `git diff HEAD -- gsj_rollout/ vendor/ corpus/ mcp-service/ forgejo/ pins/ spike/ staging/ tests/` → empty.
files: this repo — docs/CHARTER.md +37 ([CP-26] §7 paragraph); docs/VERDICT.md +4 wishlist rows (22–25); docs/prompts/CP-26.md, docs/reports/CP-26.md (new). External repo — example_project/RUNBOOK.md +192/−59 (the fix pass); example_project/install.sh +43/−15 (gate hoisted, runnable cure, CUDA advisory); example_project/requirements.txt +20/−3 (GPU-path closure + the cu126 command); example_project/config.yaml +13/−4 (in-place header, traces_dir); README.md +6 (the wheel-not-checkout honesty rider); FINDINGS.md +26 rows (F-14–F-39).
tests: `./.venv/bin/python -m pytest -q` → **136 passed** (unchanged — no code was touched anywhere)
adrs: none        assumptions: none (A-13/A-14/A-15 exercised, unchanged)
gap_register: §7 [CP-26] paragraph (no row moves; tally stands 21/7/2/1/1); wishlist rows 22–25 opened; rows 17, 19, 20, 21 referenced with new evidence, unmoved
questions:
  - Corrupted prompt spans, reconstructed and applied: (a) "handed tand standing them up" → *handed **them**, and standing them up*; (b) "what *ion that list" → *what **isn't on** that list*; (c) "`python train.py` againste" → *against **the estate***; (d) "the error when y it is a 404" → *the error when **you add** it is a 404*; (e) STOP wall "No thinking-on." → read as the standard hard-wall clause (CP-25's "g-on" precedent).
  - Where the stranger runs: **on the estate box itself** (trainer + rollout server share the H200 — the measured CP-21 topology; the run book's "runs anywhere with a GPU" was exercised in its same-box form). Default applied; the sync leg's workstation assumption became finding F-29 precisely because of it.
  - The estate was FULLY torn down at session start (BRINGUP's between-CPs norm) — stood up by operator pre-flight (Forgejo ~1 min, MCP ~2 min warm re-index `index_reused: true`, vLLM ~5 min; healthcheck.sh full pass) before the stranger clock started, per the prompt's "tested separately if at all". Default applied.
  - The sync leg was NOT completed from the workstation after its on-box failure was diagnosed — the friction was the finding, CP-21 already proved the mechanics, and the serving engine was left on the pinned model rather than flipped mid-CP. Default applied.
  - Findings numbering: continued the external register at F-14 (its own convention; the predecessor's F-NN style as instructed).
next: CP-27 advisory — the strangerward library lift: wishlist 19 (`cli.py` main guard) + 21 + 23 (`config.py` validators, ~6 lines) + 22 (`cli.py` serve prints) are one coherent small lift; independently, publishing (wishlist 17 + a mirror push) retires F-14, the only BLOCKER. The examples-repo train.py polish is wishlist 25.

---

## The fresh-environment proof

`~/cp26-stranger/` on the H200, created this CP: both repos as
`git archive HEAD` trees (examples @ dcf6c2a, library @ f96306f — no
.git, no dist/, no venvs), `.venv` created by install.sh, Polar venv
created by the stranger from Polar's README, snapshot downloaded fresh.
Nothing reused from `~/cp21*` / `~/cp17*` / `~/cp04prime`; the operator's
`~/gsj-harness-rollout-server` checkout untouched by the stranger run
(its `vendor/polar/.venv` was deliberately NOT used). Inherited: the
three estate services only (Forgejo, MCP, vLLM), per the prompt. One
timing caveat, stated honestly: pip's and uv's caches are per-user and
were warm from the operator's prior CPs, so install timings below are
lower bounds — a cold box adds the ~2.5 GB torch/ray download
(~10–20 min) and Polar's closure.

## Step 2 — install, as written

| act | wall | outcome |
|---|---|---|
| `bash install.sh` (attempt 1) | 1m15.7s | exit 1 at the wheel gate — actionable message, but AFTER the full closure installed (F-31), with cu13x torch silently landed (F-16) |
| the printed cure, verbatim | 3 dead ends | `python: command not found` → `No module named build` → `No module named pip` (F-15) |
| the invented workaround | 1.8s | `./.venv/bin/pip install build && ./.venv/bin/python -m build --wheel` → wheel |
| `bash install.sh` (attempt 2) | 15.9s | exit 0, `install OK` — printed in the same closing block as TWO warnings: CUDA "driver too old (found version 12080)" and the packaged-pins reference-estate warning (F-16, F-39) |

The resulting env imports `gsj_rollout.checks`, `verl.protocol`,
`pyarrow` (install.sh's own check). The BRIDGE and the GPU path are not
checked — which is exactly where F-17 later fired.

## Step 3 — the six values, and which needed out-of-band knowledge

On the reference estate, five of six SUPPLY values in the example were
ALREADY CORRECT and verifiable in-band (curl healthz / health /
`/v1/models` for the byte-exact served name): `clone_url_for`,
`mcp_url_base`, `serving_base_url`, `estate.model`,
`polar.gateway.public_url`. The one real edit: `traces_dir` — and the
example's own value was un-creatable on this very box (root-owned
`/data`, F-26). **Out-of-band knowledge consumed**: (1) how to OBTAIN
the two repos at all (F-14 — the step-zero blocker); (2) the MCP
secret's value/location (`~/.gsj-mcp-secret-cp04prime` — operator
handover, F-22); (3) the engine's snapshot revision for `--snapshot`
(matched by luck, F-23). The six values themselves: none unguessable ON
this estate — CP-25's classification held; the unguessables moved to
the seams around the config (secret, revision, repos).

`load_config` accepted the honest config. Error-quality probes: unknown
key → EXCELLENT (`'receiver.traces_dir': Field required; section
'receiver': unknown key 'trace_dir'`); a required LINE deleted whole →
pydantic-shaped `'polar.gateway': Input should be a valid dictionary…`
naming no field (F-25); `serving_base_url` + `/v1` → ACCEPTED
(runtime: bare 404 on `/v1/v1/…`); `gateway.port` ≠ `public_url` port →
ACCEPTED (runtime: connection refused on the advertised URL). Wishlist
21's two traps confirmed armed, with their misleading runtime shapes
now measured.

## Step 4 — the run, honest counts

Server side: `gsj-rollout serve` renders the topology and prints the
two Polar commands — in the FOREGROUND only (block-buffered under
nohup, F-20). `vendor/polar/.venv` did not exist; provisioned from
Polar's own README after installing `uv` (undeclared prerequisite,
F-21). Receiver 8300 + Polar rollout 8080 + gateway 8100 up; gateway
registered.

- **collect** (`--collect-only`, defaults = 8 attempts × 9 train rows):
  05:43:50 → 05:50:27 — **71/72 attempts qualified in 6m37s**, six
  episode containers pooled in parallel, 1 quarantine
  (`LP6:zero_logprob_rate_at_mask1:2900/8928>0.25`). Against the run
  book's primed expectations (19:1, ~36/hr) the surprise INVERTED —
  the measured stranger reaction was to distrust the gate, not the
  yield (F-18).
- **dry-run**: 6.2s, clean — 71 bodies, reward **1/71** (case_0002:t14;
  inside the stated 1/27–1/112 band — this expectation was exactly
  right), 9 GRPO groups, no singletons, conversion contract holds.
- **the GPU step**: FIVE attempts. #1 `No module named 'uvicorn'`;
  #2 `'fastapi'`; #3 `RuntimeError: driver too old` at the first CUDA
  call (the F-16 trap; its cure — `pip install 'torch==2.13.0+cu126'
  --index-url https://download.pytorch.org/whl/cu126` — written
  nowhere, and the tagless form silently no-ops); #4 `'peft'`;
  #5 (05:56:40 → 06:16:11, ~19.5 min, ~90 GB on GPU 0, one absorbable
  allocator-OOM warning): recompute mean|Δ| **0.013731** vs floor 0.008
  — on weights verified BYTE-IDENTICAL to the engine's (sha
  `f47f7117…` both sides; the unpinned download matched pin `c1899de…`
  by luck), so the excess is composition variance and the printout had
  no reading semantics (F-23); advantages min −0.3536 / mean 0.0000 /
  max +2.4749, nonzero 8/71 (the rewarded group, centred); **one
  optimizer step: grad_norm 0.1970, pg_loss 0.0158**; HF export
  written.
- **the sync leg**: failed DIAGNOSED — `serve-updated.sh` on the box
  dies `ssh: Could not resolve hostname h200-admin` after announcing
  the engine stop (the script is a workstation tool; the run book said
  "estate-side" — the wrong side, F-29). Engine verified healthy.
- **module-form probe** (hypothesis f): `python -m gsj_rollout.cli
  serve --config config.yaml` → exit 0, zero bytes of output, no
  server.

Where a stranger would have concluded "this is broken": the five
GPU-step attempts (F-16+F-17 compounding — the worst stretch of the
run); the silent nohup'd serve (F-20); the sync's hostname error
(F-29). Messages a stranger could not act on: the pydantic
dict-error (F-25), the bare 404 / connection-refused shapes (wishlist
21), torch's "update your driver" on a shared box (F-16), the replay
line's unexplained 1.7× floor (F-23).

## Step 5 — the findings, in full (external register F-14–F-39)

Severity: **blocking** / **costly** / **cosmetic**. Fix-location in
parentheses. Full table rows with evidence: `FINDINGS.md` in the
examples repo.

1. **F-14 · blocking · (operator/estate)** — No documented way to obtain
   the inputs: the examples repo has NO remote; the library repo —
   needed twice (wheel build + `vendor/polar/`) — is unpublished, and
   its public GitHub mirror is six commits stale (CP-19, pre-dating the
   consumer surface) and named in no stranger-held document. A stranger
   stops at minute zero and emails the operator. Both trees were
   side-loaded by hand.
2. **F-15 · costly · (install.sh + run book + README)** — The wheel-build
   cure was un-runnable as printed: sibling-checkout layout stated
   nowhere (the README actively said "a wheel, not a checkout"), and the
   printed `python -m build` failed three ways on a stock box (no
   `python`, no `build`, no system pip). The workaround (use install.sh's
   own venv) had to be invented.
3. **F-16 · costly, blocking for the GPU step · (install.sh +
   requirements + run book)** — The torch trap in three acts: cu13x
   default lands silently; "install OK" prints in the same block as the
   driver-too-old warning; the cure's index URL appears nowhere and the
   obvious re-pin (`==2.13.0` without the `+cu126` tag) silently no-ops
   in 2.4s. Failure waits for the first CUDA call, with torch advising a
   driver update the stranger cannot perform.
4. **F-17 · costly · (requirements.txt)** — The stated verl closure is
   the DESK closure; the GPU path needs uvicorn, fastapi, peft — three
   ModuleNotFoundError crashes, one per invocation. CP-21's venv carried
   them by accident.
5. **F-18 · costly (trust) · (run book)** — Collection expectations
   primed the wrong pain: the 19:1 headline is the strict standard, the
   ~36/hr figure a serially-fed engine; measured 71/72 in 6m37s pooled.
   The calibrating 27/28 relaxed figure existed but was buried; nothing
   was dated. The stranger's measured reaction: distrust the gate. (The
   reward-sparsity expectation, by contrast, was exactly right.)
6. **F-19 · costly · (run book + example config)** — "Copy config.yaml
   and edit" vs a `--config` default of `config.yaml` and commands that
   pass no flag: the literal reading edits a copy nothing reads; on this
   estate the unedited example then crashes serve at startup mkdir of
   root-owned `/data/gsj/traces`, reading as permissions.
7. **F-20 · costly · (run book; library wishlist 22)** — The session's
   only instructions (topology + the two Polar commands) are
   block-buffered stdout: backgrounded with nohup, the log holds only
   the pins warning while the receiver listens silently.
8. **F-21 · costly · (run book; library wishlist 22)** —
   `vendor/polar/.venv` assumed everywhere, provisioned by no
   gsj-authored document; the printed path is relative to an unstated
   cwd; the only recipe is Polar's upstream README, which assumes `uv` —
   not on the box, and the run book's own A-14 line assumed it too.
9. **F-22 · costly · (run book; mcp-service wishlist 24)** — The MCP
   secret has no acquisition path (operator handover only) and no
   verify-before-episode probe; a mismatch first surfaces mid-episode as
   failed tool calls.
10. **F-23 · costly · (run book; config wishlist 23)** — `--snapshot
    <pinned HF dir>`: no fetch command, no stated pin, no drift
    semantics. The engine's revision is estate lore; this run matched it
    by luck, and the replay printout (0.0137 vs floor 0.008 on identical
    weights) gave no way to tell drift from variance.
11. **F-24 · cosmetic · (run book)** — "the four URLs" vs six values;
    `model` is a name, `traces_dir` a path.
12. **F-25 · cosmetic · (config schema wishlist 23)** — A required line
    deleted whole → comment-only section → YAML null → pydantic's
    "Input should be a valid dictionary" naming no field.
13. **F-26 · cosmetic · (example config)** — The example `traces_dir`
    was un-creatable on the very box the example mirrors.
14. **F-27 · cosmetic · (run book; example-code wishlist 25)** — The
    promised "reward distribution" is 71 unaggregated lines; collect
    likewise (71/72 was hand-counted).
15. **F-28 · cosmetic · (requirements + run book)** — Post---no-deps,
    env-changing pip installs print an ERROR-labelled "verl requires
    hydra-core / pybind11 / pylatexenc / tensorboard / wandb" block;
    deliberate, explained nowhere at the point it appears.
16. **F-29 · costly, blocking for the sync leg · (run book; example-code
    wishlist 25)** — The run book sent the sync "estate-side"; the
    script is a workstation tool (`GSJ_VLLM_SSH_HOST`, default alias
    `h200-admin`) and on the box dies with a DNS-shaped error after
    announcing the engine stop. train.py's closing printout repeats the
    wrong side.
17. **F-30 · costly · (run book)** — The Run commands don't run as
    written: bare `python` doesn't exist, `gsj-rollout` lives in a venv
    no document activates.
18. **F-31 · costly · (install.sh)** — The wheel gate ran LAST: a cold
    box pays the full closure download to reach an inevitable abort.
19. **F-32 · costly · (run book)** — `example_project/` is presented as
    the project but is dead without sibling `verl_bridge/` and
    `slime_bridge/`.
20. **F-33 · costly · (run book)** — "Estate bring-up is the library
    repo's staging/ recipes" is wrong: the authoritative walk
    (BRINGUP.md) lives in the PREDECESSOR repo, which the stranger
    neither holds nor hears named.
21. **F-34 · costly · (run book; example-code wishlist 25)** — GPU
    selection undocumented and hardcoded to the first visible device;
    this run's success was tenancy luck.
22. **F-35 · costly · (run book)** — The GPU leg: ~20 min of
    near-silence whose most visible line is an allocator-OOM WARNING
    (absorbable — F-12/F-13's shape); no stated duration or ~90 GB
    footprint anywhere.
23. **F-36 · cosmetic · (run book)** — The A-14 pip-install-into-Polar
    instruction was stale: the printed commands already inline the
    PYTHONPATH.
24. **F-37 · cosmetic · (run book)** — Two trace stores (`traces_dir`
    vs `collected/`), relationship unstated.
25. **F-38 · cosmetic · (run book)** — `rebuild_taskbank.sh` needs a
    library venv no trainer step creates.
26. **F-39 · cosmetic · (run book; checks.py half noted in wishlist
    22)** — The packaged-pins warning shouts on EVERY import, and on the
    reference estate the correct response is to do nothing.

All 26 were adversarially verified against the artifacts and transcripts
before this report (8 grouped refuters + 2 completeness critics, the
CP-25 pattern): 9 confirmed outright, 7 corrected in the writing (F-14's
"unfetchable from anywhere", F-16's counts, F-18's central charge
narrowed, F-19's consequence, F-21's "no pointer", F-28's package list,
F-29's severity), and F-30–F-39 are the critics' catches — misses of my
own first pass, kept because they survived the same verification.

## Step 6 — fixed vs not fixed

**Fixed this CP (docs + example, 18 findings)**: F-15, F-16, F-17, F-18,
F-19, F-24, F-26, F-28, F-30, F-31, F-32, F-33, F-35, F-36, F-37, F-38,
F-39 fully; F-14/F-20/F-21/F-22/F-23/F-27/F-29/F-34 got their
words-can-help half (the run book now names the handover, the
foreground-first rule, the provisioning walk, the secret hazard, the
fetch-with-revision command, the grep, the workstation topology, the
CUDA_VISIBLE_DEVICES instruction).

**Not fixed, and why**: F-14's cure is an operator act (publish /
push the mirror — wishlist 17 stands); F-20/F-21/F-39's code halves are
`cli.py`/`checks.py` (wishlist 22); F-25 and F-23's config halves are
`config.py` logic (wishlist 23, beside 21); F-22's probe is
mcp-service-side (wishlist 24); F-27/F-29/F-34's train.py polish is
example-code outside Step 6's enumerated set (wishlist 25 — external
repo, no library freeze involved, deferred only for scope honesty).

## CP-25's hypotheses (a)–(g), scored

- **(a) CONFIRMED, soft**: the documented collect path (train.py) never
  needed a CLI; the missing bank-submit was felt only as "no way to
  smoke ONE row before 72" — a wish, not a wall (wishlist 20 stands).
- **(b) CONFIRMED, both halves, as described**: schema accepts both
  traps; runtime shapes now measured (bare 404 / connection refused).
- **(c) CONFIRMED and WIDER than its wording**: bring-up itself was
  inherited, but the c-shaped hole reached INTO the stranger run
  through the session-start seam — Polar venv provisioning (F-21), the
  secret (F-22), the sync topology (F-29) are all estate-lore leaks;
  and the run book's one bring-up pointer aimed at the wrong repo
  (F-33).
- **(d) NOT HIT** — same estate, packaged pins matched; the
  re-derivation walk was never needed (by design of the run).
- **(e) NOT HIT** — collect and train shared the box; the
  cross-machine artifacts-dir gap stays latent, stated-not-enforced.
- **(f) CONFIRMED as latent** — measured exit 0 / zero output / no
  server; the run book's closing list does warn it, and the Run
  section's console-script form kept the natural path off it.
- **(g) CONFIRMED and WORSE** — not only the examples repo: the library
  repo is needed twice (wheel + Polar), and its public mirror is six
  commits stale and unnamed. The blocked surface is both roles (F-14).

**The members nobody predicted** — the point of the exercise: F-15,
F-16, F-17, F-18, F-19, F-20, F-26, F-29, F-30, F-31, F-32, F-33, F-34,
F-35 (+ the F-36–F-39 cosmetics). Fourteen substantive findings outside
the hypothesis list, against seven on it. The worst compound: F-16+F-17
(five failed GPU-step invocations); the most insidious: F-18 (a correct
system reading as a broken one because the docs primed the wrong
regime).

## What the consumer surface now is, and what the next stranger hits

It is a real, runnable surface: from two side-loaded trees a stranger
reaches one true GRPO step and an HF export in under an hour of wall
time — collect 6m37s, GPU leg ~20 min, the rest reading and stumbling.
Every stumble is now either fixed in the documents or a numbered row.
The NEXT stranger still hits, in order: F-14 (they cannot get the trees
— the one blocker, operator-owned); the cold-cache install cost this
run's warm caches hid (~15–25 min of downloads); wishlist 21/23's
config traps if they typo (schema-silent until run time); F-22's secret
(handover, now at least named); and the sync leg unless they sit at the
operator's workstation (F-29 — documented now, still a topology
constraint, not a cured one). An honest estimate: with the fixed docs,
a competent stranger holding the handover list runs collect-to-export
in one sitting; without the handover (the secret, the revision, the
trees), they do not start.

## The honest answer, unhedged

**Could a stranger run this today?** No — not from zero, because they
cannot obtain the inputs (F-14): both repos arrive only by operator
handover, and that is a blocker before the first command. **Could a
stranger HANDED the two trees and the four-line handover note run it
today, with the fixed docs?** Yes — the whole loop through the optimizer
step and export on the reference estate, in about an hour, with no
out-of-band knowledge beyond that note; the sync leg additionally
requires sitting where the estate's ssh alias resolves. Before this CP's
fixes the same stranger would have stalled at the wheel build (F-15),
shipped a broken torch to the GPU step (F-16/F-17), and been misdirected
at the sync (F-29) — the run was completable, but only by someone
willing to invent workarounds five times. That distance — five invented
workarounds → zero — is what this CP bought.
