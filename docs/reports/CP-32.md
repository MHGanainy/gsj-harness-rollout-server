# CP-32 — M9d: the stranger test, both modes

### CP-32 REPORT
status: done
scope_drift: none — the library repo took `docs/**` only (prompt, this
report, CHARTER §7 [CP-32] paragraph, VERDICT wishlist rows 29–33); the
DoD's frozen-path diff is empty and the root suite is byte-unchanged at
150. The external repo took the Step-6 fix set (RUNBOOK ×7 edits,
config.yaml comment, train.py ×3, FINDINGS.md F-45–F-53) — docs and
example only, per the freeze-lift.
files: library — docs/prompts/CP-32.md, docs/reports/CP-32.md (new);
docs/CHARTER.md (+36: §7 [CP-32] paragraph); docs/VERDICT.md (+5:
wishlist rows 29–33). External — 4 files, +93/−31:
example_project/RUNBOOK.md +83/−31 (F-45 §Run rewrite, F-46 trainer
buffering note, the CP-31 extrapolations replaced with dated
measurements ×4, F-49's real symptom, F-48's archive sentence, F-47's
wall-at-scale rider), example_project/config.yaml +11/−3 (F-50's
comment corrected to measured behaviour), example_project/train.py
+21/−2 (line-buffering, the named masked-rows assert, "client-side" +
the receiver-check line), FINDINGS.md +9 rows (F-45–F-53).
tests: `./.venv/bin/python -m pytest -q` → **150 passed** (byte-
unchanged — no library code touched). Fixes verified live on-box: the
new assert message fires with the full diagnosis on the item-5 replay;
the happy-path dry run still ends "dry run OK (72 rows)";
`py_compile` clean.
adrs: none        assumptions: none new (A-13's drain point exercised
as designed at the sync)
gap_register: §7 [CP-32] paragraph added (no capability row moves;
tally unchanged). Wishlist rows 29–33 opened. External register
F-45–F-53 appended.
questions: (1) five corrupted spans reconstructed and applied —
"Record timints" → "Record timings"; "exactly aibes" → "exactly as
§Thinking prescribes"; "in the con the validator's message" → "in the
config — is the validator's message"; "Freeze-lift: `docsnd the
external examples repo" → "Freeze-lift: `docs/**` and the external
examples repo"; DoD "whether the A/B able" → "whether the A/B is
performable". (2) Applied default, sync ordering: the pass-2 A/B ran
BEFORE pass 1's sync leg so both modes measured the same engine
weights (CP-28's same-engine precedent); sync + probe + post-sync
collect then closed pass 1 — the walk is complete, the A/B is clean.
(3) Applied default, injected item 2 ran at `--episodes 1` (9
attempts) — the symptom is per-episode; the full 72 would have spent
13 estate-minutes to show the same three facts. (4) Applied default,
operator-role: both repos' CP-31 commits were unpushed and were pushed
before the stranger clone (recorded as F-52 rather than silently
cured). (5) Applied default: the stranger environment lives on the
estate box (`~/cp32-stranger/`, CP-26's precedent — both roles need
the one host).
next: advisory — wishlist 29 (the PyPI-era Polar print) is the one a
stranger still trips over; 30–32 are the ingest/validator surfacing
set; 28 still retires the interim pins copy; 26's CI lift and 24's
secret probe stand. The estate was torn down after the CP (engine was
serving the CP-32 checkpoint; gpu-hold containers left as found).

---

## Notes

### The fresh-environment proof (pass 1 preamble, all 2026-08-14 UTC)

Clones from the public URLs into `~/cp32-stranger/` at 14:39:29Z — both
repos in **under 2 seconds**, heads `68b3966` (external) and `5e9ad87`
(library), acquired exactly as a stranger would (the operator's push of
those commits minutes earlier is F-52). `bash install.sh` → RC=0 in
**~11 min**, the F-16 torch warning printing with its cure verbatim;
the cu126 re-pin took 70 s; the Polar venv ~1 s (warm uv cache — a
true stranger downloads for a few minutes); the pinned snapshot 2 s
(warm HF cache, same caveat). Config: **one** SUPPLY value actually
needed editing on the reference estate (`traces_dir`); all four
endpoint sanity curls passed first try. No side-loading anywhere: the
wheel came from PyPI (v0.1.0), the venv proved gsj_rollout importable,
and the sibling checkout served only `vendor/polar/`.

### Pass 1 — the honest thinking-on run, against CP-31's extrapolations

| quantity | CP-31 carried | CP-32 measured (2026-08-14) |
| --- | --- | --- |
| pooled ON collect, 72 attempts | ~15–20 min (extrapolation, 2.7× the pooled-off 6m37s) | **13m35s** (15:20:54→15:34:29Z; 6 pooled containers throughout) |
| qualification yield, pooled ON | ~98% (from serial legs) | **72/72 = 100%** (0 rejected; quarantine empty) |
| think-share | 67% median (CP-28, n=15) | **61%** (79,013/128,586 mask-1) |
| reward density ON | 3/15 (CP-28 serial) | **1/72** (mean 0.0069, max 0.5) — "not a stable constant" holds in both directions |
| replay delta ON | unmeasured; off floors 0.008 / 0.21 (F-43) | **mean\|Δ\| 0.016546; 456/128,586 (0.35%) over 0.21** — ~2× the constant, same order, sub-percent tail: the reading guidance holds as written |
| ON GPU-leg memory | "closer to the ceiling", unmeasured | **135,093 MiB / 143,771 (94%)** peak on one H200; 72 rows to 32,645 ids; **zero** allocator retries; leg wall 11m38s |
| GRPO advantages | "centre sanely when reward is denser?" | centre exactly (min −0.3536, **mean 0.0000**, max 2.4749, nonzero 8/72 — the one rewarded group) — measured under SPARSER reward, not denser |
| step | — | grad_norm 0.0221, pg_loss 0.00099, HF export written |
| sync | — | probe→sync→probe: **mean\|Δ\| 0.040184, 7,327/7,609 (96%) positions moved** (CP-21's shape); post-sync collect 9/9 accepted through the receiver |

The first accepted episode arrived with **nothing set by hand** — the
auto-set `GSJ_PINS_PATH` (train.py leg) plus the run book's inline-env
serve command (receiver leg) delivered all 72, quarantine empty. The
sentence hunt item 1 exists for is TRUE.

Messages I could not act on during the honest path: the printed Polar
commands (F-45 — acted on only via the RUNBOOK's old F-21 sentence plus
reconstruction); an empty collect log for five-plus minutes (F-46);
`serve-updated.sh`'s mid-sync `bash: -c: option requires an argument`
(F-53).

### Pass 2 — the control, and whether the A/B is performable

OFF pooled collect **72/72 in 4m36s** (15:51:21→15:55:57Z). Same-day,
same-engine pooled A/B: **2.95×** (815 s / 276 s) — the serial 2.7×
survives pooling. The three printed lines §Thinking names compared
cleanly: `collect total:` 72/72 both; `reward distribution:` 1/72 vs
**0/72** (the off control would have trained on zeros — the line made
it visible before the GPU); think-share **present at 61% vs absent**
(the off signature, as documented).

**Verdict: the A/B is performable from the run book alone.** §Thinking's
1-2-3 held: one flag, serve restarted per mode (the F-39 warning
printing on the off serve exactly as promised; the Polar pair correctly
untouched), separate `--out` dirs. The friction that remains: §Run's
honest first collect lands in the default `collected/` while §Thinking
names `collected-on`/`collected-off` — the stranger carries the
"collected ≡ ON" mapping and must add two flags to every off-leg
stage; the run book gives no stop-serve recipe (I used the port
table); and the durable archive mixes both modes in one `traces_dir`
(F-48).

### Pass 3 — the injected failures, self-diagnosis verdicts

- **Item 2 (serve without `GSJ_PINS_PATH`, collect ON)** — symptom
  matches §Thinking **verbatim**: train.py printed "9/9 attempts
  qualified … 0 rejected/quarantined", RC=0, `collected-inj2` filled —
  while `traces_dir` gained nothing (144→144) and quarantine went 0→9,
  every finding G6-only (`G6:prompt_suffix_ne_tail_ids` ×9). Verdict:
  **diagnosable from the run book, and only from the run book** — the
  bolded "check this once after your first collect" is the sole path;
  train.py's healthiest-looking line pointed away (F-51, fixed: the
  line now says "client-side" and a follow-on line sends the reader to
  the archive). This is, as the prompt said, the leg train.py
  structurally cannot catch — confirmed.
- **Item 5 (grade an ON directory under off)** — **does NOT diagnose
  itself; the documented symptom was wrong.** No G6-named ingest
  failure exists: every body ingested with rows silently masked, all
  72 reward lines printed, and the run died at `AssertionError: 72
  masked rows in a qualifying collection` — no G6, no pins, no mode in
  the words (F-49). Fixed examples-side: the assert now names the
  mode/pins hypothesis when all rows mask (verified firing on-box);
  §Thinking describes the real symptom; the library-side surfacing is
  wishlist 30.
- **Item 6 (unquoted `thinking: on`)** — **wrong shape, partially
  findable**: a raw pydantic type-error traceback (`harness.thinking …
  input_value=True, input_type=bool`), not the documented by-name
  rejection (bare `on` is a YAML boolean, never the string the comment
  claimed); train.py's own banner meanwhile printed "thinking: on"
  from the same byte. The cure is findable because KEEP-THE-QUOTES
  sits beside the edited line — the comment now states the measured
  behaviour (F-50); the named message is wishlist 32.

### Step 4 — the residuals

- **Item 7, context pressure at scale: HIT, worse than predicted.**
  Merged ids median 9,360, p90 26,253, **max 32,645 of 32,768
  (99.6%)**; **7/72 episodes ended `finish_reason: length`** (CP-28
  measured zero at n=15/leg) — and all seven QUALIFIED. Qualification
  does not screen length-terminated episodes; they entered the batch
  silently (F-47; RUNBOOK now says so; the checks option is wishlist
  30).
- **Item 8, F-44's amplification: not hit.** 144 artifact dirs
  inspected: 113 hold zero `.md`, 31 hold exactly one, none hold more —
  the graded-file lottery did not fire at this scale.

### The findings (external register F-45–F-53)

- **F-45** (pass 1, FRICTION, library-side yes → wishlist 29): the
  serve printout's Polar commands are broken under a PyPI install —
  absolute site-packages path, ENOENT. RUNBOOK §Run rewritten.
- **F-46** (pass 1, COSMETIC, FIXED): train.py block-buffered under
  redirect; a 13-minute collect showed an empty log. One line
  (`line_buffering=True`) + a RUNBOOK note.
- **F-47** (pass 1, DOC/latent FRICTION, half library → wishlist 30):
  the 32k wall is hit at scale and qualification does not screen it —
  7/72 length-terminated episodes trained on silently.
- **F-48** (pass 2, DOC/latent FRICTION, half library → wishlist 31):
  the durable archive mixes modes across serve restarts; only the
  working set is protected by `--out` discipline.
- **F-49** (pass 3, FRICTION, half library → wishlist 30): the
  mixed-dir mistake dies as an undiagnosable masked-rows assert, not
  the documented G6-named failure. Assert message fixed + §Thinking
  corrected; ingest surfacing stays open.
- **F-50** (pass 3, COSMETIC/DOC, half library → wishlist 32): the
  YAML-boolean trap fires as a pydantic type traceback, not the
  promised by-name rejection. Comment corrected.
- **F-51** (pass 3, DOC, FIXED): "0 rejected/quarantined" counted only
  the trainer's leg and read healthy during the receiver-leg mismatch.
  Now "client-side" + an archive-check pointer line.
- **F-52** (pass 1 preamble, FRICTION/process): both repos' CP-31
  commits were unpushed when CP-32 began — "public" lagged "landed" by
  one checkpoint. Pushed before the clone; a publish-step in the CP
  protocol is the durable cure.
- **F-53** (pass 1, COSMETIC, verified, library-side yes → wishlist
  33): `serve-updated.sh:81`'s `ssh … bash -c '<newline>…'` gives
  `bash -c` an empty script; the health gate then runs in the login
  shell by accident. The sync completed through it and was proven by
  the probe.

### CP-31's hunt list, scored

1. **CONFIRMED** — first-run under the shipped default with nothing
   set: 72/72 accepted, quarantine empty.
2. **CONFIRMED** — the receiver-leg mismatch symptom matches §Thinking
   verbatim; diagnosis reachable only through the run book (F-51
   fixed the misleading line).
3. **MEASURED** — 13m35s pooled ON (the 15–20 min extrapolation was
   high); yield 100%, above the ~98% suggestion.
4. **MEASURED** — replay 2× the constant but in-band; memory 94% of
   the device; think-share 61%; advantages centre exactly — under
   sparser, not denser, reward.
5. **WRONG SHAPE** — the A/B itself works as written; the mixed-dir
   failure is silent masking + an assert, not G6-named findings
   (F-49).
6. **WRONG SHAPE** — a type-error traceback, not a named rejection
   (F-50).
7. **HIT, WORSE** — 7/72 at the wall, max row 99.6% of the window, all
   qualifying silently (F-47).
8. **NOT HIT** — no artifacts dir held >1 `.md`.

Not predicted by the hunt list: F-45, F-46, F-48, F-51, F-52, F-53.

### What was fixed, what was not

Fixed (external repo, verified live): RUNBOOK §Run's Polar-command
paragraph (F-45 interim), the trainer buffering note + train.py
line-buffering (F-46), four dated-measurement replacements
(extrapolations retired), §Thinking's real mixed-dir symptom (F-49
interim) + per-mode `traces_dir` sentence (F-48 interim), the
wall-at-scale riders (F-47), config.yaml's corrected comment (F-50
interim), train.py's named assert (F-49) and client-side line + archive
pointer (F-51). Not fixed (wishlist, with finding numbers): the cli.py
print (29 ← F-45), ingest surfacing + length screening (30 ← F-49,
F-47), the receiver mode-stamp (31 ← F-48), the validator's named
boolean handling (32 ← F-50), serve-updated.sh's quoting (33 ← F-53) —
all library-frozen paths this CP could not touch.

### The honest answer

**Yes — a stranger can run this today, in both modes, from the run book
alone**, on one condition the run book itself now states: when serve
prints its two Polar commands, the stranger must take the binary from
the sibling library checkout instead of the nonexistent site-packages
path the printout offers (F-45). Every other leg — install, configure,
serve, both collects, the A/B comparison, the GPU step, the sync —
ran as printed, first try, with the shipped defaults, and the failure
modes that were injected either matched their documentation exactly
(item 2) or have now had their documentation corrected to what actually
happens (items 5 and 6). The qualitative surprise of the CP: thinking-on
at scale presses the 32k window hard enough that ~10% of episodes
truncate and still train — the next consumer decision worth a CP is
whether qualification should say so.
