### CP-22 REPORT
status: done
scope_drift: none — `gsj_rollout/`, `corpus/`, `mcp-service/`, `forgejo/`, `spike/`, `tests/` untouched (protected-path diff empty); `vendor/polar` ends the CP with ZERO diff against HEAD — the re-vendor reproduced the committed tree byte-for-byte, which is the result, not an omission; `pins/` untouched (no pinned value moved); the `prefix_merging`-refactor diagnostic ran entirely in a scratch tree outside the repo
files: 6 files changed, 477 insertions(+), 16 deletions(−) — new: `docs/prompts/CP-22.md` (verbatim), `docs/reports/CP-22.md` (this); modified: `docs/CHARTER.md` (A-8 RESOLVED, A-13 upstream-consumer note, gap rows 8/15/24), `docs/VERDICT.md` (dated M7a section: the re-vendor door NARROWED), `vendor/REVENDOR.md` (five corrections from execution), `POLAR_SHA` (re-vendor record, same SHA); `vendor/polar/` itself: ZERO diff — the result
tests: root `.venv/bin/pytest -q` → 129 passed; corpus `python -m pytest -q` → 43 passed + 1 skipped (the pre-existing ADR-0003 taskbank deferral; 44 collected); mcp-service `.venv/bin/python -m pytest -q` → 89 passed; vendored `vendor/polar/.venv/bin/pytest -q` → **175 passed / 3 failed — the same three pre-existing upstream failures CP-03 recorded, none new**; the four mandatory carried-patch tests → 25 passed; `pins/derive_pins.py` → all approved values reproduced
adrs: none — the pin did not move and the mechanism did not change (see questions)        assumptions: **A-8 RESOLVED (HOLDS, residuals named)**; A-13 gains the upstream policy_version-consumer note; A-14 gains live re-verification (the recipe-verbatim venv breaks without it — now a recipe step)
gap_register: rows 24 (pin proven reproducible), 8 (registry seam re-verified at the re-vendored tree; refactor priced), 15 (P2 re-applied byte-faithful; its anchor survives the refactor)
questions:
  - Corrupted span, freeze-lift: "GPU, no estate, no training" → "**No** GPU, no estate, no training" (the STOP wall states it uncorrupted).
  - Corrupted span, Step 2 heading: "Executllow the recipe as written" → "Execute — follow the recipe as written".
  - Corrupted span, Step 3: "`tilder_filter_wiring`" → "`test_builder_filter_wiring`" (REVENDOR.md's mandatory list).
  - Corrupted span, Step 5: "the pre-existing failures as they nowand any step" → "…as they now stand, and any step…".
  - Corrupted span, DoD: "cd corpus && python -m pyte .." → "cd corpus && python -m pytest -q ; cd ..   # 44".
  - Corrupted span, Report list: "whatesidual survives" → "what residual survives".
  - Applied default, no new ADR: recipe step 1 mandates an ADR "before touching the tree" — read as governing a *moved* pin; the pin did not move and ADR-0005's mechanism is unchanged, so the target decision is recorded here and step 1 now says so (ADRs are decisions; re-running an existing decision is not one).
  - Applied default, recipe step 6 (smoke) SKIPPED: the re-vendored tree is byte-identical to the committed tree (empty `git diff`), so the smoke cannot produce different artifact shapes, and the CP's own verification definition ("the four suites plus the vendored tests REVENDOR.md makes mandatory") does not include it. REVENDOR.md step 6 is now conditional on the tree actually changing.
  - Applied default, the refactor diagnostic: run in a scratch tree (pristine pin + the `polar`-branch versions of the files the refactor touches), never in `vendor/` — Step 4 asks what a future refactor will cost, and simulation answers it without moving the pin.
next: advisory — the re-vendor door is NARROWED, not closed; the residual that matters is the refactor actually landing on `stable`, and the survey recipe now checks its `policy_version` consumer first. No successor CP is implied by this one; M7's remaining letters are the operator's call.

---

#### The survey (Step 1) — where upstream actually is, 2026-08-12

All via `gh api` (CP-02's method; the web pages are robots-blocked):

- **`stable` HEAD: `f0e8343a` — our exact pin, unmoved since 2026-06-26.**
  Zero commits since the pin. The repo's `pushed_at` (2026-08-12, today)
  is `billxbf` pushing the `rubric-prm` feature branch; the activity log
  shows nothing landing on `stable` since PR #45.
- **The `polar` dev branch: frozen since 2026-06-06**, still 80 ahead —
  and **3 behind: it lacks #37, #43, and #45**, i.e. every 2026 `stable`
  squash-merge, including #43's logprob-extraction hardening (the fix
  CP-02 credited with closing two of the fork's three logprob holes).
  The `prefix_merging` refactor is still unlanded and has not grown:
  `prefix_merging.py` +49/−156, new `gateway/engine.py` +148, plus
  `record_utils.py`/`models.py` touches and a large transform-test wave.
- **Tags and releases: still zero.**
- **Maintainer forks**: `skzhang1` pushed 2026-08-11 —
  `harnessgen-app-exec`, 45 ahead: SWE-bench-verified examples, Apptainer
  submit scripts, a "nanobot" harness (A-11-relevant estate evidence, not
  patch-relevant). `billxbf`'s staging fork: dormant since 2026-06-27,
  still byte-tracking upstream.
- **Upstream fixes for the three patches' subjects: none, anywhere.**
  The refactored builder has zero `abort` handling (P2's subject), no
  completion filter (P1's; `record_filters.py` is still fork-born code
  that exists nowhere upstream), and no policy-version producer (P3's;
  `gateway/storage.py` untouched by the refactor). One genuine signal:
  the refactor's `_top_level_scheduler_metadata` promotes
  `{group_id, policy_version, rollout_step}` to trajectory-level
  metadata — upstream grew a **consumer** for exactly the stamp P3
  persists. No patch retires; P3's future brightened slightly.

**The target decision: re-vendor to the SAME SHA `f0e8343a`.** `stable`
is the pin's defined source (ADR-0004, REVENDOR.md step 1) and it has not
moved, so the same SHA is the only target consistent with the recorded
posture. The `polar` branch was considered and rejected as a pin: it is
an unlanded dev line, frozen two months, and 3 commits behind `stable` —
pinning it would *regress* the logprob hardening to gain a refactor that
has not landed. Per the prompt's third option, this is said plainly: the
rehearsal exercises the whole recipe end to end and proves the mechanism
without the diff — and the refactor risk was measured anyway, in a
scratch simulation (below), which is the honest half of the `polar`
option at none of its cost.

#### The execution (Step 2) — per patch: clean, fuzz, or re-anchored

Recipe followed as written: `git init` + `git fetch --depth 1` of the SHA
(SHA-verified: `git rev-parse HEAD` == the pin), `rm -rf vendor/polar`,
`git archive | tar -x`, the one-file exclusion, `bash
vendor/apply_patches.sh`. The pristine extraction differed from HEAD in
exactly the 10-path patch footprint — nothing else in the committed tree
had rotted.

- **P1 — non-agent completion filter: applied CLEAN** (exact context,
  zero rejects, zero fuzz — `git apply` is exact-or-reject).
- **P2 — abort → session ERROR: applied CLEAN.**
- **P3 — policy-version storage half: applied CLEAN.**

Byte-fidelity, held to CP-03's adversarial standard and beyond it, both
directions: forward, the re-vendored+patched tree is **byte-identical to
the committed tree** — `git status --porcelain -- vendor/` empty,
`git diff HEAD -- vendor/` empty, `prefix_merging.py` mode 100644 kept
(the fork's 100755 flip never arrived); reverse, `git apply -R` P3→P2→P1
in a scratch copy walks the tree back to the pristine pin exactly (the
only `diff -r` delta vs the pristine archive is the deliberately excluded
8.3 MB training JSONL). All 10 `--verify` symbol checks OK.

#### The verification (Step 3)

- Vendored venv rebuilt per the recipe (`uv venv --python 3.12` — 3 s,
  warm uv cache); suite: **175 passed / 3 failed — the same three
  pre-existing upstream failures as CP-03** (`test_proxy.py` sglang-router
  workers, `test_api.py` dashboard templates route,
  `test_engine_trajectory_equivalence.py` meta_info `KeyError:
  'token_id'`). Nothing new.
- The four mandatory carried-patch tests: **25 passed**.
- Root **129 passed**, corpus **43 passed + 1 skipped** (the ADR-0003
  taskbank deferral, pre-existing — 44 collected), mcp-service **89
  passed** — all unchanged.
- `pins/derive_pins.py`: **all approved values reproduced** (g6 tail ids
  skip estate-side by design, ADR-0011). The re-vendor did not move the
  wire.
- **The registry seam — the CP's one live finding**: rebuilt per the
  recipe *verbatim*, the venv does NOT host `gsj_rollout`, and the seam
  fails measured (`ModuleNotFoundError: No module named 'gsj_rollout'`) —
  Polar's `import_path` could not have loaded our harness or builder.
  With the missing A-14 install applied (`uv pip install -e ../..`):
  `ValidatingPrefixMergingBuilder` resolves, MRO
  `→ PrefixMergingBuilder → BaseTrajectoryBuilder` — subclasses exactly
  what it thinks it does. The recipe is corrected; the seam check is now
  a recipe tripwire.

#### The refactor, priced (Step 4's second question)

Scratch simulation of the named risk landing: pristine pin tree + the
`polar`-branch versions of the three files the refactor shares with the
patch footprint (`prefix_merging.py`, and P1's two fixture test files).
Result, sequential dry-run then real application:

- **P1 source hunks (per_request.py, prefix_merging.py wiring, the new
  record_filters.py): apply CLEAN against the refactored builder.** The
  stats-dict anchor survived (`chains_total`/`reconstruction_stats`
  intact; `raw_completions_total` lands).
- **P2: applies CLEAN on top of P1.** The refactored `build()` still has
  a single `status="COMPLETED"` finalize site; `session_had_abort` lands
  immediately at it, flipping `status`/`error` exactly as the header
  claims.
- **P3: applies CLEAN** (`gateway/storage.py` untouched by the refactor).
- **The only rejects: P1's two fixture-marker hunks** in
  `test_engine_trajectory_equivalence.py` and
  `test_per_request_builder.py`, both rewritten by the refactor —
  mechanical re-anchoring of `"system": "harness"` markers, not a
  re-port.

Caveats stated with the number: the simulation is a point-in-time reading
of a branch frozen since June that is 3 behind `stable` — what
squash-lands may differ, and a textual apply is not a behavioral proof
(the vendored suite at that future pin is the real gate; the refactor's
supporting files weren't executable in the scratch tree). What CP-02
feared as "expect P2 and P1's wiring to reject wholesale" measures today
as a fixture edit.

#### The cost, measured (Step 4)

- **The mechanical loop — fetch (3 s), extract (~20 s), patches (8 s),
  `--verify` + byte-fidelity tripwire (~10 s), reverse-apply proof
  (~20 s), venv rebuild (3 s, warm uv cache), vendored suite (~21 s):
  about 2 minutes.**
- Full verification (four suites in parallel — mcp-service's 46 s the
  longest — plus pins and the seam check including diagnosing its
  failure): ~3 minutes more.
- Survey + target decision: ~8 minutes. Refactor diagnostic: ~5 minutes.
- Documentation (recipe corrections, charter, verdict, this report):
  the majority of the CP — **total wall-clock ≈ 1.5 hours against the
  half-day budget.**
- **Next re-vendor, honest estimate**: a moved pin with surviving
  anchors: ~1–2 hours including verification and docs. The refactor
  landing: + P1 fixture re-anchoring and mandatory behavioral
  verification — half a day stays a safe *ceiling*; the measured
  evidence says well under. Cold uv cache adds dependency-download
  minutes to the venv step. The estimate's real content is now
  contingency, not mechanism — the mechanism is minutes.

#### REVENDOR.md — every correction execution found (Step 5)

1. **The venv rebuild omitted the A-14 `gsj_rollout` install** — the one
   defect with teeth: recipe-verbatim, the registry seam breaks and
   `polar serve_rollout` against our config would fail at import. Fixed:
   `uv pip install -p .venv/bin/python -e ../..` + the seam check as a
   verify tripwire.
2. **The byte-fidelity tripwire was practiced but unwritten** —
   `git status`/`git diff` against the previously committed tree
   immediately after patching (empty on a same-SHA run; exactly the
   upstream delta on a moved pin). Now step 4's closing check.
3. **Step 1 assumed the pin always moves** — the same-SHA rehearsal case
   (valid, occasionally worthwhile) now documented: no new ADR, record
   the decision in the CP report; survey checklist named.
4. **Step 6 (smoke) assumed the tree changed** — now conditional: a
   byte-identical re-vendor cannot change artifact shapes; skip and say
   so.
5. **The known-risks section carried an estimate where a measurement now
   exists** — the refactor risk rewritten with the CP-22 diagnostic
   (anchors that survived, the fixture-only rejects, the
   never-pin-`polar` warning with its 3-behind reason), plus the new
   `policy_version`-consumer signal as the first thing the next survey
   checks. Budget header rewritten with measured numbers; an executions
   log added.

#### A-8 (Step 6)

**RESOLVED: vendoring the release-less branch is sustainable — HOLDS**,
on second-vendor evidence: the recipe reproduces the committed tree
byte-for-byte at a different sitting, the mechanical cost is minutes, the
patch set re-applies clean by script, and the named worst-case upstream
change prices at fixture-level rework today. Residuals, named in the row:
no MOVED-pin re-vendor has run (that cost stays an estimate, now
well-bounded); the refactor diagnostic is a point-in-time measurement of
an unlanded branch; one recipe defect (the A-14 install) was found and
fixed by this rehearsal. `docs/VERDICT.md` gains the dated M7a section:
**the ADOPT verdict's last reversal door is NARROWED, not closed** — the
mechanism reproduces and the riskiest known change costs fixtures, not
patches; what keeps it ajar is only that the refactor has not actually
landed.

#### §9 abandon conditions

**None met.** The re-vendor changed nothing (byte-identical tree, pins
reproduced, suites unchanged), no patch posture broke — the opposite was
measured — and A-8's resolution *removes* the last untested assumption
bearing on the verdict rather than straining any abandon condition.
