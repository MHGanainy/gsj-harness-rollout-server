# CP-41 — M13c: namespaces, amendments, residue

### CP-41 REPORT
status: done
scope_drift: none in the frozen paths — `vendor/`, `spike/`,
`staging/`, `tests/`, `.github/`, `mcp-service/`, and every
`gsj_rollout/` module except `checks.py` all diff-empty; `checks.py`'s
one change is line 97 rewritten in place (comment only, count held at
528); `corpus/ingest_corpus.py`'s one change is line 65 (comment only,
line count unchanged, so the two external references into its line
numbers stay valid); no consumer-repo edits. The lifts are the
prompt's own. Judgment riders, each under `questions:`: three extra
bare wishlist cites qualified in the same [CP-33] paragraph C3 names
(rows 22, 27, 14 — same class, same paragraph), and the C4 numbering
note written into BOTH rows numbered 19 rather than one.
files: 19 tracked files modified (212 insertions / 28 deletions over
them, pre-report; the two new docs below add their own lines to the
commit's stat) —
forgejo/{docker-compose.yml,up.sh,create_owner.sh} (the five W2
citations qualified), docs/reports/CP-01.md ([CP-41 erratum]),
docs/corpus-contract.md + corpus/ingest_corpus.py (C2),
gsj_rollout/checks.py (line 97 in place, W9),
docs/golden/{mac,h200}/MANIFEST.md (M5),
docs/decisions/ADR-{0005,0008,0011,0019,0020}-*.md (dated amendments),
docs/CHARTER.md (§7 [CP-41] block; A-9/A-13/A-14/A-29 annotations;
gap rows 16/22 S13 notes; the [CP-33] paragraph's C3 qualifications),
docs/VERDICT.md (S11 landed-note; both rows 19 — C4 + 19(a) TAKEN),
docs/checks-spec.md (the thinking-on inventory rows),
docs/polar/README.md (all nine directories indexed),
pins/pins.gsj.json (the two S8 notes), docs/prompts/CP-41.md +
docs/reports/CP-41.md (new). Untracked residue:
dist/gsj_harness_rollout_server-0.1.1-py3-none-any.whl DELETED; what
remains in dist/ is the single 0.1.2 wheel CP-40's proof run built,
matching the PyPI release.
tests: ./.venv/bin/python -m pytest -q → 161 passed (the checks.py
comment rewrite included; the ADR-0021 equality tripwire green at 528)
adrs: none new — dated amendments appended to ADR-0005 (C8), ADR-0008
(S13), ADR-0011 (S12), ADR-0019 (C7), ADR-0020 (S12), in the ADR-0003
shape; next free number stays ADR-0026        assumptions: none new —
A-9, A-13, A-29 annotated (S14), A-14 annotated (C6); no status moves
gap_register: §7 [CP-41] block appended; NO row moves — tally
unchanged (21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD); gap
rows 16 and 22 annotated in place (S13). Wishlist: 19(a) FIXED
(the ask taken, derive proof in-row), both rows 19 carry the C4
numbering note, the M5/M6 bullet marked LANDED (S11). No external
register changes (consumer repos untouched).
questions:
  - Corrupted span, Step 0 ("Prompt at `docs/prompts/CP-4 with the
    CP") — reconstructed: "Prompt at `docs/prompts/CP-41.md`, saved
    verbatim, committed with the CP." Applied; the prompt file carries
    the corrupted spans verbatim.
  - Corrupted span, W2 ("`create_owner.sh:3` andis repo CP-29 is the
    PyPI release") — reconstructed from audit W2: "`create_owner.sh:3`
    and `:8`. In this repo CP-29 is the PyPI release, …". Applied to
    all five sites.
  - Corrupted span, Step 2's rule ("a predecessor cis qualified as
    the predecessor's") — reconstructed: "a predecessor CITATION is
    qualified as the predecessor's." Applied throughout.
  - Corrupted span, C6 ("carries none and stil pre-CP-06 prediction")
    — reconstructed per audit C6: "carries none and still reads as a
    pre-CP-06 prediction." Applied.
  - Corrupted span, Step 4 ("CP-40 made declining a visibion") —
    reconstructed: "a visible decision." Moot in the event — the ask
    was taken, not declined.
  - Corrupted spans, DoD/Report ("vendo/", "th `forgejo/`
    citations") — `vendor/`, "the five `forgejo/` citations". Applied.
  - Step 3 says "dated, in the ADR-0003 precedent's shape" — ADR-0003's
    shape is CP-tagged but undated (`**[CP-24] …**`; the dated shape is
    ADR-0007's). Default applied: ADR-0003's light shape with the date
    inside the bracket — `**[CP-41 — 2026-08-24] …**`.
  - C2's freeze-lift is "header comment only", and the audit names
    line 65 — but the file carries five MORE un-namespaced predecessor
    ADR-0047 cites (lines 2, 815, 877, 1052, 1478) the audit did not
    flag. Default: fixed exactly line 65; the five are recorded in the
    §7 block for the next conscious `corpus/` lift (they ship in the
    wheel, the same exposure class as S8).
  - S13 names gap rows 16/22; `<session_id>` also appears in gap
    row 6 — inspected: row 6's is the artifact DIRECTORY
    (`<artifacts_dir>/<session_id>/`), untouched by F-48's filename
    stamp. Left as is, correct.
  - S13's ADR-0008 half: ADRs are append-only, so lines 83/85 were not
    edited — a dated amendment records the F-48 `<pins-mode>` stamp
    instead, and the two gap rows carry matching in-place notes.
  - C3 names bare "Row 24/26/28/32"; the same paragraph carries three
    more bare wishlist cites (rows 22, 27, and "the repo's record,
    row 14"). Qualified all seven for consistency — the edit to a past
    §7 block is itself recorded in the [CP-41] block, not absorbed.
  - The prompt's W9 is `checks.py:97` only; audit W9 bundles a second
    item — the demo README's "25–50 s" band contradicting its own
    22.5 s CP-36 measurement. Consumer-repo edits are forbidden here,
    so W9's demo half joins the remaining-open ledger below, owner the
    demo's next sitting; the closure claim is scoped to the library
    half.
  - M5's mac MANIFEST cites `staging/pins/g6_tail.captured.txt` — a
    predecessor path whose FILE does exist in this repo at
    `pins/g6_tail.captured.txt`. Default: qualified as the
    predecessor's AND pointed at this repo's copy, since the address
    book being wrong was the whole finding.
next: the audit is closed — no successor audit CP is owed. What
remains lives where it is recorded: the production-prep set (gap rows
2/22, wishlist 9/24, the F-44 grader), the frozen-file records
awaiting their lifts (ci.yml's `>=3.11` comment — next `.github`
lift; mcp-service/README.md's disclaimer — next `mcp-service/` lift;
ingest_corpus.py's five ADR-0047 cites — next `corpus/` lift), and
the open audit ids enumerated with owners under notes below. Advisory
for whoever opens the next CP: the §8 rule 9 register pass now has
one more parked set to scan (this report's open-ids table).

## Notes

**The `checks.py` outcome (Step 1) — the correction fit; nothing was
raised.** Line 97 was 74 characters citing "ADR-0007(e)" unqualified;
the qualified line — `# Decisions and built-in reads are cutoff-exempt
(spec §G5, the predecessor's ADR-0007(e)).` — is 92 characters on the
SAME single line (the file's longest line is 109; there is no
formatter limit to collide with). One line replaced one line:
`wc -l` before 528, after 528, the ADR-0021 equality tripwire
(`tests/test_checks.py:785`) green in the suite run, no new line, no
allowance question, no §1 budget question. The wording matches the
spec's own qualified form at `checks-spec.md:1125` ("the predecessor's
ADR-0007(e)"), and line 97 was verified to be the ONLY unqualified
predecessor cite in the module (line 59's is qualified: "the
predecessor's constant").

**W2 — the five, and the CP-01 erratum.** The five sites now read the
way CP-01's own adjacent lines always did: `docker-compose.yml:1`
"(the predecessor's CP-29, M7a)", `:9` "(the predecessor's CP-29,
measured)", `up.sh:2` "the predecessor's CP-02 up.sh recipe",
`create_owner.sh:3` "(the predecessor's CP-33,
ADR-0046(e)/ADR-0047)", `:8` "created empty at the predecessor's
CP-33". The DoD grep now returns only qualified hits. The erratum:
`docs/reports/CP-01.md` claimed "predecessor citations qualified" for
`forgejo/` — the audit's diff against `gsj-envloader` @ v0.8.0 proved
five left byte-identical, so the claim was false when written, not
merely stale; a dated **[CP-41 erratum]** paragraph now sits at the
end of that report naming all five sites, why they mis-resolve
(this repo's CP-29 = the PyPI release, CP-02 = the Polar-pin audit,
CP-33 = the wishlist cluster, M7a = the re-vendor door, ADR-0046/0047
nonexistent), and the correction venue. Same class, same pass: the
corpus contract's header and `ingest_corpus.py:65` (C2), and the
three golden-MANIFEST `staging/collector|pins` cites (M5 — provenance
correct, address book fixed).

**The five ADR amendments (Step 3), each appended, dated, decision
untouched:**
- **ADR-0005 / C8** — decision 5's "root `requires-python` stays
  `>=3.11`" superseded at CP-19; ADR-0020 §4 named as the superseding
  decision (the cross-reference ADR-0020 never made now exists in
  both files); ci.yml's stale echo named with its owner.
- **ADR-0011 / S12** — the "tokenizer at PIN time" premise moved at
  CP-37: the same ids derived from the live endpoint alone, four HTTP
  calls, no tokenizer, venue deliberately off the pins walk;
  measure-at-serve and the landed G6 rule stand.
- **ADR-0019 / C7** — the Consequence's 9+2 named only two of three
  earmark spenders; ADR-0021's measured 12+9+2 (CP-14's TR3 tripwire
  first) now marked in the file VERDICT row 18 was flagging from
  outside.
- **ADR-0020 / S12** — the Consequence's standing risk closed at
  CP-29: published, name claimed, row 17 DONE, 0.1.1/0.1.2 since.
- **ADR-0008 / S13** — the archive filename's CP-33 mode stamp
  (`<session_id>.<pins-mode>.json`, F-48, `receiver.py:156`);
  gap rows 16/22 annotated in place to match. Row 6's
  `<session_id>` is the artifact directory — checked, not stale.
- **C6, beside them** — A-14 now carries what CP-22's report said it
  gained that day: the live seam re-verification (recipe-verbatim
  rebuild → `ModuleNotFoundError`; install applied → correct MRO; the
  REVENDOR.md recipe step + tripwire), recorded as
  "[CP-41, recording CP-22]" so the annotation's own provenance is
  honest.

**The pins-notes decision (Step 4) — taken, with the proof.** The ask
recorded at CP-40 (wishlist 19(a) / audit S8) was accepted.
Verification ran BEFORE the edit: `./.venv/bin/python
pins/derive_pins.py` → "all approved values reproduced", exit 0. The
two notes were then rewritten in place — line 86's "skill-row
RESOLUTION stays taskbank-deferred (ADR-0003)" now states resolution
happens at bank build since CP-24 (ADR-0022), values untouched; line
102's G6-as-future clause ("CP-11c or later") now states the rule
LANDED at CP-23 (`check_thinking_tail`, ADR-0021, row 14 PARITY),
exactly as designed — and the walk ran again AFTER: "all approved
values reproduced", exit 0. No value moved; the JSON is 111 lines
before and after; nothing mechanical reads the `provenance` notes
(checks.py and derive_pins.py read `["pins"]` only, receiver.py reads
top-level `mode` only; no test hashes the file). A note edit that
stayed a note edit. The fix reaches pip consumers at the next release
— pins bake into wheels at build time (ADR-0019).

**The residue.** `dist/gsj_harness_rollout_server-0.1.1-py3-none-any.whl`
deleted. What remains: exactly one wheel,
`gsj_harness_rollout_server-0.1.2-py3-none-any.whl` — CP-40's proof
build, matching the current PyPI release — so `install.sh`'s
lexical-first sibling-wheel preference now selects the artifact that
equals what PyPI serves, and the run-one-release-behind trap is
defused on this machine.

**Recorded rather than fixed, and why (each with its owner):**
- **S14's ci.yml half** — `ci.yml:23`'s header comment still quotes
  `requires-python ">=3.11"`; `.github/` is frozen this CP. Owner:
  the next `.github` lift (also named inside ADR-0005's amendment).
- **C1** — `mcp-service/README.md:6-8`'s blanket "CP/ADR numbers in
  this file are the predecessor's" is false since CP-15 put this
  repo's own numbers in the body (CP-15, ADR-0016, A-25, CP-10,
  CP-14, unmarked); the file is frozen. Owner: the next conscious
  `mcp-service/` lift — the fix is scoping the disclaimer, not
  renumbering the body.
- **The CP-41 discovery** — `ingest_corpus.py`'s five remaining
  un-namespaced predecessor ADR-0047 cites (lines 2, 815, 877, 1052,
  1478), outside C2's "header comment only" lift. Owner: the next
  conscious `corpus/` lift; exposure named (the file ships in the
  wheel).

**Which audit ids remain open after CP-39, CP-40, and CP-41, and who
owns each.** CP-39 closed W1, S1, S9, O1, O2, M1. CP-40 closed W4,
W5, S2, S3, S5, S6, C5, S4's and S14's examples halves, and decided
every orphan O3–O9 (closures, re-parks on real events, and the
production-prep transfers). CP-41 closed W2, W9's library half, C2
(its named sites), C6, C7, C8, M5, S8, S11, S12, S13, C3, C4, and
S14's library half except ci.yml (recorded), with C1 recorded.
Remaining open, fourteen ids:
- **W3** (A-28 claims the images multi-arch; `gsj-mcp-service` and
  `gsj-pi-harness` publish amd64-only) — the durable fix is wishlist
  row 40 (OPEN, the next image republish); the A-28 row annotation
  belongs to the CP that closes row 40.
- **W6** (MODEL-SURFACE's G4-hashes-emptied claim false on the
  derivation-failure path) — demo repo; owner: the demo's next
  sitting (doc-side fix named by the audit).
- **W7** (demo README describes the corpus source tree with the
  generated `md/` page name) — demo's next sitting.
- **W8** (VERDICT row 41's "slash-free path exercised" note — the
  exercised name contained a slash) — the next VERDICT register pass;
  library-side, one cell.
- **W9's demo half** (demo `README.md:218`'s "25–50 s" per-episode
  band beside its own cited 22.5 s CP-36 measurement) — the demo's
  next sitting; the library half (`checks.py:97`) closed this CP.
- **S4's remainder** (charter A-2 still names hermes as *the* engine
  requirement, unannotated — the next charter register pass; the demo
  `bootstrap.py:566-570` docstring, `read.py:767-770`,
  `preflight.py:272-278`, and MODEL-SURFACE's opener — the demo's
  next sitting, code-adjacent texts needing a demo lift).
- **S7** (library README trio: the 136-test badge paragraph, the gate
  roster omitting G6, the pre-CP-34 "nothing else" wheel claim) —
  root `README.md` was outside this CP's lift; owner: the next
  `README.md` lift.
- **S10** (wishlist row 14's arm64-only framing vs row 26's
  hosted-x86_64 evidence) — rides wishlist row 26's live trigger (the
  next CI firing buys the magnitude instrumentation, and the
  tolerance decision re-frames row 14 with data).
- **C9** (the demo's `config.yaml.example:40-41` "silently stands" vs
  its three louder texts — `bootstrap.py`'s printed cure, the README,
  MODEL-SURFACE) — the demo's next sitting.
- **C10** (MODEL-SURFACE's "four rows below" off-by-one since CP-38's
  tool-parser row) — demo's next sitting.
- **C11** (the demo's generated pins keep H200 `host`/`walk_status`/
  provenance while `derive_pins()` rewrites values —
  `bootstrap.py:657-770`) — demo's next sitting; small code change,
  production-prep adjacent.
- **M2** (MODEL-SURFACE cites `healthcheck.sh`, which exists only in
  this repo's `staging/serving/`) — demo's next sitting.
- **M3** (A-17..A-20 never assigned; only CP-12's report says so, and
  CP-06's dangling "(A-20 nuance)" is a false hit) — the next charter
  register pass: one §4 register note.
- **M4** (`git.date` parseability promised at validate, checked only
  at scaffold by git itself) — a behaviour change, forbidden here;
  owner: the next conscious `corpus/` lift, beside the ADR-0047
  cites.
The pattern in what remains: seven consumer-repo texts (W6, W7, W9's
demo half, C9, C10, C11, M2) plus S4's mixed remainder (four demo
texts and the library's A-2 row) — all frozen to this CP by the
prompt's no-consumer-edits wall; three library register cells (W3's
A-28 annotation, W8's row-41 note, M3's ghost-ids note); one README
lift (S7); one framing that rides a live trigger (S10, with wishlist
row 26); and one small validation gap (M4, a behaviour change no
audit CP may make). Nothing load-bearing, every one with a named
owner, none parked on an event that has already fired (§8 rule 9
scanned).

**Push outcome (§8 rules 8 and 9).** One repo touched — this library,
one commit (`CP-41: namespaces, amendments, residue`), pushed to
origin/main in the same sitting; the evidence — `git status -sb` with
no ahead count and `git log --oneline origin/main -1` resolving to
this CP's commit — is in the session's DoD transcript printed with
this report. The check is self-falsifying from the repository alone:
if origin/main does not carry this commit, this outcome statement is
false and the CP is not done. No consumer-repo commits this CP (none
permitted).
