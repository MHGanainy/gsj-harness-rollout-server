### CP-01 REPORT
status: done
scope_drift: none — no Polar, no vendoring, `gsj_rollout/` untouched (verified: the seven CP-00 modules are byte-identical), no estate bring-up, no Docker invoked, no writes to the predecessor (its `git status --porcelain` is empty)
files: 216 files changed, ~9,060 insertions, 14 deletions — new: `corpus/` (pipeline + 4 test modules + conftest + `corpus/staging` incl. lock + parquet), `mcp-service/` (25 files), `forgejo/` (4), `pins/` (6), `docs/corpus-contract.md`, `docs/checks-spec.md`, ADR-0002, ADR-0003, `prompts/CP-01.md`, this report, 2 component `pytest.ini`; modified: `docs/CHARTER.md` (§3 measured, §7 rows), `README.md` (status → CP-01), `.gitignore` (forgejo data/tokens), `pyproject.toml` (pytest testpaths)
tests: `pytest -q` → 1 passed · `cd corpus && python -m pytest -q` → 33 passed, 1 skipped (the 5-test taskbank module, ADR-0003) · `cd mcp-service && python -m pytest -q` (component venv) → 73 passed · `python corpus/ingest_corpus.py validate --corpus corpus/staging` → PASS (12 pass / 0 fail) · DoD grep → empty · predecessor untouched
adrs: ADR-0002 (ownership boundary), ADR-0003 (taskbank deferral)        assumptions: none added; A-9 exercised — the carried components did transfer, with the four caveats under notes
gap_register: row 1 → PARITY (contract byte-identical); row 2 notes updated (clamp + branch tooling here, TBD until CP-10); row 3 → PARITY (tooling; no bring-up ran); row 4 notes updated (builder deferred, ADR-0003); row 23 → GAP, deliberate (values not carried — stale by construction; cure = derive→re-pin walk, CP-07/CP-10/CP-11); §3 carried-over table now measured, not estimated
questions:
  - Corrupted span: Step 1 "the pcaptured G2 texts" → reconstructed "the captured G2 texts".
  - Corrupted span: Step 2's second bullet opens "** `corpus/ingest_corpus.py` imports…" (broken bold label) → reconstructed as "**Imports**: …".
  - Corrupted span: Step 3 "with no s to the predecessor" → "with no references to the predecessor" (matches the step's own grep bullet).
  - Corrupted span: Step 4 "PARITY-with-caveat if thank phase is deferred" → "…if the taskbank phase is deferred".
  - Corrupted span: Step 4's G7 bullet "Polar must surface an equivalent or closed" → "…an equivalent or the gate fails closed" (matches CP-00's note it restates).
  - Corrupted span: DoD "docs/decisions/ADR-0rship-boundary.md" → "docs/decisions/ADR-0002-ownership-boundary.md".
  - The charter layout names no home for the pins tooling → default applied: top-level `pins/`.
  - The DoD's `cd mcp-service && python -m pytest -q` cannot run in the root venv (the service stack — torch, mcp SDK — is deliberately not a root dependency) → default applied: run with the component's own recreated `.venv` active, per its README recipe; the suite itself hardcodes `mcp-service/.venv/bin/python` for subprocess servers, so the venv must exist regardless.
  - `cd <component> && python -m pytest -q` would ascend to the root pyproject and collect the wrong suite → default applied: 2-line `pytest.ini` per component + root `[tool.pytest.ini_options] testpaths=["tests"]` (the predecessor's bare-pytest convention).
  - Step 4 expected the retrieval-cutoff row to "move toward PARITY" → default applied: row 2 stays TBD with the component-level parity recorded in notes — the capability is end-to-end enforcement, which only CP-10 can prove; claiming PARITY now would be the silent-disappearance failure the register exists to prevent.
next: CP-02 — the fork audit (A-7): verify or refute the four reported defects (whitelist leak, `-9999.0` sentinel, abort→ERROR, reasoning mask); the sentinel guard's spec is already in `docs/checks-spec.md`.

#### notes

**The boundary as applied (ADR-0002).** Copy mechanism: `git archive v0.8.0 | tar -x` — read-only against the frozen predecessor, and untracked artifacts cannot travel. Came: `corpus/` (1,292-line pipeline pre-edit, 625 test lines, 163-file staging tree incl. the two committed freeze records `corpus.lock.json` + `taskbank.parquet` — the stated exception to the generated-artifacts rule: contract-mandated committed, deterministic, and not reproducible here), `mcp-service/` (25 committed files: 1,390 package + 1,594 test lines pre-edit), `staging/forgejo/` → `forgejo/` (182 lines pre-edit), six pins files → `pins/` (`derive_g2.py` + the captured G2/G3/G6/G7 evidence + the container-mode G2 singleton), `docs/corpus-contract.md` byte-identical. Did not come: `mcp-service/.venv` (1.1 GB — the entire raw-vs-own gap, confirmed artifacts: raw 1.1 GB/11.45M lines vs 3,463 committed), `data/` (live-estate clone cache + index), `tests/.corpus-bares/`, `__pycache__`, `forgejo-data/` + token files, the pinned VALUES (`pins.dev.json`, `pins.staging.json` — stale by construction under Polar's mounts), the four `h200/` derived texts (regenerable substitution products), the `gsj-pin` probe configs. Sizes after: corpus 760K · mcp-service 216K · forgejo 16K · pins 44K committed content (the DoD's `du` shows mcp-service at 1.0G because the venv was *recreated* locally per its README — environments are rebuilt, never moved).

**The taskbank decision (ADR-0003, option c).** Polar takes `TaskRequest`s, not §3.1 parquet rows; (a) would make the frozen predecessor a live dependency and (b) would inline ~200 lines to build an artifact with no consumer here. Applied: `phase_taskbank` raises the deferral (`exit 2` naming CP-07/ADR-0003), `all` prints `== taskbank == DEFERRED to CP-07` and continues, `verify` keeps the stdlib half of the bank check (file-vs-lock sha256 — the carried frozen parquet stays byte-verifiable; sha match confirmed against the lock's `9eb8e3c2…`) and reports the row-level half as deferred; a lock with no bank entry is a loud SKIP, not a FAIL, so a fresh scaffold verifies green. `build_corpus_table`/`expected_triples` deleted (recoverable from the predecessor @ v0.8.0).

**Paths and imports reconciled.** `pins/derive_g2.py`: repo-root anchor `parents[1]` → `parent`, usage strings `staging/pins/` → `pins/`, stale `build.py` help text replaced; its case manifest (`corpus/staging/corpus.lock.json`) now resolves in-repo. `forgejo/`: bind mount `../forgejo-data` → `./forgejo-data` (re-rooted inside the component; also `down.sh`'s wipe paths), token-path messages `staging/forgejo/` → `forgejo/`, predecessor citations qualified. `mcp-service/`: the ONE predecessor-library import (`tests/helpers.py: gsj.envloader.gates.extract_case_search_pages`) inlined byte-faithfully (regexes verbatim — they are the compatibility contract; audited identical to `gates.py:381–396`); `requirements.txt`'s pyarrow test-dep note dropped with it; README/config/compose/Dockerfile references to `staging/README.md`, `staging/dataset.frozen.json`, `sandbox/Dockerfile` qualified as the predecessor's or retargeted to `corpus/staging/corpus.lock.json`. `corpus/ingest_corpus.py`: the three lazy library imports removed per ADR-0003; docstring updated. One comment line in `corpus/staging/corpus.yaml` qualified ("the predecessor's staging/dataset.frozen.json") — a YAML comment, no hash input; everything else under `corpus/staging` is byte-identical (lock and parquet md5-verified against the tag).

**Tests moved vs. passing vs. skipped.** corpus: 37 moved (7 scaffold + 17 validate + 5 taskbank + 8 verify) → 38 after adaptation; 33 pass, 5 skip (the taskbank module, one module-level skip marker, reason stated in-file). Within verify: `test_doctored_parquet_fails_sha_and_rowset` rewritten library-free against the surviving sha check, `test_missing_taskbank_phase_fails` rewritten to assert the deferral-is-not-an-error behavior, `test_all_with_only…` adapted, `test_taskbank_phase_is_deferred` added (the ADR-0003 surface). mcp-service: 53 test functions moved → 73 passing (parameterization), 0 skipped, no test reaches the predecessor: the bares build from the sibling `corpus/staging` through this repo's own pipeline at import time, and the audit confirmed the rebuilt refs byte-equal the lock's — the determinism parity proof survives the move. Root: 1 passing.

**Unanticipated couplings (A-9 caveats).**
1. The `gsj-pin` generator is predecessor *library* code (`gsj/envloader/pin.py`, imports `store.canonical_json`) — the charter's carried-over table implied it moves; it cannot without carrying library modules. Corrected in §3: the derive tooling + captured evidence + `docs/checks-spec.md` carry the capability; re-pin tooling lands with `checks.py` (CP-11).
2. The mcp-service suite hardcodes `mcp-service/.venv/bin/python` for its subprocess-server tests — environment *recreation* is load-bearing for the suite, not just for deployment.
3. The same suite reaches into the sibling `corpus/` tree at helpers-import time (bare-building + fact ground truth) and hardcodes corpus ground-truth constants (`ALL_REPOS`, `CASE_PAGES`) — the side-by-side layout was preserved, so this survives unmodified, but any future corpus change breaks mcp tests by design.
4. The compose project name `gsj-envloader-staging` is the live H200 estate's identity and was deliberately kept (renaming would strand the running instance) — it sits in a `.yml` file, which the DoD grep's `--include='*.yaml'` does not scan; recorded here so the exception is visible, not accidental. The captured G2 texts likewise embed predecessor host paths (`/home/sysadmin/gsj-envloader/…`) by nature — byte-load-bearing hash inputs, exempt from reconciliation (ADR-0002).

**`docs/checks-spec.md` landed** — seven pins + approved-set format, the four hashing conventions (with the `tools.captured.json` → `a7a7956b…` canonical-JSON anchor re-verified against the predecessor during the audit), the byte-stable `G{n}:{slug}` vocabulary (example slug corrected to the predecessor's actual `G5:search_page_gt_timestep` constant), the logprob discipline incl. the `-9999.0` sentinel rejection, G7's chain-snapshot quadruple (field names verified in `gates.py`), the H-41 loud-failure lesson, G3's wire mechanism, G5's backstop regexes, and the carried-evidence inventory.

**Verification.** Beyond the DoD: a four-auditor adversarial pass (byte-identity vs the tag, stale-path sweep, pipeline-edit review with the full phase × flag matrix executed live, doc-consistency against measured reality) found zero blockers; its five factual findings (miscounted files in ADR-0002, the invented G5 slug, ADR-0003's stale skip claim) were fixed before commit.
