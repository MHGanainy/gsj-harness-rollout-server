# CP-43 — the demo, in brief

### CP-43 REPORT
status: done
scope_drift: none — the demo repo's lift is the prompt's own; the
library takes `docs/**` only (this prompt, this report, the §7 [CP-43]
block); no other library path touched and the root suite is
byte-unchanged at 161.
files: library — docs/prompts/CP-43.md, docs/reports/CP-43.md (new);
docs/CHARTER.md (+1 block: §7 [CP-43]). External — the demo repo
**github.com/MHGanainy/gsj-rollout-demo**, one commit, pushed:
`16810d3` (7 files, +357/−88: README.md +255/−100 — the front-door
restructure; bootstrap.py +124 — the W6/C11 code fixes; FINDINGS.md
+25 — the closure note; docs/MODEL-SURFACE.md, config.yaml.example,
preflight.py, read.py — the S4/C9/C10/M2 texts).
tests: `./.venv/bin/python -m pytest -q` → **161 passed**
(byte-unchanged — no library code touched). Demo-side: the three
edited Python files compiled; `derive_pins` executed on all four
derivation paths (reference model; non-reference + endpoint down;
thinking-on non-reference + endpoint down; non-reference + derivation
success, stubbed) with every artifact assertion green; the README's
transcript excerpt machine-diffed against the reader's actual output —
verbatim except the one marked trim.
adrs: none        assumptions: none touched
gap_register: §7 [CP-43] block appended; no row moves; tally unchanged
(21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD). Demo FINDINGS.md
carries the dated closure note; no new row minted, **next fresh id
stays F-70** (the CP-44 sitting's examples README quotes this README's
numbers and the F-span — verified matching: ~2.5 min cold `up`, 6–7 GB,
~20–40 s per episode, F-69 highest).
questions:
  - Corrupted span, Step 2 ("the two platform fa the amd64 pull; …")
    — reconstructed: "the two platform facts (the amd64 pull; a
    non-Qwen endpoint derives automatically but the serve argv is
    theirs to write)". Applied; the saved prompt carries the corruption
    verbatim.
  - Step 6's "FINDINGS updtated" read as "updated". Applied as a dated
    closure note in the demo register — the closed ids are audit ids,
    not run findings, so no F row was minted.
  - The read-first list names a demo `RUNBOOK.md`; the demo repo has
    none (the RUNBOOK is the examples repo's). Default applied: read
    the demo's actual surfaces (README, MODEL-SURFACE, FINDINGS,
    estate/) plus CP-36's report; treated the mention as a slip.
  - Concurrency, recorded for the log: at this sitting's start CP-42's
    prompt sat untracked with no report; mid-sitting a parallel session
    executed and pushed CP-42 (`97a9dde`) and a third parallel session
    ran CP-44 (examples repo pushed independently). Commit order was
    negotiated over session messaging — CP-42 → CP-43 → CP-44, each
    sitting staging only its own charter §7 hunk. The [CP-44] block and
    docs/prompts/CP-44.md remain in this working tree for that sitting,
    deliberately uncommitted here (this commit's CHARTER was rebuilt
    from HEAD + the [CP-43] block only, and the [CP-44] block restored
    to the working tree byte-exactly after the commit).
  - The DoD's 'One commit: `CP-43: the demo, in brief`' applied to the
    library commit (the workflow's `CP-XX:` convention); the demo
    commit follows that repo's descriptive-message convention and names
    library CP-43 in its body — the CP-36 precedent.
  - The audit offered W6 doc-side or code-side; code-side taken, since
    C11 already opened the same function — the doc's claim became true
    instead of qualified.
next: advisory — what the stranger readers flagged that this CP noted
rather than fixed: the CP-nn/F-nn provenance stamps are now linked but
remain names a stranger takes on faith; the endpoint requirements got a
one-place summary in the README but their normative home stays
config.yaml.example; and wishlist row 40 (multi-arch publish) is still
the ARM stranger's tax — the demo auto-cures it, the pull cost remains.

## Notes

**The two-minute question, answered by strangers.** Two fresh-context
readers were given the finished README cold. The desk reader: *"Yes. By
the end of the 'What to expect, measured' section I know what I get …
what it costs … and what could go wrong — each pre-labeled 'normal, not
broken' … I would spend the afternoon,"* crediting the honesty signals
(a failed-task episode as the flagship excerpt, the composed cold-`up`
figure admitted as composed) for making the unverifiable numbers
believable. The excerpt reader confirmed the fenced transcript is the
reader's real output. The front door now leads with the sequence
picture, the measured numbers, and a real trajectory — the three things
the prompt ordered — inside the first three screens.

**The shape of the front door (Steps 1–4).** One paragraph + the
bring/command/get sequence picture; the expectations section moved to
position two with every number sourced (and the framing corrected
mid-CP when a stranger reader caught "every number below comes from
CP-36" over-claiming — CP-32's 72/72 and CP-38's Llama result now say
so); the `case_orchard@t2` excerpt — chosen because it renders the
cutoff-holds line AND the deliverable-NONE header line, the two lines
that carry the demo's argument, and honest about being a floor-model
episode; the corpus tree drawn from the actual `corpus-synthetic/`
tree, closing W7 by construction (`timestep-<T>/pages/`, with `md/`
explicitly named as the generated layout a stranger must not build).

**Step 5 — all eight demo-side audit ids closed, none merely
recorded.** Everything the audit left to "the demo's next sitting"
belonged in files this sitting was already editing. The two that took
more than one round: **C10** — the first fix ("the four [endpoint]
rows below") re-embedded the off-by-one it was curing, because the
context-window row is [endpoint]-classed on vLLM too; the four
template-derived rows are now *named* in the cell. **C11** — three
rounds: the H200 `host`/`walk_status`/G1-G2-provenance rewrite (round
1), then the new host string itself over-claimed on the failure path
(round 2: `derived_at` now records FAILED derivations in the artifact,
not just the scrolling console), then the thinking-on packaged doc's
collective `non_g6_sets` block was caught claiming byte-equality the
generated file no longer has (round 3: rescoped to what is actually
carried). W6 was taken code-side: G4's hashes are emptied for ANY
non-reference model before the endpoint derivation runs, so the
MODEL-SURFACE claim the audit called false is now true on every path —
all four paths executed with artifact assertions, including the
derivation-success path via a stubbed endpoint.

**The discipline.** An 11-agent adversarial workflow over the
uncommitted tree (two stranger readers, one verifier per audit id, one
code review at high effort) plus a follow-up verifier for the reopened
ids; the reviewers confirmed 2 real defects and the verifiers reopened
2 closures, all fixed and re-verified before the demo commit — the
CP-35/CP-36 pre-commit discipline, held. The stranger readers' fixable
frictions (gate names undefined at first use, the endpoint contract
scattered across four sections, the images/containers arithmetic, the
9.9 s-vs-22.5 s timing ambiguity, a silently rewrapped excerpt line)
were all fixed in the same sitting; their un-fixable one (checkpoint
stamps as jargon) is linked and recorded under `next:`.

**Push outcome (§8 rules 8 and 9).** Two repos touched. The demo:
commit `16810d3` pushed to origin/main (`a849336..16810d3`), status
clean — stated above with its stat. The library: one commit
(`CP-43: the demo, in brief`) pushed to origin/main in this same
sitting, on top of CP-42's `97a9dde`; the evidence — `git status -sb`
with no ahead count and `git log --oneline origin/main -1` resolving to
this CP's commit — is in the session's DoD transcript, and the check is
self-falsifying from the repository alone: if origin/main does not
carry this commit, this statement is false and the CP is not done. The
[CP-44] §7 block and prompt stay in the working tree for their own
sitting, by the negotiated order.
