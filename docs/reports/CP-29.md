# CP-29 — M8d: publish

### CP-29 REPORT
status: done — `v0.1.0` is on PyPI and TestPyPI, both repos are public and
current, and **F-14 is retired by demonstration**: a scratch venv outside
every repo ran `pip install gsj-harness-rollout-server` with no index
flags and no local wheel, and `checks.validate_session_result` on the
real CP-09′ body returned `[]`. One qualification, stated here rather
than buried: Step 4's "tag at a CI-green commit" was not satisfiable in
full — CI's first-ever run on CP-20..CP-28 failed in the corpus-suite
job (the CP's headline finding, wishlist 26, two frozen-path causes) —
so the tag sits at `1565813` with the **release-relevant** jobs green
(root 143, wheel + packaged-pins install proof, mcp-service) and the red
job's subject shipping in no artifact. The applied default is under
`questions:`.
scope_drift: none. This repo's diff is `docs/**` + `README.md` only —
`pyproject.toml` untouched (no metadata defect surfaced; `twine check`
PASSED and the README rendered clean under PyPI's own renderer before
anything was pushed). The DoD's frozen-path diff is empty. `.github/**`
was deliberately NOT touched even though wishlist 26 names it: a
deps-only ci.yml fix still leaves the corpus job red on the fixture half
(`corpus/tests/`, frozen), so the coherent ~5-line cure is recorded for
the next CP instead of half-shipped from inside a freeze. The examples
repo's edits (install.sh, RUNBOOK.md, README.md, requirements.txt,
FINDINGS.md) are external, fully lifted by precedent (CP-26/CP-27), and
the prompt's Step 5 asks for them by name.
files: this repo — `docs/prompts/CP-29.md`, `docs/reports/CP-29.md`
(new); `README.md` (the install section: published fact + the examples
repo's URL); `docs/CHARTER.md` (+63: the §7 [CP-29] paragraph);
`docs/VERDICT.md` (row 17 → DONE (CP-29) with the proof; rows 26–27
opened). External repo (one commit, `CP-29 (library): publish`) —
`example_project/install.sh` (PyPI-first, sibling wheel = developer
override), `example_project/RUNBOOK.md` ("What you need in hand" now
carries clone URLs; the install paragraph), `example_project/requirements.txt`
(header comment), `README.md` (the honesty rider updated), `FINDINGS.md`
(F-14 → RETIRED with the proof).
tests: `./.venv/bin/python -m pytest -q` → **143 passed** (unchanged).
Also measured, because it is the finding: the corpus suite is **56/58
locally** (2 failures = the stale fixture vs the CP-27 validator) and
**39/58 in CI** (17 more from the corpus job's stale dependency set) —
neither introduced this CP; both invisible until this CP pushed.
adrs: none — ADR-0020 §5's "stop at the path" decision is *reversed by
the operator's own out-of-band act* (registering the pending publishers)
plus this CP's execution; ADR-0023's local-wheel-first ordering is
updated in place in the examples repo, its stated justification (the
unclaimed name) having expired. Neither needed a new decision record;
both records now describe the past accurately.
assumptions: none added.
gap_register: §7 [CP-29] paragraph (no row moves; tally stands
21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD); wishlist row 17 →
DONE (CP-29); rows 26 (CI red at the tag) and 27 (0.1.0's immutable
stale landing README) opened.
questions:
  - Corrupted span, Step 1 first bullet: "does anything in the local
    history need to *notCheck for secrets…" → reconstructed "…need to
    *not* go public? **The secret sweep**: check for secrets, tokens,
    estate hostnames, and internal paths…" — applied; the sweep ran
    exactly that list, both repos, full history.
  - Corrupted span, Step 2: "does the tree a stranglone contain" →
    "…the tree a stran**ger c**lone**s** contain…" — applied; verified
    via fresh clones of both public repos from outside.
  - Corrupted span, Step 3: "that is tfinding" → "that is **the**
    finding" — applied (and it was: the finding just landed one step
    later, in CI rather than the release workflow).
  - Corrupted span, Step 5 heading: "## Step 5 — Upblication makes
    stale" → "## Step 5 — U**pdate what pu**blication makes stale" —
    applied.
  - Applied default, the tag placement: "a CI-green commit" is
    unsatisfiable for any commit containing CP-20..CP-28 without fixes
    in `corpus/tests/` (frozen) and `.github/**` (unlifted). Tagged
    `1565813` anyway, on three grounds: the release-relevant jobs are
    green on that exact commit; the red job's subject (`corpus/`) ships
    in neither artifact — the release workflow's own header doctrine;
    and the red is fully diagnosed as two stale-guard defects, not a
    library defect (the CP-27 validator at the center of it is *correct*
    and test-proven). The alternatives — fixing frozen paths, or a
    second commit before the tag — each break an explicit rule to
    satisfy an implicit one.
  - Applied default, 0.1.0's embedded README: published with the
    pre-CP-29 README, per the prompt's own ordering (publish at Step 4,
    the doc pass at Step 5, one commit at the end — the tag structurally
    cannot point at a commit containing this report). Recorded as
    wishlist 27 rather than worked around with a two-commit deviation.
  - Applied default, the install.sh version spec:
    `gsj-harness-rollout-server>=0.1.0` (not `==`), so the examples repo
    tracks releases; a consumer needing an exact version has the
    sibling-wheel override, which is the same mechanism developers use.
next: advisory — **the wishlist-26 cure first, wherever the next CP
opens**: one line in `corpus/tests/test_taskbank.py` (drop the `/v1`)
plus the ci.yml corpus-job deps/label/comment refresh turns the badge
green, and the badge is the first thing every stranger now sees on both
landing pages. The next real release (0.1.1+) heals wishlist 27's stale
landing text for free. Standing: wishlist 20 (bank-submit CLI), 22's
residuals (missing-venv hint; F-39's warn-once needs its own ADR against
the 528/528 tripwire), 24 (the secret probe). M8 is done: the consumer
surface exists (CP-25), survived a stranger (CP-26), was lifted
strangerward (CP-27), and is now obtainable by one (CP-29).

---

## Notes

### Preflight, including the secret sweep's result

**The remote's actual state**: `origin/main` was at `26a2bba` (CP-19) —
nine commits stale, three more than F-14's "six" (CP-27 and CP-28 landed
after that measurement). Pushing added CP-20..CP-28: 64 files,
+9615/−279, 42 of 43 new files under `docs/` (the 43rd:
`corpus/staging/taskbank.parquet`, the CP-24 bank). No local tags
existed in either repo before `v0.1.0` was minted.

**The secret sweep** — five parallel finders by pattern class
(credential values, network identity, personal/paths, high-entropy,
examples-repo-deep) over the library's unpushed range and the examples
repo's entire history, every non-benign candidate adversarially
verified, a completeness critic over the whole sweep (tags,
committed-then-deleted files, submodule URLs, binary blobs, the 0600
file's content shape). 49 raw findings → 47 unique → 15 verified + 32
benign-by-inspection; the critic added zero. **Result: no blocker.**

- **No credential value, token, or key anywhere in either history.**
  Assignment-shaped greps, entropy sweeps, and base64 sweeps all clean;
  every 64-hex string is a documented sha256 pin; the `sk-polar-*`
  strings are Polar session ids (scanner bait, not keys).
- **The CP-04′ 0600 scratch file's contents never entered git** —
  `git log -S 'gsj-mcp-secret' --all` finds exactly one commit, and it
  is prose.
- **Two note-level newly-public items, neither push-stopping**: the MCP
  secret's *filename* (`~/.gsj-mcp-secret-cp04prime`, one occurrence,
  the CP-26 report — the same sentence documents that the value moves
  by operator handover), and `/data/gsj/traces` estate paths.
- **Every estate IP the push adds was already public**: `172.28.9.10`
  (Forgejo container, RFC1918), `172.28.9.1` (compose gateway),
  `192.168.0.158` (the Mac workstation LAN fixture) — all on
  `origin/main` since CP-19 or earlier; the new commits only add more
  occurrences of identical values. `h200-admin` (ssh alias) and
  `/home/sysadmin` (the H200 user) likewise already public or
  note-level. The examples repo's history: same classes, no values,
  `GSJ_MCP_TOKEN_SECRET` appears as an env-var *name* and a `<secret>`
  placeholder only.

**The wheel**: `python -m build --wheel` locally → 14 files, all nine
exclusions and all three inclusions PASS — the identical file list
CP-19 asserted, now at 44,778 bytes (checks.py grew at CP-23, cli/config
at CP-27).

**The metadata**: `twine check dist/*` → PASSED. The README rendered
through `readme_renderer[md]` — PyPI's own renderer — with no warnings;
all six markdown links and the badge image are absolute (CP-19's
relative-link finding stays fixed). The stale-once-published lines
("Until the first PyPI upload lands", the examples repo "not yet
published") were identified before the burn; the prompt's ordering
makes them 0.1.0's permanent landing text (wishlist 27).

**The version**: `0.1.0` in `pyproject.toml`, matching the tag the
workflow guard would check. Burned knowingly: the name was verified
still unclaimed on both indexes the hour before (JSON API 404 for it,
200 for the `httpx` control — the CP-19 method; the HTML pages are
behind a bot challenge and not an oracle). Nobody raced us in the ten
checkpoints wishlist 17 stood open.

**The operator's out-of-band steps, verified where possible**: GitHub
environments `pypi` and `testpypi` both exist (0 protection rules);
the pending publishers are invisible from here by design — the OIDC
exchange in Step 3 is their only test, and it passed.

### What the remote gained

`26a2bba` → `1565813`: the verl bridge and loop (CP-20/21), the
re-vendor rehearsal (CP-22), G6 and the budget mechanism (CP-23), the
taskbank (CP-24), the consumer surface (CP-25), the stranger test
(CP-26), the strangerward lift (CP-27), the thinking evaluation
(CP-28). Verified from outside by a fresh clone: the tree a stranger
clones now contains the full `gsj_rollout/` surface (all eight modules
including `builder.py`) and `vendor/polar/` with the three patches —
the library repo is needed twice (wheel build and Polar) and both needs
are now served. **The examples repo** (created this CP:
`github.com/MHGanainy/gsj-harness-rollout-server-examples`, full
history pushed): the stranger gets `example_project/` complete
(RUNBOOK, config, taskbank, train.py, install.sh, requirements),
both bridges, and FINDINGS.md. What each repo gives a stranger, and the
split: the library is the *published package's source* plus the server
role's Polar; the examples repo is *how you run it* — and the library
README now says exactly that with a working URL (it said "not yet
published" until this CP's commit).

### The TestPyPI rehearsal, job by job (run 31770864323, workflow_dispatch)

First-ever execution of `release.yml`. Three things had never run;
all three passed:

1. **build the wheel and prove it** — SUCCESS, 24s. Checkout,
   setup-python 3.12, `pip install build twine`; the tag-vs-version
   guard correctly **skipped** (not a tag ref); wheel built; **the
   exclusion assertions passed in CI** (nine exclusions, three
   inclusions — the second of the never-executed things); `twine check`
   PASSED; the install proof from outside the checkout printed
   `PROOF OK`; artifact uploaded (upload-artifact@v7).
2. **publish to TestPyPI** — SUCCESS, 14s. download-artifact@v8 paired
   correctly with the v7 upload; **the OIDC exchange succeeded** — no
   `invalid-publisher`, so the operator's TestPyPI pending publisher
   (owner/repo/workflow/environment) is exactly right — and **the index
   accepted PEP 639's SPDX expression + `license-files`** (the third
   never-executed thing; `twine check` is not the same validator, but
   they agreed). TestPyPI's JSON API then served `0.1.0`, wheel-only,
   `license_expression: Apache-2.0`.
3. **publish to PyPI** — **SKIPPED, 0s**, exactly as designed: the
   `v*`-ref gate held structurally. A dispatch cannot reach the real
   index; the rehearsal proved the whole path with no route to it.

### The TestPyPI install proof (verbatim shape)

Scratch venv outside every repo (the session scratchpad), Python 3.13.
TestPyPI has no dependency mirror, so:
`pip install --index-url https://test.pypi.org/simple/
--extra-index-url https://pypi.org/simple/ gsj-harness-rollout-server`
— unambiguous about the package's own source because the name did not
yet exist on the real index. Output:

```
PINS_PATH: <venv>/lib/python3.13/site-packages/gsj_rollout/pins/pins.gsj.json
session: sk-polar-44620742-9323-4202-9b58-474b4ed45f26
findings: []
PROOF OK — the trainer leg works from the TestPyPI artifact
```

plus the CP-19 pins signal (the reference-estate `UserWarning`) firing
in exactly its one ambiguous configuration — installed wheel, no
`GSJ_PINS_PATH`, no checkout — and silent in neither of the other two.

### The v0.1.0 run, job by job (run 31771190099, push of tag v0.1.0 at 1565813)

1. **build the wheel and prove it** — SUCCESS, 20s; this time the
   tag-vs-version guard **ran** and passed (`tag v0.1.0 matches version
   0.1.0`); exclusions, twine, install proof all green again.
2. **publish to TestPyPI** — SUCCESS, 18s; the rehearsal's upload
   already held 0.1.0 and `skip-existing: true` absorbed the repeat,
   exactly its stated purpose.
3. **publish to PyPI** — **SUCCESS, 16s.** The real index's pending
   publisher accepted the OIDC exchange; no `skip-existing` here by
   design, and none was needed. `gsj-harness-rollout-server 0.1.0` is
   live: wheel-only (44,778 bytes), `License-Expression: Apache-2.0`,
   `requires_python >=3.12`, deps `httpx, pydantic, pyyaml` +
   `pytest; extra == "dev"`, all four project URLs intact.

### The PyPI install proof — F-14 retired (verbatim shape)

Fresh scratch venv outside every repo, **no index flags, no local
wheel**: `pip install gsj-harness-rollout-server` → 0.1.0. Then:

```
PINS_PATH: <venv>/lib/python3.13/site-packages/gsj_rollout/pins/pins.gsj.json
session: sk-polar-44620742-9323-4202-9b58-474b4ed45f26
findings: []
PROOF OK — the trainer leg works from the PyPI artifact, no flags, no local wheel
```

That is CP-16's proof against the public artifact — the exact
demonstration F-14's retirement required. What a stranger sees on the
project page: correct metadata everywhere (name, version, licence,
floors, URLs, wheel-only), and two warts — the embedded README's
pre-publication install text (wishlist 27, immutable for 0.1.0) and the
CI badge currently red (wishlist 26, heals live when the fix lands).

### The finding: CI red at the tag (wishlist 26)

CI's first contact with five checkpoints of drift, and the guard that
would have caught each layer earlier is the thing that never ran.
The corpus job: 19 failed / 39 passed. Decomposed by measurement, not
guess: **(a)** the job installs `pytest pyyaml` and comments "no import
of the root package" — CP-18 facts; since CP-24 the taskbank phase
needs pyarrow (its lazy-import named error exits 2 — 15 of the
failures) and two tests import `gsj_rollout` (2 more); the job label
"(44)" is also stale (58). **(b)** With full local deps the suite still
fails 2/58: `corpus/tests/test_taskbank.py:37` pins
`serving_base_url: http://serve.invalid:8000/v1` — the exact trap
wishlist 21 documented and CP-27's validator now rejects at load,
naming the key and the cure. The validator is right; the fixture
predates it; the two components meet only in CI, and CI never ran
between CP-19 and this push. Neither failure is in the artifact:
`corpus/` ships in neither wheel nor sdist (asserted twice in CI this
CP). The release-relevant jobs on the tagged commit — root suite,
wheel + packaged-pins install proof, mcp-service — are green.

### The examples-repo decision

**Pushed** — created public and current at CP-27's head, after its
entire history cleared the same sweep (it had never been public, so
everything in it was newly-exposed by definition; nothing rose above
note level). The reasoning: F-14 explicitly named the missing remote as
the blocker's other half; the run book, install script, requirements,
taskbank, and both trainer bridges live only there; the wheel's own
README names the repo. A published wheel whose run book lives nowhere
fetchable is half a cure — the prompt's words, and the sweep left no
reason to accept half.

### What publication made stale, and what changed

- **Library `README.md`**: "Until the first PyPI upload lands…" →
  "Published at CP-29 (`v0.1.0`, wheel-only — ADR-0020)", with the
  from-checkout build retained as the run-ahead-of-a-release path; the
  examples repo reference gained its URL and lost "not yet published".
- **`install.sh` (examples)**: ADR-0023's local-wheel-first ordering
  **reversed** — PyPI by default, the sibling-checkout wheel taking
  precedence when present (the developer path, announced when taken).
  The F-31 fail-fast property is kept by installing the library first,
  before the multi-GB requirements download. The F-15 error path (the
  un-runnable printed cure) disappears entirely: there is no gate to
  fail.
- **`RUNBOOK.md` (examples)**: "What you need in hand" no longer opens
  with two unfetchable trees — it opens with two `git clone` URLs and
  the statement that the *trainer role needs nothing else checked out*;
  the operator-held list shrinks to estate knowledge (F-22's secret,
  F-23's revision, the endpoint values). The install section describes
  the PyPI-first order.
- **`README.md` + `requirements.txt` (examples)**: the CP-26 honesty
  rider ("today a consumer holds both trees") updated to the new truth —
  one tree for the trainer role, both for the server role.
- **`FINDINGS.md` F-14**: BLOCKER → **RETIRED at library CP-29**, with
  the no-flags install proof as evidence.
- **`docs/VERDICT.md`**: wishlist 17 → DONE (CP-29); rows 26–27 opened.

### CP-26's stranger list, re-ordered now that F-14 is gone

Was: F-14 (cannot get the trees) → cold-cache cost → config traps →
F-22's secret → sync topology. Now: **(1)** the red CI badge on both
landing pages (wishlist 26 — the one thing every stranger sees first,
and it says "broken" about a working artifact); **(2)** 0.1.0's stale
landing text (wishlist 27); **(3)** the cold-cache install cost
(~15–25 min CP-26's warm caches hid); **(4)** server role only: F-22's
secret (named, unprobeable — wishlist 24), the site-packages
`vendor/polar` path from a wheel-installed serve (wishlist 22
residual), the sync's workstation topology (F-29, documented); **(5)**
the pins warning shouting on the reference estate (F-39). The config
traps left the list at CP-27 (they fail at load, by name). And the
structural change: the trainer role now starts from **zero handover** —
the first stranger in this project's history who can begin without
emailing the operator.
