# CP-42 — the library, in brief

### CP-42 REPORT
status: done
scope_drift: none in substance — `README.md` and `docs/**` only, the
frozen-path diff empty (`gsj_rollout/`, `vendor/`, `corpus/`,
`mcp-service/`, `forgejo/`, `pins/`, `spike/`, `staging/`, `tests/`,
`.github/` all untouched). One protocol deviation, not scope:
concurrent CP-43/CP-44 material appeared uncommitted in this checkout
mid-CP (see questions) and the DoD's clean-tree line is deliberately
unmet rather than absorbing it.
files: README.md rewritten in place (139 → 197 lines; the old front
door replaced, not shadowed), docs/CHARTER.md +16 (the §7 [CP-42]
block), docs/prompts/CP-42.md + docs/reports/CP-42.md (new). Commit
stages exactly these four paths — docs/CHARTER.md partial-staged so
the foreign in-flight [CP-44] block stays out of this commit.
tests: ./.venv/bin/python -m pytest -q → 161 passed (no code touched)
adrs: none        assumptions: none
gap_register: §7 [CP-42] block appended; NO row moves — tally
unchanged (21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD). Audit
ledger: **S7 CLOSED** (the README trio — the 136 count, the G6
omission, the pre-CP-34 wheel claim — all corrected in the rewrite);
no other audit id touched; the CP-41 open ledger stands at thirteen,
owners unchanged. §8 rule 9 scan: no remaining park's named event
fired this CP (no VERDICT register pass, no charter §4 register pass —
but see questions — no consumer-repo sitting, no CI firing for
wishlist row 26).
questions:
  - Corrupted span, Step 0 ("Prompt ompts/CP-42.md`") — reconstructed:
    "Prompt saved verbatim at `docs/prompts/CP-42.md`; commit with the
    CP." Applied; the prompt file carries the corruption verbatim.
  - Corrupted span, Step 2.2 ("This is the property nobody else ") —
    reconstructed: "…the property nobody else provides." Applied as
    the cutoff section's framing sentence.
  - Corrupted span, Step 4 ("sampling and codec provenance, anonnd
    the two open gaps") — reconstructed: "…, anonymous read), and the
    two open gaps". The anonymous-Forgejo-read residual (gap row 2,
    decided [CP-40], audit O4) is the sourced estate limitation that
    fits the span; "anonymization" appears nowhere in the record
    (grepped VERDICT, CHARTER, corpus docs), so if that was the
    intended word it is left out under the no-new-claims rule and the
    anonymous-read line stands in its place.
  - Corrupted heading, DoD ("run aut") — reconstructed: "run at the
    end". Applied: every line of the block was executed (transcript
    below).
  - **The DoD's `git status --porcelain → empty` is unmet, and
    deliberately.** Mid-CP, this checkout gained uncommitted work that
    is not CP-42's: `docs/prompts/CP-43.md` and `docs/prompts/CP-44.md`
    (untracked) and a `[CP-44]` block in `docs/CHARTER.md`'s working
    tree — the session-start snapshot was clean, and the CP-43/CP-44
    prompts name their own freeze-lifts (the demo and examples repos
    "plus docs/** in the library for the report"). Default applied:
    commit exactly CP-42's four files (CHARTER partial-staged), leave
    the foreign material in the working tree untouched, and state the
    residue here. Sweeping another CP's in-progress record into this
    commit — or deleting it to satisfy a checklist line — would each
    be worse than the dirty-tree deviation they'd cure. The residue at
    CP end is exactly: ` M docs/CHARTER.md` (the [CP-44] block only),
    `?? docs/prompts/CP-43.md`, `?? docs/prompts/CP-44.md`.
  - M3 and S4's library half are parked on "the next charter register
    pass". This CP appends to §7 (the routine every-CP update) and did
    NOT treat that as the parked event — matching CP-41's own
    convention, which wrote those parks while itself appending to §7.
    Recorded: if the operator means any §7-touching CP to count as the
    pass, both parks fire at the next opportunity; rule 9's own text
    ("no row may wait on … a cadence nothing schedules") sits uneasily
    with this wording either way.
  - The CP-36 evidence row says "demo README the only input" — the
    stranger test's input was the demo repo's README, not this one;
    the row is worded to keep that claim exact.
next: the sibling front doors (CP-43 the demo, CP-44 the examples) are
in flight in this checkout's working tree — whoever commits them
should note their charter blocks land in chronological order behind
[CP-42], and that this CP's commit deliberately excluded them. Beyond
that, what the audit left is unchanged from CP-41's ledger.

## Notes

**The diagrams as landed — three, as prompted, all ASCII.**

1. *The shape*: task in at the top, validated trace out; Polar's box
   (task API, scheduling, sandbox lifecycle, capture proxy,
   reconstruction) drawn around the sandbox; the estate (Forgejo, MCP
   service, engine) outside as operator-run; our code named at the
   edges — builder plugged into Polar's reconstruction, receiver +
   checks on the callback leg. Drawn to the measured truth of the
   collect path: the trainer polls Polar's task API and re-runs
   `checks.validate_session_result` itself (client.py's ADR-0008 §3
   docstring), it does not read the receiver's disk.
2. *The cutoff*: the two walls and the audit — the filesystem wall
   (branch `timestep-T`, `--depth 1`, remote removed, reflogs
   scrubbed — CP-11), the retrieval wall (host-side HS256 mint with
   claims `{case_id, timestep, episode_id, exp}`, secret never in the
   sandbox, service-side verification with `page ≤ T` pre-filtered
   from verified claims only — CP-07), and the trace-side G5 audit
   (pages ≤ T, shallow, zero remotes, branch == `timestep-T`,
   contiguous 1..T — checks.py).
3. *The two roles*: server (the estate list + `serve` + the two
   operator-run Polar processes) vs trainer (`pip install`, client +
   checks, pydantic + httpx only, no GPU) — with the sentence the
   confusion always needs: the wheel alone runs no episodes.

**Every number in the README, with its source.** The proven table
carries a source column; the prose numbers: 1,999/2,000 own-code lines
(wc -l re-run this CP; the audit's census matches to the digit),
Polar ~14,200 driven lines (VERDICT §1, the CP-12 measure over
`src/polar`), the predecessor's ~1,800-line execution layer (VERDICT
/ charter §2), three carried patches (POLAR_SHA, `vendor/patches/`),
PyPI 0.1.2 wheel-only (pyproject, ADR-0020), wheel contents = package
+ two pins sets + `ingest_corpus.py` (pyproject force-include, audit
S7's own verification of the published wheel), suites 161 + 58 + 89
(audit honest summary; 161 re-executed this CP), masks/tokens
2965/2965 and logprob figures 0.000114 / 0.000000 / 0.005–0.007
(CP-09, CP-09′, docs/golden/COMPARISON.md), the forged-claim 12→18 →
401 and pages [1,5,7,9,11] ≤ 12 (CP-07), slime 27→one step→5623/5782
moved→8/8 (CP-17), verl 110→one step→310/310 tensors→8/8 (CP-21),
Llama 8 completions → one chain, empty quarantine (CP-38), the
stranger's ≈5 minutes + two manual amd64 pulls (CP-36, wishlist row
40), the 32-row register split (charter §7 [CP-41] tally), 25 ADRs
(ls docs/decisions), zero of 91 findings refuted (audit), A-13's zero
concurrency data points (charter §4), CP-21's mode-collapse reading
(its §6), F1's unpinned-engine default sampling (CP-09), the
anonymous-read decision (row 2, [CP-40]).

**Left out, and why.** The thinking milestone's own numbers (CP-28/30/
31) — present only as the A-22 limit and the pins path; the door
doesn't need the history. The re-vendor door (A-8, CP-22's 175/3 and
the two-minute recipe) — verdict-level detail, lives in VERDICT. The
hyperparameter blocks and the logprob-floor triangulation
(0.008 / 0.008813 / 0.009442) — bridge documentation, the examples
repo's territory. CP-37's model-surface derivation and the demo
mechanics — the demo's front door (CP-43's job, apparently in
flight). Exit codes, YAML section lists, flag rosters — reference,
not trust. The raw vendor-tree line count measured this CP (367,251
across all `vendor/polar` `*.py`) — deliberately not used: it counts
tests and examples, and the sourced ~14,200 `src/polar` figure is the
honest size of the layer we drive. "Anonymization" — unsourced
anywhere in the record; replaced by the sourced anonymous-read
residual (questions).

**The honest five-minute read.** A competent stranger gets, in order:
what the thing is, the one mechanism that distinguishes it (the
cutoff, with the attack that failed), which half they'd actually
install, and a table where every claim has a number and a place the
number lives — plus a not-done list specific enough to be believable,
which is what makes the done list believable. I think five minutes
gets them to "worth an afternoon", not to "trusted": the evidence
discipline shows, but it is self-audited evidence, and the README now
says so plainly (the audit is named, its verdict quoted, its open
items owned). The fastest remaining path from "worth an afternoon" to
"trusted" is running the demo's first trajectory — which is CP-43's
document, not this one.

**Push outcome (§8 rules 8 and 9).** One repo touched — this library,
one commit (`CP-42: the library, in brief`), pushed to origin/main in
the same sitting; `git log --oneline origin/main -1` resolves to this
CP's commit in the DoD transcript. The working-tree residue named
under questions is uncommitted by design and unaffected by the push.
No consumer-repo commits (none permitted).

**DoD transcript.**

```
$ git diff HEAD -- gsj_rollout/ vendor/ corpus/ mcp-service/ forgejo/ \
    pins/ spike/ staging/ tests/ .github/
(empty)
$ ./.venv/bin/python -m pytest -q
161 passed in 16.57s
$ wc -l README.md
197 README.md            # was 139; three diagrams + the table inside it
$ test -f docs/reports/CP-42.md && echo OK
OK
$ git status --porcelain   # NOT empty, by decision — see questions:
 M docs/CHARTER.md         #   the in-flight [CP-44] block (not ours)
?? docs/prompts/CP-43.md   #   concurrent CP material, left untouched
?? docs/prompts/CP-44.md
$ git log --oneline origin/main -1
(filled at push — see the commit that carries this report)
```
