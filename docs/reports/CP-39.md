# CP-39 — M13a: publish, and de-trap

### CP-39 REPORT
status: done
scope_drift: none in the frozen paths — `gsj_rollout/`, `vendor/`,
`corpus/`, `mcp-service/`, `forgejo/`, `pins/`, `spike/`, `tests/` all
diff-empty, root suite byte-unchanged at 161, census untouched at
1,999/2,000. The lifts taken are the prompt's own (`CLAUDE.md`,
`release.yml`, `staging/README.md`, `docs/**`, the demo repo) plus two
judgment calls listed under `questions:`: one continuation-pointer
paragraph appended to the examples repo's `FINDINGS.md` (Step 2 offered
it as an option; the no-collision requirement cannot be met without it),
and the audit document committed at `docs/AUDIT-2026-08-24.md` (it is
the specification three CPs cite by finding id, and it existed only in a
session transcript). `forgejo/`'s five mis-resolving citations (W2)
deliberately STAY — recorded, CP-41's conscious lift.
files: library — docs/prompts/CP-39.md, docs/reports/CP-39.md,
docs/AUDIT-2026-08-24.md (new); CLAUDE.md (rewritten from the CP-08
stub, S1); .github/workflows/release.yml (required tuple 3 → 5, O2);
staging/README.md (Megatron row corrected + [CP-39 erratum], W1; the
CP-38 second-family section added, S9); docs/CHARTER.md (§8 rule 8
[CP-39] amendment — ignored three CPs running, widened; §7 [CP-39]
block); docs/VERDICT.md (row 28 [CP-39] note). External — the demo repo:
FINDINGS.md (NEW — the register home for F-54–F-68, M1), one commit,
pushed. The examples repo: FINDINGS.md (continuation pointer, next fresh
id F-69), one commit, pushed. All three repos pushed; no ahead count
anywhere (§8 rule 8, as amended).
tests: ./.venv/bin/python -m pytest -q → 161 passed (suite byte-unchanged)
adrs: none        assumptions: none
gap_register: §7 [CP-39] block appended — no row moves, tally unchanged
(21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD). §8 rule 8 amended in
place. Wishlist row 28 annotated ([CP-39] — O2 is row-28-adjacent: the
tuple's missing thinking-on entry is that row's packaging, noted one
short by CP-33 in that row's own cluster). External register: F-54–F-68
homed in the demo repo's new FINDINGS.md; the examples register ends
with the continuation pointer instead of silence after F-53.
questions:
  - Corrupted span, Step 1 ("that is the more interesting fx is
    different") — reconstructed: "that is the more interesting finding;
    if it says something narrower, the fix is different." Applied: both
    halves turned out true (notes below).
  - Corrupted span, Step 3 ("The table sa04-alpha.rc1`") —
    reconstructed: "The table says `--branch 26.04-alpha.rc1`" (matches
    staging/README.md:34 as it stood, and audit W1).
  - Corrupted span, Step 4 ("Keep it it is context every session pays
    for") — reconstructed: "Keep it short — it is context every session
    pays for, not documentation." Applied: 89 lines.
  - Corrupted span, Report ("what the push added anoutside
    verification") — read as "what the push added and the outside
    verification".
  - Corrupted span, DoD ("venservice/") — reconstructed as the full
    frozen set "vendor/ corpus/ mcp-service/"; the diff below covers all
    eight frozen paths.
  - The prompt (and audit S1) say "ten test files"; the tree has nine
    `test_*.py` modules plus `conftest.py` — ten `.py` files in
    `tests/`. CLAUDE.md states what a stranger can count: nine test
    modules, 161 tests.
  - The prompt says F-52's cure was "named at CP-36 and never written";
    the record says named at CP-32 (F-52's own status cell: "the
    protocol sentence is the operator's to add") and WRITTEN at CP-33
    (§8 rule 8). Applied the prompt's own contingency for exactly this:
    the rule exists and was ignored — recorded as the finding.
  - The audit document had no committed home (it lived in the audit
    session's transcript; "read first" would be unfollowable in a month)
    — default applied: committed verbatim at docs/AUDIT-2026-08-24.md,
    with a provenance comment, so the W/S/O/C/M ids that CP-39/40/41
    cite resolve inside the repo.
  - Step 2's freeze-lift named only the demo repo, but its option list
    offered the examples-register pointer — default applied: BOTH homes,
    because "a future checkpoint minting a row from the register file
    alone must not collide at F-54" has to hold for the register file a
    future CP actually opens, and the examples file ended at F-53 with
    silence. One paragraph appended there, nothing else touched.
next: CP-40 — the consumer-surface refresh (the examples repo's lagging
statuses W4/S5/C5, the v0.1.0 texts S2, the ENOENT wheel commands W5,
the CP-27-fixed "still live" bullets S3, the Qwen-bound recipe texts in
S4's examples half) and the orphan re-owning (F-44/O7, wishlist rows
19 and 10 (O6), 24 and 26 (O8), C-3 (O3), gap rows 2 and 22 (O4/O5)).
CP-41 — the namespace collisions (`forgejo/`'s five citations W2,
recorded here as staying; the corpus contract header C2; `checks.py:97`
W9; the golden MANIFESTs M5), the ADR amendments (0005/C8, 0011 and
0020/S12, 0019/C7), and the residue removal (the stale 0.1.1 wheel in
`dist/` — noting `install.sh` prefers a sibling `dist/` wheel over PyPI,
so the documented developer path currently runs one release behind).

## Notes

**The push, and the outside verification (O1).** Three commits were
sitting local-only — `b866de9` (CP-36), `5d21f85` (CP-37), `3467524`
(CP-38) — so the public repo lacked the entire stranger-test record,
the model-surface derivation, the second serve recipe, and register
rows F-54–F-68. Pushed first thing; then a fresh clone of
`github.com/MHGanainy/gsj-harness-rollout-server` (new directory,
nothing reused) showed all three commits at HEAD, reports CP-36/37/38
present, and `staging/serving/serve-llama31.sh` +
`model-llama31-8b.env` in place. A side effect with no edit needed: the
examples register's F-14 status cell ("the library mirror pushed
current") is true again.

**§8's rule: it already said exactly this, and was ignored — the more
interesting finding.** Rule 8 (added CP-33 as F-52's durable cure) reads
"Push both repos at the end of every CP — the commit protocol ends at
the remote, not the working tree." It covered the library squarely, and
CP-36, CP-37 and CP-38 each ended with the library unpushed anyway. How
a written rule failed three times without a single false statement:
(a) its evidence clause binds only the *word* "pushed" — none of the
three reports claimed the library pushed (each says only "the demo repo
… one commit, pushed"), so silence passed the clause vacuously; (b) the
rule's home is §8, but the file every session is guaranteed to read is
CLAUDE.md — which CP-33's freeze-lift excluded, and which was itself 26
checkpoints stale, so the most-read process file was the least true;
(c) ironically, the demo repo — which rule 8's "both repos" does not
even name (it was written one CP before the demo existed) — WAS pushed
every time, because its CPs treated the push as part of the deliverable.
What CP-39 did: recorded the violation in the rule's own [CP-39]
amendment and in §7; widened the rule — every repo the CP touched, the
report states the push outcome either way, silence is non-compliance;
and copied it into CLAUDE.md's workflow section, where a session cannot
miss it. One correction to the prompt's framing, recorded under
`questions:`: the cure was named at CP-32 and written at CP-33 — what
was never written was the CLAUDE.md half, and what was never done was
the following of it.

**The register home (M1) — decision: both, with the demo file as the
register.** The fifteen rows are the demo's findings (CP-36's own
prompt already said "continuing the demo repo's register" — the
register that prompt assumed existed is what this CP creates), so
`gsj-rollout-demo/FINDINGS.md` now carries F-54–F-68 in the examples
register's five-cell format, with CP-36's severity vocabulary and each
row's status as CP-36 left it (F-54 demo-half fixed / durable half open
as library wishlist row 40; F-56 recorded, deliberately unfixed; F-67
no fix — not a defect; the rest fixed at demo `96f2329`). Why a future
row cannot collide at F-54: both files now state the same rule — the
F-series is ONE register across both consumer repos, a new row takes
the next id after the highest anywhere, next fresh id F-69 — the
examples file immediately after its F-53 row (where a minting CP's eyes
land), the demo file in its header. Minting from either file alone
lands on F-69.

**The two traps, before/after.**
- O2 (`release.yml`): before — `("gsj_rollout/checks.py",
  "gsj_rollout/client.py", "gsj_rollout/pins/pins.gsj.json")`, three
  entries, unchanged since CP-19, while 0.1.1 added the thinking-on
  pins and 0.1.2 added `ingest_corpus.py` as must-ships guarded only by
  CI — which the tag path deliberately does not re-run (release.yml's
  own header says so). After — five entries: the two wheel-member paths
  `gsj_rollout/pins/thinking-on/pins.gsj.json` and
  `gsj_rollout/ingest_corpus.py` asserted beside the original three, on
  the artifact itself. The tuple change is the promised one line (well,
  two wrapped); a five-line comment rode along naming why the tuple
  lagged — said here per the "small by design" clause.
- W1 (`staging/README.md:34`): before — "git clone … `--branch
  26.04-alpha.rc1` (Megatron-LM — Polar's stated
  slime-v0.3.0-compatible pin)", the exact pin F-06/CP-17 measured
  dying at `ModuleNotFoundError: No module named
  'megatron.training.tokenizer'`; an estate restore from the table
  failed at import. After — Megatron is the IMAGE's own
  `/root/Megatron-LM` (`1dcf0dafa`, the Dockerfile's
  `MEGATRON_COMMIT`), NOT a separate checkout, with F-06 and the
  recipe-that-ran cited; plus the [CP-39 erratum] paragraph recording
  that the contradiction and the report contradicting it were written
  by the same commit (`a647359`, CP-17) and stood for 22 checkpoints.
- S9 (same file): the CP-38 second-family recipe
  (`serve-llama31.sh` + `model-llama31-8b.env`) now has its section —
  the four re-decided legs, the distinct state files, the pidfile
  refusal — so the "reproducible from the committed tree" claim is
  whole again; the omission and its CP-38 origin are named in-file.

**CLAUDE.md's delta (S1).** Before: the CP-08 stub — `serve`/`submit`
"does nothing yet", an `__init__.py` "(empty for now)", a layout with no
`builder.py` and one test file, no version, no consumer repos, no
current census. After (89 lines, same shape, template and laws
verbatim): PyPI 0.1.2 and what the wheel ships; the census standing
(1,999/2,000, headroom 1, the ADR-0021 tripwire warning); the push rule
(F-52's cure finally on the page every session reads); the real layout
— `builder.py`, nine test modules / 161 tests, `POLAR_SHA`, the docs
tree as it is (including the audit); real command semantics (`serve`
renders the topology, prints the two Polar commands, runs the receiver;
`submit`'s ad-hoc and `--from-bank` modes with exit codes 0/1/2/3); and
the F-01–F-53 / F-54–F-68 register split. Nothing binding was dropped.

**What this CP does not do — CP-40 and CP-41 inherit:**
- the consumer-surface refresh (the examples repo's lagging statuses,
  the v0.1.0 texts, the ENOENT wheel commands) — CP-40
- the orphan re-owning (F-44, rows 19/10/24/26, C-3, rows 2 and 22) —
  CP-40
- the namespace collisions (`forgejo/` — its five mis-resolving
  citations stay under freeze this CP, recorded; the corpus contract
  header, `checks.py:97`, the golden MANIFESTs) — CP-41
- the ADR amendments (0005, 0011, 0019, 0020) — CP-41
- the residue removal (the stale 0.1.1 wheel in `dist/`) — CP-41, and
  note that `install.sh` prefers a sibling `dist/` wheel over PyPI, so
  the documented developer path currently runs one release behind
