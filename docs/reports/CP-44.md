# CP-44 — the examples, in brief

### CP-44 REPORT
status: done
scope_drift: none — the freeze-lift (the examples repo + `docs/**` here)
was honoured exactly: the examples repo's diff is `README.md` only; this
repo's diff is `docs/prompts/CP-44.md`, `docs/reports/CP-44.md` (this),
and the `docs/CHARTER.md` §7 [CP-44] paragraph. No code touched in
either repo; the bridge READMEs and their mode riders read and left
verbatim.
files: examples repo — 1 file, `README.md` +222/−49 (commit `ef827d8`,
**pushed**: `663f07a..ef827d8 main -> main`). This repo — 3 files, all
docs, one commit `CP-44: the examples, in brief`, **pushed** (the
outcome verified at the remote before this CP claimed done; this
sitting's library commit landed third by agreement — see questions).
tests: `.venv/bin/pytest -q` → **161 passed** (no code touched;
docs-only CP)
adrs: none        assumptions: none
gap_register: §7 [CP-44] paragraph added — no row moves, tally
unchanged (**21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD**).
External register: current, no new rows, next fresh id **F-70**
unchanged.
questions:
  - Numbering gap: this repo's prompts and reports ended at CP-41 when
    the CP-44 prompt arrived. Default applied: proceeded as CP-44 —
    and the gap explained itself mid-CP: CP-42 and CP-43 are executing
    in PARALLEL sittings sharing this library checkout (a cross-session
    message from CP-43's sitting, which found CP-42's staged set and my
    unstaged [CP-44] hunk in the same tree). Sequencing agreed with
    CP-43's sitting: CP-42's library commit first, CP-43's second
    ([CP-43] inserted between the [CP-42] and [CP-44] §7 blocks), this
    one last after a pull; this sitting touched no shared index until
    both landed. The one-CP-at-a-time rule reads per-sitting here — the
    operator launched the parallelism.
  - The DoD's "One commit" vs the two-repo reality: CP-16–CP-21
    precedent applied — one commit in EACH repo, the examples commit
    titled per that repo's convention (`CP-44 (library): …`), this
    repo's carrying the DoD title.
  - The verification pass surfaced three audience-blocking silences the
    prompt's steps did not name — the task domain (train on WHAT?), the
    task-bank size, and the cost of leaving Qwen3-0.6B. Default
    applied: added, one sentence each, sourced (RUNBOOK; taskbank 12
    rows / 4 cases / 9 train; family-bound flags per library CP-38) —
    the audience's first questions belong on the front door.
next: advisory — the one adoption question the front door still cannot
route is licensing (vendored Polar, the corpus): no roll-up exists in
either repo to point at. A future docs CP could add one to the library
README and give the front door a one-line pointer. CP-43's sitting was
asked to flag if its demo-README restructure moves any of the measured
figures this front door quotes (cold `up` ~2.5 min, 6–7 GB, 20–40 s per
episode, F-54–F-69); none flagged as of this report.

---

## Notes

### What landed

The examples README rewritten as the front door for its actual
audience — a trainer engineer with a model, a trainer, and GPUs,
deciding whether to train with this:

- **Step 1**: the one paragraph (traces in, batches out, two trainers —
  plus what the agent actually does: the legal-corpus task, the
  temporally-scoped sandbox, the estate defined at first use) and the
  one picture — trace → bridge → batch with the three assertions drawn
  ON the path, and the paragraph under it saying why they are the
  reason the bridge exists (silent importance ratios, finite sentinels,
  trust-nothing-that-crossed-a-wire).
- **Step 2**: the bridges table — trainer, batch shape, proven-by
  (CP-17's and CP-21's loops, numbers inline), real-vs-doubled in
  tests, stack cost — with the page-global rider that both proof loops
  ran thinking-off, and ownership stated in bold: consumer code, not
  library code (ADR-0018); a bug in `bridge.py` is the trainer's and
  this register's. What each still needs from you: reward (F-02),
  weight sync, grouping (F-10).
- **Step 3**: the measured costs, every number mode-labelled and
  checkpoint-attributed (the F-18/F-42 discipline applied to the front
  door itself): collection 13m35s ON / 4m36s OFF pooled = 2.95×
  (CP-32), the ≈19-attempts figure explicitly framed as the STRICT
  standard's rate rather than the loops'; the relaxed-vs-strict split
  (89 of 112 discarded under strict, off); length-termination
  surfaced-never-screened (ADR-0025, 7/72 ON at the 32k wall); reward
  1/27 · 0-at-56-then-1/112 · 1/72-ON-vs-0/72-OFF and "not a stable
  constant"; both replay floors (0.008/0.21 off, 0.016546/0.35% ON)
  with reading guidance; the ~1-min restart sync with A-13 stated as
  situation-proven, not mechanism; the GPU step at 94% of an H200.
- **Step 4**: the seven-item not-solved list from CP-17's and CP-21's
  "what a real training run would need": non-restart sync, concurrency
  (A-13 + P3), denser reward / stronger actor / more tasks (the bank is
  12 rows), the F-08 guard (Polar's vendored post-processor, correctly
  attributed), entropy/KL control (the measured mode-collapse onset),
  throughput, checkpoint retention. Plus the cost that is not this
  repo's to count: the estate, with the demo repo's measured
  from-nothing figures and the F-33 three-repo truth.
- **Step 5**: FINDINGS current (no new rows; F-70 next); pushes stated
  above.

### The verification, and whether a trainer engineer could decide

The draft went through a six-checker adversarial pass (three
fact-checkers against CP-17/CP-21/CP-32+16+20, a cross-document
consistency checker against the RUNBOOK/bridge READMEs/FINDINGS, a
reference checker, and a cold read by the target persona), then a final
regression pass. The CP-21 and CP-32/16/20 checkers returned **zero
discrepancies**. Caught and fixed before commit: one real error — the
first draft attributed F-08's defective post-processor to slime where
it is Polar's vendored file (the exact routing distinction the
register's "library-side?" column exists to preserve); one implication
error — "episodes drained first" implied a drain mechanism where A-13
was satisfied by construction; and four mode-label misses against the
section's own labelling promise.

The DoD question, answered by the cold read (a senior RL-infra persona,
README only): **yes — a decision is reachable from the page.** The
persona's decision: run a bounded verl-path pilot on one spare H200;
do not adopt for production training today — the page's own numbers
earn the first and rule out the second (one-step loops, restart-based
sync, no concurrency, reward at 1-in-tens, mode-collapse onset at a
single step). The three silences the persona said kept it a pilot
rather than a yes — task domain, task volume, model portability — were
added before commit. The residual it named that this CP could not
honestly write: the licensing roll-up (next, above).
