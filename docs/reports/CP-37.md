# CP-37 — M12a: the model-dependent surface

### CP-37 REPORT
status: done
scope_drift: none — the library repo takes `docs/**` only (prompt, this
report, CHARTER §4 A-30 + §7 [CP-37], checks-spec §G6 [CP-37] note,
VERDICT rows 35/38 annotated + 41/42 opened); the DoD's frozen-path diff
is empty and the root suite is byte-unchanged at 161. The offered
`pins/derive_pins.py` lift was **not taken** — decided and said under
notes: the derivation does not belong there. All code landed in the demo
repo, whose lift was unrestricted.
files: library — docs/prompts/CP-37.md, docs/reports/CP-37.md (new);
docs/CHARTER.md (§4 +1 row A-30; §7 +1 block [CP-37]); docs/checks-spec.md
(§G6 +1 block [CP-37]); docs/VERDICT.md (rows 35/38 [CP-37] annotations,
rows 41/42 NEW). External — the demo repo
**github.com/MHGanainy/gsj-rollout-demo**, one commit, pushed: `f546abd`
(bootstrap.py: `derive_endpoint_pins` + the foreign-model branch of
`derive_pins` + eot/glue threading through `write_rollout_yaml` + the
`provider/model` slash guard; preflight.py: the end-of-turn row re-derived
from the served template, the NEW `template` prefix-extension row;
config.yaml.example: derived-eot comment + `generation_prompt_glue_ids`;
docs/MODEL-SURFACE.md NEW — the "what changes when the model changes"
document; README: probe list, quarantine narrative, "Bring your own
model" section). Evidence: scratch controls under the session scratchpad
(probe_local.py + demo-ctl/), summarized verbatim below.
tests: `./.venv/bin/python -m pytest -q` → **161 passed** (byte-unchanged
— no library code touched). Demo-side: the reference control ran the real
code paths against the live reference stack (all preflight rows ok,
derivation forced through the foreign branch reproduced every pinned
value exactly, both modes — §the control below), plus a pre-commit
adversarial review workflow over the diffs (findings and fixes under
§the review below).
adrs: none (the glue-stitch exposure implements ADR-0007's dormant
mechanism as config data; the G6 meaning note extends the spec, not the
rule — ADR-0021's 528 untouched)        assumptions: **A-30 NEW** (the
turn-terminator derivation heuristic, with its measured basis and the
explicit-override cure); A-21 exercised in text (the stitch is now
reachable from the demo config for the asymmetric-template case)
gap_register: §7 [CP-37] paragraph added; no row moves; tally unchanged
(21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER · 1 TBD). Wishlist row 35
**NARROWED** (the endpoint-derivable half closed — the derivation runs at
`up`, control-proven; residual: G4's snapshot walk, `/tokenize`-less
endpoints, the first foreign episode), row 38 annotated (classified
[you]/[estate]; residual unchanged), rows **41–42 opened** (the harness's
Qwen-bound constants; non-Qwen hybrid-reasoning geometry).
questions:
  - Corrupted span, Step 1 first bullet: "`end_of_turn_toail_ids`" →
    "`end_of_turn_token_id` and the G6 tail ids" (wishlist 35's named
    tokenizer-bound trio; both are walked below).
  - Corrupted span, Step 1 classification: "**estate-sidor **a human
    decision**" → "**estate-side**, or **a human decision**".
  - Corrupted span, Step 3: "the preflight's tokenizer rows FAIL loudlye
    the G6 quarantine consequence" → "…FAIL loudly, naming the G6
    quarantine consequence" (CP-36 Step 4's own wording).
  - Corrupted span, Step 4: "why it is modeldent" → "why it is
    model-dependent".
  - Corrupted span, Step 6: "trusting an unkne" → "trusting an unknown
    one".
  - Corrupted span, DoD: "# the template probe, run against the reference
    — ve derivation vs the pins: values, reproduced or not" → two merged
    lines: "# the template probe, run against the reference — verdict"
    and "# the derivation vs the pins: values, reproduced or not" (both
    shown below).
  - Applied default, the derivation's home (the decide-and-say):
    **the demo — bootstrap derives, preflight verifies; `derive_pins.py`
    untouched.** The library walk is the reference estate's drift
    VERIFIER: it re-derives committed approved values from committed
    evidence and exits nonzero on divergence. A foreign-model deriver has
    no committed target, needs a live endpoint and the estate's config,
    and writes an estate-local pins file — all demo concepts. Folding it
    into the walk would change that tool's identity, not improve it; if a
    non-demo estate ever wants the deriver, it is ~120 endpoint-only
    lines to lift, and the residual is recorded in wishlist 35.
  - Applied default, the second tokenizer's venue: local snapshots (the
    official Llama-3.1 template via the unsloth mirror, the NousResearch
    simplified mirror deliberately kept as a wild-template data point,
    and the box-local Qwen2.5-Coder mlx snapshot as a
    same-vendor/no-think-mode midpoint) — no second engine, per the
    prompt's own boundary.
  - Applied default, derivation-failure posture: when the endpoint cannot
    be derived from (`/tokenize` absent, template ignores
    `add_generation_prompt`, text round-trip fails), the G6 keys KEEP the
    reference values under the loud NOTE naming the quarantine
    consequence and the cure — both choices quarantine every episode, and
    the reference values produce the known digest-naming mismatch finding
    rather than a missing-pins shape; G4's keys are emptied either way
    (nothing at the receiver reads them, and a wrong byte-identity is a
    pure lie in provenance).
  - Applied default, the unslashed-name guard dies at `validate`/`up`
    config load rather than warning: the alternative failure surface is a
    raise inside every episode's sandbox (`pi_harness.py:104`), which
    nothing would explain.
  - Applied default, the estate: the reference engine restored via
    `staging/serving/serve.sh` (GPU 3 — all eight GPUs were free this
    session; tenancy shifts, discovered at run time), tunneled as
    `127.0.0.1:8100`; engine STOPPED after the CP (shared-GPU courtesy).
    `staging/` is frozen this CP and was run, not modified.
next: advisory — CP-38 is the second family end to end. Bring a
Llama-3.1-8B-Instruct endpoint (the official template measured
prefix-extending here, so `chains_total: 1` is the expected — and first
genuinely at-risk — number); expect to hit: the G2 render on a non-Qwen
model (A-30's neighbor — the capture carries no model byte, but only an
episode proves the render), the tool-call FORMAT mismatch (Llama renders
tool calls as its own JSON dialect while pi parses hermes-style — the
probe cannot see sampled-format compatibility, only render symmetry),
`--tool-call-parser llama3_json` vs pi's expectations, and the
thinking-on stamp no-op (row 41). What CP-38 should measure first:
submit one episode, read `reconstruction_stats.chains_total` and the
quarantine dir before anything else.

---

## Notes

### Step 1 — the classification table (the CP)

Every artifact the model decides, measured or code-cited this session.
Classes: **[endpoint]** derivable from the endpoint's API ·
**[snapshot]** derivable from a local tokenizer snapshot · **[estate]**
knowable only on the serving host · **[human]** a decision no tool can
make.

| # | artifact | changes with the model? | class | who derives it today | wrong ⇒ |
|---|---|---|---|---|---|
| 1 | served model name | yes (trivially) | [human] | config.yaml; preflight `model` row verifies byte-for-byte | ADM1/ADM4 "no completions", every episode |
| 2 | the name's `provider/model` shape | NO — `pi_harness.py:104` requires it, but `config.py:408` prepends `estate.provider` (default `gsj/`) itself, so the raise is unreachable via the one YAML; any served name works, slashed or not | — | the library, internally (a review-verified correction: a demo guard drafted on the opposite premise was deleted before commit) | nothing, via this path |
| 3 | the chat template (served artifact) | YES — decides #4–#8 | [estate] | the serve argv / snapshot; nothing derives it FOR you, everything below FROM it | #4–#8 |
| 4 | prefix-extension property | yes | [endpoint] | NEW: preflight `template` row (`/tokenize`, two-turn render pair) | `chains_total > 1` — silent until now; nothing else surfaces it |
| 5 | `g6_expected_tail` / `_ids` | yes | [endpoint] | NEW: bootstrap at `up` (agp delta + `/detokenize` + round-trip proof), per mode | every episode quarantined `G6:prompt_suffix_ne_tail_ids` |
| 6 | `end_of_turn_token_id` | yes | [endpoint] | NEW: bootstrap at `up` → rollout.yaml builder (first non-whitespace token after assistant content — A-30); preflight re-derives and compares; explicit config wins with a disagreement WARN | reconstruction mis-splits every multi-turn episode |
| 7 | `generation_prompt_glue_ids` | yes, when needed | [endpoint] | NEW: preflight prints the candidate span on a constant divergence; stranger sets it in config.yaml (ADR-0007's stitch, now config-reachable) | only relevant for constant history-rewriting templates |
| 8 | G4 `tokenizer_hash` / `chat_template_hash` | yes | [snapshot] ONLY | nobody over an API — bootstrap writes them EMPTY for a foreign model (named, not defaulted); MODEL-SURFACE.md carries the snapshot recipe | nothing at the receiver (no trace-side G4 gate); the estate-side drift walk is what is lost |
| 9 | G2 `system_prompt_hash` | believed NO — measured at desk | — | bootstrap, from the corpus AGENTS.md by byte-substitution | — (the 81-line reference capture contains no model-identifying byte; pi composes it, so the untested axis is pi's render on a foreign model — CP-38 proves it with an episode) |
| 10 | G1 cards · G3 roster · G7 settings | no | — | corpus bytes; pi's fixed 11-tool roster; the harness constant | — |
| 11 | context window | yes | [endpoint] on vLLM (`max_model_len`), else [human] | preflight row; `context_window` config | mid-episode 400s as the real window fills |
| 12 | sampling defaults | yes | [human]/[estate] — unprobeable (wishlist 38) | nobody; the serve argv IS the policy | you train on the server's accidental distribution (unpinned vLLM: T=1.0) |
| 13 | `thinking` knob semantics | YES — Qwen feature | [human] | config; pins re-derive per mode; measured: `enable_thinking` is unused jinja context on Llama templates → non-off is a wire NO-OP there, while the archive still stamps `.thinking-on` (row 41) | a provenance stamp claiming a mode the model lacks |
| 14 | `thinkingFormat: "qwen-chat-template"` + `supportsReasoningEffort: false` | yes — for families with a DIFFERENT thinking wire format | library-frozen constants (`pi_harness.py:122-123`) | nobody — named, row 41 | a hybrid-reasoning non-Qwen family cannot express its mode |
| 15 | read.py think markers (151667/151668) | yes | demo display heuristic, already labelled Qwen-only in code | — | cosmetic: foreign thinking (if any) simply never renders as thinking |
| 16 | README's measured numbers | yes — all Qwen3-0.6B/reference-stack | — | labelled reference-bound in MODEL-SURFACE.md | expectations wrong, claims intact |

Beyond the prompt's list (the interesting part): #2 (walked into via
`pi_harness.py`'s raise, then inverted by the review's verification —
the library satisfies its own constraint internally), #7 (the stitch
was unreachable from the demo config — the cure the probe names would
have been un-followable), #14 (the harness's wire-format constants),
and the finding that **template identity varies by mirror, not just
family** (§the second tokenizer below) — a stranger who "serves
Llama-3.1" has not yet told you which template they serve.

### Step 2 — the template probe

CP-04′'s hand-measurement is now `./preflight.py`'s `template` row: two
`/tokenize` calls (messages form, `chat_template_kwargs` mirroring the
mode) render a pi-wire-shaped two-turn history and check strict
prefix-extension, `P2[:len(P1)] == P1`. `/tokenize` **suffices** — no
snapshot, no transformers, four HTTP calls total with the terminator
derivation; on an endpoint without it the row says plainly what cannot be
checked and what that costs. The probe logic first reproduced the known
answers locally: against the snapshot-EMBEDDED Qwen3 template it finds
the CP-04′ divergence **at index 37 of 41 = n−4 on exactly
`[151667, 271, 151668, 271]`** (CP-04′: 2018 of 2022, same structure,
same four ids); against the served TRL template it measures TRUE — and
live against the reference stack: `turn 2's prompt render strictly
extends turn 1's (40 -> 93 ids)`.

What a stranger is told on failure, in order of preference (all in the
row's own text, before any episode is spent): (1) Qwen3 → serve the TRL
symmetric template (the README's serve argv, measured curing exactly
this); (2) constant divergence → the row PRINTS the lost span and the
exact `generation_prompt_glue_ids: [...]` line to put in config.yaml —
the stitch is now config-reachable and the generated rollout.yaml was
validated through the wheel's own `load_config`; (3) neither → "this
model's template needs a symmetric variant before episodes are worth
spending." What the row exists to prevent — **corrected by this CP's
own review**: the prompt's premise that `chains_total: N` "is surfaced
by nothing" is FALSE — `checks.py:295` fires `G7:chains_total_ne_1` and
the receiver quarantines every split-chain episode, fail-closed. The
probe's true value, now stated everywhere the old claim stood: the
quarantine catches it AFTER each episode is spent, with the cross-turn
context already lost; the probe is where a stranger learns it before
spending any. One honest limit, stated in MODEL-SURFACE.md: the probe
proves renders extend renders; sampled-vs-rerendered identity is only
proven by a real episode's `chains_total: 1` — necessary, not
sufficient.

### Step 3 — derive, don't just detect (wishlist 35's cure)

`bootstrap.py up`, when `inference.model` is not the reference, now
derives from the endpoint's own template render and writes
`work/estate/pins.gsj.json` — exactly where `GSJ_PINS_PATH` already
points every leg. Derived: `g6_expected_tail_ids` (the
`add_generation_prompt` delta), `g6_expected_tail` (its `/detokenize`
text, accepted only if it round-trips), `end_of_turn_token_id` (A-30's
heuristic → the generated rollout.yaml's builder block; explicit config
wins with a disagreement WARN). Named, never defaulted: G4's two hashes
(written EMPTY with provenance saying why and where the snapshot recipe
is), sampling (unprobeable), and every derivation dead-end returns a
`why` that the NOTE prints with the quarantine consequence and the cure.
The say-lines, verbatim from the control run:

```
pins — 'Qwen/Qwen3-0.6B' is not the reference model; derived from the endpoint's own template render:
pins —   g6_expected_tail_ids = [151644, 77091, 198, 151667, 271, 151668, 271]  ('<|im_start|>assistant\n<think>\n\n</think>\n\n')
pins —   end_of_turn_token_id = 151645  ('<|im_end|>') -> rollout.yaml builder
pins — NOT derivable and left empty, on purpose: G4's tokenizer/chat-template hashes (byte identities no API exposes — docs/MODEL-SURFACE.md has the local-snapshot walk). ...
```

### Step 6 — the control, then the second tokenizer

**The control** — the derivation forced through the foreign-model branch
(`REFERENCE_MODEL` monkeypatched) against the live reference stack, real
demo code, the PyPI wheel:

| value | derived | pinned | verdict |
|---|---|---|---|
| off tail ids | `[151644, 77091, 198, 151667, 271, 151668, 271]` | same | **REPRODUCED** |
| off tail text | 41 bytes `<\|im_start\|>assistant\n<think>\n\n</think>\n\n` | same | **REPRODUCED** |
| on tail ids (`thinking: medium`) | `[151644, 77091, 198]` | thinking-on pins | **REPRODUCED** |
| on tail text | `<\|im_start\|>assistant\n` | same | **REPRODUCED** |
| end-of-turn id | 151645 (`<\|im_end\|>`), both modes | builder default | **REPRODUCED** |
| mode stamp | `thinking-on` preserved in the derived doc | ADR-0024 | intact |
| G4 keys | `[]` + provenance naming the walk | — | named-not-defaulted, by design |

And the full new preflight against the reference: **all seven probe rows
ok, the sampling warn the only warn** — the CP-36 control shape
preserved with two more rows.

**The second tokenizer** (local snapshots; no engine):

| snapshot | prefix-extension | derived tail | derived EOT |
|---|---|---|---|
| Llama-3.1-8B-Instruct, official template (unsloth mirror) | **TRUE** (59 → 99 ids) | `[128006, 78191, 128007, 271]` = `<\|start_header_id\|>assistant<\|end_header_id\|>\n\n` | `128009` (`<\|eot_id\|>`) |
| Llama-3.1-8B-Instruct, NousResearch mirror (simplified template) | TRUE (39 → 64) — but it never renders tool_calls | **NOT DERIVABLE** — ignores `add_generation_prompt`; refused (an empty tail would make G6 vacuous) | `128009` (after the heuristic correction below) |
| Qwen2.5-Coder-3B (mlx, no think mode) | TRUE (37 → 93) | `[151644, 77091, 198]` — identical in both modes: the mode assertion vanishes with the mode (row 42) | `151645` |

**The most valuable single number: Llama-3.1's official template is
prefix-extending as shipped** — the first non-Qwen family measured needs
no symmetric variant and no stitch for the render property. The wild
mirror is the counter-lesson: same model name, different template,
different derivability — the served artifact is the only truth, which is
why the derivation probes the endpoint and not the family. The mirror
also improved the deriver before it shipped: its unconditional trailing
generation prompt made a last-non-whitespace terminator heuristic pick
`<|end_header_id|>` (128007); the shipped rule is FIRST non-whitespace
after content, which survives it (A-30 records the assumption and its
basis).

### Step 5 — Qwen-specific and load-bearing (named, not fixed)

1. **G6's subject** — the 41-byte empty think block IS a Qwen
   hybrid-reasoning artifact. Off-family the gate asserts template
   integrity only, and ADR-0024's two-direction mode assertion vanishes
   (both modes derive the same tail — measured on two families). Said
   plainly in spec §G6 [CP-37]: **G6's thinking meanings are
   Qwen-family-specific.** A different hybrid-reasoning geometry needs
   its own CP-28-style measurement → row 42.
2. **The thinking surface** — `pi --thinking` → `enable_thinking` kwargs;
   measured a wire no-op on Llama templates; the archive still stamps
   the mode → row 41; MODEL-SURFACE tells strangers to keep `off` on
   modeless families.
3. **The `<|im_start|>assistant` shape** — everywhere as DATA (pins,
   captured tails), nowhere as code except read.py's labelled display
   heuristic and the retired preflight probe (this CP removed the
   hardcoded `'<|im_end|>'` probe string — the one place the SHAPE was
   load-bearing in code).
4. **`pi_harness.py`'s settings** — `thinkingFormat:
   "qwen-chat-template"`, `supportsReasoningEffort: false`
   (library-frozen; row 41). The `provider/model` split turned out NOT
   load-bearing for served names — `config.py` satisfies it internally
   (row 41 records the corrected mechanism).
5. **The reference serve argv** — `--reasoning-parser qwen3`,
   `--default-chat-template-kwargs '{"enable_thinking": false}'`,
   `--tool-call-parser hermes`, the TRL template: four Qwen-bound legs a
   foreign estate must re-decide; README labels them, MODEL-SURFACE
   classifies them [estate].
6. **The demo's measured numbers** — all reference-stack; labelled so.

### The review (pre-commit discipline)

An adversarial review workflow (24 agents: 3 dimension reviewers — code
correctness, claim truthfulness against the session's measurement log,
stranger consistency — each finding verified by a dedicated refuter) ran
over both diffs before either commit: **21 findings raised, 19
CONFIRMED, 2 refuted — all 19 fixed and re-controlled** before any
commit was made. The two that mattered most were both about MY claims,
not my code:

1. **The slash guard was wrong** (confirmed against `config.py:54,408`,
   verified by me before deleting): the library prepends
   `estate.provider` (`gsj/`) to every model name itself, so
   `pi_harness.py:104`'s provider/model raise is unreachable through the
   one YAML and unslashed served names simply work. The guard was
   deleted; every doc that transmitted the false consequence (four
   places) was corrected to the actual mechanism.
2. **"chains_total is silent" was false — including in the CP prompt
   itself** (confirmed against `checks.py:295`): `G7:chains_total_ne_1`
   quarantines every split-chain episode, fail-closed. Every surface
   that said "no gate fails / nothing surfaces" (seven places across
   both repos, this report's own draft included) was rewritten to the
   true framing: caught after each episode is spent — the probe is the
   before.

The code findings, all reproduced by the verifiers against mock
endpoints before I fixed them: a `StopIteration` crash on a
history-TRUNCATING template (the exact class the probe hunts —
`next()` now defaults to the truncation index), 200-with-`null`
`/tokenize` bodies crashing the deriver's never-raise contract (both
gates now type-check), unvalidated config scalar types crashing a
multi-minute `up` at its last step (now `die()` at load), YAML-boolean
glue ids coerced to `[1, 0]` (bools now rejected), unguarded reads of
generated files crashing the diagnostic (now degrade to rows), and the
preflight's own cures being un-followable as written (the glue cure
never cleared the FAIL — the row is now armed-glue-aware; the eot cure
lacked the re-run; the tail cure pointed at a file that does not exist
in the demo; two MODEL-SURFACE recipes the WARNs promised did not exist
— both written). Also corrected on review evidence: A-30's eos claims
re-scoped to the two config files actually read this session
(`[151645, 151643]` / scalar `151645`; Llama `[128001, 128008,
128009]`), the "vacuous empty tail" rationale (checks.py treats it
fail-closed), "/tokenize is all it needs" (it needs `/detokenize` too),
and row 42's Coder attribution (the mode-pair was then actually
measured: byte-identical). After the fixes: compile clean, negative
tests die loudly at load, and the full control suite re-ran green —
derivation REPRODUCED on all values in both modes, preflight all-ok
with the sampling warn the only warn.

### DoD, run and shown

```
$ git diff HEAD -- gsj_rollout/ vendor/ corpus/ mcp-service/ forgejo/ \
    spike/ staging/ tests/ .github/
(empty)
$ ./.venv/bin/python -m pytest -q
161 passed in 16.32s
# Step 1's classification table                       (§Step 1 above — the CP)
# the template probe, run against the reference — verdict:
#   [ ok ] template   turn 2's prompt render strictly extends turn 1's
#                     (40 -> 93 ids) — multi-turn episodes reconstruct as one chain
#   (and the probe logic reproduces CP-04's divergence against the embedded
#    template: index 37 of 41 = n-4, exactly [151667, 271, 151668, 271])
# the derivation vs the pins (forced foreign branch, live reference stack):
#   off:    eot=151645 REPRODUCED | tail ids REPRODUCED | tail text REPRODUCED | G4 empty by design
#   medium: eot=151645 REPRODUCED | tail ids REPRODUCED | tail text REPRODUCED | mode: thinking-on
# the second tokenizer's derived values               (§Step 6 table: Llama-3.1
#   official — prefix TRUE, tail [128006, 78191, 128007, 271], EOT 128009)
# the "what changes" document: gsj-rollout-demo docs/MODEL-SURFACE.md
$ git -C ~/mg/workspace/gsj-rollout-demo log --oneline | head -3
f546abd the model-dependent surface: derive from the endpoint, probe the template, write down what changes
96f2329 what the first true stranger hit: the arm64 wall, the outcome line, the portable export, the missing sentences
cc9aeff the endpoint preflight, the thinking knob, and the walkthrough
$ test -f docs/reports/CP-37.md && echo OK
OK
$ git status --porcelain      # after commit — and the demo repo is clean too
(empty)
```
