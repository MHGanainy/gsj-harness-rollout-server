### CP-00 REPORT
status: done
scope_drift: none
files: 16 files, all new (initial commit): CLAUDE.md, README.md, pyproject.toml, .gitignore, docs/CHARTER.md, docs/decisions/ADR-0001-repo-identity.md, docs/reports/CP-00.md, prompts/CP-00.md, gsj_rollout/{__init__,pi_harness,receiver,checks,config,client,cli}.py, tests/test_scaffold.py — ~670 insertions
tests: pytest -q → 1 passed; DoD also green: `import gsj_rollout` prints 0.1.0, `gsj-rollout --help` exits 0, CLAUDE.md has exactly the six headers in order, 11 `| A-` rows and 31 `| N` gap rows in docs/CHARTER.md, ADR-0001 present
adrs: ADR-0001        assumptions: A-1…A-11 seeded
gap_register: all 31 rows seeded — 24 TBD, 6 DROPPED (16 quarantine, 17 store, 18 ready/mix, 19 staleness, 20 serve accounting, 21 collation), 1 GAP (31 tool roster visible in config)
questions:
  - `<PREDECESSOR_PATH>` arrived as an unfilled placeholder → default applied: `/Users/elganayni/mg/workspace/gsj-envloader`, verified present at tag v0.8.0 (HEAD = CP-35 release commit).
  - The prompt does not say where the new repo lives, and the CP was invoked from inside the frozen predecessor → default applied: sibling directory `/Users/elganayni/mg/workspace/gsj-harness-rollout-server`, fresh `git init -b main` (law 3 forbids nesting it in the predecessor).
  - Corrupted span: Step 1's header list reads "`## Scope lawsssumptions`" and names five items for "six sections" → reconstructed as `## Scope laws` · `## Assumptions`.
  - Corrupted span: "A chec pushes past it must stop and justify" → "A checkpoint that pushes past it must stop and justify".
  - Corrupted span: "saved verbatim to `prompts/CP-Xnd committed with the CP" → "`prompts/CP-XX.md` and committed with the CP".
  - Corrupted span: "(R2) if confused, search rt" → "search for prior art" (matches the predecessor's R2).
  - Corrupted span: §3's third table "*writ_harness.py` 50–150, …" → a table titled *written here* whose first row is `pi_harness.py` 50–150.
  - Corrupted span: "vendoring a release-less branch is sust components transfer unmodified" → two merged assumptions, reconstructed as A-8 "…is sustainable" and A-9 "the carried components transfer unmodified".
  - Corrupted span: "CP-11 surviving gateP-12 the verdict" → "CP-11 surviving gates · CP-12 the verdict".
  - Corrupted span: "if our harness bakes ther into argv" → "bakes the roster into argv".
  - Corrupted span: the Step 3 tree line "`│   � reports/CP-00.md`" → `docs/` holds `CHARTER.md`, `decisions/`, `reports/CP-00.md`.
  - Corrupted span: ADR-0001's "three  rather than two packages" → "one package rather than two", from the surviving rationale (Polar's `slime_bridge` precedent; light trainer install); recorded as such in ADR-0001.
  - Corrupted span: DoD line "grep -c '^| A-' .md" → target reconstructed as `docs/CHARTER.md`.
  - Tension: Step 3 says modules are "empty but for a docstring" while the DoD asserts `gsj_rollout.__version__` and a zero-exit `--help` → default applied: `__init__.py` carries `__version__ = "0.1.0"` and `cli.py` the sanctioned stub `main()`; nothing else anywhere.
next: CP-01 — the moves (corpus/, mcp-service/, forgejo/, pins tooling, corpus contract), flipping gap rows 1/3/23 toward PARITY; define the ownership boundary of mcp-service/ before moving it (see notes).

#### notes

**Gap register as seeded.** 31 rows. DROPPED (all deliberate, owner named in-row): quarantine, store, ready/mix, staleness, serve accounting, collation — the store-coupled serving stack is the trainer's problem. GAP: row 31, *tool roster visible in config* — with the accurate mechanism recorded: the predecessor's `tools_allowlist` is a required `TaskConfig` field rendered to `--tools` argv, and G3 hashes the roster **as sent on the wire**, so a harness that hard-codes the roster in argv leaves G3 without a pinned input. Everything else TBD, each row naming the CP that resolves it.

**Assumptions.** A-1 prefix_merging correctness (not line-verified) · A-2 proxy handles pi 0.83.0 · A-3 pi package identity (RESOLVED) · A-4 cutoff injectable via `run_steps()` · A-5 callback payload sufficiency · A-6 slime OPD against Polar traces · A-7 the four reported fork defects UNVERIFIED, not false · A-8 release-less vendoring sustainable · A-9 carried components transfer unmodified · A-10 reward sparsity is method/scale · A-11 Apptainer deferred, Docker keeps CP-09 single-variable.

**What surprised me reading the predecessor for §7.**

- G3 does **not** hash the `tools_allowlist` config field — it hashes the tools array as sent on the wire (`tools_wire`, canonical JSON, the one hasher shared with the pin layer). The prompt's GAP framing ("a hashed config field") is close but not exact; the register records the wire-roster mechanism, which is stricter: matching it requires reproducing key order, whitespace, and schema shape of the wire encoding.
- The gates use four different hashing conventions (UTF-8 text sha256 for G1/G2; canonical-JSON sha256 for G3/G7; git-blob OID sha1 + template-string sha256 for G4; no hash at all for G6 — a verbatim tail compared with `endswith`), plus a byte-stable `G{n}:{slug}` failure vocabulary that downstream forensics greps. `checks.py` inherits all of that exactly.
- G7 is not a static config check: it also demands a gateway-side chain snapshot of exactly (1 active chain, 0 rollbacks, 0 dropped trainable tokens, 1 finalized trajectory). Polar's capture layer must surface an equivalent or G7 fails closed — now implicit in gap row 15.
- H-41 is the strongest argument for `checks.py`: sglang was silently load-bearing for tool-call parsing, and its *absence* produced gates-green but tool-free episodes, measured live. Validation must fail loudly when the parser stack is incomplete — plausible-looking degenerate traces are the enemy.
- The predecessor's logprob discipline is that `rollout_log_probs` are finite and ≤ 0 everywhere, with a literal `0.0` placeholder at mask==0 positions — so the fork's reported `-9999.0` sentinel (A-7) is precisely what predecessor-style validation rejects; the guard translates into `checks.py` almost verbatim.
- The G2 approved set is path-sensitive (the checkout path is the only case-dependent span of the system prompt) and was re-derived at nearly every image, rename, or mount change across the predecessor's history — the clearest single illustration of why the capture layer was expensive. Polar's different mount paths will change every G2 hash on day one; expect the derive→re-pin walk at CP-06/CP-07.
- The carried-over line counts in §3 (~1,076 corpus, ~1,038 mcp-service, ~120 forgejo) do not match raw counts over the trees (1,917 · ~852k — the latter clearly an embedded venv/vendored artifacts · 182). The figures evidently count own code only; CP-01 must define the ownership boundary before moving `mcp-service/`.
- No Apptainer anywhere in the predecessor — its sandbox story is Docker-only, wrapped around H200-specific networking archaeology (static container IP for Forgejo, `host.docker.internal` for MCP, SSH-tunnel bind for serving) that gap row 3 warns against inheriting blindly.
- Docker was bolted *inside* the unchanged `GsjCaseTask` lifecycle rather than replacing it, and dismantling the dev-harness accretion later took two dedicated checkpoints (CP-32 retirement, CP-34 purge) — the accretion pattern this repo's size budget exists to prevent.
