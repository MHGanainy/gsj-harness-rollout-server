# CP-40 — M13b: the consumer surface, and the orphans

### CP-40 REPORT
status: done
scope_drift: none in the frozen paths — `gsj_rollout/`, `vendor/`,
`corpus/`, `mcp-service/`, `forgejo/`, `pins/`, `spike/`, `staging/`,
`tests/`, `.github/` all diff-empty, root suite byte-unchanged at 161,
census untouched at 1,999/2,000. The lifts are the prompt's own (both
consumer repos unrestricted, `docs/**` here). Judgment riders taken
inside the lift, each under `questions:`: S14's examples half and the
`smoke.py` table line (same-pass accuracy), §8 rule 9 built as one
sentence (Step 3's own one-line allowance), demo F-69 minted (gap row
2's demo half), and the stranger re-read's round-2 yield folded in the
same sitting (F-52, F-18, the bridge `cd` depths, the PyPI-first run
books, the dist-trap bullet). One untracked side effect stated
plainly: the install proof built
`dist/gsj_harness_rollout_server-0.1.2-py3-none-any.whl` in the
library's gitignored `dist/` — it sits beside the stale 0.1.1 that
CP-41 removes, and it is what the glob proof installed.
files: library — docs/prompts/CP-40.md (new), docs/reports/CP-40.md
(new), docs/CHARTER.md (§7 [CP-40] block; rows 2 and 22 annotated in
place; §8 rule 9), docs/VERDICT.md (wishlist rows 9, 10, 16, 19-pins,
22, 24, 26, 38 dispositioned in place). External — the examples repo,
one commit: README.md, FINDINGS.md (eleven cells + the continuation
pointer), example_project/{RUNBOOK.md, config.yaml, install.sh,
requirements.txt}, slime_bridge/{README.md, requirements.txt,
cp17_loop/README.md}, verl_bridge/{README.md, requirements.txt}. The
demo repo, one commit: FINDINGS.md (F-69; header provenance; next
fresh id F-70).
tests: ./.venv/bin/python -m pytest -q → 161 passed (suite
byte-unchanged). The corrected bridge installs proven by execution,
both shapes: the sibling-glob form installed 0.1.2 with the stale
0.1.1 sitting beside it (the newest-wheel pick working as written) and
the PyPI form installed 0.1.2 from the live index — slime suite 14
passed both times.
adrs: none        assumptions: none
gap_register: §7 [CP-40] block appended; rows 2 and 22 annotated in
place; NO status moves — tally unchanged (21 PARITY · 7 DROPPED ·
2 GAP · 1 BETTER · 1 TBD). §8 gains rule 9: parks name observable
events, and every CP's register pass checks them. Wishlist: rows 10,
16 CLOSED and row 24 CLOSED for the evaluation (reasons in-row); row 9
and row 22's F-39 half dispositioned (production-prep transfer;
declined for cost); rows 19 and 26 re-parked on real events; row 38
cross-referenced to gap row 22. External register: nine lagging cells
caught up (F-02, F-04, F-18, F-20, F-21, F-23, F-25, F-43, F-52),
F-42's rider-vouching corrected, F-44 re-owned; F-69 minted demo-side;
next fresh id F-70 stated identically in both files.
questions:
  - Corrupted span, Read-first ("`docs/re-39.md` §what this CP does
    not do") — reconstructed: `docs/reports/CP-39.md` §"What this CP
    does not do". Applied.
  - Corrupted span, S3 ("teaches three bugs CP-2odule-form no-op…") —
    reconstructed: "teaches three bugs CP-27 fixed: the module-form
    no-op, the `/v1` suffix, the port/`public_url` mismatch" (matches
    audit S3). Applied.
  - Corrupted span, Step 2 ("kept with an owner and a trigger that has
    not firee-parked with a real condition") — reconstructed as the
    three-outcome rule: "kept with an owner and a trigger that has not
    fired, re-parked with a real condition, or closed with the
    reason". Applied to every orphan.
  - Corrupted span, O4 ("ths-Forgejo-read residual") — reconstructed:
    "the anonymous-Forgejo-read residual" (audit O4's words). Applied.
  - Corrupted span, Step 3 ("rows whoseait expires go quiet") — "rows
    whose wait expires go quiet". Applied.
  - Corrupted span, DoD ("git lne origin/main -1") — `git log
    --oneline origin/main -1`. Applied.
  - The prompt says rows 2 and 22 are "the last two GAPs in the
    register"; the two GAP-status rows are 12 and 22 (rows 4 and 14
    closed at CP-24/CP-23; row 2 is PARITY carrying the estate
    residual). Default applied: read as "the two open estate-posture
    residuals" — exactly audit O4/O5's referents — and both answered
    plainly; the correction is recorded in the §7 block rather than
    silently absorbed.
  - W5 says "install.sh already solved this with a glob; use it" —
    the glob is used, but NOT install.sh's `${WHEEL[0]}` first-match
    selection: with two wheels in `dist/` the lexical first is the
    OLD one (the run-one-release-behind trap CP-39 recorded for
    CP-41), and the DoD's proof run would have installed 0.1.1. The
    bridge commands take the NEWEST (`ls -t | head -n1`); the
    divergence is stated in the RUNBOOK's new dist-trap bullet and in
    the §7 block.
  - S14's examples half (RUNBOOK root count 150 → 161, config.yaml
    zero-logprob band 4–25% → 1.8–25%) and O9's `smoke.py` table line
    are not in Step 1's citation list — taken anyway, because Step 1's
    own bar is "accurate, not just corrected" and all three sit in
    files this pass had open. Recorded, not silent.
  - Step 3 says "do not build it here unless it is genuinely one
    line" — rule 9 is one sentence (wrapped) with a provenance
    parenthetical, and it was built; the fuller mechanism (a
    `waiting-on:` field + a report-template line) is proposed under
    notes with its cost, not built.
  - F-69 minted in the demo register for gap row 2's demo half — the
    prompt's "both consumer repos take their own commits" wants a demo
    commit, and a register row is the durable home for a posture no
    demo document stated; severity vocabulary is CP-36's with the
    provenance tagged ("minted by the audit, not by a run pass").
  - Wishlist 19(a)/audit S8 (the pins stale notes) needs a `pins/`
    lift no scheduled CP owns — the register now ASKS CP-41's sitting
    to take it beside its other in-file corrections, recorded in-row
    so that declining is a visible decision under rule 9. CP-41's
    prompt stays its author's.
next: CP-41 — the namespace collisions (`forgejo/`'s five citations
W2, the corpus-contract header C2, `checks.py:97` W9, the golden
MANIFESTs M5), the ADR amendments (0005/C8, 0011 and 0020/S12,
0019/C7), and the residue removal (the `dist/` 0.1.1 wheel — the
examples surprise list now warns about the trap it powers; deleting it
leaves this CP's 0.1.2 build, which matches PyPI). Plus the recorded
ask: wishlist 19(a)/S8's pins notes at the same sitting.

## Notes

**The refresh, then the stranger re-read — and what the re-read was
for.** Pass one closed the prompt's list: the two bridge install
commands stopped naming a 0.1.0 wheel that ENOENTs and now glob the
newest sibling build (W5); the five v0.1.0-as-current texts say
0.1.2/CP-34, and the slime requirements' "no release channel exists
yet" block now names PyPI (S2); the RUNBOOK's closing surprise list
dropped the three CP-27-fixed bugs as named departures and kept what
is still true — the MCP-secret bullet stays (S3); the slime rider's
pins sentence tells the both-mode-files truth (S6); seven register
cells caught up with the checkpoints that outran them (W4/S5/C5); and
the two Qwen-bound recipes are family-labelled — the four legs' VALUES
named family-bound per CP-38, the EOT recipe deriving the family's OWN
token with Llama's `<|eot_id|>` → 128009 as the worked example and the
plain statement that `<|im_end|>` has no answer outside ChatML (S4).
Then the required stranger re-read ran against the refreshed tree, and
its verdict was the honest one: **accurate with residue** — the
refresh's own claims all verified (a parallel adversarial fact-check
confirmed all eleven against primary evidence: the validators at
`config.py:63/:179` with their named tests, the `__main__` guard and
its subprocess tests, the flush calls, `model_revision`, both pins
files inside the built 0.1.2 wheel, CP-32's 0.016546/0.35%/2.95×,
CP-17's A-26 HOLDS, CP-38's `llama3_json`/128009/1.8%, the 161, and
`smoke.py`'s CP-17 provenance) — but the re-read caught four things
pass one missed, and they were folded in before the commit: an
**eighth and ninth lagging cell** (F-52 still taught §8 rule 8's
protocol sentence as unwritten — it was written at CP-33, ignored
three CPs, widened at CP-39; F-18 carried "~9.5 min" against its own
cited report's thrice-stated **6m37s**), the bridge recipes' **wrong
`cd` depth** (one level short from the bridge dir, in the exact blocks
W5 had just edited), the bridge run books' **missing PyPI path** (both
installed only from a sibling `dist/` while their own requirements
headers — updated the same pass — name PyPI; both now lead with the
PyPI line, proven by a second run), and the **dist-override trap**
stated in no consumer document (install.sh prefers any sibling wheel
and takes the lexical FIRST — now the surprise list's newest bullet,
including why the bridge commands deliberately pick newest instead).
The answer to Step 1's closing question, after round 2: **yes,
accurate — not just corrected.** What a stranger reads now matches
what the library ships, what PyPI serves, and what the reports
measured, and the two remaining register-cell "OPEN"s a stranger
encounters (F-44's grader, the demo's F-69) are open on purpose with
named owners. Two glosses were left standing, deliberately:
RUNBOOK's "(current since CP-29)" (true as written; the CP-36–38
staleness window is O1's history, closed at CP-39) and the historical
0.1.0 notes, which are framed as history and stay.

**The orphans — each disposition and its reasoning (this is the CP).**

- **O3 / C-3 — CLOSED, satisfied in substance by CP-32.** C-3 existed
  to establish that thinking-on collection at scale works and what it
  costs; CP-32's 72-episode pooled-ON run put every one of those
  numbers on the record — 72/72 qualified, 2.95× pooled wall-clock,
  replay mean|Δ| 0.016546 in-band, reward density 1/72, one real
  optimizer step — and nothing about the mechanism remains unmeasured.
  What C-3 imagined beyond that, a training-scale ON corpus, is
  production collection: it needs an estate and a training goal, not
  an evaluation checkpoint. Disposition note lives in the §7 [CP-40]
  block — the note the audit found missing. Closed, not re-parked,
  because the evaluation has no further question C-3 answers.
- **O4 / gap row 2 — DECIDED: production-prep, not permanently
  accepted** (annotated in place). Eight bring-ups ran with anonymous
  Forgejo read because every one was an evaluation estate — synthetic
  corpus, nothing measured whose value depended on adversarial cutoff
  evasion. That reasoning expires at the first bring-up someone
  intends to train from: the credential/egress choice (the cure named
  since CP-11) is that bring-up's, made before episodes run. The
  demo's widening is confirmed and now recorded on the demo's own
  register (F-69): compose sets no sign-in requirement, the
  per-episode clone URLs embed no credentials — anonymous read is
  load-bearing there — and the no-host-ports mitigation is one the
  sandboxed agent lives inside of, so it mitigates nothing against
  the agent.
- **O5 / gap row 22 — DECIDED: production-prep, not permanently
  accepted** (annotated in place). The cure was named "at CP-04′
  bring-up" and CP-04′ ran without it; no evaluation CP owns an estate
  to build the provenance surface on, and pretending a future lift
  will is the park shape rule 9 now forbids. Per-episode binding is
  real work the first production bring-up must do — permanently
  accepting it would leave trained-on traces unattributable to engine
  identity, the exact F1 lesson. The sampling half's rebirth as
  wishlist row 38 is cross-referenced both ways: one residual,
  recorded twice, now owned once, by production bring-up.
- **O6 / wishlist row 19 (pins seam) — re-parked on a real event,
  half-owned.** "Cheap at the next `pins/` lift" failed as a trigger —
  CP-30 lifted `derive_pins.py` itself and the row did not move. New
  terms: (a), the stale ADR-0003 note (= audit S8), goes to the next
  conscious pins lift with the register's recorded ask that CP-41's
  sitting take it; (b) and (c) wait on the first REAL card edit on any
  estate — an event that has not occurred anywhere — with the row as
  that editor's recipe.
- **O6 / wishlist row 10 (skills interior strictness) — CLOSED.** The
  documented exception IS the decision: contract v2 states it
  honestly, two corpus lifts passed the validator with the file open
  and neither tightened it, and no consumer row has hit it in 26
  checkpoints. Reopens only on a real stranger hit, as a contract
  decision.
- **O7 / F-44 (the grader) — re-owned.** The CP-32 park expired with
  the A/B it protected; CP-32 also measured the lottery not firing at
  that scale (144 artifact dirs, none multi-`.md`), so the defect is
  real and latent. New owner and trigger: whoever runs the next
  reward-graded collection fixes the grader FIRST — the declared
  per-record artifact path, the predecessor's `rlvr/grade.py:66`
  shape — and no re-park without a named event. Not fixed here: Step
  2 allows dispositions, and a grading-semantics change belongs to a
  CP that will run the grader.
- **O8 / wishlist row 24 (secret probe) — CLOSED for the
  evaluation.** The cadence it deferred to has not existed since
  CP-15 and nothing schedules one; this row was the only reason such
  a lift would open. Its evaluative value is banked — F-22 and the
  row name the failure shape, the measured cost, and the probe
  design. Building the probe is adoption hardening; it joins the
  production-prep set with gap rows 2/22 and does not reopen inside
  the evaluation.
- **O8 / wishlist row 26 (the ULP flake) — kept, on a live trigger.**
  The row's unblock condition fired at CP-33 and was spent evidencing
  the flake EXISTS; the magnitude is still missing because the oracle
  prints no numbers on failure. New terms: the NEXT CI firing of the
  flake, the CP standing nearest takes the `mcp-service/` lift that
  makes the test print |Δ|/cosine — §4 demands the tolerance be
  decided with data. A red run is loud; this trigger cannot expire
  silently.
- **O9 / wishlist row 9 (sandbox-side card hash) — production-prep,
  transferred.** The oldest open row; no evaluation CP is scoped to
  lift `pi_harness.py` for it, and the exposure is bounded — since
  CP-13a the workspace echo binds each episode to the exact commit
  and tree it read, so what G1 cannot see is a tampered estate repo
  between `verify` runs. The row stays adoption's instrument.
- **O9 / wishlist row 22's F-39 half (pins warn-once) — CLOSED,
  declined for cost.** A cosmetic silence is not worth an ADR-0021
  allowance plus a `checks.py` line at 528/528 with headroom 1; the
  warning is truthful, merely repetitive. Reopens only if a consumer
  row records the repetition as more than noise — and then as its own
  ADR, as always stated.
- **O9 / wishlist row 16 (carried-patch tests in CI) — CLOSED, with
  the reason.** The four tests can only regress when `vendor/`
  changes; `vendor/` changes only at a re-vendor; REVENDOR.md step 5
  makes them mandatory exactly there (M7a's door), and the audit
  re-ran them by hand: 25 passed. A CI job would re-execute an
  immutable tree on every push — cost without coverage. Reopens if a
  re-vendor ever lands without the recorded step-5 run.
- **O9's minor half** — `smoke.py` joined its own run book's file
  table, one line.

**Rows 2 and 22, answered plainly:** production-prep, both — neither
is permanently accepted, and neither is any evaluation CP's to fix.
The owner is the first production bring-up; the triggers are real
events that have not fired; the designs are named in-row (credentialed
clone URLs or egress policy; the estate provenance surface binding
serve argv, generation-config and codec identity per episode). "Open"
now means exactly that, instead of meaning nothing.

**Step 3 — the mechanic, the smallest catch, and its cost.** Decay
mode 2 happens because annotation is event-driven by checkpoints
standing at the DONE side; nothing ever stands at the OPEN side. Built
here (the one-line allowance): **§8 rule 9** — parks name observable
events, and every CP's register pass checks them; a parked row whose
event has occurred moves in the CP that finds it. Proposed, not
built — the fuller version: (a) a literal grep-able `waiting-on:`
token in every parked row, making the scan mechanical instead of
judgment-driven, and (b) one line in the report template
(`gap_register:` gains "parked triggers scanned: none fired | <list>")
so the scan is a DoD checkbox rather than a memory. Cost, honestly:
the retrofit is ~a dozen open rows once (this CP effectively did it
for the orphan set); per-CP, minutes — reading the open rows' trigger
lines, a set this CP just shrank and closures keep shrinking; the
template line is a CLAUDE.md edit that was NOT in this CP's
freeze-lift, so it waits for the next CLAUDE.md lift. What even the
fuller version cannot catch: a trigger firing while no CP runs —
bounded staleness (caught at the next CP's scan) instead of unbounded
(caught at the next audit). That bound is the point.

**The push outcomes (§8 rule 8, as amended).** All three repos
committed and pushed this CP; outcomes stated per repo in the DoD
transcript below the report: the library (this commit), the examples
repo (one commit), the demo repo (one commit) — each verified at the
remote with no ahead count.
