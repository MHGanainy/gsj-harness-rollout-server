# VERDICT — should we adopt Polar? (CP-12, closing M3)

Date: 2026-08-09. Status at CP-12: **ADOPT PROVISIONALLY**.
**Status now (2026-08-11, CP-17): ADOPT — both converting conditions are
met.** (2026-08-25, CP-45: the predecessor is archived; its frozen-alive
fallback term had already expired at that conversion — the last dated
section below.) The two dated sections at the end of this document record how each
condition closed; §5's body is preserved as written so the conversion can
be audited against the conditions as they were stated in advance, not as
they might be remembered. This document is standalone: it cites the
checkpoint reports (`docs/reports/CP-XX.md`) for evidence but does not
require reading them.

## What was tested

`gsj-envloader` (the predecessor — frozen; the fallback while the adopt
was provisional, archived since CP-45) runs our
corpus episodes with ~1,800 lines of its own episode execution and
capture: a checkout→run→harvest→reset lifecycle with docker bolted
inside, per-episode gateway sessions, and a finalize pipeline. That layer
worked and was externally verified, but it was expensive: approved sets
re-derived at nearly every image or mount change, a capture transport
that changed three times, a silently load-bearing parser dependency that
once produced gates-green but tool-free episodes (H-41), and two
dedicated checkpoints just to dismantle the dev harness around it.

This repo tested one bet across twelve planned checkpoints — thirteen
executed, CP-11 having split in two (CP-00 – CP-11b) — in three
milestones: **can NVIDIA's Polar own that layer**, leaving us a thin
shell — the corpus, the temporally-scoped retrieval service, a harness
for our pinned agent (pi 0.83.0), and the validation? Episodes run
against an *estate* — the Forgejo git host, the MCP (Model Context
Protocol) retrieval service, and the inference engine brought up around
the repo. The success criterion (charter §5): *one real episode against
our corpus, through our MCP service, with the cutoff enforced, producing
a trace whose token ids, `loss_mask`, and logprobs verifiably match the
golden reference the predecessor produced for the same task.* Five
abandon conditions were stated in advance (charter §9) so the decision
would not be made under sunk cost.

---

## 1. What did Polar give us?

The whole layer the predecessor paid ~1,800 lines and years of
maintenance for, plus capabilities nobody here would have built. At the
pin, Polar is ~14,200 lines of Python (measured over `src/polar`); we
drive it with 1,496 lines of shell, of which only ~400 (the harness, 237;
the builder subclass, 159) touch episode execution at all.

- **The episode lifecycle.** Sandbox start/exec/upload/download/stop
  behind a runtime-agnostic interface, per-session async gateway workers,
  heartbeats, timeouts, teardown. Ran live from the first vendor: 4
  parallel sandboxes with clean teardown (CP-03), our pi under
  DockerRuntime end to end on real corpus episodes (CP-07), seven
  submissions in one collection loop (CP-09). We wrote zero lifecycle
  code — `pi_harness.py` is contract (clone, mint token, render config,
  download artifacts), not machinery.
- **Proxy capture across provider dialects.** The gateway proxied pi
  0.83.0's traffic **unmodified** — zero translation errors, `messages`
  and `tools` byte-identical from harness to wire (CP-06's three-way
  diff). It absorbed pi's unconditional streaming with a synthetic SSE
  reply pi parses, and its capture normalizers read vLLM, SGLang, and
  OpenAI token-id dialects. The predecessor's capture transport changed
  three times; here it is Polar's problem.
- **Trajectory reconstruction** (`prefix_merging`) — the central bet.
  Line-verified faithful to its paper's algorithm with no tokenizer
  anywhere in the builder, so assistant tokens are engine-sampled ids,
  never a re-rendering (CP-05, adversarially re-verified). Then verified
  empirically against the predecessor's golden reference on the same
  task: masks exact, `prompt_ids` byte-identical, capture agreement at
  mean |Δ| = 0.000114 on identical contexts (CP-09; §4 below).
- **Async staging.** Push-by-callback into our receiver plus an
  independent poll path — the trainer never blocks on collection (CP-08).
- **Two runtimes.** Docker (exercised throughout) and Apptainer (native
  at the pin, unexercised here — register row 30).
- **Eleven harness preset modules** at the pin (claude_code, codex,
  gemini_cli, hermes, mini_swe_agent, openclaw, opencode, openhands_sdk,
  pi, qwen_code, shell — counted at CP-12), plus the two seams that
  matter more than any preset: `import_path` loaded **our** non-preset
  harness with zero vendored edits (CP-06), and the builder registry let
  a validating subclass insert itself into trajectory construction with
  zero patches (CP-05/CP-07). The predecessor is single-harness by
  design — register row 29 is this repo's one BETTER.
- **The parts nobody would have built**: background evaluator-runtime
  prewarming during the agent run, the Apptainer runtime, and the rollout
  API itself (manager, dispatch, task polling, a dashboard) — pure upside
  we inherit without owning.

## 2. What did it cost?

- **Three carried patches, forever.** Upstream at the pin has no
  non-agent completion filter at all (every auxiliary harness call would
  become a trainable trace carrying session reward), treats a mid-chain
  abort as a clean COMPLETED session, and loses in-memory metadata before
  persistence (CP-02: D1/D3/D5, all confirmed at source). None of the
  fixes exists on any upstream branch; none cherry-picks cleanly. P1
  (filter), P2 (abort→ERROR), P3 (policy-version stamping) are hand
  adaptations we re-anchor on every re-vendor. P1's protection then
  measured **inert for pi** — pi streams unconditionally, defeating the
  filter's shape test — so our builder subclass carries the replacement
  check (CP-06).
- **The vendoring posture.** No releases, no tags, squash-merges on
  `stable`, and the real development line (`polar`) 80 commits ahead with
  a pending `prefix_merging` refactor (+49/−156) that will force
  re-porting two patches. First vendor cost ~one working day; re-vendor
  is estimated at ~half a day with the recipe and symbol tripwires in
  `vendor/REVENDOR.md` (CP-03) — an estimate, not yet a measurement: no
  second vendor has been executed (assumption A-8 stays live).
- **Silent degradation is the layer's universal failure mode.** The
  source audit catalogued nine modes (S1–S9, CP-05) in which the builder
  emits a clean-looking `COMPLETED` trajectory that is degenerate —
  split chains, truncated merges, mis-split interstitials, amputated
  tails, mixed policy versions — with **nothing at the pin raising an
  error for any of them**, and zero value-level logprob validation
  anywhere in the tree (grep-verified twice). Three silent classes were
  hit live, not hypothesized: **S1** at CP-03 (a token-id-less backend
  yielded 10 single-completion chains reported `full=10, truncated=0`),
  **S4′** at CP-07 (the Qwen3 no-think template's asymmetric glue split
  every multi-turn episode into per-turn chains, silently COMPLETED),
  and — beyond the catalogue — **F1** at CP-09 (a snapshot shipping no
  `generation_config.json` silently sampled at neutral defaults; no
  trace-side evidence exists even in principle).
- **The validation we wrote because of it.** `checks.py` (407 lines,
  both wire legs), the validating builder subclass (159), and the
  receiver's admission layer (143) exist in this size *because* the
  layer degrades silently rather than failing — nearly half of our
  1,496 lines, with the pins walk (re-deriving every approved-set hash
  from live evidence, `pins/derive_pins.py`) and the checks spec beside
  them outside the count. The shell is thin on execution and thick on
  evidence, which is the correct shape but was not free.
- **The honest half of the bill.** Much of this is the price of
  harness-agnosticism, not defect. A layer that must accept any harness
  and any engine dialect cannot fail loud on shapes it does not know;
  degrade-to-empty is what makes the retokenization guarantee general.
  The predecessor had its own version of the same disease (H-41's
  silently load-bearing parser). The difference is that here the cures
  are all in **our** code — config, subclass, checks — which is exactly
  the architecture the charter mandates.

## 3. What do we still own, and is it the right set?

`corpus/` (the source of truth + ingestion), `mcp-service/` (retrieval
with per-episode JWT cutoffs, enforced server-side from verified claims —
the tamper probe returns 401, CP-07), `forgejo/` (git-host bring-up),
`pi_harness.py`, `builder.py`, `checks.py`, and the config/CLI/client
surfaces. Scrutinized both ways:

- **Should Polar be doing any of it?** Partly, yes — and the seam
  handles it. The glue-stitch repair in `builder.py` (ADR-0007) works
  around a template-asymmetry failure of Polar's grouping that other
  trainer and RL frameworks (verl, OpenRLHF, NeMo-RL) have open in the
  same class, and Polar's own
  builder README describes message-level grouping that does not exist at
  the pin — if upstream's pending refactor lands it, our stitch retires
  (it is already scheduled to go dormant at the template flip). The
  session-level S-checks arguably belong upstream too; at the pin nobody
  validates, so we must, and the zero-patch subclass is the right
  insertion point either way. The rest — corpus, MCP cutoffs, the
  harness contract — is our world, not the rollout layer; Polar's pi
  preset (pinned at 0.67.68) knows nothing of our estate, token mint, or
  clone discipline. Correctly ours.
- **Should a trainer be doing any of it instead?** The client leg
  already IS the trainer's (law 6 runs `checks.py` on both sides;
  `client.py` is the reference implementation of the trainer's leg and
  could migrate wholesale). Storage, retention, mixing, staleness,
  collation were dropped to the trainer at CP-00 and stayed dropped. The
  receiver is the one piece a skeptic could challenge — it exists so bad
  traces die at the source with forensics attached rather than reaching
  a trainer at all; 143 lines is a defensible price for that. Nothing we
  hold fails the scope law: nothing stores, schedules, scores, weights,
  versions, or trains.

## 4. Does it work?

**The success criterion is MET, on the Mac pair** — CP-04's golden
reference plus CP-09's comparison, both run on a Mac estate; primed
checkpoint numbers (CP-04′/CP-09′) are their pending H200 re-runs —
(CP-09, verdict PASS WITH FINDINGS). Exactly what that means: one real
episode
(`case_0001` / `timestep-12` / `skill:summarize`) ran through
`gsj-rollout submit` against our corpus and MCP service; the cutoff held
(every search-result page ≤ 12, the tampered token rejected 401);
the collected trace was compared field-by-field against the golden
reference the predecessor produced for the same triple, under a written
contract (`docs/golden/COMPARISON.md`):

- `loss_mask` semantics **exact** (zero tolerance — mask 1 exactly on
  engine-sampled spans, boundaries aligned to turn structure, on both
  traces);
- sampling-independent tokens **byte-identical** — `prompt_ids`
  2965/2965, and the interstitial glue vocabulary identical across
  stacks (sampled ids are compared per-trace by decode fidelity, exact
  on both — sampling makes cross-stack id equality unreachable by
  design, measured at CP-04);
- logprob capture agreeing with the predecessor's at mean
  |Δ| = 0.000114 on identical-context positions, captured values proven
  RAW on both stacks;
- **no divergence attributable to Polar.** The findings are ours (F1
  engine config, F2 stitch semantics) and the platform's (F3/F4/F5 — a
  teacher-forcing gap and MLX bf16 numerics appearing symmetrically on
  both stacks).

What it does **not** mean: Mac numerics do not govern — the platform
cannot teacher-force, so the replay leg ran beside the engine, and by
assumption A-16 the H200 pair re-establishes production numbers. One
model (Qwen3-0.6B), one task family, two-turn episodes, thinking off.
And the gates' pins — the gates being the predecessor's seven
episode-acceptance checks G1–G7 (hash and evidence rules over each
trace), re-implemented here in `checks.py` — were derived from and
validated against the same two episodes this repo owns; a genuinely
fresh episode first passes them at CP-04′ (CP-11b said this plainly; it
bears repeating here).

**And the honest gap: nothing has been trained.** The predecessor
trained end to end — three regimes, per this CP's charge; the repo's
own record cites the recorded runs in `gsj-envloader-examples` as the
evidence (charter §2). This repo has produced traces and proved they
match the predecessor's — the strongest statement this evaluation could
make short of training, and not the same achievement. A-6 — that slime,
the trainer framework Polar ships a bridge for, can run OPD (the target
training regime) against Polar traces — is the one assumption bearing
on this verdict that remains unresolved; its if-false column predicted
at CP-00 that it "weakens the CP-12 verdict," and that is precisely the
provisionality below. Gradient plumbing, throughput under batch load,
weight-sync semantics (P3 is inert until a trainer declares versions;
A-13's rule — drain all in-flight sessions before every weight sync —
is untested under a real sync) — all unexercised.

## 5. Adopt or not?

**ADOPT PROVISIONALLY.**

What the evidence supports — the architecture: Polar owns the layer, the
shell stayed thin (1,496 lines against the predecessor's ~1,800 for
execution alone), the criterion is met on the Mac pair, and across
thirteen executed checkpoints **no §9 abandon condition ever fired**
despite each
being deliberately approached: `prefix_merging` matched the golden; the
proxy ran pi unmodified; the cutoff injected per-episode and survived a
forged-claim attack; G5's census reconstructed from the trace; the size
law held. Every defect found — and this evaluation found real ones —
had its fix in our code, never in a fork of Polar's.

What it does not yet support — the outcome: production numerics and a
training loop. So the adopt is provisional, with the conditions stated
as decisions, not hopes:

- **Converts to full ADOPT when both hold:**
  1. **CP-04′/CP-09′** — the H200 golden pair passes with the replay run
     as written and the template flip per the inherited DoD (charter
     §6); per A-16 that verdict governs numerics.
  2. **M4** — the slime bridge runs one OPD loop on collected traces,
     consuming masks and logprobs, resolving A-6.
- **Reverses to DO NOT ADOPT if:** CP-09′ shows a mask or
  retokenization divergence attributable to Polar with no fix in our
  code (§9's first condition — then cost Path C: take `prefix_merging`
  as an algorithm, keep our own capture); or the slime bridge proves
  unusable and a trainer-side adapter costs more than Path C; or a
  re-vendor (the named risk is the pending `prefix_merging` refactor)
  breaks the patch posture and A-8 fails.
- **While provisional:** the predecessor stays alive and frozen as the
  fallback (law 3); no production collection on the Mac pair (A-16);
  the wishlist's items 1–4 land before or with M4 (landed at CP-13).
  [CP-45, 2026-08-25: this frozen-alive fallback condition was a term
  of the *provisional* adopt and EXPIRED with the conversion on
  2026-08-11 (CP-17 — converting condition 2's dated entry below). It
  did not survive into the full ADOPT as an obligation; it survived as
  unrecorded fact until CP-45 retired the fallback role and archived
  the predecessor (ADR-0026). The evidence role — the goldens'
  collecting stack — continues, archived.]

Not full ADOPT, because adopting a rollout server that has never fed a
training step would be inflation. Not DO NOT ADOPT, because no stated
abandon condition is met and the architecture passed every test it was
given. This is the honest middle, with its exit doors labeled.

### 2026-08-11 — converting condition 1: MET (CP-09′, the H200 pair)

The H200 golden pair is done and the pair that governs is green:
`docs/golden/COMPARISON.md` executed in full against
`docs/golden/h200/`, with the replay run **as written** through the
serving engine for the first time — verdict **PASS WITH FINDINGS**
(`docs/reports/CP-09prime.md`). Masks exact (the zero-tolerance row, on
both traces), `prompt_ids` byte-identical, decode-fidelity exact to the
byte, glue constants identical, discipline clean, cutoff held; the
reversal condition (a mask or retokenization divergence attributable to
Polar) did not fire — **nothing in the comparison is attributable to
Polar's code**. The one finding is the platform's: capture-time
decode-path numerics sit above the contract's CP-18-anchored replay
bounds on BOTH stacks symmetrically (golden 0.005246 / collected
0.007141 mean |Δ| vs 0.005; replay itself bit-deterministic at exactly
0.000000), and the identical-context capture floor is ≈ 30× the Mac's
(0.003672 vs 0.000114) — invisible trace-side, recorded in
`docs/checks-spec.md` §CP-09′, binding on any replay-style trainer
check. A-1 is resolved on the governing platform; A-16's counterpart is
closed; charter §5's criterion is met on both pairs.

**The verdict now stands on**: condition 1 met (this entry) + condition
2 open — **M4's training loop is the only thing between ADOPT
PROVISIONALLY and full ADOPT.** The reversal doors narrow accordingly:
the CP-09′ door (mask/retokenization divergence) is closed by
measurement; what remains are the slime-bridge door and the re-vendor
door, as written above.

### 2026-08-11 — converting condition 2: MET (CP-17, the loop) → **ADOPT**

The loop closed on the H200, measured at every step
(`docs/reports/CP-17.md`; evidence in `docs/polar/h200-loop/`):
**collect** — 28 submissions through `gsj-rollout submit`, 27 qualifying
(COMPLETED, findings `[]`, `chains_total == 1`, cutoff held), zero
receiver rejections, zero quarantines; **convert** — 27 bodies → 27
**real** `slime.utils.types.Sample`s through the CP-16 bridge with its
three assertions live and `checks` running trainer-side from the packaged
wheel pins; **train** — one optimizer step in real slime v0.3.0 +
Megatron, `global_batch_size 27`, `train/grad_norm 0.4513` (non-zero and
below the clip, so unclipped), on a **non-degenerate** reward (1 of 27
episodes cited 4 pages within cutoff → 1.0), TIS consuming our captured
logprobs; **sync** — checkpoint reload (torch_dist → HF → the engine
restarted under the same served model name with all four legs
byte-identical), proven three ways, decisively by a probe whose control
measurement on identical weights is `mean|Δ| = 0.000000, nonzero 0/5782`
and whose across-the-sync measurement is
`mean|Δ| = 0.041835, nonzero 5623/5782`; **collect again** — 8/8
qualifying against the updated policy, gates green, cutoff held, all 8
still converting clean.

**A-6 is resolved** — slime runs against Polar traces, end to end, for
real. **A-26 holds** — the vendored adapter's `Sample` surface is the
installed surface (`session_id`, `group_id`, `remove_sample`,
`Status.FAILED` all accepted on first construction). **A-13's drain rule
met a real sync** and was satisfied *by construction* (serialized loop,
zero in-flight sessions verified at the boundary) — the situation was
exercised, the mechanism was not.

**Verdict: YES WITH FINDINGS**, three of which are named with owners —
Polar's vendored LOO post-processor explodes an advantage to 1e6 on
degenerate variance (only `clip_grad` prevents divergence, nothing warns);
slime silently no-ops an entire train loop while reporting SUCCESS when
`--load` reads as a resume; Polar's documented Megatron pin lacks the
module its own comment requires. All three are worked around at
trainer-side cost; none is in this repo's code, and **nothing in
`gsj_rollout/` had to change for the loop to run.**

**The CP-12 verdict converts to ADOPT.** What that means: the architecture
and the outcome both have evidence — traces produced by this server drove
a real optimizer step and came back as an updated policy demonstrably
serving the next collection. What it does **not** mean: one loop at 0.6B
on 27 episodes is not a training result, and nothing here shows learning
(the post-sync reward and length changes are one crude step at n=8, not a
curve). **What would still reverse it**: a re-vendor that breaks the patch
posture — the pending `prefix_merging` refactor, A-8, still untested, no
second vendor executed. The slime-bridge door is now closed by
measurement; the re-vendor door is the one that remains.

### 2026-08-12 — M6: the second trainer, and the boundary (CP-20 + CP-21)

**The trainer-agnostic claim is now measured, not argued.** M6 asked
whether the boundary this repo drew — task → sandbox → agent → trace,
nothing else — holds against a second trainer with a completely
different batch shape. It does, at both halves:

- **M6a (CP-20, the bridge)**: callback-shaped `SessionResult` → real
  verl `DataProto` (padded, uid-grouped, tensor — nothing like slime's
  per-sample objects), 26 fixture-driven tests against real verl, zero
  changes in this repo.
- **M6b (CP-21, the loop)**: collect → convert → train → sync → collect
  closed on the H200 — **YES WITH FINDINGS** (`docs/reports/CP-21.md`):
  112 submissions (110 qualifying), one real verl optimizer step
  (`TrainingWorker`/FSDP, `pg_loss −0.0944`, `grad_norm 2.32` pre-clip,
  GRPO advantages over ONE uid group of 110 — F-10 closed by design),
  the captured logprobs load-bearing (sequence-TIS; recompute agreement
  mean |Δ| 0.009442 = the third instrument at the CP-09′ floor), the
  sync proven with a zero-noise instrument, and a second collection
  8/8 qualifying against a demonstrably different policy (no learning
  claim — the post-sync batch is format-copying with visibly narrowed
  lengths, the entropy/KL caution for any real run). This repo's diff:
  **`docs/**` only**. The findings (verl's F-12/F-13, reward sparsity,
  the clipped step) are all trainer-, evaluator-, or config-side — none
  at the boundary.

**What two loops prove that one could not**: the same server surface —
the same wheel, the same callback JSON, the same estate recipes — fed a
per-sample trainer (slime) and a padded-batch trainer (verl) through
one submit path and one sync script, with the server changed in exactly
zero places. Scope law 1 held under contact twice; the DROPPED
trainer-side rows (16–21) were needed nowhere, twice.

### 2026-08-12 — M7a: the re-vendor door — NARROWED (CP-22)

The last reversal door on this verdict was written as: *a re-vendor
breaks the patch posture and A-8 fails*. CP-22 executed the second
vendor — the first since the recipe was written — and the door did not
fire (`docs/reports/CP-22.md`):

- **The mechanism is rehearsed, not estimated.** Upstream `stable` had
  not moved (still `f0e8343a`, still no tags or releases), so the
  rehearsal re-vendored to the same SHA and exercised the whole recipe:
  the re-vendored tree came out **byte-identical to the committed tree**
  (empty `git diff`, file modes kept), all three patches applied clean by
  script, reverse-apply walked back to the pristine pin exactly, same
  175/3 vendored-suite split, all approved pins reproduced, the registry
  seam resolving. Mechanical cost: ~2 minutes; with full verification
  ~15. **A-8 is resolved** (charter §4), with its residuals named there.
- **The named risk inside the door is now priced.** The pending
  `prefix_merging` refactor (frozen on the `polar` branch since
  2026-06-06), simulated as landed: P1/P2/P3's source hunks all apply
  clean — P2's finalize-site anchor and P1's stats-dict anchor both
  survive — and only P1's two fixture-marker test hunks need mechanical
  re-anchoring. The wholesale re-port the door feared is, against
  today's refactor, a fixture edit.

**The door is NARROWED, not closed**: no moved-pin re-vendor has run,
and the refactor measurement is a point-in-time reading of an unlanded
branch — what eventually squash-lands may differ, and the vendored
suite at that future pin remains the real gate. But the failure mode
the door named — the patch posture breaking at a re-vendor — now has
measured evidence against it on both halves: the mechanism reproduces,
and the riskiest known upstream change costs fixtures, not patches.
One finding the rehearsal surfaced is recorded where it belongs: the
recipe's venv rebuild omitted the A-14 `gsj_rollout` install (followed
verbatim, it breaks the registry seam) — corrected in
`vendor/REVENDOR.md`, which has now been followed once and reflects the
execution, not the intention.

### 2026-08-12 — M7b: the budget, and G6 — LANDED (CP-23)

The last designed-but-unlanded gate is live, and the budget mechanism
that blocked it is now self-enforcing (`docs/reports/CP-23.md`). Wishlist
row 18 closed in the order its finding demanded: the CP-11 banking pass
re-applied first (`checks.py` 520 → 497, AST identical modulo docstrings,
suite unmodified), the arithmetic stated before any rule code (G6
measures 31 lines; 497 + 31 = 528 > 520 — the two do not close), then
ADR-0021 raised the allowance to **528, the landed size exactly, zero
headroom by design, machine-checked by a suite tripwire** — the answer
to ADR-0014's earmark being spent by CP-16/CP-19 without either
noticing. `check_thinking_tail` landed exactly per ADR-0011's design
(register row 14 → PARITY; the running tally 20 PARITY · 7 DROPPED ·
3 GAP · 1 BETTER · 1 TBD): tokenizer-free ids-`endswith` of the CP-04′
pin over the turn-1 `prompt_ids` suffix and each pre-turn interstitial,
fail-closed at zero turns, clean on all four real bodies, one doctored
failure per clause, the single-turn case proven actually-checked. What
it asserts under the symmetric template: thinking stayed off at every
turn opening the merged stream carries. The Phase-C caveat is recorded
in the spec §G6: as pinned, a thinking-on estate fails every episode
loudly — C-2 re-pins, re-conceives, or retires the gate before the
first thinking-on episode reaches a receiver.

### 2026-08-12 — M7c: the taskbank — LANDED (CP-24)

The register's last deliberate deferral resolves
(`docs/reports/CP-24.md`; ADR-0022 resolving ADR-0003; register row 4 →
PARITY, the running tally 21 PARITY · 7 DROPPED · 2 GAP · 1 BETTER ·
1 TBD). What landed is deliberately not the predecessor's §3.1 bank —
Polar still takes `TaskRequest`s — but the consumer's enumeration:
`phase_taskbank` builds one flat row per `(case, timestep, prompt)`
carrying the triple, the lock-sourced `split` (ADR-0015's row-spec,
honored verbatim), `prompt_source`, the free row's verbatim text or the
skill row's **resolved** card text, and `sandbox_image` — every column
either the triple or a `render_task_request` argument, so a row submits
with zero translation and **zero `config.py` change** (the CP-13/CP-14
parameters fit as built). Deterministic (byte-identical rebuilds, sorted
rows, no timestamps), sha256 in the lock, and `verify` finally runs the
row-level half deferred since CP-01: counts vs the lock, triples
exactly-once and set-equal to the tree, split/text/image re-derived from
the tree. G1's end-to-end story closed with it, proven fixture-driven on
both real callback bodies: card bytes → bank text → render-computed hash
→ task metadata → trace metadata → `check_skill_card` ∈ the pinned set.
The frozen staging bank's 12 triples and splits reproduced exactly; its
bytes did not (the §3.1 nesting dropped by decision) — the lock records
`ae9e0bbd…`. Wishlist row 6 closes; row 9 (sandbox-side hashing) is
restated, narrower but real: binding G1 to the bytes the episode
actually saw.

### 2026-08-25 — CP-45: the predecessor archived; the frozen-alive term expired at conversion

Housekeeping on the verdict's own terms; no condition moves. The
"while provisional" clause in §5 kept the predecessor frozen-alive as
the fallback; that term was scoped to the provisional adopt and expired
when the verdict converted — 2026-08-11, CP-17 — though no entry said
so until now. CP-45 retires the fallback role explicitly and archives
`gsj-envloader`: one README-header commit on top of the v0.8.0 tag (the
only write any checkpoint ever made there — law 3's single permitted
exception, ADR-0026), and the GitHub repo set to archived, read-only.
The evidence role continues: it is the collecting stack for both golden
references (`docs/golden/mac/`, `docs/golden/h200/`), cited by A-1's
resolution — v0.8.0 stays readable, cloneable, and platform-frozen. The
traces are what they are regardless.

---

## The register, closed (charter §7 — 31 rows, final for M3)

**18 PARITY · 6 DROPPED · 5 GAP · 1 BETTER · 1 TBD.**

The number that matters — what the predecessor has that this repo does
not, and whether each absence was decided or is owed:

| capability (row) | dropped or gap? | disposition |
| --- | --- | --- |
| store, ready/mix, staleness, serve accounting, collation, quarantine's retention half (16–21) | **DROPPED, deliberate** | the trainer's problem, decided at CP-00 with owners named; the at-source rejection half of quarantine landed here (CP-08) |
| G5's checkout-census clauses (in row 13) | **DROPPED, deliberate** | decided CP-11: a sandbox-filesystem property no trace carries; enforced instead by the hardened `--depth 1`/no-remote/no-reflog clone |
| taskbank builder / skill-row resolution (4) | **GAP, deliberate deferral** (ADR-0003) | fix named: `TaskRequest`-shaped builder in `client.py`'s orbit + `prompt_source`; wishlist |
| gate G1 skill-card (9) | **GAP** | measured unimplementable: no trace states which card resolved; unblocked by `prompt_source` (wishlist item 1) |
| gate G4 codec, receiver-side (12) | **GAP by decision** (ADR-0011) | verified estate-side by the pins walk at bring-up instead; per-episode binding stays open (row 22) |
| gate G6 thinking-off (14) | **GAP** | tokenizer-free design recorded; needs `g6_expected_tail_ids` pinned at the next walk (CP-04′) |
| full trace provenance (22) | **GAP, half deliberate** | the channel is proven; sampling/codec/image identity are estate provenance (F1, ADR-0011) — the estate surface is CP-04′'s |
| G7's settings-hash clause (in row 15) | in-row residual | no settings evidence rides the callback (measured); one-line harness echo, wishlist |

Everything else the predecessor had is at PARITY here with live evidence
(the cutoff end to end with a rejected forgery, capture fidelity, the
merged multi-turn chain, the pins walk reproducing every derivable
predecessor value, bounded collection, the logprob discipline), row 29
(multi-harness) is BETTER, and row 30 (Apptainer/HPC) is the one TBD —
never reachable on this estate, reason in-row.

## The wishlist, consolidated (the next working CP's agenda)

Ordered by what unblocks the most. Items 1–4 are one freeze-lift CP
(~10–40 lines total — the size law's new headroom is for exactly this);
items 5–6 ride CP-04′ and the taskbank milestone. **[CP-13] Items 1–4, 7
and 8 are DONE** (`docs/reports/CP-13.md`): G1 landed (row 9 → PARITY),
G7's settings clause landed (row 15's residual closed), the H-41 mirror
is complete by test, the receiver's two failure shapes are cured over
HTTP, and both stale documents tell the truth. Items 5 and 6 stay with
their owners (CP-04′; ADR-0003), and CP-13's own adversarial pass opened
one new item — **9**, the sandbox-side card hash that is row 9's
remaining delta from the predecessor's instrument.

| # | item | needs freeze-lift of | deferred since / why | unblocks |
| --- | --- | --- | --- | --- |
| 1 | **DONE (CP-13)** — `prompt_source` (+ resolved-card hash) in `TaskRequest.metadata` | `config.py` | CP-11b measured G1 unimplementable without it; rooted in the CP-01 taskbank deferral | **gate G1** (row 9), and with #6 closes row 4 |
| 2 | **DONE (CP-13)** — the H-41 knob's YAML mirror (`reject_toolless_roster` in `ChecksConfig`, mirror-completeness test) | `config.py` (same lift) | CP-11b: config logic was frozen; drift declared in the docstring | the operator arming the toolless-roster check from the one YAML |
| 3 | **DONE (CP-13)** — the settings echo (harness renders its `settings_json` into trace-reachable metadata via the gateway registry merge, A-23) | `pi_harness.py` | CP-11b measured zero settings evidence on the callback | **G7's settings-hash clause** (row 15's residual) |
| 4 | **DONE (CP-13)** — the receiver's pins-failure seam (was: missing KEY → 400 masquerade, missing FILE → dropped connection mid-batch; now: 500 naming the key/path, batch atomic) | `receiver.py` | CP-11b verification measured both shapes live; fail-closed already, ugly | operational robustness at the wire; blocks nothing downstream |
| 5 | **DONE (CP-04′)** — `g6_expected_tail_ids` pinned under the served tokenizer: `[151644, 77091, 198, 151667, 271, 151668, 271]`, derived and tokenizer-verified estate-side at the H200 pins walk (`pins/pins.gsj.json`; `derive_pins.py` re-verifies wherever transformers is importable) | none — the next pins walk (CP-04′; blocked at CP-12 by the checkpoint's own no-re-pin rule) | ADR-0011: the tokenizer exists at pin time only, estate-side | **gate G6** (row 14), incl. the first-turn `prompt_ids` clause — the rule itself still needs a `checks.py` freeze-lift (CP-11c or later) |
| 6 | **DONE (CP-24)** — the taskbank landing (`TaskRequest`-shaped builder, skill-row resolution) — **[CP-14] the split half is now fully specified** (ADR-0015: per-row `split` from the lock's `cases.<id>.split`, passed to `render_task_request(split=…)` — landed and test-proven; no split decisions left for the builder). **Landed as ADR-0022**: `corpus/ingest_corpus.py`'s `taskbank` phase builds flat rows shaped for `render_task_request` (triple + `split` + `prompt_source` + verbatim `prompt_text` / **resolved** `skill_card_text` + `sandbox_image`), deterministic with the sha in the lock, `verify` running the row-level half; a row submits with zero translation and **no `config.py` change** — the CP-13/CP-14 parameters fit as built, which was those checkpoints' aim. (The landing is `corpus/`-side, not `client.py`'s orbit as this row guessed at CP-12 — the builder produces rows a consumer renders, not `TaskRequest`s directly, per ADR-0022 §1) | `client.py`'s orbit + `corpus/` | ADR-0003 (CP-01): no consumer existed; still true until a trainer drives volume — resolved with Phase D's consumer story as the named driver | row 4 (→ PARITY); G1's end-to-end story with #1 (closed — proven on both real callback bodies) |
| 7 | **DONE (CP-13)** — `mcp-service/README.md` one-liner (said "at CP-11"; the regexes landed at CP-10) | `mcp-service/` | frozen three CPs running (CP-10/11/11b) | auditor accuracy only |
| 8 | **DONE (CP-13)** — `README.md` status section (said "CP-01 — the moves"; now states the CP-12 verdict, the working server, and the test commands) | none standing — excluded only by CP-13's lift list | noticed at the CP-12 re-read; eleven checkpoints stale | the repo's front door telling the truth |
| 9 | **NEW (CP-13)** — G1's card hash computed **sandbox-side**, from the episode's own checkout, the way the predecessor did (`gsj-envloader task.py:878-885`). **[CP-24] Restated now that the bank states a hash**: the statement is honest — computed at render from the corpus-level card the taskbank resolved, verified against the pins, with `verify` holding the bank to the tree — so what sandbox-side hashing still buys is exactly one thing, and it is real: binding G1 to the bytes **the episode actually saw**. A checkout drifted from the corpus (a bad push, a tampered estate repo, a stale clone) passes today's G1 with a true statement about the *corpus* while the *sandbox* read something else; corpus-side `verify` only catches it when someone runs `verify` against that estate. Narrower than before CP-24 (bank staleness vs the tree is now a named `verify` FAIL), but not closed | `pi_harness.py` | CP-13's own verification: the submit-side statement landed, but a drifted `skills/<name>/SKILL.md` in a case repo stays invisible to G1 | row 9's remaining delta from the predecessor's instrument. **[CP-40] Dispositioned: production-prep, transferred** — the oldest open row (audit O9); no evaluation CP is scoped to lift `pi_harness.py` for it, and the exposure is bounded: since CP-13a the workspace echo binds episode N to the exact commit and tree it read, so what G1 cannot see is a tampered/stale estate repo *between* corpus-side `verify` runs. The sandbox-side hash stays the named instrument if adoption wants G1 tamper-evident; this row is its record and the production-prep list is its owner. `waiting-on: the first production bring-up wanting G1 tamper-evident — ~10–15 pi_harness.py lines + a checks.py comparison, so also the §3 budget decision and an ADR-0021 allowance` |
| 10 | **NEW (CP-14), CLOSED (CP-40)** — `skills/<name>/` interior strictness: the validator ignores files beside `SKILL.md` and the scaffolder silently drops them from repos (v1-inherited behavior, surfaced by CP-14's stranger-read pass; contract v2 now documents it as an honest exception rather than leaving it a surprise) | `corpus/` | CP-14 scoped to the split; tightening skills validation is a separate contract decision | the "everything it can check, it checks" doctrine holding for the one directory it currently skips. **[CP-40] CLOSED — the documented exception IS the decision**: contract v2 states it honestly, two corpus lifts passed the validator with the file open (CP-24's +289 lines, CP-30's) and neither tightened it, and no consumer register row has hit it in 26 checkpoints of runs; "cheap at the next corpus/ freeze-lift" was a park whose trigger fired twice without the row moving (audit O6). Reopens only on a real stranger hit, as a contract decision — not as a lift rider. `waiting-on: a real stranger hit on a skills/<name>/ interior file` |
| 12 | **NEW (CP-17) + DONE (CP-18)** — `staging/serving/run/` (the serve script's runtime `endpoint.env` + `tunnel.pid`) is untracked and un-ignored, so every estate bring-up leaves the tree dirty and every CP's `git status --porcelain` DoD line has to step around it. One `.gitignore` line — landed as `staging/serving/run/`, three comment lines of provenance above it. (CP-18's lift enumerated `.github/**`, `pyproject.toml`, `README.md`, `docs/**` and did **not** name `.gitignore`; its Step 5 named this item by number. The explicit instruction was taken over the enumeration for one inert line, and the excursion is declared in `docs/reports/CP-18.md` `scope_drift`) | root `.gitignore` — outside CP-17's `docs/**` + `staging/**` lift, so CP-17 removed the directory at teardown instead | first noticed CP-17 (the artifacts predate it — they were already untracked at session start) | the clean-tree DoD stops depending on an operator remembering to delete a run directory |
| 14 | **NEW (CP-18)** — `mcp-service/tests/test_backend.py::test_stored_vectors_are_the_pinned_encoders` asserts BIT-equality between the stored Chroma vector and a fresh encode. The property it guards (the pinned MiniLM embedded the corpus; Chroma's default ONNX EF never touched it) holds everywhere measured, but the oracle is host-dependent: on `linux/arm64` 15 of `case_0001`'s 51 vectors differ by ≤ 1 ULP (worst |Δ| 1.490e-08, cosine 0.999999940) while a wrong embedder would sit at cosine ≈ 0.80. Passes on x86_64 and on macOS, so CI is green and nothing is owed today | `mcp-service/` | opened CP-18; no action taken deliberately — a tolerance would weaken a real canary for an architecture this project does not deploy on | the day the suite must run on arm64 (an arm64 runner, an Apple-silicon container, a Graviton box), which is when it needs a tolerance-based sibling rather than an edit. **[CP-51] SUPERSEDED into row 26** — the premise ("passes on x86_64 … CI is green and nothing is owed today") failed eight times on GitHub's ubuntu-latest x86_64 runners (the run ids are in row 26; audit S10); the arm64 event never fired and no longer decides anything — the oracle's magnitude question is row 26's, instrumented there this CP. **[CP-55]** row 26 closed (the ADR-0016 amendment sets the bound); one correction to this row's premise: its "≈ 0.80" was CP-18's different-chunks baseline, not a substitution measurement — the real default-EF hazard measures max|Δ| 1.155e-07..2.719e-07 with cosine within 1 ULP of 1.0 (CP-55) — and the arm64 numbers recorded here sit inside the new bound (1.490e-08 ≤ 4e-08): an arm64 runner would now pass, so the tolerance-based sibling this row waited for exists and is the oracle itself. `waiting-on: nothing — closed into row 26` |
| 15 | **NEW (CP-18)** — `tests/test_receiver.py::test_a_staging_failure_leaves_no_partial_batch_and_no_orphan_tmp` needs a non-root user: it chmods the staging dir to `0o500` and expects the write to fail, which `CAP_DAC_OVERRIDE` defeats, so the assertion reads `200 == 500` under root. Hosted `ubuntu-latest` runs as `runner`, so CI is unaffected | `tests/` | opened CP-18 (found in a root container during the pre-flight rehearsal, not on the runner); frozen this CP and correct as written — the finding is about where the suite may run, not about the test | anyone moving CI to a `container:` job, or running the suite inside a Docker build — both root by default, both would see a spurious failure. `waiting-on: CI moving to a container: job, or the root suite run as root — a skipif then, ~2 test lines` |
| 16 | **NEW (CP-18), CLOSED (CP-40)** — `vendor/polar/tests` is not run by CI, including the four carried-patch tests `vendor/REVENDOR.md` makes mandatory at re-vendor time (`test_record_filters.py`, `test_builder_filter_wiring.py`, `test_prefix_merging_abort.py`, `test_storage_policy_version.py`). They are fixture-driven and would fit a hosted runner; what stops them is that they need Polar's own uv venv and belong to the re-vendor recipe, which runs at a re-vendor and not on every push | `.github/**` only (adding a fifth job); `vendor/` stays frozen either way | opened CP-18 — the CP's own verification pass caught that "the fixture-driven half" was one suite short of true, and the caption now names the omission instead of implying coverage | a re-vendor that regresses a carried patch being caught by CI rather than by the operator remembering step 5 of `REVENDOR.md`. **[CP-40] CLOSED, declined with the reason** (audit O9 — the four mandatory tests run in no CI): they can only regress when `vendor/` changes, `vendor/` changes only at a re-vendor, and `REVENDOR.md` step 5 makes them mandatory exactly there (M7a's door is where that binds; the 2026-08-24 audit re-ran them by hand: 25 passed) — a CI job would re-execute an immutable tree on every push, cost without coverage. Reopens if a re-vendor ever lands without the recorded step-5 run. `waiting-on: a re-vendor landing without REVENDOR.md's step-5 run recorded` |
| 19 | **NEW (CP-24)** — the pins seam for card *edits*, three small facts found by CP-24's adversarial pass, none fixable this CP (`pins/` frozen): (a) `pins/pins.gsj.json`'s `skill_card_hash` provenance note still says "skill-row RESOLUTION stays taskbank-deferred (ADR-0003)" — stale since CP-24 (resolution now happens at bank build; the *values* are untouched and correct); (b) `pins/derive_pins.py` hardcodes the two staging skill names, so a brand-new card needs a script edit before it can be pinned at all; (c) nothing warns at bank build when a resolved card's hash is not in the estate's pins — the first symptom of a routine card edit is a wave of `G1:skill_card_hash_not_approved` quarantines (the contract now tells the data-prep team to warn the operator, but a build-time advisory where pins are reachable would catch it mechanically) | `pins/` | opened CP-24; the pipeline is deliberately pins-unaware (a moved component; law 6's approved sets are the rollout side's) — the *seam between the two* is what has no owner | a card edit landing as a pins re-derivation instead of as quarantined episodes. **[CP-40] Re-parked on a real event, and half-owned** (audit O6: "cheap at the next pins/ lift" failed as a trigger — CP-30 lifted `derive_pins.py` itself and the row did not move): the (a) stale-note half is audit S8's pins note and belongs to the next conscious `pins/` lift — the register asks CP-41's sitting to take it beside its other in-file corrections, and records the ask here so declining it is a visible decision; (b) and (c) wait on the first REAL card edit on any estate, an event that has not yet occurred anywhere, with this row as that editor's recipe — §8 rule 9 now governs the park. **[CP-41] The ask was TAKEN — (a) is FIXED**: both stale notes in `pins/pins.gsj.json` (line 86's taskbank-deferred clause and line 102's G6-as-future clause — audit S8) now state the CP-24/ADR-0022 and CP-23 truths; `derive_pins.py` reproduced every approved value before AND after the edit — a notes-only change, no value moved; the correction reaches pip consumers at the next release (pins bake into wheels at build time, ADR-0019). (b) and (c) stay parked on the first real card edit. (Numbering: this table carries TWO rows numbered 19 — this pins-seam row (CP-24) and the cli-guard row below (CP-21, DONE CP-27); CP-27's "wishlist 19 → DONE" means the cli row, while CP-24/CP-33/CP-40/CP-41 references mean this one — the same slip class as items 10/11, 12/13 and 13/17, recorded in both rows rather than silently renumbered, audit C4). **[CP-51] (b) LANDED**: `derive_pins.py` globs `estate/corpus/staging/skills/*/SKILL.md` instead of naming the two cards — a new card is pinnable with no script edit; the walk reproduced every approved value before and after. (c) stays parked.  **[CP-83, recording CP-82] The literal card-edit event has occurred on an executed scratch estate.** The audit edited the skill card and `AGENTS.md` through `update`; both converged and printed their G1/G2 consequences before work. The estate therefore supplies the operator warning already, without making the standalone corpus pipeline pins-aware. The remaining bank-build request is the narrower pipeline-boundary decision above, not a missing estate warning; phase 1 does not reverse corpus ownership or alter pins. `waiting-on: an operator-approved pins-aware bank-build contract and a direct pipeline card-edit proof warning about the unapproved hash before episodes run` |
| 13 | **NEW + DONE (CP-18)** — continuous integration: nothing ran the suites but an operator on a Mac, so the only evidence that any of them still passed was a paragraph in the last report to run them. Landed as one `.github/workflows/ci.yml`, four jobs, Python 3.12 only (root 129 · corpus 44 · mcp-service 89 · wheel build + the CP-16 packaged-pins install proof from a venv outside the checkout), push + pull_request on `main`, with the covered/not-covered statement in the file's header comment and beside the README badge. (CP-18's Step 5 called this "wishlist item 12"; item 12 was CP-17's `.gitignore` row — the CI work had no row until this one, the same numbering slip CP-16 hit and recorded) | none — `.github/**` was new, and CI config is outside the size law (CHARTER §3, [CP-18]) | never registered: the repo has had suites since CP-01 and a public remote since CP-13a, and no checkpoint until now was scoped to connect them | every later CP starts from a checkout whose fixture-driven half is known-green, instead of from a claim |
| 11 | **NEW + DONE (CP-16)** — the trainer leg from an installed wheel: `pins/` did not ship (`pyproject` packaged `gsj_rollout` only), so `checks.PINS_PATH` resolved into site-packages and every trainer-side `validate_session_result` raised `PinsConfigurationError` — recorded at CP-11b, dispositioned there as "both legs run from the checkout by design"; M4's bridge killed that design assumption. Landed as ADR-0017: pins force-included into the wheel (`gsj_rollout/pins/`), `PINS_PATH` resolves `GSJ_PINS_PATH` → checkout → packaged copy, mismatch fails loudly as `*_not_approved`; proven from a scratch venv against the real CP-09′ body (findings `[]`). (The CP-16 prompt called this "wishlist item 10"; item 10 is the skills-interior row above, untouched — the trainer-leg fix had no row until now) | `pyproject.toml` + the `checks.py` seam (CP-16's lift) | never registered — CP-11b called it noise while both legs ran from checkouts | **the slime bridge** (examples repo; at `slime-cp17` since its CP-69), and any trainer that installs rather than clones |
| 17 | **NEW (CP-19), DONE (CP-29)** — the package was not on PyPI until CP-29. The release path existed and was rehearsed locally end to end (wheel clean and exclusions asserted, `twine check` PASSED on both artifacts, CP-16's install proof green from a scratch venv outside every repo against the real CP-09′ body), but nothing was uploaded: the operator was asked and chose to stop at the path rather than make an irrevocable first upload (ADR-0020 §5). **The name is unclaimed on both indexes and therefore NOT reserved** — verified via the JSON API (`gsj-harness-rollout-server` → 404, `httpx` → 200; the HTML project pages sit behind a bot challenge that returns 200 for names that do not exist, so the HTML is not an oracle). Anyone may claim the name before we do; accepted, because the alternative is an irrevocable upload made to win a race. (The CP-19 prompt called this "wishlist row 13"; row 13 is CP-18's CI row — the same numbering slip CP-16 hit at item 10/11 and CP-18 at item 12, recorded here in the row rather than silently renumbered). **[CP-29] DONE — published, and proven from the public index.** The operator registered both pending publishers out-of-band; CP-29 pushed the mirror current (nine commits, CP-20..CP-28), rehearsed via `workflow_dispatch` — the first-ever execution of `release.yml`: build + CI-side exclusion assertions green, the OIDC exchange accepted by TestPyPI (no `invalid-publisher`), PEP 639's SPDX expression + `license-files` accepted by a real index, the PyPI job structurally skipped — then tagged `v0.1.0` at `1565813` (CP-28) and the tag run published to TestPyPI and PyPI, all jobs green. The proof, CP-16's shape against the PUBLIC artifact: a scratch venv outside every repo, `pip install gsj-harness-rollout-server` with no index flags and no local wheel → the packaged pins resolve from site-packages, the real CP-09′ body → findings `[]`. The examples repo simultaneously gained its public remote (github.com/MHGanainy/gsj-harness-rollout-server-examples). The name-race risk this row carried for ten checkpoints closed unexercised: 404 on both indexes the hour before the upload | none — `pyproject.toml`, `.github/workflows/**`, `LICENSE` and `docs/**` were all lifted at CP-19 and all landed | opened CP-19; nothing before it was scoped to publish | **two prerequisites, both operator-side and neither performable from this repo**: (1) a pending publisher configured at test.pypi.org and pypi.org (owner `MHGanainy`, repo `gsj-harness-rollout-server`, workflow `release.yml`, environments `testpypi` / `pypi`) — done by the operator before CP-29; (2) a `v0.1.0` tag pushed at a commit CI has already validated — pushed at CP-29 (with the CI caveat that is wishlist 26). The workflow did the rest, exactly as this row predicted |
| 18 | **NEW (CP-19), DONE (CP-23)** — `checks.py` was at **520 / 520**, exactly on ADR-0014's ceiling, so **G6's tokenizer-free ids rule could no longer land inside it**. ADR-0014 reserved the 23 lines above 497 for G6 "and nothing else"; measured from `git show` at CP-23, three CPs eroded it without noticing — CP-14's TR3 tripwire took the first 12 (497→509), CP-16's resolver seam 9, CP-19's pins signal the last 2 (the CP-19 account here originally named only the last two). **Resolved at CP-23, both mechanisms in order**: the CP-11 banking pass re-applied (520 → 497, AST identical modulo docstrings, suite unmodified), the arithmetic stated (G6 measures 31 lines; 497 + 31 = 528 > 520), then ADR-0021 raised the allowance to **528 — the landed size exactly, zero headroom, enforced by a suite tripwire** so the silently-spent-earmark failure cannot recur. G6 landed the same CP (register row 14 → PARITY) | `checks.py` (banking + the gate), `tests/test_checks.py` (the tripwire), ADR-0021 | opened CP-19 by its own arithmetic; the earmark was silently eroded across CP-16 and CP-19 and is surfaced here rather than discovered mid-checkpoint | **gate G6** (register row 14), the last designed-but-unlanded gate — landed |
| 19 | **NEW (CP-21), DONE (CP-27)** — `gsj_rollout/cli.py` had no `if __name__ == "__main__"` guard, so `python -m gsj_rollout.cli serve …` defines `main` and **exits 0 silently having done nothing** — an invocation that looks like success (the console-script entry `gsj-rollout` is the only working form). Hit live at CP-21 bring-up: the receiver "started" three times with an empty log before the cause was found; the estate now runs the CLI via `python -c "from gsj_rollout.cli import main; …"`. Two inert lines, but `gsj_rollout/` was frozen at CP-21. **[CP-27] Landed at the freeze-lift**: the guard is in, `python -m gsj_rollout.cli --help` exits 0 and prints, `serve` with a bad config exits 2 saying why — both proven by subprocess tests that fail with the guard removed | `cli.py` (any future freeze-lift; counts ~2 lines against §3) | opened CP-21; every estate that installs no console script (venv-less hosts driving the CLI through an interpreter path) | a silent no-op stops masquerading as a started server. (Numbering: the table's second row 19 — the pins-seam row above (CP-24, re-parked) shares the number; CP-27's "wishlist 19 → DONE" means THIS row, CP-24/CP-33/CP-40/CP-41 references mean the pins row — recorded in both rows rather than silently renumbered, audit C4) |
| 20 | **NEW (CP-25), DONE (CP-33)** — `submit --from-bank <parquet> [--row K]` submits an ADR-0022 row with zero translation (triple, prompt text, prompt_source, split from the row; the image pin checked against `runtime.image`; pyarrow lazy behind a named error, now a dev-extra so CI runs the path). Original finding: the CLI cannot submit a taskbank row. `gsj-rollout submit` is definitionally `prompt_source="free"` (frozen `cli.py` — resolution never happens there), so the CP-24 bank — the deliverable a consumer is handed — submits only through Python (`render_task_request` with the row's columns, as `example_project/train.py` does). A `--from-bank <parquet> [--row K]` flag is the natural fix; it raises the pyarrow-in-core question ADR-0022 deliberately kept out of the root closure, so the flag likely belongs behind a lazy import with a named error, the corpus pipeline's own pattern | `cli.py` | opened by CP-25's stranger read: the run book's collect path is `train.py --collect-only` where a CLI one-liner would be expected | a consumer collecting from the bank without writing Python |
| 21 | **NEW (CP-25), DONE (CP-27)** — two config traps the comments carried but the schema could reject: (a) `polar.gateway.public_url`'s port vs `polar.gateway.port` — one fact stated twice; a mismatch dispatches to a URL nothing listens on, found only at the first dead submit (the CP-25 example itself shipped this mismatch in draft and was caught by its own validation pass); (b) `estate.serving_base_url` ending in `/v1` — Polar's proxy appends `/v1/chat/completions` itself and the suffixed form 404s as `/v1/v1/…` (the CP-04′ measured trap, today a comment). Each is a two-line model validator; both were out of CP-25's lift (defaults and comments only, logic only where a default can't express the thing — these are cross-field logic) | `config.py` (a logic lift; ~4 lines against §3) | opened by CP-25's stranger read; both traps are documented in the example config but documentation is the weakest guard the file has. **[CP-27] Both validators landed**: the `/v1` suffix and the port mismatch fail at load naming the key, the measured symptom, and the fix (messages verbatim in the CP-27 report); the correct forms accepted; each test fails with its validator removed. The executable checks are the estimated size — the growth beyond it is the actionable messages, accounted in CHARTER §3 [CP-27] | the two likeliest stranger misconfigurations failing at load time, naming the key, instead of at the first dead episode |
| 22 | **NEW (CP-26), PARTIALLY DONE (CP-27)** — the `gsj-rollout serve` session-start prints were correct and invisible: the topology print (the rendered path + the two Polar commands — the session's only instructions) is block-buffered python stdout, so under `nohup … > log` the log holds only the pins warning while the receiver listens silently (external F-20); the printed Polar path is RELATIVE to an unstated cwd and assumes a `vendor/polar/.venv` that no fresh checkout has, with no hint when it is missing (F-21); and the packaged-pins warning prints in full on every import even on the reference estate where the correct response is to do nothing (F-39). Three small serve-time changes: flush/line-buffer before serving, print absolute paths + a missing-venv hint, and consider a warn-once (or estate-matched silence) for the pins line | `cli.py` (+2 lines-ish against §3); the pins-warning half touches `checks.py` (ADR-0021 tripwire — needs its own decision) | opened by CP-26's stranger run: the operator backgrounded serve exactly as a stranger would and lost the instructions. **[CP-27] Two of three landed**: every pre-block serve print carries `flush=True` (verified through a pipe with the process still running — the test fails with the flush stripped), and the printed Polar path is absolute via the computed repo root (F-21's one-line half; note a wheel install's repo root is site-packages, loud-but-wrong — the missing-venv HINT stays open here with it). **[CP-33] The missing-venv hint landed** (the F-45 fix's other branch: an existing `vendor/polar` with no `.venv` prints the REVENDOR.md recipe pointer). **The F-39 pins warn-once half stays OPEN**: it lives in `checks.py`, 528/528 under the ADR-0021 equality tripwire, and needs its own ADR | the server's own instructions surviving a nohup, and a fresh estate learning what to provision from the error instead of from spelunking. **[CP-40] The F-39 warn-once half CLOSED — declined for cost** (audit O9: "needs its own ADR" that no CP is scoped to write): a cosmetic silence — the warning is truthful, merely repetitive on the reference estate — is not worth an ADR-0021 allowance plus a `checks.py` line at 528/528 with §3 headroom 1. Reopens only if a consumer register row records the repetition as more than noise, and then as its own ADR, as always stated. `waiting-on: a consumer register row recording the pins warning's repetition as more than noise — then its own ADR-0021 allowance` |
| 23 | **NEW (CP-26), DONE (CP-27)** — two config-schema strangerward gaps beside row 21's validators: (a) a required-key line deleted whole leaves a comment-only section parsing as YAML null, and the error is pydantic's "Input should be a valid dictionary or instance of GatewayNodeConfig" — naming no field where the unknown-key/missing-leaf errors are excellent (external F-25; normalize None sections to `{}` so the field-level message fires); (b) the served snapshot's revision has no home in the YAML — the engine pins a revision, `--snapshot` must match it, and CP-26's stranger matched it by luck (F-23; an optional `estate.model_revision` would let the pin travel with the config and lets train.py-shaped consumers verify before the GPU) | `config.py` (a logic lift; rides with row 21's ~4 lines) | opened by CP-26's error-quality probes and the snapshot-by-luck moment. **[CP-27] Both halves landed**: `_null_sections_to_empty` normalizes null sections model-driven (not a hardcoded list), so the deleted-line mistake reports `'polar.gateway.public_url': Field required`; `estate.model_revision` is an optional in-band pin the server never reads — a train.py-shaped consumer can verify `--snapshot` against it before the GPU | the deleted-line mistake naming its field, and the snapshot pin arriving in-band instead of over a handover chat |
| 24 | **NEW (CP-26), CLOSED for the evaluation (CP-40)** — was: OPEN, untouched at CP-27 and again at CP-33, `mcp-service/` frozen both times — the MCP HMAC secret cannot be verified before it is spent: gateway and service must share `GSJ_MCP_TOKEN_SECRET`, an inherited estate's value arrives only by operator handover, and a wrong value first surfaces MID-EPISODE as failed tool calls (external F-22). A cheap authenticated probe (the service already has `/health`; an HMAC-checked variant, or a one-shot `verify-secret` request the gateway can issue at startup) would fail the mismatch at session start, naming the env var | `mcp-service/` (a moved component — its own release cadence), plus a gateway-side probe call | opened by CP-26: the stranger's secret came from out-of-band memory of the operator's dotfile | a wrong secret costing one loud startup error instead of a quarantined episode. **[CP-40] The closing reason**: the row deferred to "`mcp-service`'s own release cadence" and no such cadence has existed since CP-15 — nothing schedules one, and this row was the only reason such a lift would open (audit O8). The finding's evaluative value is fully banked: F-22 and this row name the failure shape, its measured cost, and the probe design. Building the probe is adoption hardening — production-prep, owned with gap rows 2/22's estate-posture set; it does not reopen inside the evaluation. `waiting-on: the first production bring-up — the probe is estate-side (an HMAC-checked /health variant in mcp-service + a caller in estate.sh mcp-up / the demo preflight, ~40 lines, no library line)` |
| 25 | **NEW (CP-26), DONE (CP-27), examples-repo-side** — train.py polish the stranger run showed and CP-26's fix set (docs + example config + install.sh + README only, by the prompt's enumeration) deliberately did not touch: an aggregate summary line for collect and for rewards (71/72 and 1/71 were hand-counted — external F-27), a device argument or CUDA_VISIBLE_DEVICES passthrough instead of the hardcoded first-visible-GPU (F-34), and the closing sync pointer saying "estate-side" where the sync is workstation-side (F-29's second half — the run book is fixed, the last thing the script prints still misleads) | the examples repo's `example_project/train.py` (external repo — no library freeze involved; deferred only to keep CP-26's fix set to the enumerated files) | opened by CP-26; **[CP-27] all three landed**: `collect total:` and `reward distribution: k/N nonzero` aggregate lines (F-27), `--gpu N` → `CUDA_VISIBLE_DEVICES` before the first CUDA call with the nvidia-smi instruction in `--help` (F-34), and the closing printout sends the sync WORKSTATION-side naming `GSJ_VLLM_SSH_HOST` (F-29); RUNBOOK + FINDINGS rows updated in the same external commit | the script's own output matching the corrected run book |
| 26 | **NEW (CP-29), DONE (CP-30)** — resolved exactly as diagnosed, at the estimated size where it executes: the fixture line dropped its `/v1` (`corpus/tests/test_taskbank.py:37`; the validator untouched), the corpus job's deps refreshed from `corpus/requirements.txt` plus `pip install -e .` (two taskbank tests import the root package), labels/comments to measured truth (root 150, corpus 58). Counts: 56/58 local and 39/58 CI before → 58/58 local after (the CI job's exact recipe reproduced in a fresh venv); CI's own green arrives with this commit's push and was verified live in-session, the badge healing on both landing pages on that run. The original finding, kept for the record: main's CI was RED at the published tag. The corpus-suite job fails 19/58 on CI's first contact with CP-20..CP-28 (nothing was pushed between CP-19 and CP-29, so five checkpoints of drift arrived at once), and the red decomposes into two independent layers, both measured at CP-29: (a) `ci.yml`'s corpus job still installs only `pytest pyyaml` and its comment still claims "no import of the root package" — both true when written at CP-18, both false since CP-24 (the taskbank phase needs pyarrow behind its lazy-import named error, exit 2; two tests import `gsj_rollout`) — 17 of the failures, plus a stale job label ("corpus suite (44)": the suite is 58); (b) with full deps the suite still fails 2/58 — `corpus/tests/test_taskbank.py:37`'s `MINIMAL_CFG` pins `serving_base_url: http://serve.invalid:8000/v1`, the exact `/v1` trap wishlist 21's CP-27 validator now rejects at load. The validator is right and doing its documented job; the CP-24 fixture predates it, and no checkpoint ran the corpus suite against the new config code because CI is where the two components meet and CI never ran. What a stranger sees: the RED BADGE fronting the README on GitHub AND on the PyPI landing page ("green means the fixtures still pass" reads as "broken"). At the tag itself the release-relevant jobs — root suite (143), wheel + packaged-pins install proof, mcp-service — are green; the red job's subject ships in no artifact | `corpus/tests/` (one fixture line) + `.github/**` (corpus-job deps, label, comment) — the first frozen at CP-29, the second untouched deliberately since a deps-only fix still leaves the job red on (b) | opened CP-29 by the push itself; invisible for five checkpoints precisely because the guard that would have caught it is the thing that never ran | the badge telling the truth again, and CI actually guarding the corpus suite's post-CP-24 closure — a ~5-line lift, and the first thing any next CP should do. **[CP-33] The mcp-service bit-exactness flake question: WAITS, recorded** — the CP-33 prompt reports CP-30's run hitting a one-ulp flake on the hosted runner, but no committed report evidences it (this row and CP-30's report record only green; row 14 documents the arm64 ULP case, deliberately untouched), the last three CI runs are green, and §4's rule holds: a reported defect stays UNVERIFIED until a checkpoint verifies it. `np.allclose` would weaken the guard whose subject is ADR-0016's named hazard on unverified evidence — if the flake fires in a committed run, that run's numbers make it decidable (`mcp-service/tests` was frozen this CP regardless). **[CP-33, same day] It fired — on this CP's own push**: run `32126927920` (ubuntu-latest x86_64), `test_stored_vectors_are_the_pinned_encoders` failed `chunk p0009c0002 vector is not the pinned encoder's` at a commit whose mcp-service tree is byte-identical to three prior green runs — the hosted-runner nondeterminism is now EVIDENCED, not rumored (the CP-33 follow-up commit records it; the job re-ran green on the follow-up push, which is the flake shape, not a cure). What is still missing is the MAGNITUDE: the oracle's failure message prints no |Δ| or cosine, so the tolerance decision remains blocked on an `mcp-service/` lift that first makes the test print its numbers on failure — decide the ADR-0016 tolerance question with data, not with this log line. **[CP-40] Re-owned on a live trigger** (audit O8: the "mcp-service cadence" park was void, and the row's own unblock condition fired at CP-33 and was spent evidencing the flake EXISTS): new terms — the NEXT time the flake fires in CI, the CP standing nearest takes the `mcp-service/` lift that makes the oracle print its numbers on failure (|Δ|, cosine), because §4's rule demands the tolerance be decided with data; a red run is loud, so this trigger cannot expire silently (§8 rule 9). **[CP-51] It expired silently six times.** The named event fired on CP-41 `32756175360` (2m36s after the park was committed), CP-44 `32772191162`, CP-45 `32862926817`, CP-47 `32879812703`, CP-48 `32900016996` and CP-49a `33059773150` — and before the park on CP-33 `32126927920` and CP-34 `32140353530` — always `test_stored_vectors_are_the_pinned_encoders`, chunk p0009c0002, on `ubuntu-latest` x86_64, while CP-42/47/48/49/50's rule-9 passes recorded "no event fired" and CP-48/49 filed the red as "runner-side, cause UNVERIFIED" without naming this row: the 2026-08-27 sweep's headline, and why §8 rule 9 grew teeth this CP. **A1 LANDED**: the oracle now states the magnitude in its failure message — `estate/mcp-service/tests/test_backend.py:107-112`, forced locally with a 1e-7 perturbation: `chunk p0001c0000 vector is not the pinned encoder's: max|Δ| 9.686e-08, cosine 1.000000000` — strictness unchanged (`np.array_equal`; 89 passed). Row 14 superseded into this row. **[CP-55] DONE — the tolerance decided (ADR-0016 amendment, 2026-08-28).** The token's event fired three times with one measurement — CP-53 run `33171819467` attempts 1 and 2, CP-54 run `33178592668`: chunk p0001c0000, `max|Δ| 7.451e-09, cosine 1.000000119`, docs-only diffs, deterministic per runner image (the image itself unrecorded — CI states only `ubuntu-latest`) — and CP-55 measured the hazard end the record never had: the quoted "wrong embedder ≈ 0.80" was CP-18's different-chunks baseline under the SAME encoder (reproduced: 0.799976), while the actual substitution hazard — Chroma's default EF, measured an fp32 ONNX export of the SAME checkpoint (90 MB, unquantized; not the quantized model ADR-0016 assumed) — sits at max|Δ| 1.155e-07..2.719e-07, cosine within 1 ULP of 1.0, on all 51 case_0001 rows: 7.75× above the worst benign drift (arm64's 1.490e-08), not six orders. The bound: `max|Δ| ≤ 4e-08` (the geometric midpoint of the measured margin — 2.7× above the worst drift, 2.9× below the substitution floor) AND `cosine ≥ 0.999` (the wrong-model floor; cosine measured unable to resolve either near class — every row of both lands within 1 ULP of 1.0). `np.array_equal` retired; `test_identity_bound_fails_wrong_in_either_direction` proves both directions (the drift classes pass, the measured substitution delta fails, a wrong-direction vector fails the cosine floor alone); mcp suite 90. What the oracle no longer catches, by decision: per-component drift ≤ 4e-08 — bit-exactness across environments is not asserted. WHY the runner's arithmetic differs stays unknown (reduction order / BLAS / torch build); the amendment's reopen conditions — a drift materially above 7.451e-09 crossing the bound, a substitute under ~1.155e-07, any encoder re-pin — stand in for the investigation. `waiting-on: nothing — closed (CP-55: ADR-0016 amended, the bound set)` |
| 28 | **NEW (CP-31), DONE (CP-33)** — the wheel force-includes `pins/thinking-on/pins.gsj.json` beside the off reference (`gsj_rollout/pins/thinking-on/`, data only, zero `checks.py` lines; sdist root set extended so the wheel-from-sdist build keeps it; ADR-0019/0024 posture unchanged — default resolution still means thinking-off, `GSJ_PINS_PATH` selects). The packaged path is documented in the README pins section; the examples repo's interim copy AND its drift guard retired (train.py locates the packaged file via `find_spec` before its first import). Proven: in-suite layout test resolves the packaged on-file to the 3-id tail; the 0.1.1 wheel lists both files. Original finding: the wheel carries only the thinking-OFF reference pins, so a `pip install`-only consumer cannot run thinking-on at all: `pins/thinking-on/pins.gsj.json` lives in the library checkout and ships in no artifact (ADR-0019 packaged the off set at CP-16; ADR-0024 deliberately kept default resolution meaning thinking-off and made `GSJ_PINS_PATH` the thinking-on surface — correct, but the *file* the variable must point at has no pip-reachable home). Felt the moment the consumer surface's shipped default flipped ON (CP-31; external F-40): default-on + unset variable = every first-run episode quarantined G6-only. The durable cure is data-only: package `pins/thinking-on/` beside the existing packaged copy (`gsj_rollout/pins/thinking-on/`, the ADR-0017/0019 force-include mechanism as-is) plus a documented packaged path — selection stays explicit-env, ADR-0024's posture survives unchanged, and the examples repo's interim byte-copy (with its train.py drift warning against the installed wheel's reference sets) retires. Related, half-noted in external F-41: `GSJ_PINS_PATH` is read once at `checks` import (`checks.py:118`, CP-11b) — consumer scripts must set it before their first import; a first-*use* re-read (or a consumer-surface sentence in the library docs) would remove the ordering trap. **[CP-39] the artifact-level assert catches up**: `release.yml`'s required-entries tuple — the tag path's only guard, since that workflow deliberately does not re-run CI — still asserted the original three entries through two releases (one entry short at 0.1.1, two at 0.1.2); CP-33 noted it one short (this row's file) as "a one-line follow-up for the next `.github` lift", CP-34 took that lift, executed release.yml to publish 0.1.2, and added a second unasserted must-ship instead; found two short by the audit (O2), closed at CP-39 with `gsj_rollout/pins/thinking-on/pins.gsj.json` and `gsj_rollout/ingest_corpus.py` asserted beside the original three | `pyproject.toml` (package-data only; no `checks.py` line — the resolver already honours the variable) | opened CP-31: the examples repo could not be given a default-on surface without discovering the wheel has no on-mode pins to point at; deferred because CP-31's freeze-lift excluded `pyproject.toml` ("a library change is a finding and CP-33's problem") | a pip-only thinking-on estate; retiring the examples repo's interim pins copy and its drift risk |
| 27 | **NEW (CP-29), DONE (CP-33) — 0.1.1 cut at the CP-33 commit refreshes the landing page (verified live post-tag, the CP-30 badge precedent: the one-commit protocol cannot carry its own release result). [CP-30] had noted: heals at the next release (0.1.1+), PyPI's only mechanism; the badge half of the page already healed live when wishlist 26 landed (the badge image is fetched, not embedded)** — 0.1.0's PyPI landing page immutably embeds the pre-publication README. The wheel bakes the README at build time; the tag sits at `1565813` (CP-28); and the CP-29 README fix lands in the CP-29 commit AFTER the tag — structural, not an oversight: the one CP commit must contain the report, the report must contain the publish results, so the tag cannot point at it. The project page therefore says "Until the first PyPI upload lands…" about itself, and "not yet published" about an examples repo that has a public remote, until the next version uploads (PyPI descriptions cannot be edited without a new version). The badge half of the page heals on its own when row 26 lands — the badge image is fetched live | none — the repo README is already fixed in the CP-29 commit; the cure is any next release (0.1.1+), PyPI's only mechanism | opened CP-29; structural to the prompt's own ordering (publish at Step 4, the doc pass at Step 5, one commit at the end) | the landing page telling the truth, at the cost of a version bump |
| 29 | **NEW (CP-32), DONE (CP-33)** — the printout now detects the resolved binary's absence and says which case it is: an installed wheel (clone the library repo; commands print with `<checkout>` placeholders) or a checkout with an unbuilt venv (the REVENDOR.md recipe pointer — row 22's hint), each branch test-proven and the NOTE naming the checked path. Original finding: under a PyPI install, `cli.py`'s serve printout renders the two Polar commands against the installed package location, producing an absolute `…/site-packages/vendor/polar/.venv/bin/polar` that ships in no wheel (external F-45; verified ENOENT at the stranger test — run as printed, the command dies "No such file or directory", and only the RUNBOOK's rewritten §Run sentence rescues it). The checkout era never showed this: the resolved path existed there, which is why F-21's original wording survived CP-29 and CP-31 unnoticed | `cli.py` — detect an installed-wheel layout and print a placeholder + words (or a `--polar-root` flag) instead of a path the library cannot know | opened CP-32 (the first CP to run serve from a wheel rather than a checkout); RUNBOOK carries the interim cure | the printed commands being runnable as printed, on both install shapes |
| 30 | **NEW (CP-32), DECIDED + DONE (CP-33)** — (b) is **ADR-0025: surfaced, never screened** — length-terminated episodes stay trainable, every collection surface prints `length-terminated: K/N` (cli submit; train.py collect and grade; the bridges' TRUNCATED label aggregated at grade), the spec carries the posture, and a fail-closed screen is priced as a future `CheckPolicy` knob needing its own ADRs; (a) landed examples-side — train.py prints `masked rows by reason:` with the G6 finding names before the assert turns them fatal. The measured halves, for the record: (a) the trainer-side G6 re-check at ingest MASKS foreign-mode rows instead of naming findings — the documented "fails with G6-named findings" symptom does not exist; the real symptom was `AssertionError: 72 masked rows in a qualifying collection`, undiagnosable until train.py's CP-32 assert message (external F-49; the examples-side mitigation names the hypothesis only when ALL rows mask); (b) length-terminated episodes qualify silently — 7/72 thinking-on episodes ended `finish_reason: length` and every one entered the training set with no line saying so (external F-47) | `checks`/bridge — surface the computed G6 findings on the ingest return (or a first-class mode-mismatch error), and a policy option to flag/reject `finish_reason: length` episodes (H-41's shape; whether to train on truncation stays the consumer's call) | opened CP-32 | a mixed-mode grade failing in words, and truncated episodes visible before the GPU spends on them |
| 31 | **NEW (CP-32), DONE (CP-33)** — the receiver stamps the resolving pins document's declared `mode` (ADR-0024; absent = thinking-off; unreadable = pins-unresolved) into every archived filename, quarantine included: `<session_id>.<mode>.json`. Test-proven: an off-mode body under the on-pins quarantines as `.thinking-on.json`. Per-mode `traces_dir` stays the documented cleaner discipline. Original finding: the receiver's archive is not per-mode: across a serve restart both modes' accepted traces land in one `traces_dir`, distinguishable only by content (external F-48; 144 undifferentiated files after the A/B). The `--out` discipline protects the trainer's working set; nothing protects the durable archive, and re-grading an archived mix reproduces F-49's failure | `receiver.py` — stamp the process's pins-mode into the layout (per-mode subdir or a filename token), one line at write time | opened CP-32; RUNBOOK's interim cure is "point each mode at its own traces_dir" | an archive that can be re-graded years later without guessing which serve wrote which file |
| 32 | **NEW (CP-32), CLOSED (CP-33) — found already cured at HEAD**: CP-30 landed both the `True → "on"` mapping and its test (`test_thinking_yaml11_bare_spellings`); F-50's traceback was measured against the 0.1.0 WHEEL, tagged at CP-28, one commit before the validator — the artifact lagged, not the code. 0.1.1 ships it; the examples config comment now states the ≥0.1.1 behaviour. Original finding: the YAML-1.1 boolean trap fires as a raw pydantic traceback, not a sentence: bare `thinking: on` arrives as boolean `True` and the validator refuses the TYPE (`Input should be a valid string`), while the config comment promised rejection "by name" and train.py's pre-read banner simultaneously prints "thinking: on" from the same byte (external F-50; comment corrected examples-side at CP-32) | `config.py` — map YAML booleans in the validator (`False`→"off" exists; `True`→"on" with words, or emit the named sentence the comment promised) | opened CP-32 | the trap's cure arriving in the error message instead of a comment the stranger must re-find |
| 33 | **NEW (CP-32), DONE (CP-33)** — the health gate rides `bash -s "$PORT"` with a heredoc, the script's own delivery idiom for its other two remote blocks; a root-suite content tripwire (`tests/test_staging_scripts.py`) fails if executable `bash -c` returns, and `bash -n` guards the syntax. Original finding: `staging/serving/serve-updated.sh:81`: `ssh "$SSH_HOST" bash -c '<newline>…'` word-joins so the remote `bash -c` gets an empty first line as its whole script; it prints `bash: -c: option requires an argument` and the health-wait body then executes in the remote LOGIN shell by accident (external F-53; the CP-32 sync completed and verified 0.040184 mean|Δ| through it) | `staging/serving/serve-updated.sh` — one line: `bash -s` (or quote the remote command whole), frozen at CP-32 by the prompt's freeze list | opened CP-32 | the sync's health gate running by design rather than by accident |
| 34 | **NEW (CP-34)** — the demo estate's two GHCR packages (`gsj-polar`, `gsj-mcp-service`) were pushed and verified pullable AUTHENTICATED, but ghcr.io creates new packages PRIVATE and exposes no API to change that (REST PATCH → 404; the visibility toggle is web-UI only) — measured with the anonymous-token probe that returns 200 for the already-public `gsj-pi-harness` and 403 for both new packages. Until the operator flips them (package → settings → Danger zone → Change visibility → Public, both packages), a true stranger's `./bootstrap.py up` fails at the image pull — with the bootstrap's own message naming the out-of-band load as the cure, but the cure a stranger should get is the pull just working. **[CP-36] DONE — RETIRED, verified live**: the operator flipped both; anonymous token probes answer 200 on all three packages, and the CP-36 stranger's genuinely cold `up` pulled from GHCR with no credentials at all (the 4.25 GB mcp image in 18.0 s). The spirit of the row moves to row 40: the pull now works per-PLATFORM, and two of the images publish only one (F-54) | operator, web UI — no repo change can do it | opened CP-34 by the first image publish; `gsj-pi-harness` was flipped the same way at its own first publish, which is why the corpus contract could always name it | the demo's one-command promise being true for someone with no credentials at all |
| 35 | **NEW (CP-34)** — the first-episode pins walk for a foreign estate is not one-command. The bootstrap derives what the corpus alone determines — G1's approved set from the corpus's own skill cards, G2 by AGENTS.md byte-substitution on the reference capture (identity-tripwired) — so a reference-model estate accepts its first episode with zero manual pins work. But the tokenizer-bound values (G6's expected tail, `builder.end_of_turn_token_id`, the estate-side G4 walk) are the reference estate's, and a stranger serving a non-Qwen model gets episodes rejected naming those digests: the ADR-0019 design working, with the cure (the pins walk, `docs/checks-spec.md`) documented but manual. The bootstrap prints exactly this warning when `inference.model` differs from the reference. **[CP-36] Not exercised** — the stranger test's endpoint was the reference stack (the control), so the preflight's FAIL rows and this row's manual-walk cure were reviewed, not suffered; the row is CP-37's named subject. **[CP-37] NARROWED — the endpoint-derivable half is CLOSED**: the demo bootstrap now derives the G6 tail and `builder.end_of_turn_token_id` from the endpoint's own template render (vLLM `/tokenize`+`/detokenize`) at `up` and writes them into the estate pins where `GSJ_PINS_PATH` points — no manual walk; the control run (the derivation forced through the foreign-model branch against the reference stack) reproduced every pinned value exactly in both modes, and a Llama-3.1 tokenizer derives sane values (tail `[128006,78191,128007,271]`, EOT 128009). The residual, named: G4's two byte hashes (snapshot-only — the bootstrap writes them EMPTY for a foreign model, recipe in the demo's MODEL-SURFACE.md), endpoints without `/tokenize` (derivation refuses loudly, reference values kept with the quarantine consequence named), and the first foreign EPISODE through the seam (CP-38). **[CP-38] the episode half is CLOSED**: the first foreign episode ran through the demo's own printed one-liner (Llama-3.1-8B-Instruct via the unsloth mirror, `llama3_json` parser, the snapshot's embedded Meta template, generation config pinned) and was **ACCEPTED — `chains_total: 1`, 8/8 completions merged, zero findings**, with the full preflight all-ok before it and the derivation's live values equal to CP-37's offline snapshot measurement exactly. Residual now: G4's snapshot walk and `/tokenize`-less endpoints | `pins/derive_pins.py`'s orbit + the demo bootstrap — a walk that runs from one captured episode's evidence and rewrites the estate pins file | opened CP-34; invisible before because every prior estate served the reference model | a stranger with their own model reaching an accepted first episode without reading the spec. **[CP-51]** the residual is demo-side code (a snapshot-reading G4/G6 derivation beside the endpoint one, ~30–60 `bootstrap.py` lines, no library line). `waiting-on: an estate serving an engine without /tokenize, or a foreign model wanting the G4 snapshot walk — the demo's next sitting after one exists` |
| 36 | **NEW (CP-34)** — the G2 reference capture (`pins/container/system_prompt.container.derived.txt`) is carried by the demo repo as a provenance-stamped COPY (`estate/system_prompt.reference.txt`, sha-tripwired against the packaged pins singleton at every bootstrap run, so drift fails loudly rather than silently) — the same interim-copy shape CP-31 used for the thinking-on pins and CP-33 retired into the wheel (row 28). The durable answer is the same one: force-include the capture beside the pins files so the bootstrap reads it from the installed wheel | `pyproject.toml` (a third force-include line) + the demo bootstrap dropping its copy | opened CP-34; the copy exists because CP-34's freeze-lift allowed pyproject changes only for Step 3's validate path, and stretching that lift mid-CP for a second packaging change was declined | one source for the G2 derivation, on every install shape. **[CP-51]** counted: `pyproject.toml` +2 (force-include + sdist), `release.yml` tuple +1, `tests/test_wheel_pipeline.py` +1–2, demo `bootstrap.py:673` ~3 — wheel bytes, so a version bump, and the demo's `gsj-polar` image (`ARG LIB_VERSION`) rebuilt after it. **[CP-60] DONE, the library half — the park fired.** The capture rides the 0.1.3 wheel at `gsj_rollout/pins/container/system_prompt.container.derived.txt` (force-include + sdist entry; `release.yml`'s tuple asserts it on the artifact; the demo's sha tripwire moved in-suite — `tests/test_wheel_pipeline.py` proves the shipped file hashes to the packaged `system_prompt_hash`, `f56e8a6e…`). One correction to the CP-60 prompt, measured: the file the demo tripwires is THIS one (row's own name), not `pins/system_prompt.captured.txt` (`13223ec4…`, the raw capture) — the packaged singleton and the demo's copy are byte-equal to the container-derived text. The demo half is the demo checkpoint's: `bootstrap.py:696` reads the packaged file via `find_spec` and drops `estate/system_prompt.reference.txt`; the `gsj-polar` image rebuilt at `LIB_VERSION=0.1.3` (A-28). `waiting-on: the demo checkpoint (F-70) — the tripwired copy retired against the installed 0.1.3 wheel` **[CP-61] DONE — the demo half; CLOSED.** The demo's `estate/system_prompt.reference.txt` (sha `f56e8a6e…`, byte-equal to the packaged singleton) and its tripwire are retired: `bootstrap.py`'s `reference_capture()` reads `gsj_rollout/pins/container/system_prompt.container.derived.txt` from the installed wheel via `find_spec`, tripwires it against the packaged `system_prompt_hash` (`f56e8a6e…` ∈ it, verified on the 0.1.3 wheel) and slices the AGENTS span between pi's `<project_instructions path="/workspace/AGENTS.md">` … `</project_instructions>` markers — which also retires `estate/AGENTS.reference.md` (CP-35's untripwired second copy): the sliced span is byte-equal to it (sha `5308b28e…`, 1,244 bytes, once in the capture), so the byte-substitution derivation is unchanged and has one source. `synthetic/make_corpus.py` writes the worked example's AGENTS.md from the same slice. `gsj-polar` rebuilt at `LIB_VERSION=0.1.3` (A-28). `waiting-on: nothing — closed` |
| 37 | **NEW (CP-34)** — the F-45-cured serve printout knows two install shapes (checkout, wheel) but not the third that now exists: inside the `gsj-polar` estate image the `polar` console script is simply on PATH, and the NOTE prints clone-the-repo advice beside two `<checkout>`-placeholder commands that no container user should run. Cosmetic — the demo's compose never shows a stranger this printout (the receiver service runs `serve` non-interactively) — but the printout's whole point is being right about which case you are in | `cli.py` — a third branch: resolved binary on PATH → print the bare `polar serve_rollout -c …` commands | opened CP-34, by the first install shape where the binary is on PATH | the printout being right in all three shapes it can be read from. **[CP-51] CLOSED — COSMETIC**: the demo's compose never shows a stranger this printout, and the third branch would spend 4–6 `cli.py` lines at §3 headroom 1; reopens only on a register row from a container user who read it. `waiting-on: nothing — closed` **[CP-61] REOPENED by the row it asked for** — the demo's F-77: `docker logs gsj-demo-receiver`, where the demo sends a stranger whose Polar leg did not come up, opens with this printout's `<checkout>/vendor/polar/.venv/bin/polar serve_rollout …` block (the receiver runs `gsj-rollout serve` in the `gsj-polar` image, where `polar` is on PATH — exactly the third shape). Still cosmetic in effect (the demo's compose says the block is not for the reader), and still 4–6 `cli.py` lines at §3 headroom 1. `waiting-on: a cli.py allowance — §3 headroom above the 4–6 lines the third branch costs` **[CP-62]** Not touched by row 51 (f)/(g) — the 409 hint and the topology re-render are other lines of `cli.py` that wait on the same allowance; the printout this row is about is unchanged (the cp62 receiver's log opens with it again, from the checkout shape this time — correct there). Unchanged token. **[CP-65] CLOSED — the third branch landed under the allowance it waited on (ADR-0028).** `serve` resolves `shutil.which("polar")` when the vendored venv binary is absent and prints the bare runnable pair (`polar serve_rollout -c …`, `<secret-env>=<secret> polar serve_gateway -c …`) with no `PYTHONPATH`, no `<checkout>`, no NOTE — the estate image's shape (F-77) now reads its own commands; the wheel and unbuilt-venv hints unchanged behind it (their tests re-pinned `which → None`, so they stay deterministic on a host with a PATH `polar`; the pipe test's subprocess PATH pinned empty for the same reason). The branch fires ONLY when the found binary sits beside `sys.executable` — the co-installed shape; a foreign PATH `polar` falls through to the hints (the CP's review demonstrated a stub hijacking the printout; `test_serve_printout_foreign_path_polar_still_hints` pins the discriminator). +7 lines under ADR-0028; `tests/test_cli.py::test_serve_printout_third_shape_bare_polar_on_path` fails with the branch removed. Reaches wheel consumers at the next release. `waiting-on: nothing — closed` |
| 38 | **NEW (CP-35)** — sampling defaults are the one demo-estate assumption no API probe can verify: pi sends NO sampling parameters (by design), so the engine's generation defaults ARE the sampling policy, and OpenAI-compatible servers expose no endpoint that states them (`/v1/models` carries `max_model_len` but nothing of temperature/top-p; vLLM's `--generation-config` is argv-only). The demo's preflight names the gap out loud with the consequence (unpinned vLLM samples parameterless requests at T=1.0 — the CP-09 F1 lesson) and what to check server-side, but cannot turn it into a PASS/FAIL row; G7's settings hash catches pi-side settings drift, not engine-side sampling. **[CP-36] Confirmed as the control's shape** — on the stranger run's passing preflight the sampling warn was the only non-ok row, read exactly as designed; the README now also carries the reference serve argv with `--generation-config` called out as load-bearing (F-60), which is the stranger-facing half of the attestation this row still owes. **[CP-37] Classified and placed**: the demo's MODEL-SURFACE.md files sampling under "what no probe can check" — a [you]/[estate] value whose only carrier is the serve argv; the residual (a real attestation) is unchanged | engine-side provenance, not a repo change: a serving-stack attestation (the pins walk capturing the served generation_config alongside the tokenizer values, or a vLLM extension surfacing generation defaults over the API) | opened CP-35 (Step 6 — the one endpoint difference the preflight could not close) | a stranger learning their sampling policy from a preflight row instead of from a training run's distribution. **[CP-40] Cross-referenced at last** (audit O5): this row is gap row 22's sibling (misnumbered "gap row 2" until CP-51) — the sampling half of gap row 22's provenance residual, reborn demo-side at CP-35 with no pointer either way until now; gap row 22's [CP-40] note names the shared owner — the first production bring-up. **[CP-51] The recording half exists**: `pins/derive_pins.py` now states the served generation config's sha256 (the file `--generation-config` points at) beside the pins and `--record` writes it into the pins documents' `provenance.engine` (gap row 22 [CP-51]) — so the sampling policy the walk measured is attributable to every archive produced under those pins. Not an attestation: the walk reads the FILE, not the engine's applied defaults, and the demo preflight's WARN row stays the stranger-facing half. **[CP-58] The recording half ran for real on the H200**: `pins/derive_pins.py --record` at the eval estate's serve, before the CP's episode, wrote `provenance.engine` into both pins documents — `generation_config_sha256` `2325da0f…` (the snapshot's file, byte-equal on the box to the `~/gsj-vllm/genconfig/generation_config.json` the engine's `--generation-config` names), beside the served model id, the snapshot revision, the tokenizer blob and the served template sha (gap row 22 [CP-58], field by field). The attestation half is untouched: the walk still reads the FILE, not the engine's applied defaults, and the demo preflight's WARN row stays the stranger-facing half. `waiting-on: the first production bring-up — its own --record at its serve (CP-58's is the eval estate's); an engine-side attestation of applied sampling stays that bring-up's` |
| 39 | **NEW (CP-35)** — the archive's MESSAGES lose thinking-on reasoning: vLLM's `--reasoning-parser` strips `<think>` from `content` into `reasoning_content`, and Polar's assistant-message capture has a `reasoning` field (different name) that is null in every body measured (153 CP-32 archives + the CP-34/CP-35 episodes). The reasoning survives ONLY in the token arrays, so any message-side consumer (the demo transcript, a future grader reading messages) must decode mask-1 runs with the served model's tokenizer (A-29) — a rendering dependency the archive itself was supposed to spare | vendored Polar, one mapping: capture `reasoning_content` (vLLM's field) into the message's `reasoning` at trace assembly — a carry-patch of the law-4 kind; the demo reader then renders ON transcripts tokenizer-free | opened CP-35 (the Step-1 body reading); invisible before because nothing read the messages of an ON trace until the transcript view existed. **[CP-36] Exercised end-to-end by the stranger run** — P3 confirmed verbatim (per-turn `thinking happened this session but cannot be shown` with the pip-install cure in the message; after installing, the decode resolved through read.py's cache→hub tokenizer chain). The carry-patch stays the durable cure | reasoning readable from the archive alone, no tokenizer required. **[CP-51] FOLDED into A-29** as the documented tokenizer dependency — and corrected: the evidence base has a counter-example on disk, `docs/polar/thinking-on/episode-on.accepted.json` (CP-30, H200) carries `reasoning_content` POPULATED on all three assistant messages, so "null in every body" is false for at least one served vLLM, and which response field the served stack emits was never measured (no vLLM version pin appears in CP-30/32/34/35/36 or `estate/serving/*.sh`). `waiting-on: a measured vLLM response field on the served stack — reopens then as a carry-patch row (vendor/, outside the size law)` |
| 40 | **NEW (CP-36)** — the estate's images are single-platform where it hurts: `gsj-mcp-service:0.3.0` and `gsj-pi-harness:pi0.83.0-3` publish linux/amd64 only (`gsj-polar` is multi-arch), and an ARM docker REFUSES a manifest list with no matching platform rather than falling back to emulation — the CP-36 stranger's `up` died at the mcp pull and, cured, again at the sandbox pull (F-54, the run's one blocking wall; the sandbox message even misdiagnosed the cause as unreachable ghcr.io). The demo bootstrap now cures it locally — on ARM hosts it pulls both images explicitly `--platform linux/amd64` and says so; measured working: the first MCP embed ran 115 s under emulation, episode wall unaffected (22.5/38.6 s) — but the cure a stranger should get is the pull just working, natively | an operator/publishing op, no repo change here: build arm64 variants and push a multi-arch index under the SAME pinned tags — provenance-sensitive, not a casual buildx: a rebuild today resolves different layers than the measured artifacts, so the publish needs its own build-and-verify pass (the harness image doubly so — its content feeds G2/G3) | opened CP-36 by the first ARM stranger run; invisible before because every prior estate host was amd64 | Apple Silicon strangers getting the demo's one-command promise natively, no emulation asterisk. **[CP-51]** live manifest probe 2026-08-27: `gsj-mcp-service:0.3.0` and `gsj-pi-harness:pi0.83.0-3` still linux/amd64 only, `gsj-polar` multi-arch; no newer tags. The harness image has NO build recipe in this repo (built from the archived predecessor's `sandbox/`), so its republish is a pins-walk-verified rebuild (its content feeds G2/G3), not a casual buildx; the mcp image builds from `estate/mcp-service/Dockerfile` (amd64 by `DOCKER_DEFAULT_PLATFORM`, README:49) and the demo's `polar.Dockerfile:20-21` carries the buildx template. A-28 qualified this CP. `waiting-on: an operator republish of the two images under the same tags — nothing schedules one; a library release does not force it` **[CP-61] The mcp half CLOSED; the harness half stays.** `ghcr.io/mhganainy/gsj-mcp-service:0.4.0` is now a two-platform index: linux/amd64 `846f7d73…` is the local build `6e27f067…` the H200 loaded at CP-58, byte-for-byte; linux/arm64 `f0f36043…` is CP-59's native build `32a78aae…` — composed with `buildx imagetools create` from the two MEASURED images (this row's own provenance warning: no fresh buildx resolving different layers). Verified: anonymous `docker pull` on an arm64 daemon with no `--platform` (F-54's exact failing step) and with `--platform linux/amd64`, an anonymous registry-token manifest fetch listing both platforms, and the demo's bring-up ran the published tag natively (first embed 21.6 s vs ~2 min emulated). `gsj-pi-harness:pi0.83.0-3` is still linux/amd64 only (no build recipe in this repo; its content feeds G2/G3 — the [CP-51] note stands); the demo's `ensure_image` keeps the `--platform linux/amd64` cure for it alone and says so. `waiting-on: a pins-walk-verified republish of gsj-pi-harness under the same tag — nothing schedules one` **[CP-64] CLOSED — the harness half published two-platform under the same tag, the pins-walk-verified way.** The recipe was recovered from the archived predecessor's `sandbox/` (byte-identical at v0.8.0, `git diff v0.8.0 HEAD -- sandbox/` empty; self-contained since its CP-34 — three pin files, `npm ci` off the committed lockfile) and every pinned input is still live (pi-coding-agent 0.83.0 and pi-mcp-extension 1.5.0 on npm with lockfile-equal integrity, `mcp==2.0.0` on PyPI, `node:22.16.0-bookworm-slim` last re-cut 2025-06-11 — before the 2026-08-06 original build). Rebuilt BOTH platforms from it: the amd64 rebuild's `/opt/pi` tree is byte-identical to the published image (41,052 files, one tree digest); the fresh arm64 build differs in exactly the four `@mariozechner/clipboard-*` platform natives, the materialized `.package-lock.json` naming them, and newer `/opt/mcp-venv` transitives (outside the remote-MCP episode path). Proven by the pins with one episode per platform, this row's own standard: on the H200 through the amd64 rebuild (`sk-polar-c2b7498c…`, COMPLETED/stop, G2 `f56e8a6e…` and G3 `a7a7956b…` both ∈ approved, `run_trace_checks` `[]`, docker events tying the session container to the rebuild tag, all three estate tokens 0 occurrences in the trace) and on the arm64 Mac through the arm64 build (`sk-polar-99512d4d…`, same hashes, findings `[]`). Published per CP-61's method — no fresh amd64 bytes: the index was composed with `buildx imagetools create` from the EXISTING measured amd64 child `864dc885…` (byte-unchanged — the H200's frozen load `f7a6d63a…` and every prior trace's provenance stand) plus the fresh arm64 `1dd08d75…` (pushed as `pi0.83.0-3-arm64`); new index digest `1fc083d4…`. Verified anonymously: a credential-less registry-token manifest fetch listing both platforms with the amd64 child unchanged, `docker logout` then `docker pull` on this arm64 daemon with no `--platform` (F-54's exact failing step) → arch arm64, and `--platform linux/amd64` resolving. Residue for the demo's next sitting: `ensure_image`'s amd64 cure for this image is now dead code (harmless — it gates on a missing platform that no longer happens). **[CP-66] the residue taken**: the demo retired the cure (`amd64_only`, the fallback, `daemon_arch` — dead since this row closed), its README's emulation paragraph rewritten as history, F-54's register cell closed-in-status; proven by a fresh-wheel-venv arm64 `bootstrap.py up` whose plain pull (no `--platform`) resolved this tag native arm64, 40 s to a standing estate. `waiting-on: nothing — closed` |
| 41 | **NEW (CP-37)** — the harness's Qwen-bound constants, enumerated while making the model surface explicit, all library-frozen this CP: `pi_harness.py` hardcodes `thinkingFormat: "qwen-chat-template"` and `supportsReasoningEffort: false` in the models.json it renders (a family needing a DIFFERENT thinking wire format cannot be expressed without a library change); the `provider/model` split in `pi_harness.py:104` looks like a served-name constraint but is NOT one — `config.py:408` prepends `estate.provider` (default `gsj`) itself, so `model_name` is always slashed via the one YAML and the raise is unreachable there (verified under CP-37's adversarial review, which killed a demo guard drafted on the opposite premise); it guards only direct/foreign constructions of the harness. And a non-off `thinking` on a family without the mode is a measured wire no-op (`enable_thinking` is unused jinja context on Llama templates) while the archive still stamps `.thinking-on` — a provenance label claiming a mode the model lacks. Each is named with what it would need; none bit yet. **[CP-38] still none bit, now by measurement rather than absence**: the first foreign episode ran thinking-off by design (the stamp half deliberately unexercised — MODEL-SURFACE tells modeless families to keep `off`), the wire-no-op half was measured LIVE (the derivation's off/medium mode pair returns byte-identical tails and EOT against the served Llama-3.1 endpoint), and the served-name mechanics worked end to end (`model_requested == model_used ==` the served name — audit W8, struck at CP-51: the exercised name was `unsloth/Meta-Llama-3.1-8B-Instruct`, slashed; the slash-free path was NOT exercised, and CP-38's report never claimed it) | `pi_harness.py` (a thinkingFormat/compat surface in the one YAML; a name-shape relaxation) — a §1-lift library change, priced only when a real family needs it | opened CP-37 by the Step-1 enumeration; invisible before because every estate served Qwen | a non-Qwen family running with its own thinking format, and provenance stamps that never claim a mode the model lacks. `waiting-on: a served family that needs a thinking wire format other than qwen-chat-template, or a modeless family stamped .thinking-on — a §1-lift library change priced then, with the §3 budget decision` |
| 42 | **NEW (CP-37)** — ADR-0024's per-mode pin geometry is Qwen-measured and does not transfer blind: its two-direction mode assertion exists because Qwen's off-tail (7 ids, empty think block) and on-tail (3 ids) are non-suffix siblings. On a family with NO thinking mode both modes derive the SAME tail (measured: both Llama-3.1 templates AND Qwen2.5-Coder render byte-identically under enable_thinking true/false, so the derived tails are identical per mode) — G6 degrades to template integrity and the mode assertion vanishes, correctly but silently; a family with a DIFFERENT hybrid-reasoning geometry (always-think, or an off-signature that is not an empty block) needs its own CP-28-style measurement before per-mode pins mean anything there. **[CP-38] the no-mode half is MEASURED LIVE**: both modes derive the same tail/EOT against the served Llama-3.1 endpoint, and the accepted foreign episode's G6 held as pure template integrity — the degradation is real, correct, and silent exactly as the row predicted. The different-geometry half (a real non-Qwen hybrid reasoner) remains theoretical | a measurement CP against a real hybrid-reasoning non-Qwen family (the CP-28 shape: two collections, the tail geometry, the pin decision), spec §G6 [CP-37] carries the degradation note | opened CP-37; the spec's G6 subject was Qwen's empty think block since CP-23 | per-mode pins whose mode assertion is real on whatever family the estate serves. `waiting-on: a hybrid-reasoning non-Qwen family served on an estate — a CP-28-shaped measurement CP then (two collections, the tail geometry, a pin decision)` |
| 43 | **NEW (CP-38)** — the two-terminator family geometry, measured on the first live foreign episode: Llama-3.1 under tools SAMPLES `<\|eom_id\|>` (128008) as its tool-call stop while its template re-renders those turns closed with `<\|eot_id\|>` (128009). Nothing in `gsj_rollout/` needed to change — the vendored merge is canonical-vs-canonical (renders against renders; the probe's prefix-extension row IS the chain-merge condition, and CP-37's "sampled-vs-rerendered identity is an unprobed extra condition" claim is withdrawn in MODEL-SURFACE), the sampled stop rides the stream as mask-1 data followed by the canonical close at mask-0, and the two defenses already fail closed (`A15:end_of_turn_token_id_not_configured` at the source builder; `G7:chains_truncated` at the receiver). The named residuals: (a) vendored Polar's EOT auto-detect picks the last SAMPLED token of the first stopped completion — on a two-terminator family that is the stop token, which never appears in the render, so a direct Polar consumer relying on auto-detect truncates every chain to its first turn (the gsj builder never relies on it, by A-15); (b) trainers consuming such streams should know the mask-0 close is canonical, not sampled — the trace teaches the model's real protocol (`<\|eom_id\|>`) while the context stream stays template-canonical | nothing, today — a characterization row: the demo path derives and configures the render-side closer; price a Polar auto-detect fix only if a consumer without the gsj builder materializes | opened CP-38 by the first family whose stop id differs from its template's closer | a second two-terminator family (or a trainer consuming these streams) confirming the geometry generalizes. **[CP-51] CLOSED — RECORDED**: a characterization, nothing waits on this repo (A-15 and G7 fail closed; the CP-38 measurement is now annotated on A-15). `waiting-on: nothing — closed; a second two-terminator family or a consuming trainer reopens it as a new row` |
| 44 | **NEW (CP-51)** — the wheel-shipped stale citations, moved into a row from the CP-41 ledger's "recorded, not fixed" prose and CP-50's stale-by-design list (§8 rule 9's teeth: parks live only as rows): `estate/corpus/ingest_corpus.py`'s five un-namespaced predecessor `ADR-0047` cites (:2, :815, :877, :1052, :1478) and :815's runtime string `"Generated by corpus/ingest_corpus.py"` (a pre-CP-50 path written into every generated record); the three post-CP-50 path comments inside `gsj_rollout/` — `checks.py:92` (`mcp-service/README.md`), `pi_harness.py:7` and `:78` (`corpus/ingest_corpus.py`) — in-place rewrites with no line-count change (CP-41's `checks.py:97` precedent, so no §3 question); and audit M4 (`git.date` is promised validated but only git rejects it, at scaffold, naming no corpus file — a parse check in `load_corpus` beside `ingest_corpus.py:233-240` plus one corpus test, ~8 + ~10 lines). Every one is wheel bytes (`ingest_corpus.py` is force-included; PyPI immutability makes each a version bump), so they ride ONE release together with wishlist 36 and ADR-0027's sdist note | `estate/corpus/ingest_corpus.py` + its tests, three `gsj_rollout/` comment lines in place, `pyproject.toml` version | opened CP-51 from the CP-41 ledger and the 2026-08-27 sweep; the CP-41 park "the next conscious `corpus/` lift" is the wording §8 rule 9 now rejects | a wheel whose pipeline cites this repo's own numbering and paths, and validates what its contract promises. **[CP-60] DONE for everything wheel-shipped — the park fired.** Shipped in 0.1.3: the five `ADR-0047` cites qualified as the predecessor's (`:2` names gsj-envloader's numbering outright; `:848`, `:932`, `:1107`, `:1555` — the numbers drifted since CP-51's: +32 by CP-58, +22 at CP-59, +1 here where the lock comment became two list elements), the lock's runtime string now `Generated by ingest_corpus.py (estate/corpus/ in the checkout, gsj_rollout.ingest_corpus from the wheel; …)` — true on both install shapes (the tracked freeze record `estate/corpus/staging/corpus.lock.json` still carries the old 6-element `_comment` and the next `taskbank`/`scaffold` on staging rewrites it to 7 — this CP regenerated nothing, the freeze-record bytes untouched; nothing compares `_comment`, the corpus suite is 67 green). Leftover, outside this row's enumeration: three un-namespaced predecessor `ADR-0041` cites remain in wheel-shipped files (`ingest_corpus.py:935`, `pi_harness.py:7`/`:78` — the two lines rewritten here took the path, not the cite, because the qualifier does not fit one-for-one), and the three `gsj_rollout/` comments rewritten in place (`checks.py:92` → `estate/mcp-service/README.md`; `pi_harness.py:7` → `estate/corpus/ingest_corpus.py`; `:78` → the function name `mint_admin_token` instead of a line range that had already drifted 797-810 → 933-948) — `wc -l gsj_rollout/*.py` 2,000 before and after, `checks.py` 528. Audit M4 (`git.date` parse check) is SOURCE lines in a comments-only lift and did not ship — moved to row 50. `waiting-on: nothing — closed; M4 lives in row 50` |
| 45 | **DONE (CP-58)** — was NEW (CP-51): CP-50's operational debt, moved into a row from its report and §7 paragraph: the H200's gitignored instance data (`forgejo/forgejo-data`, `forgejo/.token*`, `mcp-service/data`, `staging/serving/run`) sat at the pre-CP-50 paths; a `docker compose up` from `estate/` without the four `mv` commands in `estate/README.md` §"If your checkout predates `estate/`" mounts an empty `./data` and starts a blank Forgejo — reads as data loss, is a wrong mount. **[CP-58] The named event fired — the H200 bring-up — and the moves ran, operator-pasted and ssh-verified before and after**: every source gone, every destination present at the same size (8.1 M / 2.7 M / 16 K, the two 40-byte tokens `0600`), and `estate.sh up` on the moved data answered `existing token still valid` with the four `gsj-staging/case_*` repos listed by the API — the moved admin token authenticating against the moved instance is the proof it was a rename, not a rebuild. Two findings the README's block never met, both now in it: the box's `~/gsj-harness-rollout-server` was never a git checkout but an rsync-era copy (0.1.0, no `estate/`) — so the migration was a fresh clone at the same absolute path with both path-bound uv venvs adopted unchanged (same `POLAR_SHA`), the moves out of the renamed old copy; and `forgejo-data`'s top directory is root-owned, so its cross-parent rename needs `sudo` (Linux rewrites the moved directory's `..`). The old copy stays as `~/gsj-harness-rollout-server.pre-cp58`, code husks only | none — operator, on the box; done | opened CP-50, closed CP-58 | none remaining — the survey that found the record wrong about the box (no git checkout, a pre-CP-57 store) is the CP-58 report's |
| 46 | **NEW (CP-57)** — the encoder oracle's |Δ| bound under a NON-default embedding model is a scale-transposed extrapolation, not a measurement. CP-57 made `embedding.model` follow the config (the store records `{model, revision, dimension}` and refuses to serve under another; the chunk window is checked against the model; an unresolvable id fails at startup named) and had to say what `test_backend.py`'s bound means once the model is not MiniLM: the |Δ| end transposes as 4e-08·√(384/d) (A-31: unit-vector component scale is 1/√d, float32 drift is relative — numerically the CP-55 constant at the default model, reasoned elsewhere), the hazard end is the cross-model class (measured at CP-57, MiniLM vs bge-small on the same texts: cosine 0.23–0.31 — the 0.999 floor catches it by ~0.7; at another dimension the store refuses the vector outright). What is NOT known: any non-default model's benign drift across deployment hosts (the same-process re-encode measured 0), and its nearest same-checkpoint substitute. Related, frozen-side: `.github/workflows/ci.yml`'s HF cache key is MiniLM-only and never re-saved, so the second proof model (~45 MB) downloads once per CI run (at suite import — the suite then runs offline, no load touches the hub); the job label, CLAUDE.md:73/82 and the root README's dated proven-claims row (README.md:76) still say 89 (the suite is 107 since CP-57, 90 since CP-55). **[CP-60]** the label half fixed with `.github/` lifted (`ci.yml` 89 → 107, 58 → 67, 161 → 164; README.md:76's dated row and CLAUDE.md stay); the HF cache-key half unchanged | `.github/` (the cache key), nothing else — the measurement is a sitting, not a lift | opened CP-57; the extrapolation is the honest interim, and the bound is loud in the direction it can be wrong (a benign drift above it FIRES with its numbers printed, the CP-55 shape) | the first production re-pin's oracle standing on that model's own numbers. `waiting-on: the first embedding re-pin on a corpus meant for training (embedding.model ≠ the default) — measure the two ends for that model (benign drift across its hosts; its nearest substitute) and replace the transposition with them (ADR-0016 (c))` |
| 47 | **NEW (CP-58)** — the MCP's bare clone cache can persist the read token in a file: `gsj_mcp_service/ingest.py` `clone_or_fetch` runs `git clone --bare <url-with-userinfo>` on a cold start, and git writes that URL — credential included — into `data/clones/<case>.git/config` as `remote.origin.url` (a warm cache is refreshed by `fetch <explicit url>`, which writes nothing; the H200's cache, cloned anonymously before CP-58 and fetched with the token at CP-58, holds none — measured: 0 files under all four bares). Found by the CP-58 review, outside the CP's lift (`estate/mcp-service/` code was frozen; `config.yaml` only). Bounded: uid-1000-only host state under the compose's `./data`, never a trace, never a repo; the demo estate (cold-cloned under sign-in since CP-56) carries it today. **[CP-59] Measured on a created estate**: `estate/bringup.py`'s cold MCP wrote the read token into 4/4 `mcp-data/clones/<case>.git/config`; the run directory is `0700` for it (the exposure stays the run's operator) | `estate/mcp-service/gsj_mcp_service/ingest.py` — `init --bare` + the existing explicit-URL fetch (no `origin` remote ever written), or `remote set-url origin <bare url>` after the clone; one test asserting the token appears nowhere under the git dir; an image re-cut | opened CP-58 | a host-side reader of the MCP's data volume learning the read token — the same reader could read `estate/forgejo/.token*`, so the exposure class is unchanged, but "the value never lands in a file" is false for this one file until it is fixed. `waiting-on: the next estate/mcp-service/ lift — init --bare + explicit fetch, test-pinned, image re-cut` **[CP-61] Not folded into this CP's image cut — re-measured unchanged.** The CP offered folding the fix (`init --bare` + explicit fetch, test-pinned, 0.4.1) into the multi-arch publish; the lift covered `estate/mcp-service/{Dockerfile,README.md}`'s version header only — not `ingest.py`, not the service's suite — so the published 0.4.0 index carries the same `clone_or_fetch`, and the demo's fresh estate measured the read token in 2/2 `work/runs/demo/mcp-data/clones/<case>.git/config` (the run directory 0700, as CP-59 left it).  **[CP-83] The published-tag event already fired; the implementation is still OPEN.** CP-79 published MCP 0.4.1 and 0.5.0 and CP-82 executed the latter, so waiting for a tag above 0.4.0 no longer describes a future event. A source re-read at CP-83 still finds `clone_or_fetch` passing the credentialed URL to cold `git clone --bare`; no new secret value was read or printed for this register check. Phase 1 keeps run data private and preserves it on failed teardown; it does not change the frozen MCP clone path. `waiting-on: a service clone-storage regression proving the read token is absent throughout a cold bare cache, followed by publication of the MCP image containing that fix` |
| 48 | **NEW (CP-59)** — the demo/library consolidation, decided and not done: `gsj-rollout-demo/bootstrap.py` and `estate/bringup.py` are one design twice — the demo always creates its estate in compose (Polar included), the library script creates or adopts each service and stops at the estate. CP-59 read the demo whole: the phase order, the secret-in-env pattern, the idempotent re-run and the `/health` wait transferred; the demo-bound parts (fixed owner, in-network-only URLs, `check_corpus_yaml` dying on a foreign `corpus.yaml`, Polar/receiver in compose, `ensure_amd64_image`, the pins derivation) did not. The consolidation: the wheel force-includes `estate/bringup.py` beside `ingest_corpus.py`, the demo's `bootstrap.py` becomes a `config.yaml` reader that calls it with `--answers` (plus its own compose for the Polar leg and its pins derivation, which the library script deliberately does not do) | `pyproject.toml` (the force-include — a wheel change, so it rides a release), the demo repo | opened CP-59 | one bring-up, two entry points; a demo stranger exercising the production tool. **[CP-60] The library half DONE — the park fired, with a finding.** `estate/bringup.py` ships in 0.1.3 as `gsj_rollout/bringup.py` (`python -m gsj_rollout.bringup`), home unchanged, outside the size law (the reading CP-34 gave `ingest_corpus.py`: §3 counts `wc -l gsj_rollout/*.py`, a build-time copy adds no line to it, CP-59 already landed the 1,893 lines outside the census with no ADR — 2,000/2,000 held). **The finding**: unlike the pipeline it was NOT standalone by construction — the pre-shim 0.1.3 artifact died at `import gsj_rollout.bringup` (`bringup.py:72: ModuleNotFoundError: No module named 'ingest_corpus'`, measured in a scratch venv) because it found the pipeline, its subprocess path and `runs/` relative to the checkout. Cured by a two-way layout shim in the file itself (a declared `estate/` excursion, +10 lines: `CHECKOUT` = is the pipeline beside me; else `from gsj_rollout import ingest_corpus`; `INGEST = ic.__file__`; `RUNS` = `./runs` from the wheel, never site-packages; the record's `script` name; the closing printout's Polar path), test-pinned in the root suite from the wheel layout. What the demo checkpoint inherits from the wheel shape: no `--runs-dir` flag (runs default to `./runs` under cwd), the `--corpus` default is checkout-relative (must be passed), argparse's prog says `bringup.py`, `script_version` records the library version and no commit, the `next —` block prints a bare `polar` for the consumer's own Polar, the `--help`/generated-file headers still say `estate/bringup.py` and `estate/runs/<name>/` (the docstring is the checkout's), every verb — `--help` included — prints `checks`' ADR-0019 packaged-pins warning because `from gsj_rollout import ingest_corpus` runs the package `__init__`, and `--mcp create` from the wheel needs `--mcp-image` naming a pullable image (the default `gsj-mcp-service:0.4.0` is a local-build tag with nothing to build from). Fixed in the shim after the review measured it: the pyarrow refusal's remedy said `pip install -r estate/corpus/requirements.txt`, a checkout-only file — `pip install pyarrow` from the wheel. `waiting-on: the demo checkpoint (F-70) — bootstrap.py becomes a config.yaml reader calling python -m gsj_rollout.bringup up --answers …; the gsj-polar image rebuilt at LIB_VERSION=0.1.3` **[CP-61] DONE — the demo half; CLOSED here, the residuals move to row 51.** `gsj-rollout-demo/bootstrap.py` is a reader (1,241 → 1,163 lines after the pre-push review's fixes; what remains is the demo's own — the three-value config reader, the pins derivation, the Polar leg — not one function of `bringup.py` re-implemented): config.yaml's three values → `work/bringup-answers.yaml` → `python -m gsj_rollout.bringup up --answers … -y` run from `work/` (so the run lands at `work/runs/demo/`; `GSJ_PINS_PATH` names the demo's pins, derived FIRST, so no verb warns), then the demo's own two legs — the pins and the three Polar containers, its `rollout.yaml` re-addressed from the bring-up's host-run shape. Measured on this Mac (the working tree, images present): `up` 51.2 s (the bring-up 41.9 s of it), warm re-run 13.3 s (`reused: 8; backfilled: 0`, Polar not recreated), `down` 5.5 s, `up` after `down` 33.7 s; one accepted episode in 21 s; `clone_credential_env` names the read token in both configs and its value is absent from both archived traces. The wheel-shape inventory, disposed: no `--runs-dir` — worked around (cwd); `--corpus` — passed; the `estate/bringup.py`/`estate/runs/` prog and headers — visible in `work/runs/demo/*.yaml`, recorded (row 51); `script_version` without a commit — the library version IS a wheel's provenance, accepted; the bare `polar` closing block — muted by the demo, which runs that leg itself; the packaged-pins warning — cured by `GSJ_PINS_PATH`; `--mcp create`'s local-tag default — the demo pulls the published index and answers `mcp_image`. `waiting-on: nothing — closed` |
| 49 | **NEW (CP-59)** — the `gsj-mcp-service` image is published linux/amd64 only and **0.4.0 segfaults under qemu on an arm64 daemon** (`qemu: uncaught target signal 11` at the embed step, the container left "running" with a dead process; 0.3.0 ran 10 days emulated as the demo's MCP — F-54's "emulation works" is false for 0.4.0). `bringup.py` reads the container's log tail when `/health` goes silent and refuses naming the native build (`docker build --platform linux/arm64 … estate/mcp-service`, `--mcp-image`); the six CP-59 runs used that build. Production is amd64 and unaffected | the image publish (`estate/mcp-service/Dockerfile` is arch-agnostic already), the demo's `MCP_IMAGE` pin | opened CP-59 | an Apple-silicon stranger not building the image by hand. `waiting-on: the next gsj-mcp-service image cut (row 47's re-cut) — publish linux/amd64 + linux/arm64 under one tag and re-measure F-54` **[CP-61] CLOSED.** 0.4.0 published linux/amd64 + linux/arm64 under one tag (row 40's [CP-61] note has the digests and the anonymous-pull proofs); F-54 re-measured on this arm64 daemon: the plain pull resolves the native variant and the demo's first embed ran 21.6 s natively. The bring-up's arm64 refusal text and its native-build default stay correct for a LOCAL tag and are cosmetic against the published one (row 51 (d)). `waiting-on: nothing — closed` |
| 50 | **NEW (CP-60)** — audit M4, split out of row 44 because it is source, not comments: `corpus.yaml`'s `git.date` is promised validated by the contract but only git rejects it — at `scaffold`, mid-push, naming no corpus file (`load_corpus`'s `GIT_KEYS` check, `ingest_corpus.py:244-250` at CP-60, requires three non-empty strings and parses none). ~8 lines (a parse check that names the file and the field) + one corpus test | `estate/corpus/ingest_corpus.py` source + `estate/corpus/tests/` (wheel bytes, so a version bump rides it) | opened CP-51 inside row 44; CP-60's lift was comments-only | a scaffold refusal a stranger can act on.  **[CP-83, recording CP-82 B07] The refusal event fired; OPEN, no date-validation fix in phase 1.** A copied corpus with `git.date=definitely-not-a-date` passed `validate` 4/4 in 0.319 s, then its temporary file-rail scaffold failed with git's invalid-date error in 0.274 s. This is the existing M4 obligation, not a new duplicate row. Fixed name/email/date still determine corpus SHAs (CP-82 §7 item 1). Phase 1's only named `ingest_corpus.py` change is row 76's notice; CP-82 priced B07 but its §8 phase text did not expressly assign the validation change. `waiting-on: operator acceptance of a fixed-date validation specification and a corpus-validation checkpoint proving the audit's invalid date refuses while supported fixed dates preserve commit SHAs` |
| 51 | **NEW (CP-61)** — what the demo measured as a wheel consumer of `gsj_rollout.bringup`, each a library-side gap the demo works around rather than forks: **(a)** the emitted `rollout.yaml` addresses a host-run Polar only — `polar.rollout.host` 127.0.0.1 with no `public_url`, `receiver.host` 127.0.0.1, `traces_dir`/`artifacts_dir` as host paths — and no flag says "Polar runs elsewhere" (the demo re-addresses those values into `work/estate/rollout.yaml` and re-renders); **(b)** `--mcp create` checks image PRESENCE and never pulls, and its default is a local build tag — a consumer must pull and pass `--mcp-image` (the demo does; on the H200 the no-pull is the point, so this is a default, not a rule); **(c)** `pins_g1_check` reads the PACKAGED set even when `GSJ_PINS_PATH` names the estate's own, so every demo `up` prints a false `WARNING: 1/1 skill card(s) are not in the packaged approved set (G1): ['brief']` for a corpus whose pins the demo derived one step earlier; **(d)** on an arm64 daemon the native-image default is announced (`defaulting to the native image gsj-mcp-service:0.4.0-arm64`) BEFORE the `--answers` value is read — the published image ran (run.json, compose, the container agree), the line said otherwise; **(e)** no `--runs-dir` (the cwd decides), and the wheel's `--help`, `GENERATED by estate/bringup.py` headers and `estate/runs/<name>/` strings; **(f)** the client's fixed `--task-id gsj-task`: a second `submit` while one runs answers `rollout server unreachable or errored … 409 Conflict` with no hint that `--task-id` exists (measured: the stranger's second episode). **(g)** `gsj-rollout serve` re-renders `topology.rendered.yaml` beside its config at EVERY start, receiver included — a receiver container sharing the config directory with Polar's two containers rewrites the file they read at their start (the demo gives the receiver a byte-copy in its own directory); **(h)** with `--gateway-host` answered the rollout/gateway/receiver ports are still picked by a HOST-port scan (`8080 is busy on this host; using 8081`) — meaningless for a leg that runs in containers, and the advice names a flag a reader of the demo cannot pass. (a)–(e), (h) are `estate/bringup.py` lines (outside the size law; a wheel change rides a release), (f) and (g) `cli.py`/`client.py` lines under the size law | `estate/bringup.py`; `cli.py` for (f) | opened CP-61 by the demo's first run on the wheel (F-70–F-77 in the demo's register) | a consumer standing up an estate without re-addressing its config, pulling by hand, or reading a false warning. `waiting-on: the 0.1.4 release (bringup.py rides the wheel) — (a)–(e), (h); (f) and (g) wait with it on a cli.py allowance` **[CP-62] The bring-up residuals taken; (f)/(g) recorded, not taken.** **(a)** answered as a documented shape plus a switch: `--polar-leg host|container` (answer `polar_leg`, recorded; decided before any pull) — `container` needs `--gateway-host` (the probe measures this host, not where that gateway runs — refused up front without it), binds the rollout API and the receiver on `0.0.0.0`, warns on a loopback engine URL, and the closing block and the generated `rollout.yaml` header name what only the consumer can re-address (`polar.rollout.public_url`, `receiver.public_url`, `receiver.traces_dir`, `harness.artifacts_dir`, `estate.serving_base_url` as the gateway container dials it; the demo's `containerize_rollout_yaml` cited as the worked example); `status` names the ports as the consumer's instead of probing loopback. **(b)** decided: a registry reference that is absent is pulled once (`image_pull`, said out loud; a bare build tag is not — `image_has_registry` is Docker's own first-component rule), and the wheel's default is the published two-platform index `ghcr.io/mhganainy/gsj-mcp-service:0.4.0` while the checkout's stays the local build tag the H200 loads; the refusal names both routes. **(c)** fixed at the source: `pins_g1_check` reads `gsj_rollout.checks.PINS_PATH` — the same resolver the validators use (`GSJ_PINS_PATH` > checkout > packaged) — records `pins_path`/`pins_source`/`not_in_approved_set` (the old `not_in_packaged_pins` key is gone; the demo's correction line keys on it and so stops printing by itself), warns naming the document and, when it is the packaged set, says to set `GSJ_PINS_PATH`; on the cp62 run: `every skill card (2) is in the approved set at …/pins/pins.gsj.json (checkout)`. **(d)** the native-image line is printed after the answer is read, only when the native build is what runs, and only from the checkout (the wheel's default is the index, native on both platforms). **(e)** `--runs-dir` on every verb (a `status`/`down` without it refuses naming the flag); `PROG` (`estate/bringup.py` | `python -m gsj_rollout.bringup`) in the generated headers, the `.env` header, the refusals and the closing block; the wheel's `--help` docstring rewritten at parse time (`./runs/<name>/ (--runs-dir)`). **(h)** with `--polar-leg container` the three ports are the conventional 8080/8200/8300 unscanned (an explicit one taken as given) — the demo answers `polar_leg: container` (0.1.3 ignores the key). **(f)** and **(g)** stay: both are `cli.py` lines at 2,000/2,000 — (f) would extend the `httpx.HTTPError` message at `cli.py:163` to name `--task-id` on a 409 (an in-place string edit adds no line, but the lift covered `cli.py` "only if the size law allows" and the default was recorded, not taken — the demo README already names `--task-id`); (g) needs a guard around the render at `cli.py:38-41`. Row 37's reopen is not touched by either — they share its allowance, not its printout. `waiting-on: the 0.1.4 release — (a)–(e), (h) ride the wheel then; (f) and (g) on a cli.py allowance (§3 headroom above the lines they cost), with row 37` **[CP-63] The wheel half DONE — the park fired; 0.1.4 carries the bring-up.** The released wheel (18 entries) ships CP-62's `gsj_rollout/bringup.py` (116,125 B): `--forgejo-image`, `--polar-leg`, `--runs-dir`, the registry pull-when-absent, `pins_g1_check` on `checks.PINS_PATH`, the `PROG` strings — the packaged `FORGEJO_IMAGE` read back from the installed artifact as `codeberg.org/forgejo/forgejo:16.0.3` on both install proofs (TestPyPI, PyPI), and the demo's from-PyPI stranger run measured the pull with no mirror step (the CP-63 report carries the timing). The demo retired what the release obsoletes: floors to `>=0.1.4`, the `--forgejo-image` pre-guard and the 0.1.3-only G1 correction branch gone, the README mirror recipe rewritten as the historical cure for 0.1.3. (f)/(g) are the row's residue, unchanged. **[CP-65] (f)/(g) DONE — the row is fully closed; the allowance arrived as ADR-0028 (the law at 2,016 exactly, machine-checked).** **(f)** the `httpx.HTTPError` message carries a 409-only hint naming `--task-id` (+2 — CP-62's zero-line estimate priced an UNCONDITIONAL edit, which would put 409 advice on every exit-3 transport error against the CP-27 standard; the conditional reads `exc.response.status_code`, and the 500 path is asserted hint-free). **(g)** `serve` renders to a string and the guard never raises: a bytes compare under EAFP (an undecodable/unreadable/vanished file counts as different and re-renders — the CP's review reproduced a text-mode read dying on non-UTF-8 where the old code overwrote), byte-identical content left untouched (a receiver restart is a no-op on the shared file — same inode, same mtime), and a real re-render lands via a pid-unique tmp + `os.replace` (two concurrent serves cannot cross-truncate a shared `.tmp` — the review demonstrated the fixed-name race landing a 0-byte topology), no residue (+7). Both fail their tests with the change removed. `cli.py` at 257; on the wire for wheel consumers at the next release. `waiting-on: nothing — closed` |
| 52 | **NEW (CP-61)** — the bring-up's Forgejo pin is dead on its registry: `codeberg.org/forgejo/forgejo:16.0.2` (`FORGEJO_IMAGE` in `estate/bringup.py`, also `estate/forgejo/docker-compose.yml` and the demo's retired compose) still resolves to an index on codeberg, but BOTH platform manifests that index lists answer 404 — measured 2026-08-30 with an anonymous registry token (`16.0.1`, `16.0.3`, `16.0`, `16` answer 200 on both platforms; `16.0.2` 404 on both; `docker pull` fails identically with and without `--platform`). Every estate so far had the image locally (this Mac since 2026-08-18, the H200 by `skopeo copy` at CP-17); the demo's from-nothing run is the first host to pull it and died at `docker compose up forgejo` (F-78). A registry-side event outside every repo — the Forgejo/Gitea container registry's cleanup is known to drop an index's child manifests — and the bring-up offers no `--forgejo-image`, so no consumer routes around it | `estate/bringup.py` `FORGEJO_IMAGE` (+ `estate/forgejo/docker-compose.yml`, `estate/README.md`) re-pinned to a live tag — contract-checked, since the bring-up's admin-CLI and token-minting measurements (CP-59: `--random-password`, tokens for another user only under basic auth) were made on 16.0.2 — or a `--forgejo-image` answer; a wheel change, so it rides a release | opened CP-61 by the from-nothing run | any new host standing an estate up: today its only cure is the bring-up's own refusal text — load the image out-of-band from a host that holds it (the CP-61 run did exactly that: `ssh h200-admin docker save … \| docker load`). `waiting-on: the 0.1.4 release (bringup.py rides the wheel) — with row 51; re-measure codeberg's answer for 16.0.2 at that cut, since a re-push there would moot the re-pin` **[CP-62] CLOSED — re-pinned, with the route around.** Re-measured 2026-08-30 16:51 UTC with an anonymous registry token, both platforms: `16.0.2`'s index still served, its amd64 (`398cb21d…`) and arm64 (`c7fb3ea8…`) manifests still 404, `docker pull` failing identically with either `--platform`; `16.0.1`, `16.0.3`, `16.0`, `16`, `15.0.3`, `16.0.3-rootless` 200 on both; `16.0.3`, `16.0` and `16` are ONE index, `sha256:7c4e1db440be…`; no `16.0.4`/`17` yet. **A second finding**: the Forgejo project's own mirror `code.forgejo.org/forgejo/forgejo` serves `16.0.2` at codeberg's exact index digest (`2fdfe28b…`) with both platform manifests 200 — the CP-59-measured bytes are still pullable, through the mirror. **The pin**: `codeberg.org/forgejo/forgejo:16.0.3` in `estate/bringup.py` (`FORGEJO_IMAGE`, with `FORGEJO_IMAGE_DIGEST` recorded and checked after a pull — a re-cut tag warns), `estate/forgejo/docker-compose.yml`, `estate/README.md`; by tag, not digest, because a `docker load`ed image (the H200's only route — dockerd's pulls are firewalled there) carries no digest and a digest reference would never match it. **The route around**: `--forgejo-image <ref>` (answer key `forgejo_image`; a re-run defaults to its record, a change prints `changed:`), recorded in `run.json` with the image id, RepoDigests, platform and whether this run pulled it; the pull is now the script's own step, so a dead tag is a refusal naming the flag, the mirror, `name@sha256:…` and the out-of-band load. **The CP-59 measurements repeated on 16.0.3** (`16.0.3+gitea-1.22.0`), every one unchanged: `forgejo admin user create --random-password` prints `password is '…'` and the create path parsed it; `POST /users/{owner}/tokens` under an admin TOKEN → `401 auth method not allowed`, under basic auth → 201 with `sha1`; `REQUIRE_SIGNIN_VIEW` — anonymous `GET /api/v1/version` 403 (`Only signed in user is allowed to call APIs`) and anonymous `git ls-remote` 401 on the closed estate, 200 and the refs on a `--anonymous-read` one of the same tag, the credentialed read listing the four refs; `/api/healthz` 200; the owner/repos/branches shapes and the past-the-end empty page as before. **Proven**: the image removed from this daemon, `bringup.py up` from nothing pulled it and stood the estate up in 79.4 s (Forgejo 27.4 s with the pull); the re-run `reused: 8; backfilled: 0`; `--forgejo-image codeberg.org/forgejo/forgejo:16.0.1` (absent → pulled) stood a second estate up under `--runs-dir` with the record saying `16.0.1`/`3eb3107b…`; `--forgejo-image …:16.0.4` (no such tag) refused naming the flag; one episode accepted in 24.6 s with the credentialed clone, no secret value in the trace. Frozen-side, recorded: `estate/forgejo/down.sh:18` still names `16.0.2` for its root-owned wipe helper (a new host without that image would fail the `--wipe` path there — the lift named the compose file only); `tests/fixtures/golden-mac/MANIFEST.md` is historical. The H200's live instance runs its local 16.0.2 until 16.0.3 is `skopeo copy`'d there (the compose comment says so). What a stranger gets: from the checkout, the pull; from PyPI **0.1.3 — still the dead pin**, but a cure with no second host (the mirror's digest-equal `16.0.2`, `docker pull` + `docker tag`, the demo README's recipe, measured); from 0.1.4, the pull and the flag. `waiting-on: nothing — closed` **[CP-63]** 0.1.4 is published: what this row closed on now reaches a PyPI stranger — the packaged pin `16.0.3` read from both install proofs' artifacts, and the demo's from-PyPI `bootstrap.py up` pulled it with no mirror step (the report carries the timing). The demo README's mirror recipe is rewritten as the historical cure kept for anyone still on 0.1.3. **[CP-64]** the re-pin reached the H200: 16.0.3 skopeo-loaded (amd64 id `f02d6cf9…`), the instance `down`/`up`'d on its migrated data in the compose comment's order, the admin token surviving; and the closing note's `down.sh:18` wipe-helper literal moved 16.0.2 → 16.0.3 under this CP's `estate/forgejo/` lift. |
| 53 | **NEW (CP-65)** — two tracked echoes of the size-law number sit OUTSIDE CP-65's freeze-lift and still say 2,000 after ADR-0028 moved the law to 2,016: `README.md:6` ("the 2,000-line shell" — current-state prose, the PyPI long_description and the GitHub front page; its `:75` audit row "1,999/2,000" is dated to the 2026-08-24 audit and stays historical) and `.github/workflows/ci.yml:27` (the size-law comment "outside the 2,000-line budget"). Neither is on ADR-0012/0028's mandatory echo list (CLAUDE.md law 2, charter §1/§3/§8/§9, the guide — all moved in-CP); fixing them at CP-65 would have been a second undeclared out-of-lift excursion, so they are parked HERE, in a register row with a token, rather than only in the untracked report (§8 rule 9 (a)) | `README.md:6` "2,000-line" → "2,016-line"; `ci.yml:27` "2,000-line budget" → "2,016-line budget" — two one-word edits riding whichever CP next lifts those files; every release CP touches `README.md` (the version literals move each cut — the CP-60/CP-63 pattern), so the next release is the natural carrier | opened CP-65, by its own review (the census lens) | an external reader of the front page or the CI file inferring the law is still 2,000 while the suite machine-checks 2,016. **[CP-67] DONE — CLOSED at the token's own named event (the release CP, which lifted both halves).** Each echo verified against the tree before the edit, not the prompt: `README.md:6` "2,000-line shell" → 2,016 (`wc -l gsj_rollout/*.py` = 2,016, ADR-0028's equality green), `ci.yml:27` "2,000-line budget" → 2,016; the (164) pair CP-65's report annotated as riding the same `.github/` lift moved beside them — the `ci.yml:4` header comment and the root job's display name → 169 (verified by collection: 169 passed). The CP-67 sweep (four refereed sweepers over the tracked tree) found two more false echoes of the row's class in lifted paths, fixed in the same commit: `README.md:96` "the root suite (164 tests…)" → 169, and `docs/README.md:3` "1,999 lines" → 2,016 — a pre-2,000-era echo that escaped both the CP-56 and CP-65 sweeps. ~~`waiting-on: nothing — closed (CP-67: the release lift arrived; all six echoes moved)`~~ **[CP-75] REOPENED — the same class, now six echoes: ADR-0033 moved the law to 2,034 and the pair outside every lift says 2,016 again** — `README.md:6` ("the 2,016-line shell") and `.github/workflows/ci.yml:27` ("outside the 2,016-line budget") — plus `README.md:102`'s "32 ADRs" (ADR-0033 written this CP; the sentence counts the untracked directory) and the root-suite count the CP-67 closure moved beside them — `README.md:96` "(169 tests", `ci.yml:4` "the root suite (169)", `ci.yml:40` `name: root suite (169)` (176 by collection since this CP's +7). The mandatory list moved in-CP (CLAUDE.md law 2, its layout line and its two suite counts, charter §1/§3/§8/§9, the guide's three lines — `how-it-works.md:15`/`:24` and `guide/README.md:9` — and `docs/README.md:3`, the CP-67 sweep's find, stale a third time and caught by this CP's review); these six ride the same carriers as before — every release CP lifts `README.md` (the version literal moves each cut), and `ci.yml` the next `.github/` change — each number re-verified against the tree at that lift, never copied from this row. ~~`waiting-on: the 0.1.7 release (README.md:6, :96, :102) and the next .github/ change (ci.yml:4, :27, :40) — six literal edits: "2,016-line" → "2,034-line" twice, "32 ADRs" → "33 ADRs", "(169" → the collected count three times`~~ **[CP-80] The token's event fired whole — the 0.1.7 release IS the next `.github/` change — and five of the six echoes moved, each re-verified against the tree, never copied from this row:** `README.md:6` "2,016-line" → 2,034 (`wc -l gsj_rollout/*.py` = 2,034, ADR-0033's equality green), `:96` "(169 tests" → 176 (by collection), `:102` "32 ADRs" → **35** (the token said 33; ADR-0034 and ADR-0035 were written at CP-78/CP-79 after it — the row's own rule, re-verify at the lift, is why), `ci.yml:4` and `:40` "(169)" → 176 — and the pair of the same class the token did not list, `ci.yml:6`/`:70` "mcp-service suite (107)" → 134 (by collection; 114 + 20 since CP-79). NOT taken: `ci.yml:27` "outside the 2,016-line budget" — the CP-80 lift named `.github/` for the suite labels only ("nothing else in `.github/`"), and a size-law comment is not a label; a one-word edit left standing under an explicit exclusion beats an undeclared excursion (the CP-75 row-60 shape). `waiting-on: a freeze-lift naming .github/workflows/ci.yml without a labels-only restriction — ci.yml:27 "2,016-line budget" → the law's number at that lift, re-verified against wc -l, never copied from here` |
| 54 | **NEW (CP-71)** — the slimmed manifest reaches a PyPI stranger only at the next release: the published wheels (<= 0.1.5) force-include the PRE-CP-71 `ingest_corpus.py`/`bringup.py`, so from PyPI the owner allowlist still stands, the three estate fields are still required, and there is no `scaffold` verb; the demo's generator therefore still writes all three fields (annotated as deprecated/ignored — its bootstrap floors at `>=0.1.4` and its own `corpus_sandbox_image` reader dies without the key), to be dropped when the CP-71 release is its floor. **CP-72 adds the rename to the same shipment**: the wheel's estate tool becomes `gsj_rollout/estate.py` (`python -m gsj_rollout.estate`, with the folded `validate`/`ingest` verbs) and `python -m gsj_rollout.ingest_corpus` starts printing its deprecation notice — published wheels <= 0.1.5 keep the old names | the release CP: nothing to code — both files are force-included, the change rides the wheel; then the demo repo drops the three fields from `synthetic/make_corpus.py`'s template and retires `bootstrap.py`'s `corpus_sandbox_image` die-if-missing | opened CP-71 (ADR-0030) | a PyPI consumer scaffolding a modern corpus against an old wheel, refused by the very rules the checkout retired. `waiting-on: the next release (the CP-71 pipeline/bring-up ride the wheel), then the demo's floor bump` **[CP-74] The wheel half — the token's first event: 0.1.6 is this release (tag `v0.1.6` on the release commit; run ids in the record commit).** What a PyPI consumer now gets, proven on the built artifact from a scratch venv before the tag and repeated from both indexes after it: `python -m gsj_rollout.estate scaffold --out <dir>` writes a corpus whose `corpus.yaml` is `name`/`owner`/`git:` only and `validate` passes it unmodified (1/1, no warning); the same tree with `forgejo.base_url`/`mcp.url_base`/`sandbox_image` added validates with exactly three `DEPRECATED` warnings and rc 0 — never a failure; the owner allowlist is the shape check. The demo half (drop the three fields from `synthetic/make_corpus.py`, retire `bootstrap.py`'s `corpus_sandbox_image` die-if-missing) rides the demo commit after publication — the token's second event, noted on this row by the record commit. **[CP-74, the record] DONE — the second event fired in the same CP: demo commit `672bab5` drops the three fields from `synthetic/make_corpus.py` (the template is `name`/`owner`/`git:`) and retires `bootstrap.py`'s `corpus_sandbox_image` die-if-missing — the sandbox image is the demo's own `sandbox_image` ANSWER to the tool (`SANDBOX_IMAGE`, pre-pulled as before), landing in `rollout.yaml`'s `runtime.image`, `run.json` and every bank row; proven from a fresh clone against PyPI's 0.1.6 (validate, `up`, one accepted episode). Both tokens' events fired — row closed.** |
| 55 | **DONE (CP-72)** — was NEW (CP-71): three tracked echoes of the retired two-owner allowlist sit OUTSIDE CP-71's freeze-lift: `estate/README.md:79` ("`corpus.yaml`'s `owner` (`gsj-staging` \| `gsj-prod`)" in the bring-up answers table), `estate/estate.sh:57` (the usage hint "try: estate.sh owner gsj-staging" — cosmetic), `estate/forgejo/create_owner.sh:19` (the usage string "(gsj-staging \| gsj-prod)" — the script itself takes any owner). None is load-bearing (`estate.sh:64/:70`'s staging-token guards encode the H200 estate's concrete values, unchanged in meaning); all are stale prose about what an owner CAN be | three one-line edits riding whichever CP next lifts `estate/README.md` / `estate/estate.sh` / `estate/forgejo/` | opened CP-71, by its own out-of-lift scan | a reader of the estate recipe inferring the allowlist still stands while the validator enforces a shape. fired at CP-72 (the `estate/` lift): all three echoes rewritten — the answers table says any usable Forgejo username (CP-71's rule), and both usage strings frame `gsj-staging` as an example, not a list |
| 56 | **NEW (CP-71)** — the frozen staging lock's `_comment` header predates the current `write_lock` text: `estate/corpus/staging/corpus.lock.json:3` still reads "Generated by corpus/ingest_corpus.py (ADR-0047)" while `write_lock` (since the CP-50/CP-60 moves) writes the two-line checkout/wheel wording — so ANY scaffold of the tracked tree dirties the lock by the header alone (+2/−1; measured this CP: every ref SHA, census, prompt id and the taskbank block byte-identical — CP-71 restored the file rather than commit a passing fixture edit). Latent, cosmetic, but it silently breaks the "straight-enter run leaves the tree clean" property CP-59 measured | regenerate the lock once under a deliberate `estate/corpus/staging/` lift, verifying the diff is the `_comment` lines only (the SHAs are load-bearing; the header is not) | opened CP-71, by run A's `git status` | any CP that runs the pipeline on the tracked staging tree and asserts a clean tree afterwards. `waiting-on: the next estate/corpus/staging/ lift` |
| 57 | **NEW (CP-72)** — the frozen `.github/` still names the pre-rename tool, and one of the two is release-blocking: `release.yml:103`'s required wheel tuple asserts `gsj_rollout/bringup.py`, which the wheel no longer contains — **the next `v*` tag or dispatch rehearsal hard-fails at the build job until the tuple row moves to `gsj_rollout/estate.py`** (release.yml's own comment demands tuple and force-include move together; CP-72 could not — `.github/` was not lifted). The benign half: `ci.yml`'s display labels — `corpus suite (67)` vs 90 actual since CP-72 (88 at CP-71), the root/mcp labels still true (169/107) | the next CP that lifts `.github/` — the release CP at the latest, in its release commit | opened CP-72, priced by its Step-1 sweep | the release path itself. `waiting-on: the next .github/ lift (the release CP must take the tuple edit in the release commit)` **[CP-74] DONE — CLOSED at the token's own named event (the `.github/` lift, taken in the release commit as its first hunk).** `release.yml`'s required tuple row `gsj_rollout/bringup.py` → `gsj_rollout/estate.py` (now `:105`; the comment above it names this row), verified against the built artifact before the push — 18 entries, all seven required present, the six banned prefixes and CP-19's nine names absent, no `bringup.py` alias — and then by the rehearsal `workflow_dispatch` (its run id in the record commit): the run that would have failed at the tuple on any tag since CP-72. The benign half: `ci.yml`'s `corpus suite (67)` → `(109)` at `:5`/`:51`, checked by collection (root 169, corpus 109, mcp-service 107 — the other two labels were already true). Nothing else in `.github/`. |
| 58 | **NEW (CP-72)** — current-state echoes of the old name in paths CP-72 was not lifted for, recorded not fixed (the CP-60 class): root `README.md:106` (the Licence section's wheel-contents sentence names `bringup.py`; :43's "0.1.5 wheel-only" list stays TRUE of the published wheel until the release, and :92's build-time-copy enumeration already omitted the estate tool before CP-72), `CLAUDE.md` (the layout tree, the `estate.sh` verb table, the wheel force-include note — bringup.py five times), and the two PNGs whose decks live outside the repo (`docs/guide/img/estate-bringup.png` — also still the tracked *filename* the estate README references — and `wheel-contents.png`, rendered at 0.1.4 showing `bringup.py`) | one-line edits at each file's next lift; the PNGs re-render from the sibling diagrams dir at the next release | opened CP-72 | a reader of the frozen pages typing a command that now prints a different name. `waiting-on: the next lift of README.md / CLAUDE.md, and the next release's diagram re-render` **[CP-74] DONE — CLOSED at the token's own named events (the README.md / CLAUDE.md lift and the release's diagram re-render, both this CP).** Each claim verified against the tree before the edit, not the row: `README.md:43` (0.1.6, `estate.py`), `:106` (`estate.py` / `gsj_rollout.estate`; `gsj_rollout.bringup` on wheels 0.1.3–0.1.5), `:92` (the wheel enumeration names `estate.py` too), `:94` (the `estate/` row names `estate.py` and its seven verbs — an extent beyond the row: a partial enumeration made fuller, not a lie corrected), `:102` ("26 ADRs" → 32, `ls docs/decisions`); `CLAUDE.md:5` (0.1.6, `estate.py`; and the F-register claim, stale since the examples' CP-69 minted F-79 there — F-01–F-53 + F-79 / F-54–F-78, next fresh id F-80), `:10`, `:51`, `:79` (the `estate.py` tree line with the seven verbs), `:80`, plus the counts the tree contradicted — `:59` ADRs 0001–0032, `:73`/`:85` corpus 67 → 109; the `estate.sh` `bringup` VERB at `:78` kept by design. The PNGs — the diagrams dir was reachable: `wheel-contents.png` re-rendered at 0.1.6 (`estate/estate.py` → `gsj_rollout/estate.py`, `python -m gsj_rollout.estate`), `estate-bringup.png` re-rendered with its caption moved off `ingest_corpus.py` onto `estate.py up`/`ingest` (filename unchanged — the estate README's reference stands), and two version stamps the same deck visit brought current, `two-roles.png` ("wheel-only 0.1.6") and `api-surface.png` ("version 0.1.6"); the trainer-guide alt text says rendered at 0.1.6. Recorded, not fixed (frozen path): `tests/test_wheel_pipeline.py:57`'s docstring still says release.yml "must move at its next lift" — the next `tests/` lift (row 60's) rewrites it as history. |
| 59 | **NEW (CP-72)** — the demo repo's side of the rename, deferred whole: `bootstrap.py` probes `find_spec("gsj_rollout.bringup")` (:152), Popens `-m gsj_rollout.bringup` (:746), and its README/compose comments name the module — every published wheel its `LIB_MIN=(0,1,4)` floor can resolve ships `gsj_rollout.bringup` and none ships `gsj_rollout.estate`, so rewriting now breaks the demo against every installable wheel, and bumping the floor first breaks it today (CP-72's sweep, verified against PyPI). Also carried: bootstrap.py:749-750 parse the estate tool's stdout by the literal prefixes `== run <name> ==` and `next — ` — CP-72 kept both byte-identical; the demo CP must re-verify that contract | the demo CP after the release that publishes `gsj_rollout.estate`: bump `LIB_MIN`, move the find_spec/Popen/docs, re-verify the stdout mute | opened CP-72 | the bring-your-own path's F-register. `waiting-on: the next release, then the demo's floor bump` **[CP-74] DONE — CLOSED at the token's own events (0.1.6 on PyPI, then the demo's floor bump — both this CP, in that order).** Demo commit `672bab5` (pushed): `LIB_MIN` `(0, 1, 4)` → `(0, 1, 6)`; `find_spec("gsj_rollout.estate")`; the `-m gsj_rollout.estate` Popen, the say line, the `--help` hint and the answers-file header; `validate` through the estate verb (the pipeline's own entry now prints a notice); every `>=0.1.4` remedy → `>=0.1.6`; README and compose comments. **The stdout contract re-verified on 0.1.6 from a FRESH CLONE**: `== run demo == runs/demo/` reached stdout (line 96 of 171) and from `next — ` on nothing did — zero `next — ` lines, zero `serve_rollout`/`serve_gateway`/`PYTHONPATH=`/`--polar-leg container` markers after the header; `up` rc 0 in 38 s, one accepted episode (`sk-polar-f1b18784…`, `case_orchard@t2`, 7 s) through the demo's own printed recipe, `down --wipe` clean. `POLAR_IMAGE` `gsj0.1.3` stays — a real published image (A-28; the CP-63/CP-67 posture, upheld by this CP's referees). |
| 60 | **NEW (CP-73)** — the frozen root suite pins the six-verb metavar: `tests/test_wheel_pipeline.py:154` asserts the exact brace line `{scaffold,validate,up,ingest,status,down}` in `--help`, and CP-73 added `update` — so `estate.py` pins argparse's metavar to the six-verb summary (main()'s comment names this row) while `update` is fully listed with its own help line and in the docstring. The summary line understates the verb set by one until the assertion moves to the seven-verb tuple and the pin retires — one test edit, one metavar-line deletion, in the same change | `tests/test_wheel_pipeline.py` paired with `estate/estate.py`'s metavar | opened CP-73 (a natural metavar would red the frozen assertion; a re-worded seven-verb pin cannot contain the asserted substring) | the help summary line's honesty. ~~`waiting-on: the first tests/ lift (test_wheel_pipeline.py:154's tuple moves with the pin's retirement)`~~ **[CP-75] The token's event fired in part, and the row was NOT taken.** This CP lifted one `tests/` file (`tests/test_config.py`; `tests/test_cli.py`'s census literal moved under ADR-0028's echo clause, not a lift) — "the first tests/ lift" by the token's own words. The row's fix pairs `tests/test_wheel_pipeline.py:154`'s tuple with the deletion of `estate.py`'s metavar pin in ONE change, and neither file was in the lift (`estate.py`'s was the `.env` strings only), so taking it would have been two undeclared excursions for a cosmetic line. Re-parked on the event the token should have named.  **[CP-83, recording CP-82] OPEN.** The audit executed all seven verbs from the public 0.1.7 wheel; `--help` listed `update` separately while retaining the six-name brace summary. Phase 1 names corpus-side estate tests, not `tests/test_wheel_pipeline.py`, so the paired metavar/assertion change remains frozen under CP-82 §7 item 3. `waiting-on: a commit changing tests/test_wheel_pipeline.py's seven-verb expectation and retiring estate.py's six-verb metavar together, with wheel help verified` |
| 61 | **NEW (CP-73)** — `update` syncs at the repo/lock/bank layer; at the index layer the granularity is the service's, and it is corpus-wide: `corpus_fingerprint` (`estate/mcp-service/gsj_mcp_service/index.py:142`) hashes every case's `main` SHA into ONE fingerprint and the rebuild path re-embeds every case on any mismatch — measured at CP-73 on the cp73 estate: a one-page edit to one case moved the fingerprint and re-embedded all cases (`index_reused=False`). `update` says so in its plan instead of hiding it; per-case incremental reindex (per-case fingerprints + selective rebuild) is an mcp-service capability it does not have, and mcp-service was frozen this CP | `estate/mcp-service/` — the next image cut (rows 47/49's event) | opened CP-73, measured live | what `update` costs on a REAL corpus — the whole-corpus re-embed is exactly the expensive step item 7's review exists to protect. `waiting-on: the next mcp-service image cut` **[CP-79] DONE — the mechanism, as the drop's side effect.** `fingerprint.json` now records `collections: {<case_id>: sha, decisions: sha}` (`index.collection_fingerprints`: a case hashes its own `main` SHA, the chunking, the model, the store format and the chroma version; the decisions collection its drop component and the chunking, or the generator's seed and size) and `state._reusable` loads every collection whose record matches, rebuilding the rest — never under `always`. Measured on a copy of the session store cut back to the pre-CP-79 record: a drop added re-embedded `decisions` alone, every case's two segment rows untouched, `progress[<case>].reused` true; the drop removed re-embedded the synthetic 30 alone; a store from before the record whose whole fingerprint equals the config's *without* the drop keeps its cases (`_reusable`'s legacy inference — every estate's store today); a whole-store match backfills the record on first reuse; a whole-store load that fails on one corrupt collection keeps the healthy ones in the record (the review's catch); a start that keeps everything (a removed case) reports `index_reused: true`. `estate.py update`'s plan text corrected — it priced a one-case edit as a whole-corpus re-embed. `waiting-on: nothing — closed (CP-79)` |
| 62 | **NEW (CP-75)** — the `.env` cure reaches `submit`, not the gateway leg. `config.py` resolves one named variable (`clone_credential_env`); the other, `mcp_token_secret_env`, is a NAME it passes through the TaskRequest whose VALUE `pi_harness.py:242` reads from the environment of Polar's `serve_gateway` process — a different process and a frozen module — so with an `estate.py` run directory the operator still types `(set -a; . runs/<name>/.env; set +a; polar serve_gateway …)` for that one terminal (the receiver and `serve_rollout` never needed it, and the pre-CP-75 closing block that said "each sources .env first" was over-broad on two of three — corrected this CP). The operator's ask was no environment variables at all; this CP met the trainer-side half. The server-side half is an `estate.py` launcher verb that starts Polar's two processes (and the receiver) with the run's `.env` injected into the CHILD's environment — the pattern `estate.py` already uses for its pipeline phases (`:1594`, `:2980`: `{**os.environ, …}` with the three named secrets spread from `run_.env`), read not exported — so no terminal ever sources; outside the size law (`estate/`), no library change. The alternative, `pi_harness` reading a file path carried in `settings`, is a §1-lift library change under the law and puts a host path on the wire; not preferred. Two residues ride the same event: **(a)** `estate/rollout.h200.yaml:37`'s comment "the var MUST be exported, or submit raises" is over-strong since CP-75 (the `.env` beside the config also satisfies; for that by-hand path, whose tokens are the `forgejo/.token*` files and whose directory holds no `.env`, the export stays the practical route) — outside the lift, moved with the H200 recipe; **(b)** the demo's recipe, carried twice (`README.md:344-350` and the `up`/`status` printout, `bootstrap.py:1005-1011`), stays correct and is not lifted — its `submit` runs inside the pinned `gsj-polar:f0e8343a-gsj0.1.3` image (library 0.1.3, no seam), so a `LIB_MIN` floor bump retires nothing; it retires when the demo re-pins a `gsj-polar` cut on a wheel ≥ 0.1.7 (A-28's rebuild duty) AND mounts the run's `.env` beside the config the container reads (`/estate/rollout.yaml` → `/estate/.env`; today the secrets sit in `work/runs/demo/.env`, unmounted). **[CP-81] The demo half is DONE — both conditions met and the retirement measured.** `ghcr.io/mhganainy/gsj-polar:f0e8343a-gsj0.1.7` was cut two-platform (`LIB_REF=v0.1.7`/`LIB_VERSION=0.1.7`, index `98361f0a…`, both children anonymous-pullable, the pulled image reporting `0.1.7` with `_named_value` in its packaged `config.py`), the demo's `POLAR_IMAGE` and `LIB_MIN` moved to it, and the run book — the README's one-liner, the `up`/`status` printout — now mounts `work/runs/<run>/.env` read-only at `/estate/.env` and exports nothing: an episode submitted from a shell holding **zero** `GSJ_` variables was collected and accepted (`sk-polar-dd62726f…`, `case_orchard@4`). The old subshell still works and is named in the README as no longer needed. What remains of this row is the **gateway leg only** (`pi_harness.py:242` reading `GSJ_MCP_TOKEN_SECRET` from Polar's `serve_gateway` environment): in the demo that leg is a compose service with its own `--env-file`, so no terminal sources there either, but the `estate.py` launcher verb this row asks for — for an operator running Polar's two processes by hand — is still owed | `estate/estate.py` (a launcher verb; outside the size law); `estate/rollout.h200.yaml` (one comment); the demo repo at its image cut | opened CP-75, by the CP's own Step 2 ("whether the two existing seams can share the mechanism" — measured: they cannot, the second seam is not `config.py`'s) and its review (the two residues) | an operator who never types an export on either side; the H200 pre-production bring-up's three-terminal recipe (CP-70's walkthrough shape) is where the residue is felt.  **[CP-83, recording CP-82] Gateway obligation remains OPEN.** The audit followed the three-process host recipe and accepted a fresh library episode in 5.034 s with submit reading the adjacent `.env`; only the gateway process required the MCP secret in its environment. The fresh demo's container submit likewise accepted with its existing `.env` mount. Standalone `ingest` without `GSJ_MCP_TOKEN_SECRET` refused correctly in 0.287 s and succeeded when the named secret was supplied. CP-83 phase 1 keeps these different process requirements explicit; its bounded run/recovery work does not add a launcher or revise `rollout.h200.yaml`. B01/B02/B06/B16 concern the estate command's own run ownership, record and cleanup boundary, not supervision of the three host processes. `waiting-on: an estate launcher command with explicit ownership of receiver and Polar child processes, demonstrated by a host-recipe accepted episode without terminal sourcing and an updated rollout.h200.yaml comment` |
| 63 | **NEW (CP-75)** — a refusal raised at render reaches a `submit` operator as a Python traceback with exit 1, not the `gsj-rollout: …` line with exit 2: `cli.py`'s `_load` wraps `load_config`'s `OSError`/`ValueError` (`:26-30`) but `render_task_request` at `:160` is called bare, so CP-56's "unset or empty" refusal always surfaced this way and CP-75's two new ones (the malformed line; absent from both) inherit the shape — the message is the traceback's last line, greppable but wrapped in twenty lines an operator did not ask for, and the exit code says "partial", not "config". Measured by the CP's review on the installed console script | `cli.py` — a `try/except (OSError, ValueError)` around the render, **+3** under the size law (a §3 budget decision) | opened CP-75, by its review (the messages lens); pre-existing since CP-56, unrecorded until now | the CP-27 standard on the seam this CP built. `waiting-on: a consumer register row from an operator who hit the traceback shape on a real refusal, or the next cli.py change under the size law — +3 then, with its ADR` |
| 64 | **NEW (CP-75)** — `estate.py`'s `.env` writer claimed a parity it does not have: `_env_quote` writes `'\''` for an inner quote and its docstring said POSIX `.` and compose's dotenv parser read it back identically — `.` does; **Docker Compose v2.40.2 refuses the whole `--env-file`** on that form (`line N: unexpected character "\" in variable name`), measured at CP-75. No minted value can carry a `'` (`token_hex`, `token_urlsafe`, Forgejo tokens, Forgejo's `--random-password`), so every created run to date composes; an ADOPTED admin password typed by an operator could, and then every `compose()` call of a run that created a service fails two phases later with an error naming a line, not a cause. The same writer accepts a value holding one of Python's eight other line separators (`\x0b`, `\x0c`, `\x1c`–`\x1e`, `\x85`, U+2028/9), writes it as one physical line, and `config.py`'s reader (which splits on them) refuses with a `splitlines` line number while `Run.load` silently truncates — loud in the safe direction on the library side, silent on the tool's | `estate/estate.py` — `Run.write_env` refuses a `'` and the eight separators as it refuses a newline (one condition, its message naming compose); `_env_quote`'s docstring corrected this CP to say what compose does | opened CP-75, by its review (the grammar and estate-tool lenses) | a typed adopt-side secret that breaks compose late and opaquely.  **[CP-83, recording CP-82 B05] OPEN; phase 2's operator choice is unanswered.** On synthetic temporary files the audit again round-tripped an apostrophe through the estate writer/loader but real Compose rejected its escaped form; vertical-tab and U+2028 values were accepted then split or truncated by the loader. Minted URL-safe secrets do not exercise this boundary. Phase 1's run-record work does not choose or alter the credential grammar, and `gsj_rollout/config.py` remains frozen at the 2,034-line equality. The earlier one-condition refusal proposal above is not an accepted operator decision. ~~`waiting-on: the operator choosing arbitrary single-line compatibility or an explicit shared credential grammar, followed by synthetic writer/load/submit/real-Compose conformance for that choice`~~ **[CP-84 B05] CLOSED IN SOURCE; public delivery remains OPEN.** The operator chose a restricted shared grammar. All three audit disagreements re-reproduced: writer/loader accepted an apostrophe that real Compose refused, and vertical-tab/U+2028 values were silently shortened by the estate loader while the package reader refused malformed quoted lines. The proposed printable-ASCII/no-apostrophe grammar needed one measured addition: Compose 2.40.2 rejects an odd run of terminal backslashes. The accepted credential grammar is nonempty printable ASCII U+0020–U+007E, excluding apostrophes and odd terminal backslash runs; controls, DEL, non-ASCII and all value-level line separators refuse. Spaces, internal backslashes and even terminal runs remain literal. Secret files allow exactly one optional terminal LF as framing; CRLF/multiple LFs refuse, with no whitespace trimming. Acquisition from environment/file/prompt/fallback, minted tokens, the writer and legacy loading now refuse by character class with the action to rotate or use a compatible token, without exposing values. ADR-0037 records the contract; no core parser change or size allowance was needed. Grammar pricing measured 389 synthetic contexts: all Python/shell round-trips passed, while real Compose rejected exactly the three odd-terminal-backslash cases. The final 56-case credential suite includes 384 supported contexts through real Compose and an actual CLI submit HTTP POST with byte-identical synthetic credential input; the sink deliberately returns 503, so that test establishes transport, not episode success. A separate fresh local-engine episode was accepted and read in this phase (charter §7). The frozen root test still requires apostrophe writing; its honest failure is row 80, not an exception to the grammar. Public PyPI 0.1.7 still lacks these boundaries. `waiting-on: a PyPI release containing CP-84's credential boundary changes, followed by supported writer/load/submit/real-Compose conformance and unsupported mint/adopt refusal from that fresh installed artifact` **[CP-85] CLOSED for public consumers at PyPI 0.1.8**, wheel SHA256 `e41023855a323dd879c9ee9554ef2bb1462c75a938b624e8dca490cbd446bc64`. The waiting-on publication event occurred: both fresh TestPyPI and PyPI installs passed supported writer/load/core/shell/real-Compose/submit bytes and class-naming unsupported mint/adopt refusals (check 13, 28 discriminating cases, no skips). Row 80 is also closed; no production grammar exception. **[CP-87, R-21] Compose-build assumption recorded and measured.** Credential byte preservation is not asserted for every build. CP-84 proved 384 supported contexts on workstation `2.40.2-desktop.1`; the independent review reports its distinct 391/391 matrix on `2.20.3`, `2.27.1`, `2.33.1` and `2.40.2-desktop.1`, and identifies the zero-skip CI runner as Compose `2.38.2` on `ubuntu-24.04/20260831.293`. CP-87 repeated the exact 384-context supported matrix on the H200 build `2.37.1+ds1-0ubuntu2~24.04.1`: **384/384 byte-identical** across the actual writer, estate loader, core reader, POSIX shell and Compose `config --environment` (0.577 s; no containers). Its workstation repeat was also 384/384 on `2.40.2-desktop.1` (0.664 s). If the assumption fails on another build, supported adopted credentials, especially backslash endings, can decode differently or fail at Compose. `waiting-on: before claiming an additional Compose build supported, run this matrix there and record its exact version and byte agreement` |
| 65 | **NEW (CP-76, landed CP-77)** — the decisions collection is a fixture, not corpus data (CP-70 item 6): 30 seeded English templates, one un-window-checked vector each, text served from a length-checked sidecar, fingerprinted as `{seed, size}`; the fixture's text has entered an accepted deliverable as case facts (h200-fidelity Timeline) and the golden's response bytes at mask 0. Fix: the CP-76 proposal (§5 of its report), `decisions.path` additive, the 30 as the no-path fallback. (Drafted verbatim in CP-76's read-only report, appended here by CP-77 — the first CP after it that writes to the register.) ~~`waiting-on: the CP-78 decisions spec`~~ **[CP-78] The token's event fired — the spec exists**: `docs/decisions-surface.md` v1 (ADR-0034) with its conformance fixture `docs/decisions-surface/` (three real BGH decisions, byte-identical, + `expected.json`), three independent implementers reproducing the expected output from the text alone. Nothing built: the service stays at the spec's level 0. What the row now waits on is the implementation. `waiting-on: the building CP (estate/mcp-service lifted) — decisions.py implementing docs/decisions-surface.md §3–§4, the service reproducing docs/decisions-surface/expected.json byte for byte, the wrapper and the level-2 hit (§7), decisions.path additive with the 30 as the no-path fallback` | `estate/mcp-service/`, `estate/estate.py`, the roster (§5's cost table) | opened CP-76; CP-70 item 6 restated | a decisions tool that retrieves law, not a placeholder that trains **[CP-79] DONE — built, at level 2, image `0.5.0`.** `decisions.py`'s rii parser and unit walk (a second reading of §3–§4, the generator kept byte-unchanged as the no-path fallback) reproduces `docs/decisions-surface/expected.json` byte for byte — the suite's first test — and spec §4.7's census over the March 2026 drop (33,979 files → 740,849 units: 667,099 Randnummern, 73,750 section units; 2,231 files without a Randnummer, none without a unit; 462,896,397 characters; 22,831 ECLIs; 3 s over eight processes). `decisions.path` additive (`estate.py up --decisions-dir`, a read-only mount), the 30 the default with every `== 30` assertion untouched; units through the one splitter (`ingest.split_spans`) and `check_chunks_fit`, embedded in CP-77's batches; the envelope `{query, k, hits, index_commit}` with the level-2 hit (`ecli` absent when the source has none, `rn` null for a section unit, `excerpt` the best piece, no `page`/`file`) — `-> dict` the one declaration byte moved, G3 and G2 unmoved (the roster suite live). What the three discriminator files caught in the build: nothing — the first cut reproduced all six on its first run; what the CP's review caught instead: the numbering anomaly judged per section (every Urteil flagged, the cross-section duplicates invisible; fixed decision-global — 13 duplicate and 101 non-contiguous files, the spec's 114), a §6 coverage gap at a unit's edge (a soft hyphen the tokenizer gives no offset — one real unit), and §3.1's header-normalization sentence contradicting the reference extractor on two files (row 75). One episode through a cp79 estate on this workstation with a 200-file drop: the CP-79 report. The thirty chosen decisions stay owed (row 74); the corpus contract's half (row 73). `waiting-on: nothing — closed (CP-79)` |
| 66 | **NEW (CP-76, landed CP-77)** — the service's builders add every vector in one `collection.add` and chroma 1.5.9 refuses > 5,461 items; `_recreate_collection` orphans the previous HNSW dir on every rebuild (39 in cp73). Both bite any real corpus (cases or decisions) before any design lands — batch the adds, sweep the orphans (mcp-service, image cut) | `estate/mcp-service/` | opened CP-76 by measurement (§7 item 10 of its report) | any collection above 5,461 vectors, and a store that stops growing by one full copy per rebuild **[CP-77] DONE — both halves, one image; the shape of each measured first.** 66a: the ceiling is `32766 // 6` from the SQLite statically linked into the wheel's Rust bindings (3.46.0 via sqlx-sqlite, `MAX_VARIABLE_NUMBER=32766`; `client.get_max_batch_size()`; the interpreter's `sqlite3` reports 250,000 on both hosts and does not enter it; CP-76's Python-side `embeddings_queue.py` shares the formula, not the number — it would give 41,666 here) and **a count of items, not items × metadata keys** (5,461 passes with 0/1/12/40/120 keys, 5,462 fails with any — `ValueError: Batch size of 5462 is greater than max batch size of 5461`, venv and Linux image alike); both builders now encode and add in `ADD_BATCH_SIZE = 1000` batches (5.46× under, clamped to what the client reports, one progress line per batch and a `build` block in `/health` — kept, showing the last batch, when a build fails), every existing store (≤62 chunks, 30 decisions) one batch and bit-unchanged (a pre-CP-77 and a CP-77 build of the staging corpus compared byte-for-byte on every collection, sidecar and the fingerprint), reused by fingerprint; `corpus_size: 0` refused at config load (the batched builder would otherwise come up ready and fail at the first query). 66b: `delete_collection` in 1.5.9 never removes the directory (same process, fresh process, opened first, a later start, `reset()` — all measured; the 1.5.9 Rust sources have no `remove_dir` on the path, the wheel's only `rmtree` is the Python SegmentAPI's the bindings never call); `sweep_orphans` removes after every drop and once per start-that-loads-or-rebuilds (on the reuse path after the load's checks pass — a refused store stays byte-untouched, tested; two assumptions named in its docstring: one writer per store, and `chroma.sqlite3` and its directories one snapshot — a database restored alone over a newer tree is a re-embed) exactly the directories no `segments` row names, refuses everything else — a live or half-filled collection's, a non-uuid name, a symlink, a file, everything when `chroma.sqlite3` is unreadable, and (the review's catch: the first cut let `rmtree` raise and errored the start under every posture) an entry it cannot remove is skipped with its reason, never a failed start (ADR-0016 CP-77 amendment argues the interrupted-rebuild case: CP-57's record has no fingerprint before the first drop, so the orphan is never a servable store); the cp73 copy 45 → 6 dirs (39 swept, 6.56 MB); a real `SIGKILL` mid-batch refused under `never`, rebuilt under `if-stale`, and by probe over an existing complete store, between decisions batches, via `/admin/reindex` and under another model — process-kill safety (the record is written without fsync), not host-crash. Proofs: mcp-service 114 (seven new tests; the identity oracle now re-encodes in the builder's slices — row 69), a 6,000-decision build in the suite, 19,215 chunks + 12,000 decisions at scale (A-25 re-measured — one leg departs, row 68), the same 6,000-decision config erroring on the `0.4.0` image and building on `0.4.1`. **Image `0.4.1` built on the workstation in both halves (native arm64 `gsj-mcp-service:0.4.1`, emulated amd64 `:0.4.1-amd64` — the CP-58 recipe, cached pip layer) and neither published nor loaded**: the GHCR two-platform push, the H200 `save | load` (its `compose.yml` now asks for `0.4.1`, so `estate.sh mcp-up` there needs the load first), and the tag bump in `estate/estate.py` (`MCP_IMAGE_PUBLISHED`, frozen this CP) and `estate/README.md` are the residual — an `estate.py` estate runs `0.4.0` until then and fails any collection above 5,461. `waiting-on: the next CP with estate/ lifted or an H200 hold — push ghcr.io/mhganainy/gsj-mcp-service:0.4.1 as a two-platform manifest from the two local images, load the amd64 half on the H200, bump estate.py + estate/README.md` **[CP-79] The residual DONE.** `ghcr.io/mhganainy/gsj-mcp-service:0.4.1` published as a two-platform index composed from the two measured images with `buildx imagetools create` (CP-61's method — no rebuild): index `d27327a4…`, amd64 child `c04cf75a…` (config `2359b148…`), arm64 child `08a36172…`; both children fetched with an anonymous registry token; the amd64 half loaded on the H200 through skopeo as uid 1000 (`gsj-mcp-service:0.4.1`, image id `2359b1481312` = the child's config digest). `estate.py`'s `MCP_IMAGE_PUBLISHED` and `estate/README.md` bumped — and moved again the same CP to `0.5.0` (row 65), published the same way (index `17704c06…`, amd64 `a38f9f63…`, arm64 `92d90ced…`, both anonymous; loaded on the H200 too). The pre-production estate on the box (`gsj-preprod-mcp`, `0.4.0`, a CP-69 checkout) was not restarted: its stored fingerprint `4954c167…` is reproduced byte for byte by the new code from its record, so an upgrade there reuses its store. `waiting-on: nothing — closed (CP-79)` |
| 67 | **NEW (CP-76, landed CP-77)** — the roster's blind spots, measured: a tool's RETURN shape is in no pin (mcp 2.0.0 emits an output schema, pi-mcp-extension 1.5.0 drops it) and a parameter DEFAULT change (k=5→50) does not move G3 (pi drops defaults) — only `test_roster_pin.py`'s source literal catches either; G2 carries each tool description as pi's 120-char collapsed snippet, so a declaration edit moves one gate or two; the hash has never moved and dropping it fails 63 of the root suite's 176 tests. Record, and rehearse a re-pin before one is needed. ~~`waiting-on: the first declaration edit on any estate — the CP-78 decisions spec if it moves a signature`~~ **[CP-78] The token's second event resolved — the spec moves no signature**: `search_decisions(query: str, k: int = 5)` and its docstring stay byte-identical (spec §7.1, §13.1); the wrapper and the richer hit are body changes, and the return annotation is outside the roster hash (re-verified at CP-78 with the annotation change included — the report's C1). The first event has not occurred. `waiting-on: the first declaration edit on any estate — a get_decision tool or a where filter, both deferred by the spec's §11` | `pins/`, `tests/fixtures/`, both consumer repos | opened CP-76 by refutation (V1, V2, V16 of its report) | a re-pin walk that exists before the roster moves |
| 68 | **NEW (CP-77)** — HNSW's full-candidate fetch is not exhaustive at scale, and two builds of the same inputs differ at the chunk tail: at 19,215 chunks the `where`-filtered fetch of the whole in-cutoff set returned 9,571 of 9,581 (T=650), 4,820/4,819 of 4,825 (T=325), 19,203 of 19,215 (T=1300) — the unfiltered decisions fetch 12,000 of 12,000 — with each of two independent builds missing a different handful from rank ≈450 down (stored vectors bit-identical; the distance-sorted lists identical on every id both return); a bounded fetch of 100 or 1,000 already differs between builds at the top, 10 agrees. Same-store and builder-vs-loader stay IDENTICAL 7/7. The cause is not established: the CP's review rebuilt the same 19,215 bit-identical vectors in the same order under `hnsw.num_threads: 1` and still got two graphs (the persisted config carries no thread knob), and it is not tuned by ADR-0016's own decision. ADR-0016 Mechanics 4's "parity with the exact scan" holds at 213 (where `test_cutoff_prefilters_candidates_not_postfilters` asserts it — its message would misdiagnose a graph miss as post-filtering at scale — and `test_batched_build_serves_what_a_single_add_served` compares two builds at raw level) and is ≈0.1% short here; the cutoff invariant is untouched (a miss lowers recall, never leaks) and the top-k pages were unaffected in 3/3 probes — a measurement, not a guarantee. Nothing to fix for the frozen estate (≤101 chunks); the decisions design's `n_results = len(corpus)` over hundreds of thousands is the fetch that must be bounded and its recall measured. ~~`waiting-on: the CP-78 decisions spec — its fetch bounded (ADR-0016's bounded-fetch decision) and its recall measured at the corpus's size`~~ **[CP-78] Fired in half**: the spec bounds the fetch as a rule — §8.4: the candidate fetch MUST be a function of `k`, never of the corpus size, and an implementation's record SHOULD state its bound and measured recall — and leaves the bound's value to the implementation (§12). The recall measurement needs the store, which this CP did not build. `waiting-on: the building CP — DecisionsIndex.search rewritten to the spec's bounded fetch (§8.4) and its recall against an exact scan measured at the real corpus's size, the number recorded in this row and in A-25` | `estate/mcp-service/gsj_mcp_service/index.py` (`CaseIndex.search`, `DecisionsIndex.search`), A-25 | opened CP-77 by the A-25 re-measurement at scale | a retrieval design that states its recall instead of inheriting a parity that stopped being true **[CP-79] Fired — the fetch bounded, its recall measured, a second ceiling found; the case half stays open.** `RiiDecisionsIndex.search` fetches `DECISIONS_FETCH_PER_K × k` = 200·k pieces — a function of k, never of the corpus. Against an exact numpy scan over every stored vector of a 1,999-file drop (an every-17th-file slice of the March 2026 drop: 43,760 units, 71,946 pieces; 60 queries — 52 sentence-sized slices of the drop's own units, 8 free-text): the unbounded HNSW fetch is exact (recall 1.000 at k = 5 and k = 20; the graph returns 71,941 of 71,946), so a bound's loss is HNSW's search beam — `hnsw:ef_search` 100, widened by `n_results`, and identical results at 25, 50 and 100 pieces prove it: 20·k 0.953 at k = 5 (one query 0 of 5) and 0.984 at k = 20; 100·k 0.993/0.998; **200·k 1.000 (60/60, the exact order) at k = 5 and 0.998 (min 0.95, top-1 60/60) at k = 20**; 400·k 1.000/0.999; on the free-text queries 200·k is 1.000 at both. A-25 re-measured at this scale (`measure_determinism.py --decisions-path`, two runs): same-store and builder-vs-loader IDENTICAL 8/8 both runs; two independent builds identical on every tool-level probe and the raw case probe, identical on a 100-piece decisions fetch (the first run, under 20·k), and at the shipped 1,000-piece fetch sharing 999 of 1,000 ids in a different order — the build-to-build tail of CP-77, located inside the fetch, the returned units unchanged. The identity oracle's benign end on the real store: 4,000 vectors re-encoded in the builder's slices, max\|Δ\| 1.49e-08 (arm64's known class), min cosine 0.99999994, 2,345 of them short pieces. **Found**: chroma 1.5.9's read path refuses a `query` that includes metadatas or documents above **32,763** results (`too many SQL variables` — the write path's variable limit again; ids and distances alone fetch the whole store), so `CaseIndex.search`'s full-candidate fetch is not only ≈0.1 % short at 19k: it is refused outright above 32,763 in-cutoff chunks (≈ 2,200 pages at 220/40; no estate has more than 62). `waiting-on: the first case above 32,763 in-cutoff chunks, or the full 34k-file drop (≈ 1.16 M pieces) — CaseIndex.search bounded the same way, and both scripts re-run at that scale` |
| 69 | **NEW (CP-77)** — the vectors' last bits depend on which texts share an encode call (sentence-transformers length-sorts a call and pads per `embedding.batch_size` mini-batch), and on short texts that drift is not benign-sized: the 30 decisions in sevens vs one call max\|Δ\| 9.686e-08, vs singles 1.192e-07; full-window 220-token chunks 0, but **page-tail chunks move** — 73 of the staging 213 (43–219 tokens) by up to 1.1e-07 in sevens and 1.4e-07 singly, and at scale (19,215 chunks, 1,957 page-tail) a whole-collection re-encode differs from the 1,000-batched store by 1.397e-07 on 67 rows, all page-tail, none full-length, while a re-encode in the build's own batches stays at 1.490e-08. Three consequences. (a) `ADD_BATCH_SIZE` shapes the bytes above 1,000 items and is un-fingerprinted: a change owes an `INDEX_FORMAT` bump (the chunker's rule, ADR-0016 CP-57) — DONE as a rule this CP. (b) The identity oracle (`test_stored_vectors_are_the_pinned_encoders`) re-encoded a whole collection in one call and sampled rows of 222, 49 and 96 tokens: right at 51 chunks (one call), wrong above 1,000 — it would fail by sampling luck (0.35 % of rows over the bound, above the 1.155e-07 substitution floor) on a store that IS the pinned encoder's — **FIXED this CP**: it re-encodes in the builder's slices (`index.batch_spans`), the identical call for every existing store; CP-55's bound stands where it was measured. (c) **OPEN**: `embedding.batch_size` shapes the same bits at any collection larger than the smaller batch size (measured: 30 decisions in one call at batch_size 1/7/16 vs 32 → 1.19e-07/1.04e-07/8.4e-08; 64 vs 32 → 0 only because 30 < 32) but is a config key `corpus_fingerprint` does not hash, `estate/estate.py` prints it as "speed only, not identity" (`MCP_FINGERPRINT_KEYS` excludes it) and CP-73 proved a `batch_size: 64` merge reuses the store — a byte-determining value outside the fingerprint, in `estate/` (frozen this CP): decide it as a fingerprint component or an accepted, documented drift. And the first corpus of short, varied-length texts meant for training owes CP-55's clause (c) — both ends re-measured on its own chunks. ~~`waiting-on: the CP-78 decisions spec — (c) decided (embedding.batch_size fingerprinted or its drift accepted in writing, estate.py's "speed only" line corrected), and the oracle's two ends re-measured on the first corpus of short varied-length texts (CP-55 clause (c), A-31)`~~ **[CP-78] The token named the wrong event**: the spec deliberately leaves batching, the model and the window to the implementation (§12) because none of them changes what an agent sees, so (c) is not the spec's to decide — it stays an `estate/` decision, and `estate/` was frozen this CP; the oracle's re-measurement needs the real store, not built. Both halves re-parked on the events that can perform them. `waiting-on: the first CP with estate/ lifted — (c) decided (embedding.batch_size fingerprinted or its drift accepted in writing, estate.py's "speed only" line corrected); and the building CP — the oracle's two ends re-measured on the real decisions corpus (CP-55 clause (c), A-31)` | `estate/mcp-service/tests/test_backend.py` (the oracle — fixed), `estate/mcp-service/gsj_mcp_service/index.py` (`corpus_fingerprint`), `estate/estate.py:944-953, :1046`, ADR-0016 | opened CP-77 by the batched-build test's first failure and its review's measurements at scale | an oracle that compares like with like, and a fingerprint that hashes everything that moves the bytes **[CP-79] Both halves taken.** (c) decided with `estate/` lifted: `embedding.batch_size` is identity — a component of every fingerprint (`index._embedding_component`, whole-store and per-collection) **present only when it leaves the pinned 32** (`PINNED_ENCODE_BATCH_SIZE`), so the stores every estate built at 32 keep their bytes and a moved value re-embeds (the bytes would differ); the one estate that ever merged `batch_size: 64` over a 32-built store (cp73, this workstation, down) rebuilds once under `0.5.0` — its config and its bytes never agreed; `estate.py`'s review line now prices it as identity (`MCP_FINGERPRINT_KEYS` gained it) and the mcp-service README says so. The oracle's benign end re-measured on the first real corpus of short, varied-length texts (the 71,946-piece drop store): 4,000 vectors across four builder slices, 2,345 of them under the window, max\|Δ\| 1.49e-08 and min cosine 0.99999994 against the CP-55 bound 4e-08 — the composition rule holds; the substitution end (chroma's bundled ONNX MiniLM) was not re-run (the image runs offline and the pinned default embedder is unreachable by construction; CP-55's floor 1.155e-07 stands where it was measured). `waiting-on: nothing — closed (CP-79)` |
| 70 | **NEW (CP-78)** — the degradation rule is proven for the grammar, not for the policy. `docs/decisions-surface.md` §9.3: a hit without an `rn` key (gsj-next's level 1) admits only the bare citation `dec:<doknr>`, so no valid citation exists that production cannot resolve — but a policy trained only on level-2 responses (every hit carrying `rn`) may write `:rn:` from habit when it first meets level-1 hits, and nothing measures that today. The spec asks the training corpus to include level-1 responses and the evaluation to report the grounded-citation rate on level-1 hits separately; neither exists. **[CP-81] The level-2-WITH-CLAUSE half is now observed, twice, and the answer is no.** CP-79 saw a 0.6B policy meet the grammar with no clause in its `AGENTS.md` and not use it. CP-81 put the §9.5 clause into the demo corpus's `AGENTS.md` (so the pinned system prompt carries it — 4,989 chars, sha `ebfa5e5f…`) and ran two episodes on the thirty synthetic decisions: **both searched, both received level-2 hits carrying `rn`, both named the decisions by bare `doknr` in prose, and neither wrote a single `dec:` token** (`sk-polar-dd62726f…` 5 hits, 4 bare doknr; `sk-polar-c2aeed8a…` 5 hits, 5 bare doknr; the §9.1 regex finds nothing in either trace). So the clause alone does not produce the citation in an untrained 0.6B policy — the identifier is copied, the grammar is not — which is evidence for exactly what this row says is missing: the grammar needs to be exercised UNDER REWARD, not merely instructed. The reader now measures it per episode (`read.py`'s `citations` line and `decision_census`: tokens written, grounded vs ungrounded, bare doknr counted separately, and whether the prompt carried the clause), so the rate this row asks for is computable from any archive. The level-1 half — a response mode without `rn`, and the grounded rate measured on it — is untouched.  | `estate/mcp-service/` (a level toggle or fixtures), the examples' grader (F-80's `dec:` regex + trace-grounded validation) | opened CP-78 by writing the rule down | the claim "safe against gsj-next as it stands" resting on a measurement instead of a grammar argument **[CP-83, recording CP-82] OPEN; no new reward rule.** The audit's fresh accepted orchard@4 trace searched once, received five decision hits, wrote five bare doknr mentions and zero `dec:` tokens; no deliverable was written. The existing artifact/page grader gave zero to each of three CP-81 traces, including the fabricated-Rn one, and therefore did not distinguish silence with hits from an ungrounded final-prose token. These are observations, not grounds to choose a penalty. The reversed-result and failed-write probes are consumer evidence defects B13/B14 (demo F-82), which phase 4 must repair before phase 6 uses the reader as its oracle. Phase 1 records the measurements only; the level-1 policy proof remains absent. `waiting-on: the operator's citation-reward truth table specifying the graded object and silence/ungrounded costs, identity-joined reader fixtures passing, and a level-1 reward evaluation reporting its grounded-citation rate` **[CP-86 phase 4]** The demo now provides an ID-aware observational export v2 with explicit missing/unmatched results and attempted/succeeded/failed/unknown write evidence. The fabricated CP-81 stranger citation remains ungrounded; the other two recovered CP-81 stranger bodies and CP-82's fresh body remain silent with five hits. Two original CP-81 rehearsal bodies remain unavailable locally, so the full five-body re-read proof is incomplete. No reward added; row stays OPEN. ~~`waiting-on: recover and freeze both rehearsal bodies and reproduce their corrected-reader censuses, then an operator citation-policy truth table and measured level-1 reward/grounding behavior`~~ **[CP-87 / R-34]** The review established that CP-81’s own `down --wipe` destroyed both rehearsal bodies in the demo checkout’s ignored `work/traces`; they are not on the H200. The historical five-body re-read remains unavailable, and recovery is retired as a prerequisite rather than advertised as box work. The frozen demo register and historical measurements are unchanged; no citation reward or policy is added here. `waiting-on: an operator citation-policy truth table and a measured level-1 reward/grounding evaluation; any claimed historical re-read additionally requires independently recovered original bytes` |
| 71 | **NEW (CP-78)** — the spec's one-canonical-copy arrangement (§0) needs a pointer on the other side to be real: gsj-next is not modified by CP-78 and owes nothing, so today the spec is reachable from gsj-next only by someone who knows it exists. Until a pointer line (permalink + surface version) lands in gsj-next's own record, "two readers" is one reader and an intention. `waiting-on: gsj-next's operator deciding to adopt — a commit there naming docs/decisions-surface.md by permalink and version, and the fixture copied under its tests/fixtures/` | none here — gsj-next's, on its schedule | opened CP-78 by the §0 decision | the surface being a contract between two systems rather than a document in one |
| 72 | **NEW (CP-78)** — the unit rule's named losses, candidates for surface version 2: (a) one-word headings (`I.`, `II.`, `a)` — ≈ 37,000 rows) ride at the tail of the Randnummer they follow because nothing structural separates them from the plain text that resumes after a quotation (1,071 rows of ≥ 12 words in the same position); (b) 946 substantive rows (≥ 12 words) after a section's last anchor — annexed statute text, documentation-office notes, closing paragraphs — are dropped with the signatures; (c) 69 files carry their first Randnummern as bare `<div>` text outside any `<dl>` and lose them. All three are measured (spec §4.2, §4.3, §4.6), none is known to matter for retrieval or citation, and each would change `expected.json` and bump the surface version. `waiting-on: a measurement that any of the three affects a retrieved hit or a grounded citation — a query the agent needs that lands in a dropped row, or a cited Randnummer whose tail heading misleads; until then version 1 stands` | `docs/decisions-surface.md` + `docs/decisions-surface/expected.json` (a regeneration) | opened CP-78 by the corpus probe (309,200 unanchored rows classified by position, style and length) | a version-2 rule that is worth a fixture regeneration, or the record that none is |
| 73 | **NEW (CP-79)** — the decisions drop arrives by path, not by contract: `estate/corpus/` was frozen at CP-79, so corpus-contract v3's `decisions/` entry (the third root entry of CP-76's proposal), `validate`'s clauses (spec §2.2 plus the §2.3 anomaly report), `decisions.lock.json` (file census + the drop's sha256, the value the service already serves as `index_commit`), `scaffold` writing an empty `decisions/`, and `estate.py` mounting `<corpus>/decisions` by default are not done. Today the drop is a sibling of the corpus (`estate.py up --decisions-dir`), its provenance the run record's `decisions_dir` plus `/health.decisions_drop.sha256`, and a corpus hand-over carries no decisions at all.  | `estate/corpus/` (`ingest_corpus.py`, its suite, `corpus-contract.md`), `estate/estate.py` | opened CP-79 (Step 5 of its prompt; CP-76 §5's tree) | a corpus that carries its own decisions and locks them **[CP-83, recording CP-82] OPEN; phase 7 owns the handover contract.** The audit's installed scaffold wrote no decisions directory. Both estates served the explicit sibling drop (30 files, 313 units, 317 pieces; sha256 `b414c6709a5b7d0daf4505d2ed5bdd5fcadf325e5401e0dd34ae9b76d13eaaa4`), and the same scoped decisions query returned byte-equal envelopes across the two estates. That proves the existing path interface, not decisions carried or locked by a handed-over corpus. Phase 1 does not add a root entry, lock, default mount or corpus-contract version. `waiting-on: a corpus-contract v3 ADR and a handed-over corpus passing decisions validation, file-census/drop-hash lock verification and default-mount execution in the receiving estate` |
| 74 | **NEW (CP-79)** — the thirty chosen decisions per estate corpus (the operator's decision, spec §1) do not exist: CP-79 proved the mechanism at an arbitrary scale — an every-17th-file slice of the March 2026 drop (1,999 decisions, 71,946 pieces) for the measurements and an every-170th-file slice (200 decisions, 7,219 pieces — eight batches inside the estate's container) for the estate episode — and made no thirty, because the staging corpus is four synthetic English cases about ledgers and warehouses on which no thirty BGH decisions bear; a real corpus must arrive with the thirty its author chose, and the AGENTS.md clause of spec §9.5 (`dec:<doknr>:rn:<N>`, the hit gives you both) with them — which moves that corpus's G2, as any AGENTS.md edit does. **[CP-81] The METHOD half is done and demonstrated end to end; the SUBJECT half is still owed, and the distinction is the point.** The demo corpus did the whole thing this row describes: thirty decisions **chosen by construction** — written about what its own two cases turn on (the register against a long-standing fence, prescriptive possession, accession of a ditch, the scope and survival of a registered right of way; termination for arrears, rent reduction for a flooded annex, the deferred repair) — in conforming `rii-dok v1`, with the §9.5 clause in its `AGENTS.md` and the G2 re-derivation that follows, ingested by `--decisions-dir` and searched by two episodes whose hits were relevant to the case in front of them. So a corpus author now has a worked example of the whole path, generator included (`synthetic/make_corpus.py` + `synthetic/decisions.json` in the demo repo). What CP-81 did NOT do is curate case law: the demo's cases are fiction, so its decisions are fiction too — thirty decisions written for two synthetic English cases, not thirty real decisions chosen for a corpus of real ones. Closing this row on them would claim curation nobody performed, so it stays open and re-pointed at the subject.  | a real corpus's `decisions/` (row 73) and its `AGENTS.md` | opened CP-79 (Step 6 of its prompt: the mechanism proven, curation owed, said so) | a decisions tool that teaches the agent searching decisions is useful **[CP-83, recording CP-82] OPEN; the method reproduced, the subject did not change.** The audit regenerated and served the same thirty fictional decisions (313 units, 317 pieces), and its fresh demo precedent episode retrieved five hits. Cross-estate envelopes agreed byte for byte. Neither synthetic relevance nor accepted traces constitute selection of thirty real decisions for real cases. Phase 1 leaves corpus authorship and curation untouched. `waiting-on: a corpus author delivering thirty selected real decisions for a real case corpus, with its citation instruction and derived G2 recorded` |
| 75 | **NEW (CP-79)** — spec v1 disagrees with its own reference extractor on header whitespace: §3.1's last paragraph says every header value on a hit is normalized like text (inner White_Space runs collapsed) and claims "in this drop no header value carries surrounding whitespace, so verbatim and normalized coincide", while §5.2/§7.3 say `aktenzeichen` is the source value verbatim and `reference_extractor.py` only strips — and two files of the March 2026 drop carry a doubled space inside their Aktenzeichen (`VI  ZR 114/23`, `jb-KORE600392023.xml` among them), so a §3.1 reader and the extractor return different bytes for them, a divergence the six-file fixture cannot see. The build follows the extractor (strip only, inner runs kept — gsj-next's reading too) and says so in `decisions.header_value`.  | `docs/decisions-surface.md` §3.1, `docs/decisions-surface/reference_extractor.py` | opened CP-79 by the review's parser lens over the full drop | two implementations that agree on every header byte **[CP-83, recording CP-82] OPEN; versioned specification work remains phase 7's.** CP-82 retained CP-79's two-file doubled-space discrepancy; it did not claim a new header-normalization experiment. Phase 1 changes neither the specification, extractor nor fixture. `waiting-on: an accepted surface-version decision resolving header whitespace and a fixture that discriminates the doubled-space source values across the specification, extractor and service` |
| 76 | **NEW (CP-80)** — the 0.1.7 release turned six strings under `estate/` (frozen this CP) from the current caveat into history still worded as the caveat: **(a)** the four `.env` clauses CP-75 wrote for older wheels — `estate.py:493` (the `.env` header the writer emits: "wheels through 0.1.6: source it first"), `:2238` (the generated `rollout.yaml` header), `:2319` (the container-leg closing block: "a wheel through 0.1.6 reads the environment only (-e <name>)"), `:2329` (the host-leg closing block: "on a wheel through 0.1.6, source .env in a subshell first") — and `estate/README.md:99`'s run-directory paragraph; each still TRUE (a wheel through 0.1.6 does read the environment only) but written as what an operator does now, when 0.1.7 is what `pip install` serves — so the printed recipe tells a 0.1.7 operator to source a file `submit` reads by itself; **(b)** the pipeline's deprecation notice, `estate/corpus/ingest_corpus.py:1750` — "removed no earlier than 0.1.7": CP-80 KEPT the entry and moved the earliest removal to 0.1.8 in every lifted place (`pyproject.toml:97`, `corpus-contract.md:347`, `trainer-guide.md:13`/`:20`, `server-guide.md:281`), so the packaged notice states a weaker bound than the docs — true (a removal at ≥ 0.1.8 is no earlier than 0.1.7), read from the 0.1.7 artifact, one release stale. The `docs/**` half of both moved this CP to the dated form ("the `.env` read ships from 0.1.7; wheels through 0.1.6 read the environment only" — `server-guide.md:90`, `troubleshooting.md:95`).  | `estate/estate.py` (four strings, no behaviour — the `== run` / `next — ` prefixes the demo parses stay), `estate/README.md`, `estate/corpus/ingest_corpus.py` (one string) | opened CP-80 (Step 6 of its prompt: `estate/` not lifted, the residue recorded) | a printed recipe that does not tell a 0.1.7 operator to source what `submit` reads, and a notice that says the bound the docs say **[CP-83, recording CP-82] Source reconciliation is in phase 1; public-artifact closure remains phase 3's.** The audit corrected this row's inference: the current printed submit recipe already said nothing exported and only conditionally mentioned wheels through 0.1.6. Submit accepted without sourcing; the defect was stale release presentation, plus the deprecated notice's 0.1.7 bound disagreeing with the documented earliest 0.1.8. The six named source locations are now reconciled and exercised in the current phase's tests and locally installed wheel; gateway environment input, environment-wins submit behavior, the retained deprecated entry and the demo's literal `== run` / `next — ` prefixes stay. The fresh local test wheel has 18 entries and retains version 0.1.7 (SHA256 prefix `20d94a11f67f6c`); it is distinct from the public 0.1.7 artifact and was not published. Its submit accepted a fresh library episode without sourcing in 5.021 s. Source obligations are complete; the public wheel still needs the reconciled strings. `waiting-on: a published PyPI wheel containing the six reconciled estate/pipeline strings, verified from a fresh installed artifact with automatic submit fallback and the retained deprecated-entry notice` **[CP-85] CLOSED for public consumers at 0.1.8.** The publication event occurred. Installed `estate.py`/`ingest_corpus.py` match source; checks 9/10 prove the retained entry’s truthful 0.1.8 eligibility bound and automatic adjacent-file submit fallback with environment precedence. No notice-string edit was needed. A separate frozen estate README delivery paragraph is row 81. |
| 77 | **NEW (CP-80)** — found on the artifact (law 7): the packaged `estate.py` at 0.1.7 carries `up --decisions-dir` (CP-79) but pins the retrieval image at `MCP_IMAGE_PUBLISHED = ghcr.io/mhganainy/gsj-mcp-service:0.4.1` (`estate.py:106`, `:113`; the CP-79 report's "the pins end at 0.5.0" was true of `mcp-service/compose.yml`, its Dockerfile and both READMEs — not of the tool, whose pin CP-79 moved 0.4.0 → 0.4.1 and no further), and `0.4.1`'s service — CP-77's cut, before the decisions code — parses every config section with `extra="forbid"` (`gsj_mcp_service/config.py` at `8d1f418`), so the `decisions.path` key the tool writes for a drop is refused when the service starts: `up --decisions-dir <dir>` against the default image cannot reach a healthy service and fails (derived from the 0.4.1 code, not measured — no estate this CP) instead of serving the drop. A consumer at 0.1.7 who wants decisions must add `--mcp-image ghcr.io/mhganainy/gsj-mcp-service:0.5.0` (published two-platform at CP-79; GHCR lists `0.5.0`, `0.5.0-amd64`, `0.5.0-arm64`) — the trainer guide's `estate.py` row says so (CP-80). The checkout side has the same gap: `MCP_IMAGE = "gsj-mcp-service:0.4.1"` while `mcp-service/compose.yml` builds `0.5.0`, so `estate.py up` from a checkout also serves 0.4.1 unless told otherwise; and `estate/README.md:83` (the MCP row of the `estate.py` prompt/default table) states the tool's default as `create` — "image `gsj-mcp-service:0.5.0` from the checkout … `ghcr.io/mhganainy/gsj-mcp-service:0.5.0` from the wheel" — a current-state claim that has never been true of the tool (CP-79, `4ccfabf`, rewrote that row 0.4.0 → 0.5.0 while moving the pins 0.4.0 → 0.4.1); `estate/` is frozen this CP, so it is recorded here, and it becomes true at the same pin lift with no edit of its own. **[CP-81] MEASURED, on the published images, and routed around by a consumer.** The derivation above is now a measurement: with the very config `estate.py` writes for a drop (`decisions.path: /app/decisions`), `ghcr.io/mhganainy/gsj-mcp-service:0.4.1` refuses at startup — `1 validation error for ServiceConfig / decisions.path / Extra inputs are not permitted [type=extra_forbidden, input_value='/app/decisions']` — and the container exits, while `:0.5.0` loads the same file and reports `decisions.path = /app/decisions`. The workaround this row names is what the demo now answers: `gsj-rollout-demo`'s `MCP_IMAGE` moved `0.4.0` → `0.5.0`, so its `up` serves a drop with no flag from the operator (the demo hands the tool its own `mcp_image` answer, which is why the demo never meets the refusal). The two `estate.py` pin lines are unchanged and still `0.4.1`.  | `estate/estate.py` (two pin lines; outside the size law) | opened CP-80, by the install proof's check [6] reading the packaged pins from site-packages | `python -m gsj_rollout.estate up --decisions-dir <dir>` serving a drop with no flag beyond the drop **[CP-83, recording CP-82 B03] OPEN at the public artifact.** CP-82 measured the public 0.1.7 default's complete failure: repeated 0.4.1 config exits, 22.993 s wall for an 18 s diagnostic wait, and incorrect advice to wait for CPU embedding. Its explicit 0.5.0 override reached verify 7/7 in 33.421 s. **Phase 1's source obligations are complete, including B03; public delivery remains OPEN.** CP-83 freshly reproduced the public 0.1.7 default-drop failure in 32.784 s with the wrong CPU-wait cure. The corrected wait against that same actually restarting container refused in 0.299 s and named the 0.5.0 cure. Both source defaults now select 0.5.0 and known incompatible tags with a drop refuse before work. A fresh locally built and installed 18-entry wheel (version still 0.1.7, SHA256 prefix `20d94a11f67f6c`, never published) reached default no-drop verify 7/7 in 23.051 s and default-drop verify 7/7 in 21.324 s (30 files, 313 units, 317 pieces). Its fresh submit accepted in 5.021 s; the archive was read: one turn, zero tools, 57 trainable tokens, with invented page 2/3 claims against cutoff 1. That is a successful provenance/consumer proof, not answer quality. B01/B02/B06/B16's run-boundary source repairs are also reproduced and proven (charter §7 [CP-83]); they likewise remain absent from the public 0.1.7 wheel until phase 3 ships them. The already published MCP 0.5.0 needs no image rebuild. Phase 3 must publish the library changes before any consumer may retire its working override or change its image dependency on their strength. `waiting-on: a PyPI release containing CP-83's estate fixes, followed by a fresh installed default-drop verify/accepted/read episode and the demo's A-28 image proof on that release` **[CP-85] CLOSED for public consumers at 0.1.8.** The waiting-on event occurred in full: TestPyPI and PyPI default-drop check 14 reach verify/ready on published MCP 0.5.0 without override; both installed estates accept/read fresh independently validated episodes. Demo `3286271` follows publication with floor 0.1.8 and image `f0e8343a-gsj0.1.8` (index `30e9d940…`), both platforms’ payloads verified and a fresh-clone arm64 episode accepted with the `.env` mount. B01/B02/B03/B06/B16 and phase-2 B04/B05 are all delivered in wheel SHA256 `e4102385…`. No MCP rebuild needed; the demo’s explicit working 0.5.0 override remains. |

| 78 | **NEW (CP-83, recording CP-82 B04) — host MCP preflight and the service schema disagree.** On a valid baseline the audit's nine isolated overrides passed the host review and were rejected by actual `ServiceConfig`: exact search method, `normalize=false`, `batch_size=0`, `default_k=0`, `respect_page_boundaries=false`, `max_tokens=15`, `corpus_size=0`, `revision=main` and an invalid HF model id. Each schema probe took under 0.001 s; nine full estate timeouts were not run or claimed. Phase 2 owns the compact descriptor/parity decision across the four host descriptions. Phase 1's terminal-startup diagnosis limits the misleading wait but does not establish early schema parity. | `estate/estate.py`, its corpus-side estate tests and actual MCP schema contract tests; no torch/Chroma dependency added to the light wheel merely for flag validation | opened from the audit's reproduced schema probes; not re-fixed outside phase 1 | keyed pre-side-effect refusals whose accepted inputs agree with the pinned service. ~~`waiting-on: the nine audit inputs producing early keyed host refusals and a service-schema parity suite verifying merge precedence, fingerprint bytes and printed consequence classes`~~ **[CP-84 B04] CLOSED IN SOURCE; public delivery remains OPEN.** All nine audit probes re-reproduced on CP-83's host against actual `ServiceConfig`, including `corpus_size=0`: CP-77 added the service constraint, not the host check. One `MCP_FIELDS` descriptor now owns the host key/type/range/literal/consequence/review descriptions; derived compatibility names remain. Actual-model schema and semantic contract tests exercise the service environment without adding torch/Chroma to the estate tool. Existing integer/bool input restrictions remain; previously missing string checks now refuse early, and the intentionally narrower host type contract is explicit. Additional reproduced failures are covered: scalar/list/null sections, ambiguous valid model/revision strings changing YAML type, fractional/bool chunk answers being truncated by `int()`, and malformed recorded residuals overriding dedicated binding fields. The generated file quotes only ambiguous strings; defaults remain byte-identical. All nine installed-local-wheel probes exit 1 with the keyed cure, before a run directory exists, with container inventory unchanged. An override-file chunk window of 212 plus explicit CLI 220 produces 220; search 7/9 overrides the template; a plain rerun preserves exact generated bytes and recorded residuals. The retained CP-83 store reused in 16.923 s with byte-identical fingerprint, bank and refs. A fresh store built by a CP-83 local wheel then upgraded to the CP-84 local wheel reused in 23.443 s with generated config and fingerprint bytes unchanged. Final corpus suite: 291 passed in 54.60 s; MCP suite: 164 passed in 140.61 s. The separate accepted/read episode establishes consumer provenance only (charter §7). No service source/image or core bytes moved. Both local wheels remain unpublished; public 0.1.7 lacks the fixes. `waiting-on: a PyPI release containing CP-84's descriptor and early validation, followed by fresh installed nine-probe refusals, existing-store reuse and accepted/read consumer proof, before the image-dependent consumer changes its dependency` **[CP-85] CLOSED for public consumers at PyPI 0.1.8**, artifact SHA256 `e4102385…`. The publication event occurred. Check 12 repeats all nine keyed pre-container refusals with real before/after inventory equality on both indexes. Both published-artifact estates then reused config/bank/lock/fingerprint bytes unchanged and produced accepted/read episodes independently validating to zero findings. The package contains the 14-field descriptor; service image 0.5.0 remains unchanged. |

| 79 | **NEW (CP-84) — CI corpus-suite labels still say 109 after CP-83 added 87 tests.** `.github/workflows/ci.yml` repeats `corpus suite (109)` in its coverage comment and displayed job name; CP-83 actually ran 196 cases and CP-84 adds phase-2 coverage. The job still executes the whole corpus suite, so this is stale presentation, not omitted test execution. The workflow is explicitly frozen for phase 2; the separate stale core-size comment remains row 53. Phase 2 also writes ADR-0037 while frozen `README.md:102` counts 36 ADRs and `CLAUDE.md` lists ADRs through 0036; The final collected suites are corpus **291** and MCP **164**, while CLAUDE's current descriptions still say 196/134 and CI's MCP labels also still say 134. These are phase-caused presentation residues, recorded here rather than repaired outside the lift. | `.github/workflows/ci.yml` coverage comments and corpus/MCP job names; frozen `README.md` ADR count and `CLAUDE.md` ADR/suite-count descriptions | opened CP-84; the prompt expressly requires a register row instead of a workflow edit | CI describing the suite it actually runs. `waiting-on: a commit updating the frozen current-count descriptions and CI suite labels from its collected suites and actual ADR shelf, with the resulting Actions jobs reporting those collected counts` **[CP-85 release candidate]** Collection: 176/291/164. The four stale corpus/MCP workflow labels and README/CLAUDE ADR/suite counts are corrected; Actions confirmation follows in the release record. **[CP-85] CLOSED.** Collection and actual Actions CI `33970588545` agree: root 176, corpus 291, MCP 164. Four labels changed, no other workflow content. README/CLAUDE now count the actual 37-ADR shelf and current 291/164 suites. All four CI jobs GREEN; the waiting-on event occurred. |

| 80 | **NEW (CP-84) — the frozen root suite requires the credential behavior the operator has retired.** `tests/test_config.py::test_env_file_grammar_round_trips_estate_py_writer` passes apostrophe-bearing synthetic values through `Run.write_env` and requires them to round-trip. B05 and real Compose show that accepted form is not portable; phase 2 now requires writer/adopt refusal. Both behaviors cannot be true. The test is outside the explicit phase-2 lift and remains unchanged; the final isolated root-suite run measured **175 passed / 1 failed in 18.39 s**, with this exact old expectation as the failure. An earlier run alongside other suites also timed out in unchanged `test_serve_instructions_survive_a_pipe`; that timeout did not recur in the isolated complete run and its cause is not established. This is a known root-suite failure until the stale expectation is revised, not permission to weaken the new credential boundary or disguise the red job. | `tests/test_config.py` existing writer/reader grammar regression | opened CP-84; phase 2 lifts corpus-side and service-schema contract tests, explicitly freezes root `tests/` | a fully green root suite that asserts the accepted credential contract. `waiting-on: an authorized commit revising the existing root grammar regression to assert supported round-trips and unsupported writer refusal, followed by a complete root-suite and Actions root-job pass` **[CP-85 release candidate]** The named test now cites ADR-0037 and checks supported values through writer, loader, core reader and real Compose; apostrophes refuse naming the class and leave the file unchanged. Root 176 passed (18.71 s), no skip/xfail. Actions confirmation follows in the release record. **[CP-85] CLOSED.** The authorized named test checks supported round-trips including real Compose and class-naming apostrophe refusal before file replacement, citing ADR-0037. Full root 176 passed locally (18.71 s) and in CI `33970588545` (21.23 s), without skip/xfail or writer changes. The waiting-on event occurred. |

| 81 | **NEW (CP-85) — frozen source-delivery wording after the release.** `estate/README.md:91` dates the old public wheel correctly but still says to wait for phase 3. The helper docstring in `tests/test_config.py::_estate_tool` describes legacy quote encoding; only the named row-80 test body is lifted. Both are outside this release’s permitted edits. | `estate/README.md:91`; `tests/test_config.py::_estate_tool` | observed in the phase-3 sweep; no silent freeze lift | Update the current delivery paragraph and qualify the helper’s legacy encoding description. `waiting-on: an authorized edit of these two frozen documentation locations after 0.1.8 publishes` |

## What comes next

- **CP-04′/CP-09′ — the H200 golden pair.** Repeat the collection and
  comparison on the production platform with zero Mac adaptations; run
  the replay **as written** (vLLM proper can teacher-force; the Mac
  could not — F3); execute the six-item inherited DoD (charter §6):
  render both template variants, prove the symmetric one
  prefix-preserving, adopt it via `--chat-template`, pin it in the serve
  argv and the golden reference's MANIFEST (its provenance file),
  re-derive G4, retire the glue ids leaving the stitch dormant; rerun
  `pins/derive_pins.py` (G4's measure-at-serve) and pin
  `g6_expected_tail_ids`; cure the estate residuals (credentialed clone
  URLs or egress policy — row 2, **built at CP-56**: `clone_credential_env`
  + `REQUIRE_SIGNIN_VIEW`, proven refusable, closed live on the demo estate
  and mechanism-complete on the H200 pending its frozen MCP/corpus
  consumers' read-auth lift; the generation-config pin + request
  log — F1; an estate provenance surface binding each episode to the
  bring-up measurement — row 22). *Proves:* the numbers that govern
  (A-16). *Unnecessary
  if:* never — only abandoning the H200 as the production platform
  would void it; the Mac verdict cannot govern numerics by A-16.
- **M4 — the slime bridge and one OPD loop.** Submit → collect → slime
  `Sample`s → at least one real optimization step consuming masks and
  logprobs; declare policy versions so P3 stops being inert and A-13's
  drain rule is exercised against a real weight sync.
  **[CP-16] The bridge half exists**: trainer-owned, in
  `gsj-harness-rollout-server-examples/slime_bridge/` *(at `slime-cp17`
  since the examples repo's CP-69)* (ADR-0018) — the
  conversion to slime v0.3.0 `Sample`s with three enforced assertions
  (mask-before-ratio, sentinel rejection, `checks` trainer-side), tested
  against the real CP-09′/CP-07 bodies from a wheel install (ADR-0017
  fixed the wheel's pins — wishlist 11). Still owed by the loop CP:
  the optimization step itself, weight sync + policy-version declaration
  (P3/A-13), reward attach (every real body carries `reward: null`),
  cadence against the 19-attempt qualification rate, and A-26's
  on-estate Sample-surface verification. *Proves:* the loop
  trains — converts trace-correctness into the outcome, resolves A-6,
  and is the second condition of the full adopt. *Unnecessary if:* the
  trainer of record becomes verl-only (its budget then moves to the
  verl bridge).
- **M5 — the template, then M6 — thinking, in that order.** M5 makes
  the symmetric (prefix-preserving) template the serving standard
  everywhere, retires the ADR-0007 stitch from load-bearing, and
  refreshes golden + pins on top of CP-04′'s work. M6 then flips
  `harness.thinking` on. The sequencing argument: thinking-ON is out of
  bounds for the stitch (A-22 — variable-length divergence the
  fixed-ids repair structurally cannot fix), so M6 needs M5's template
  first; and the template changes both the trajectory layer's input and
  the golden reference, so changing it in the same milestone as
  thinking would destroy attribution — one variable at a time, the same
  discipline that kept the Mac pair interpretable. *Unnecessary if:*
  M5 — upstream's pending grouping refactor tolerates asymmetric
  templates at a future re-vendor; M6 — the training recipe stays
  no-think.
  **[CP-41] Both LANDED — this bullet is history, kept for its
  sequencing argument** (audit S11): M5 at CP-04′ — the symmetric
  template serves everywhere via `--chat-template`, the stitch retired
  to dormant (ADR-0007's second amendment), golden + pins refreshed on
  the H200 pair; then thinking at CP-30/31, in exactly this order
  (per-mode pins, ADR-0024; both mode files ship in every wheel since
  0.1.1). Naming note: the dated sections above reuse "M6"/"M6a"/"M6b"
  for the verl second-trainer milestone — this bullet's "M6" is the
  original thinking milestone, not that one.
- **The verl bridge — its own milestone, informed by M4.** The cheaper
  starting point is trainer-side: the predecessor's uni-agent path
  already speaks verl, so an adapter consuming our validated callback
  bodies through `client.py` costs less than a second Polar-side bridge
  — decide after M4 shows what the slime bridge actually required.
  *Proves:* trainer-agnosticism is real, not aspirational.
  *Unnecessary if:* slime remains the only trainer.
  **[CP-20] LANDED — M6a, and the starting-point hypothesis is answered
  NO by reading uni-agent @ its pin**: its trainer-side path generates
  into TransferQueue (entry point returns `None`, `Trajectory` is a
  gateway type, no padded batch exists in it) — it cannot consume
  externally-produced trajectories, so the direct bridge was the cheap
  one (`verl_bridge/`, external ADR-0003; the boundary needed zero
  changes here). What *Proves* asked for, it got: two trainers, one
  unchanged repo.
  **[CP-21] M6 CLOSED — the verl LOOP ran** (external ADR-0004): one
  real optimizer step in verl @ `1ae9455` on 110 collected episodes,
  synced back through the unchanged `serve-updated.sh` and proven by
  the zero-noise probe (0.000000 → 0.042168, 6541/6768 positions),
  second collection 8/8 qualifying and converting. This repo's diff:
  `docs/**` only — `staging/**` ran a second trainer as committed. The
  trainer-agnostic claim is **measured, not argued** (the dated M6
  section below).
- **Production.** Gated on artifacts only the operator can supply: H200
  cluster access and estate bring-up (with the credentialed-clone posture —
  the mechanism now exists, CP-56; the H200 flip completes once its frozen
  MCP/corpus consumers authenticate their reads), the corpus beyond the
  staging cases, secret management for
  the MCP token, and the trainer integration of record. *What it would
  take:* both pairs green, M4 done, the wishlist's items 1–4 landed
  (done at CP-13).
  *Unnecessary if:* never — it is the point; the gate exists so it is
  entered deliberately.

## Sources

Charter (`docs/CHARTER.md`): §5 criterion + MET note, §7 register
(final), §9 abandon conditions, §4 assumptions. Reports: CP-02 (fork
audit), CP-03 (vendor + costs), CP-04 (golden reference), CP-05 (source
verdict, S1–S9), CP-06 (harness spike, pi dialect), CP-07 (the harness,
the cutoff proof), CP-08 (the four surfaces), CP-09 (fidelity), CP-10
(discipline + cutoff hole), CP-11 (leak cure, pins walk), CP-11b (the
gates). Decisions: ADR-0003 (taskbank), ADR-0004/0005 (pin + vendoring),
ADR-0007 (the stitch), ADR-0009 (checks allowance), ADR-0011 (codec
evidence), ADR-0012 (this CP's size-law raise).
