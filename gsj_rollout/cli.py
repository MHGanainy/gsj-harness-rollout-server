"""SERVER — `gsj-rollout serve | submit` console entry point.

CP-00 stub: prints a placeholder and exits 0, as the scaffold Definition
of Done requires. The real commands arrive at CP-08.
"""


def main() -> int:
    print(
        "gsj-rollout: scaffold only (CP-00) — `serve` and `submit` arrive "
        "at CP-08. See docs/CHARTER.md."
    )
    return 0
